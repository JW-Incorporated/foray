{
  "name": "foray-api",
  "private": true,
  "description": "Vercel serverless functions (Stage 3b, kanban t_567b570f). Scoped dependencies only — the repo root stays dependency-free by design (see CLAUDE.md). Vercel auto-detects and bundles this directory's functions.",
  "dependencies": {
    "pg": "^8.13.1"
  }
}
