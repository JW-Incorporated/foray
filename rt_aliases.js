const { run } = require("./rt_harness.js");

// ALIASES sanity + potential over-broadening
run("bbq");
run("barbeque");
run("cooking");
run("rome");
run("ww2");
run("car");
run("cars");
run("ocean");
run("plane");
run("planes");
