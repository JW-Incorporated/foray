{
  "//": "Covered-suite tests with no Swift meaning. Each has one closed reason (webview-only, dom-only, text-pin, js-module-shape) and a sentence of why. An entry claims the engine need never reimplement this rule; a reviewer should be able to check that claim from the sentence alone.",
  "foray-playback": {
    "AUDIT 2026-09-22: a FINISHED Foray offers to start over, not to resume": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "AUDIT 2026-09-22: the main button is painted from `running`, the answer its press is decided by": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "DELETING EVERYTHING leaves the page interactive, and resume works again after it": {
      "reason": "webview-only",
      "why": "The page's own storage tiers (window.forayStorage, localStorage, the durable IndexedDB/Preferences tier, quota handling) as app.js reads them. In native mode the engine owns cp_foray and friends (OWNED_PREFIXES) and the page adopts them (NE-23); these pin the web tiers."
    },
    "EVICTION: a durable row is readable by the page after localStorage is wiped": {
      "reason": "webview-only",
      "why": "The page's own storage tiers (window.forayStorage, localStorage, the durable IndexedDB/Preferences tier, quota handling) as app.js reads them. In native mode the engine owns cp_foray and friends (OWNED_PREFIXES) and the page adopts them (NE-23); these pin the web tiers."
    },
    "MIGRATION through the real page: rows already in localStorage reach the durable tier": {
      "reason": "webview-only",
      "why": "The page's own storage tiers (window.forayStorage, localStorage, the durable IndexedDB/Preferences tier, quota handling) as app.js reads them. In native mode the engine owns cp_foray and friends (OWNED_PREFIXES) and the page adopts them (NE-23); these pin the web tiers."
    },
    "MIGRATION: the page still reads its own state through the store afterwards": {
      "reason": "webview-only",
      "why": "The page's own storage tiers (window.forayStorage, localStorage, the durable IndexedDB/Preferences tier, quota handling) as app.js reads them. In native mode the engine owns cp_foray and friends (OWNED_PREFIXES) and the page adopts them (NE-23); these pin the web tiers."
    },
    "PAUSING a live Foray is not a failed start — the page stays live": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "STORAGE FAILURE MUST NOT MAKE THE PAGE INERT — the regression this suite exists for": {
      "reason": "webview-only",
      "why": "The page's own storage tiers (window.forayStorage, localStorage, the durable IndexedDB/Preferences tier, quota handling) as app.js reads them. In native mode the engine owns cp_foray and friends (OWNED_PREFIXES) and the page adopts them (NE-23); these pin the web tiers."
    },
    "a backend with no out-point watch refuses the Foray instead of playing whole episodes": {
      "reason": "js-module-shape",
      "why": "A backend without an out-point watch (setOutPoint undefined), and the manager's refusal to hand it a bounded item. The native engine has one deck type and it always carries the three-layer out-point (outpoint and deck families), so there is no blind backend to refuse."
    },
    "a control that fails over live audio is recorded as a control, not as a start": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "a control that throws over LIVE audio says so without lying about the state": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "a drifted resume is recorded as an event; a clean one is not": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "a failed start leaves a trace in the record, not only on the screen": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "a full quota with no durable tier makes lsSet return false, and health says why": {
      "reason": "webview-only",
      "why": "The page's own storage tiers (window.forayStorage, localStorage, the durable IndexedDB/Preferences tier, quota handling) as app.js reads them. In native mode the engine owns cp_foray and friends (OWNED_PREFIXES) and the page adopts them (NE-23); these pin the web tiers."
    },
    "a mount with no storage at all still renders and binds": {
      "reason": "webview-only",
      "why": "The page's own storage tiers (window.forayStorage, localStorage, the durable IndexedDB/Preferences tier, quota handling) as app.js reads them. In native mode the engine owns cp_foray and friends (OWNED_PREFIXES) and the page adopts them (NE-23); these pin the web tiers."
    },
    "a record that throws does not cost the listener the message on screen": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "a refused write is reported to the caller instead of pretending to have worked": {
      "reason": "webview-only",
      "why": "The page's own storage tiers (window.forayStorage, localStorage, the durable IndexedDB/Preferences tier, quota handling) as app.js reads them. In native mode the engine owns cp_foray and friends (OWNED_PREFIXES) and the page adopts them (NE-23); these pin the web tiers."
    },
    "a resume whose segment no longer exists still mounts an interactive page": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "a scrub past either end of the strip clamps instead of leaving the Foray": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "a snapshot from an OLDER player module leaves the label alone rather than blanking it": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "a source credit is bound, so the outbound click is measured": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "a speed changed in the mini-player's sheet reaches the page's button": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "a start that THROWS says so on the page instead of vanishing into the console": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "a start that fails puts the page back to cold, so the next tap is a real retry": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "a stored position makes the cold press a resume, and Start over clears it": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "a thumbs-down opens the sheet and records nothing until it is submitted": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "a thumbs-up records a vote — the binder that actually threw": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "an error too strange to describe still reaches the screen AND the record": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "an older player module that cannot answer the drift question does not break the page": {
      "reason": "js-module-shape",
      "why": "A stale JS module (served from a cache) missing an export the page feature-tests. The native engine and its protocol version are fixed per build (the handshake family)."
    },
    "another Foray's ticks are not this page's news": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "app.js reads and writes through window.forayStorage when one is published": {
      "reason": "webview-only",
      "why": "The page's own storage tiers (window.forayStorage, localStorage, the durable IndexedDB/Preferences tier, quota handling) as app.js reads them. In native mode the engine owns cp_foray and friends (OWNED_PREFIXES) and the page adopts them (NE-23); these pin the web tiers."
    },
    "autoplay refusal reads as a browser being careful, not as a broken app": {
      "reason": "webview-only",
      "why": "WebKit lifts an <audio> element's autoplay restriction only for a play() made inside a user gesture's task (#225), so the page must reach the element with nothing awaited first. The native engine starts AVPlayer from a bridge command with an active session; there is no gesture to spend."
    },
    "changing the speed does NOT start playback": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "clicking a quarter of the way along the strip seeks to a quarter of the Foray": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "client.js only ever reports an error that describes the attempt in front of it": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "client.js spends the gesture on the element BEFORE its first await": {
      "reason": "webview-only",
      "why": "WebKit lifts an <audio> element's autoplay restriction only for a play() made inside a user gesture's task (#225), so the page must reach the element with nothing awaited first. The native engine starts AVPlayer from a bridge command with an active session; there is no gesture to spend."
    },
    "dismissing the sheet leaves the segment unvoted": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "during a seam beat the page says Pause, not Loading — the silence is deliberate": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "every why-line the UI will render is inside the copy budget": {
      "reason": "text-pin",
      "why": "Word counts of the authored why-lines the page renders (product principle 4): copy, not playback. The engine never renders a why-line."
    },
    "mounting the Foray page binds the transport — the inert-page regression": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "next, previous, a row and the strip each reach the player": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "pause comes back through the same callback and the label flips": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "play() does not destructure its options in the signature — a null caller must not throw": {
      "reason": "js-module-shape",
      "why": "A JS function signature's destructuring of a null argument. The engine's playForay arguments arrive as a validated protocol payload (engine-contract.js)."
    },
    "pressing play reaches the player, and the callback repaints the page": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "scrubbing a Foray that has not started begins it AT that position, not at the top": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "tapping the speed button opens a picker instead of cycling directly": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "the 2px gaps between the bars are live scrubber, not dead zones": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "the Foray page carries a speed button, labelled from the player": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "the bar the listener is inside fills as it plays; the ones behind it are full": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "the clock a failed start leaves behind is the place the button goes": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "the live bar keeps moving between segment changes — it is painted above the paint guard": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "the markup app.js emits carries every hook the harness serves": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "the page hands the RESOLVED Foray to the resume lookup, not just two numbers": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "the speed button says its value to a screen reader AT NORMAL SPEED too": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "the speed button survives a Foray with nothing playable": {
      "reason": "dom-only",
      "why": "app.js's Foray page (renderForay) mounted in a stub DOM against a fake player bridge: markup, bindings, labels and what the page paints. In native mode the page is unchanged and reads the engine through the facades (NE-35 wires native Forays); nothing here is a rule the engine reimplements."
    },
    "the tap reaches playForay with nothing awaited in front of it": {
      "reason": "webview-only",
      "why": "WebKit lifts an <audio> element's autoplay restriction only for a play() made inside a user gesture's task (#225), so the page must reach the element with nothing awaited first. The native engine starts AVPlayer from a bridge command with an active session; there is no gesture to spend."
    }
  },
  "html-audio-backend": {
    "A TELEMETRY SINK THAT THROWS CANNOT BREAK THE PLAYER (#264)": {
      "reason": "js-module-shape",
      "why": "A JS telemetry callback that throws. The engine writes diagnostics rows through EngineDiagnostics (plan §4.1), a typed seam with its own recording fake, not an injected callback."
    },
    "THE SEEK PATH'S deadline is ref'd too — the half of the seams nothing covered": {
      "reason": "js-module-shape",
      "why": "Node's timer ref/unref semantics on the in-place seek's deadline. The native deadline is a main-queue timer with no such mode."
    },
    "a prime whose play() rejects does not become an unhandled rejection": {
      "reason": "webview-only",
      "why": "WebKit lifts an <audio> element's autoplay restriction only for a play() made inside a user gesture's task (#225). The native engine starts AVPlayer from native code, where there is no autoplay gesture to spend."
    },
    "a rejected play() surfaces as onError, never an unhandled rejection": {
      "reason": "js-module-shape",
      "why": "HTMLMediaElement.play() returns a Promise, and an unhandled rejection is a JS runtime concept. AVPlayer's play returns nothing; a deck that cannot play reports a failed event (AVDeckTests)."
    },
    "a released backend does not spend a gesture on a dead element": {
      "reason": "webview-only",
      "why": "WebKit lifts an <audio> element's autoplay restriction only for a play() made inside a user gesture's task (#225). The native engine starts AVPlayer from native code, where there is no autoplay gesture to spend."
    },
    "a surface's own listeners survive a handover": {
      "reason": "webview-only",
      "why": "addMediaListener re-attaches the page's DOM listeners to whichever <audio> element holds the player role. In native mode the page's events come from the engine (snapshot and events over the bridge), whichever AVPlayer is playing."
    },
    "a throwing isHidden cannot stop a load, and falls back to the visible budget": {
      "reason": "webview-only",
      "why": "A page-supplied isHidden() callback (document.hidden) that throws. The native engine reads application state natively; an unknown visibility getting the visible budget is fixtured in deck-episode (deadline-unknown-visibility-is-visible)."
    },
    "a window handler that throws cannot take the audio down with it": {
      "reason": "js-module-shape",
      "why": "A page callback (onPrefetchWindow) that throws inside the element's timeupdate. The native DeckPair's window is the engine's own decision (deck/window-*), with no page callback in the path."
    },
    "an element that refuses a playback rate does not take the Foray down with it": {
      "reason": "webview-only",
      "why": "Safari's <audio> throws on some playbackRate assignments, so the setter is guarded. AVPlayer's rate is a property that never throws; which rate the lock screen reports is deck/reported-rate-*."
    },
    "every deadline is ref'd, so it fires whether or not anything else is running": {
      "reason": "js-module-shape",
      "why": "Node's timer ref/unref semantics (an unref'd timer only fires while something else holds the event loop). The native deadline is a main-queue timer with no such mode; its firing is AVDeckTests' testANeverReadyUrlHitsTheDeadlineWithNoPreroll."
    },
    "notePlayGesture is one-shot — the restriction is only ever lifted once": {
      "reason": "webview-only",
      "why": "WebKit lifts an <audio> element's autoplay restriction only for a play() made inside a user gesture's task (#225). The native engine starts AVPlayer from native code, where there is no autoplay gesture to spend."
    },
    "notePlayGesture never touches an element that already holds audio": {
      "reason": "webview-only",
      "why": "WebKit lifts an <audio> element's autoplay restriction only for a play() made inside a user gesture's task (#225). The native engine starts AVPlayer from native code, where there is no autoplay gesture to spend."
    },
    "notePlayGesture spends the tap on the element, before there is anything to load": {
      "reason": "webview-only",
      "why": "WebKit lifts an <audio> element's autoplay restriction only for a play() made inside a user gesture's task (#225). The native engine starts AVPlayer from native code, where there is no autoplay gesture to spend."
    },
    "seek carries the precise flag into telemetry without changing behaviour": {
      "reason": "text-pin",
      "why": "Pins that the backend's English telemetry line names the precise flag. The engine's rows use the diag-tokens vocabulary (NE-04), not this string."
    },
    "the SECOND load of one source settles without yielding — why the second tap worked": {
      "reason": "webview-only",
      "why": "WebKit lifts an <audio> element's autoplay restriction only for a play() made inside a user gesture's task (#225). The native engine starts AVPlayer from native code, where there is no autoplay gesture to spend. What this pins is that the same-source path resolves inside the tap's task; the same-source rule itself is fixtured in deck-episode (same-source-is-a-seek)."
    },
    "the element is never given a crossorigin attribute": {
      "reason": "webview-only",
      "why": "An <audio> element's crossorigin attribute turns a working no-cors podcast load into a hard failure. AVPlayer has no CORS; the engine's deck never sees this attribute."
    },
    "the hidden deadline says which budget it used, so an artifact can be read": {
      "reason": "text-pin",
      "why": "Pins the backend's English `load.deadline Nms (hidden|visible)` telemetry string. Which budget applies is fixtured in deck-episode (deadline-hidden, deadline-visible); the engine's rows carry tokens, not this sentence."
    }
  },
  "interlude": {
    "an `error` reports 'error'; a rejected play() reports 'rejected'; a throwing play() refuses": {
      "reason": "dom-only",
      "why": "DOM `error` events and the promise HTMLMediaElement.play() returns. The native jingle is not an <audio> element: it is the engine's InterludePlayer behind the Interlude seam (NE-34), whose start/stop/ended contract is the fakeInterlude the manager-foray scenarios drive (NE-31j) and whose timing NE-34's Simulator tests assert."
    },
    "construction points the element at the asset and asks for it up front": {
      "reason": "dom-only",
      "why": "An <audio> element's src and preload attributes. The native jingle is not an <audio> element: it is the engine's InterludePlayer behind the Interlude seam (NE-34), whose start/stop/ended contract is the fakeInterlude the manager-foray scenarios drive (NE-31j) and whose timing NE-34's Simulator tests assert."
    },
    "describeInterlude names the decision it made, not one it did not": {
      "reason": "text-pin",
      "why": "Pins describeInterlude's English telemetry sentences. The engine's jingle rows carry closed tokens from the diag-tokens vocabulary, not these strings (the same call as describeSeam's two tests)."
    },
    "no element on this host: every method is inert and start() says so": {
      "reason": "dom-only",
      "why": "A host with no Audio constructor (Node, a non-DOM worker). The native jingle is not an <audio> element: it is the engine's InterludePlayer behind the Interlude seam (NE-34), whose start/stop/ended contract is the fakeInterlude the manager-foray scenarios drive (NE-31j) and whose timing NE-34's Simulator tests assert."
    },
    "prime() spends the gesture muted, then rewinds and unmutes; once per session": {
      "reason": "webview-only",
      "why": "WebKit lifts an <audio> element's autoplay restriction only for a play() made inside a user gesture's task (#225). The native engine starts audio from native code, where there is no gesture to spend."
    },
    "release() silences, drops the source, and refuses to start again": {
      "reason": "dom-only",
      "why": "removeAttribute('src') and load() on the element to drop its buffer. The native jingle is not an <audio> element: it is the engine's InterludePlayer behind the Interlude seam (NE-34), whose start/stop/ended contract is the fakeInterlude the manager-foray scenarios drive (NE-31j) and whose timing NE-34's Simulator tests assert."
    },
    "start() refuses an element that has not buffered, and does not play it": {
      "reason": "dom-only",
      "why": "HTMLMediaElement.readyState (HAVE_FUTURE_DATA). The native jingle is not an <audio> element: it is the engine's InterludePlayer behind the Interlude seam (NE-34), whose start/stop/ended contract is the fakeInterlude the manager-foray scenarios drive (NE-31j) and whose timing NE-34's Simulator tests assert."
    },
    "start() rewinds, forces 1.0x whatever the element had, plays, and reports active": {
      "reason": "dom-only",
      "why": "The element's currentTime and playbackRate properties. The 1.0x rule itself is fixtured (interlude/rate-is-1x). The native jingle is not an <audio> element: it is the engine's InterludePlayer behind the Interlude seam (NE-34), whose start/stop/ended contract is the fakeInterlude the manager-foray scenarios drive (NE-31j) and whose timing NE-34's Simulator tests assert."
    },
    "the URL satisfies `media-src https:` on every host, including capacitor://localhost": {
      "reason": "webview-only",
      "why": "index.html's Content-Security-Policy (media-src https:) applies to the WebView's own media elements. Native playback is not subject to the page's CSP."
    },
    "the committed placeholder is 44.1 kHz stereo 16-bit PCM, under 600 KB": {
      "reason": "js-module-shape",
      "why": "Reads the committed WAV's bytes with Node's fs. It pins the FILE that every player plays, not a rule a player implements, and it holds whichever engine plays the asset; there is nothing for Swift to reimplement."
    },
    "the element's `ended` reports once, and a stop is never reported": {
      "reason": "dom-only",
      "why": "DOM `ended` event listeners on the element. The native jingle is not an <audio> element: it is the engine's InterludePlayer behind the Interlude seam (NE-34), whose start/stop/ended contract is the fakeInterlude the manager-foray scenarios drive (NE-31j) and whose timing NE-34's Simulator tests assert."
    },
    "the placeholder peaks at about -6 dBFS, starts from silence and ends in silence": {
      "reason": "js-module-shape",
      "why": "Reads the committed WAV's samples with Node's fs. It pins the asset's loudness and clean edges, which no player computes; it holds whichever engine plays the file."
    },
    "the preference is ON unless the store says the literal 'off', and a broken store is ON": {
      "reason": "webview-only",
      "why": "The page owns cp_interlude: client.js reads it with readInterludePref and hands the engine a boolean (setInterludeEnabled, engine-contract.js). The engine never reads the key, so there is no store for it to misread."
    },
    "writeInterludePref stores 'on'/'off' under the cp_ key and reports a refusal": {
      "reason": "webview-only",
      "why": "The page's Settings switch writes cp_interlude (client.js setInterludeEnabled -> writeInterludePref) and then sends the engine the boolean. The engine persists nothing under that key."
    }
  },
  "media-session": {
    "EVERY playForay call site in app.js passes the artwork document": {
      "reason": "js-module-shape",
      "why": "Text scan: every app.js playForay call site passes discoverDoc; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "MediaMetadata is used when the engine provides it": {
      "reason": "webview-only",
      "why": "The WebView's MediaMetadata constructor. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a MediaMetadata constructor that throws does not take the page with it": {
      "reason": "webview-only",
      "why": "A throwing MediaMetadata constructor in a WebKit build. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a REMOTE stop pauses and keeps the session; only the in-page Stop and the notification's close tear it down": {
      "reason": "js-module-shape",
      "why": "Text scan: client.js's remoteStop pauses and only a close tears down; the engine's stop is registered-and-disabled with a remote stop as a pause (plan §4.5, T-7), which is NE-12s's commandAvailability and NE-18's XCTest; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "a RESTORED Foray is described to the OS as a Foray, and its clip follows the thumb (player-10)": {
      "reason": "js-module-shape",
      "why": "Text scan: mediaViewFields reads the restored Foray and the clip under the pending position; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "a handler installed BY THE BRIDGE actually calls the surface — all eight of them": {
      "reason": "webview-only",
      "why": "The WebView bridge's installed handler really calls the surface; the press-to-intent table it exercises is fixtured in media-episode through media-actions. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a handler installed by the bridge reaches a REAL manager": {
      "reason": "webview-only",
      "why": "The WebView bridge end to end into a manager; a remote press into the engine is NE-14j/NE-29j's manager scenarios. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a lock-screen press is recorded as `remote` and an in-page tap as `tap`": {
      "reason": "js-module-shape",
      "why": "Text scan: client.js passes source \"remote\" from the lock-screen surfaces; the engine records `source` on every command itself (plan §5.2, D-4); native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "a metadata setter that throws is swallowed": {
      "reason": "webview-only",
      "why": "A throwing metadata setter in a WebKit build. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a metadata write that SUCCEEDS reports writeOk true": {
      "reason": "webview-only",
      "why": "writeOk for a successful navigator.mediaSession.metadata assignment. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a metadata write that THROWS is reported as failed, not as written": {
      "reason": "webview-only",
      "why": "writeOk for a throwing navigator.mediaSession.metadata assignment (founder record 2026-09-21). createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a moving playhead writes position, a still one stops writing": {
      "reason": "webview-only",
      "why": "The bridge's tenth-of-a-second setPositionState dedupe. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a navigator with no mediaSession — desktop Safari, older browsers — never throws": {
      "reason": "webview-only",
      "why": "Desktop Safari and older browsers with no navigator.mediaSession. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a new segment DOES rewrite metadata": {
      "reason": "webview-only",
      "why": "The WebView bridge's dedupe letting a changed segment through. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a play RE-ASSERTS the metadata even when it has not changed": {
      "reason": "webview-only",
      "why": "invalidate() re-asserting the WebView metadata on play (founder record 2026-09-21); native NowPlayingPublisher writes at every transition, so there is no dedupe to invalidate (plan §4.5). createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a playbackState setter that throws is swallowed": {
      "reason": "webview-only",
      "why": "A throwing playbackState setter in a WebKit build; native mode never uses playbackState (plan §4.5, OQ-8). createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a real mediaSession is detected and reports itself supported": {
      "reason": "webview-only",
      "why": "Feature detection of navigator.mediaSession. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a setPositionState that throws is swallowed, and the render loop survives": {
      "reason": "webview-only",
      "why": "A throwing setPositionState in a WebKit build. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a stall reaches the OS view from both branches (p-car-8)": {
      "reason": "js-module-shape",
      "why": "Text scan: both mediaViewFields branches pass `buffering`; the mapping's stall rule is fixtured in media-episode (position-stall-reports-rate-zero); native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "a throw does not poison the dedupe — the next identical render retries": {
      "reason": "webview-only",
      "why": "The WebView bridge's dedupe key stamped only on a successful write. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a throwing onWrite cannot stop the lock screen from being written": {
      "reason": "webview-only",
      "why": "A throwing diagnostics sink inside the WebView bridge. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "a throwing onWrite on the state row still writes the state": {
      "reason": "webview-only",
      "why": "A throwing diagnostics sink inside the WebView bridge. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "after clear(), the next identical update writes again rather than being deduped away": {
      "reason": "webview-only",
      "why": "The WebView bridge's dedupe reset after clear(). createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "an action whose surface rejects does not become an unhandled rejection": {
      "reason": "webview-only",
      "why": "A rejecting surface must not become an unhandled promise rejection in the WebView. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "an action whose surface throws does not propagate into the platform": {
      "reason": "webview-only",
      "why": "A throwing surface must not reject into the WebView's action dispatch. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "an engine that throws on one unsupported action still gets the other seven": {
      "reason": "webview-only",
      "why": "Chromium throws from setActionHandler for an unsupported action; MPRemoteCommandCenter has no such failure mode. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "an engine with no setPositionState is not a failure — older Safari": {
      "reason": "webview-only",
      "why": "Older Safari with no setPositionState. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "an episode's next/previous are the PAGE's, and absent until the page offers them": {
      "reason": "js-module-shape",
      "why": "Text scan of client.js's episodeMediaSurface getters, plus a mediaSessionActions call whose null-neighbour rule is fixtured in media-episode (actions-absent-next-previous); native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "an unchanged view does NOT rewrite metadata — a 4 Hz redraw flickers the notification": {
      "reason": "webview-only",
      "why": "The bridge's 4 Hz redraw dedupe against Android notification flicker; native NowPlayingPublisher writes on transitions and seeks, not from a render loop (plan §4.5). createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "app.js hands over the discover pool it already fetched, not a new request": {
      "reason": "js-module-shape",
      "why": "Text scan: app.js reuses the discover pool it already fetched; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "artworkUrlsByShow harvests the discover pool by exact show name": {
      "reason": "webview-only",
      "why": "The page joins show artwork out of data/discover.json (client.js, through foray-sources.js artworkUrlsByShow) and hands the engine a finished URL per item; the engine never reads the discover pool. The engine half — a miss falls back to the app artwork, never a blank — is media/artwork-miss-falls-back."
    },
    "artworkUrlsByShow survives a missing, malformed or empty document": {
      "reason": "webview-only",
      "why": "The page joins show artwork out of data/discover.json (client.js, through foray-sources.js artworkUrlsByShow) and hands the engine a finished URL per item; the engine never reads the discover pool. The engine half — a miss falls back to the app artwork, never a blank — is media/artwork-miss-falls-back."
    },
    "clear() empties the OS slot and reports none": {
      "reason": "webview-only",
      "why": "clear() emptying the WebView session; native nil-on-finish is NowPlayingPublisher's (NE-18). createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "clearing the session is reported as a write, via=clear, state none": {
      "reason": "webview-only",
      "why": "Pins the bridge's via=clear diagnostics row. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "client.js declares no second copy of the ±15/30 numbers": {
      "reason": "js-module-shape",
      "why": "Text scan: client.js imports the ±15/30 pair rather than restating it; the Swift side is pinned by NE-04's generated EngineConstants and NE-12s's no-literal-15/30 grep; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "client.js hands the real navigator over at exactly one place": {
      "reason": "js-module-shape",
      "why": "Text scan: client.js hands the real navigator over at exactly one call site; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "client.js reads the shim's counter AFTER its flush, not in the turn of the write": {
      "reason": "js-module-shape",
      "why": "Text scan: client.js reads the Capacitor shim's counter after the shim's flush; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "client.js records what the NATIVE side received: foray:remote -> diag.remoteCommand": {
      "reason": "js-module-shape",
      "why": "Text scan: client.js listens for foray:remote from the native shim; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "codeOnly does not blind itself on a regex literal — it is the scanner's own guard": {
      "reason": "js-module-shape",
      "why": "Self-test of codeOnly, this suite's own comment and string stripper. native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "createMediaSession with no arguments at all is inert, not an exception": {
      "reason": "webview-only",
      "why": "createMediaSession's no-argument default. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "handlers keep their surface as `this`, so a method form works too": {
      "reason": "js-module-shape",
      "why": "Pins that a surface method keeps its object as `this` (Function.prototype.bind); a Swift closure has no receiver to lose, and the remote table itself is fixtured in media-episode."
    },
    "index.html's CSP already permits https artwork, so nothing here widened it": {
      "reason": "webview-only",
      "why": "index.html's Content-Security-Policy governs what the WebView may fetch for artwork; the native ArtworkCache fetches https itself, off-main (plan §4.5), under no CSP."
    },
    "liveLines ignores a commented-out call, which is the whole reason it exists": {
      "reason": "js-module-shape",
      "why": "Self-test of liveLines, this suite's own commented-out-line scanner. native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "no file under player/ touches navigator.mediaSession in code": {
      "reason": "js-module-shape",
      "why": "Text scan: no player/*.js touches navigator.mediaSession except through the bridge; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "no navigator at all yields an inert bridge that answers every call": {
      "reason": "webview-only",
      "why": "The inert stand-in for a browser with no navigator at all. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "onWrite fires exactly when metadata is really written, and not on an unchanged view": {
      "reason": "webview-only",
      "why": "The bridge's onWrite diagnostics hook fires on a navigator.mediaSession assignment. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "play() calls invalidate before setNowPlaying": {
      "reason": "js-module-shape",
      "why": "Text scan: ForayPlayer.play calls media.invalidate() before setNowPlaying; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "player/client.js drives the lock screen through this module and nothing else": {
      "reason": "js-module-shape",
      "why": "Text scan: player/client.js imports createMediaSession and calls mediaSessionView; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "release() removes every handler it installed": {
      "reason": "webview-only",
      "why": "release() unregistering WebView action handlers; native relinquish tears down its own targets (NE-17's XCTest). createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "switching from a Foray to one episode REMOVES nexttrack instead of leaving it stale": {
      "reason": "webview-only",
      "why": "A stale setActionHandler left bound to a Foray. The native command set is recomputed from the snapshot (commandAvailability, NE-12s), so there is no installed-handler set to go stale. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "the FORAY's clock is what client.js reports — the whole thing, not the segment": {
      "reason": "js-module-shape",
      "why": "Text scan: client.js's syncMediaSession reports the Foray clock; the mapping's own clock rule is NE-29j's (media family); native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "the Foray surface is installed INSIDE playForay, not merely defined somewhere": {
      "reason": "js-module-shape",
      "why": "Text scan: the Foray surface is installed inside ForayPlayer.playForay; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "the PAUSE is reported as a write, marked via=state, with the strings as they were": {
      "reason": "webview-only",
      "why": "Pins the bridge's via=state diagnostics row for a playbackState write, a property native mode never writes (plan §4.5). createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "the PUBLISHER's name is what actually reaches the platform, not ours": {
      "reason": "webview-only",
      "why": "Asserts on the object the WebView bridge assigned to navigator.mediaSession.metadata; what the mapping produces is fixtured in media-episode, and what NowPlayingPublisher writes is NE-18's XCTest. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "the artwork really reaches the platform, and falls back to ours when it must": {
      "reason": "webview-only",
      "why": "Asserts on the artwork the WebView bridge assigned; the artwork choice itself is fixtured in media-episode. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "the backend really does read the element for `rate` — the other half of #242's honesty": {
      "reason": "js-module-shape",
      "why": "Text scan of html-audio-backend.js's `get rate()` reading the HTMLAudioElement; AVDeck has no element (NE-15); native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "the bridge forwards the platform's details, it does not swallow them": {
      "reason": "webview-only",
      "why": "The WebView bridge forwards MediaSession action details; the details' meaning is fixtured in media-episode through media-actions. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "the bridge installs handlers and reports which ones landed": {
      "reason": "webview-only",
      "why": "setActionHandler registration on the WebView session; the native command set is commandAvailability (NE-12s), fixtured through media-actions. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    },
    "the episode surface is installed INSIDE play(), for the same reason": {
      "reason": "js-module-shape",
      "why": "Text scan: the episode surface is installed inside ForayPlayer.play; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "the finished state reaches the bridge too, so a done Foray clears the slot": {
      "reason": "js-module-shape",
      "why": "Text scan: both syncMediaSession branches pass `ended`; the mapping's ended rule is fixtured in media-episode; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "the lock screen is repainted from the same render tick the page is": {
      "reason": "js-module-shape",
      "why": "Text scan: syncMediaSession runs on the page's render tick; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "the lock screen's play/pause is the same call the in-page button makes": {
      "reason": "js-module-shape",
      "why": "Text scan: client.js's lock-screen play/pause call the same setRunning the in-page button does; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "the lock screen's previous/next are the page's own segment controls": {
      "reason": "js-module-shape",
      "why": "Text scan: client.js's forayMediaSurface calls forayNext/forayPrevious/foraySeek; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "the module itself never reads a global — it is pure, and that is what makes it testable": {
      "reason": "js-module-shape",
      "why": "Text scan: media-session.js reads no browser global; the Swift core's purity is pinned by shell-invariants (NE-01), not by this; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "the rate client.js reports is the ELEMENT's, not the speed the listener chose": {
      "reason": "js-module-shape",
      "why": "Text scan: client.js reports the element's rate, not the chosen one; the engine reports its true rate (plan §4.5) through the snapshot the media-episode cases take as input; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "the seam-beat state reaches the bridge from the manager, not from a guess": {
      "reason": "js-module-shape",
      "why": "Text scan: client.js reads inSeamGap from the manager; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "this module is TEXT to every search tool: no raw NUL byte anywhere in it": {
      "reason": "js-module-shape",
      "why": "Byte scan: no player/*.js carries a raw NUL, which makes grep treat a source file as binary; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "update writes metadata, playback state and position": {
      "reason": "webview-only",
      "why": "The bridge's write of metadata, playbackState and setPositionState to navigator.mediaSession. createMediaSession wraps navigator.mediaSession inside the WebView; in native mode the page writes no navigator.mediaSession (plan §3 A-2) and NowPlayingPublisher/RemoteSurface talk to MPNowPlayingInfoCenter and MPRemoteCommandCenter through the NE-18 seams, whose recording fakes carry their own XCTests."
    }
  },
  "playback-rate": {
    "a junk or stale stored row is snapped on the way OUT, not trusted": {
      "reason": "webview-only",
      "why": "cp_rate is read and written by the page through readRate/writeRate (plan §5.2: setRate — the page still writes cp_rate); the engine receives the rate as a number through setRate, and the snap it applies is the rate family's normalizeRate cases."
    },
    "a store that throws is reported, never thrown out of — a refused write is not a refused speed": {
      "reason": "webview-only",
      "why": "cp_rate is read and written by the page through readRate/writeRate (plan §5.2: setRate — the page still writes cp_rate); the engine receives the rate as a number through setRate, and the snap it applies is the rate family's normalizeRate cases."
    },
    "a stored speed round-trips": {
      "reason": "webview-only",
      "why": "cp_rate is read and written by the page through readRate/writeRate (plan §5.2: setRate — the page still writes cp_rate); the engine receives the rate as a number through setRate, and the snap it applies is the rate family's normalizeRate cases."
    },
    "an off-ladder value is snapped on the way IN too, so no junk is ever stored": {
      "reason": "webview-only",
      "why": "cp_rate is read and written by the page through readRate/writeRate (plan §5.2: setRate — the page still writes cp_rate); the engine receives the rate as a number through setRate, and the snap it applies is the rate family's normalizeRate cases."
    },
    "no label ever reads 1.5000000000000002×, or 'undefined×'": {
      "reason": "dom-only",
      "why": "The rate button's text is page DOM in both engine modes (plan §3 A-1: the page owns the UI); the engine carries the rate as a number, never this label. The snap a junk value labels as is normalizeRate's, which the rate family pins."
    },
    "nothing stored is normal speed, not zero": {
      "reason": "webview-only",
      "why": "cp_rate is read and written by the page through readRate/writeRate (plan §5.2: setRate — the page still writes cp_rate); the engine receives the rate as a number through setRate, and the snap it applies is the rate family's normalizeRate cases."
    },
    "the accessible name says the value, because aria-label REPLACES the button text": {
      "reason": "dom-only",
      "why": "Pins the rate button's aria-label sentence; an attribute of page DOM, drawn by the page in both engine modes."
    },
    "the key is exactly cp_rate — renaming it wipes user state": {
      "reason": "webview-only",
      "why": "cp_rate is read and written by the page through readRate/writeRate (plan §5.2: setRate — the page still writes cp_rate); the engine receives the rate as a number through setRate, and the snap it applies is the rate family's normalizeRate cases."
    },
    "the label is written the way podcast apps write it": {
      "reason": "dom-only",
      "why": "The rate button's text is page DOM in both engine modes (plan §3 A-1: the page owns the UI); the engine carries the rate as a number, never this label."
    }
  },
  "position-store": {
    "PositionStore writes positionRow, emits by positionEvent per item, and resumes by resumeOffsetFor": {
      "reason": "js-module-shape",
      "why": "Pins that the JS PositionStore class routes its storage writes, per-item event marks and resume reads through the pure rules; the Swift engine has its own store (EngineStore, NE-19) and shares only the rules, which the resume-rules family carries."
    }
  },
  "queue-manager": {
    "CONTINUE_TAIL appends the part-heard item": {
      "reason": "js-module-shape",
      "why": "queue-strategy.js builds the queue in the page; the engine is handed a built queue and the page's continuation hops (plan §3 A-1: the page owns decisions; §5.5), so there is no strategy for it to reimplement."
    },
    "PICKED_FIRST puts the pick ahead of the other cards, without duplicating it": {
      "reason": "js-module-shape",
      "why": "queue-strategy.js builds the queue in the page; the engine is handed a built queue and the page's continuation hops (plan §3 A-1: the page owns decisions; §5.5), so there is no strategy for it to reimplement."
    },
    "SINGLE_ITEM is the default and yields a one-episode queue": {
      "reason": "js-module-shape",
      "why": "queue-strategy.js builds the queue in the page; the engine is handed a built queue and the page's continuation hops (plan §3 A-1: the page owns decisions; §5.5), so there is no strategy for it to reimplement."
    },
    "a backend with no out-point watch refuses the Foray instead of playing whole episodes": {
      "reason": "js-module-shape",
      "why": "A backend without an out-point watch (setOutPoint undefined), and the manager's refusal to hand it a bounded item. The native engine has one deck type and it always carries the three-layer out-point (outpoint and deck families), so there is no blind backend to refuse."
    },
    "a bounded item reaching a blind backend by any other route refuses too": {
      "reason": "js-module-shape",
      "why": "A backend without an out-point watch (setOutPoint undefined), and the manager's refusal to hand it a bounded item. The native engine has one deck type and it always carries the three-layer out-point (outpoint and deck families), so there is no blind backend to refuse."
    },
    "a malformed strategy is rejected at construction, not at play time": {
      "reason": "js-module-shape",
      "why": "queue-strategy.js builds the queue in the page; the engine is handed a built queue and the page's continuation hops (plan §3 A-1: the page owns decisions; §5.5), so there is no strategy for it to reimplement."
    },
    "a second manager cannot be constructed while one is alive": {
      "reason": "js-module-shape",
      "why": "PlayerQueueManager's module-level singleton guard (corner case #19's first layer). The engine is one @MainActor ForayEngine per process by construction (plan §4.2, A-4); the single-audible half of #19 is fixtured in manager-episode (double-skip-with-slow-loads-plays-once)."
    },
    "a surface that throws in onSeamGapChange cannot break playback": {
      "reason": "js-module-shape",
      "why": "A page callback (onSeamGapChange) that throws. In native mode the page is told about the beat through the snapshot's inSeamGap and the engine event, across the bridge; no page code runs inside the engine's seam path."
    },
    "an unbounded queue still works on a backend with no out-point watch": {
      "reason": "js-module-shape",
      "why": "A backend without an out-point watch (setOutPoint undefined), and the manager's refusal to hand it a bounded item. The native engine has one deck type and it always carries the three-layer out-point (outpoint and deck families), so there is no blind backend to refuse."
    },
    "cold-launch restore of a Foray on a blind backend makes no sound either": {
      "reason": "js-module-shape",
      "why": "A backend without an out-point watch (setOutPoint undefined), and the manager's refusal to hand it a bounded item. The native engine has one deck type and it always carries the three-layer out-point (outpoint and deck families), so there is no blind backend to refuse."
    },
    "disposing during a beat leaves no timer alive": {
      "reason": "js-module-shape",
      "why": "PlayerQueueManager.dispose(), the JS object's teardown (tests, a page reload). EngineCore lives for the process; its beat is cut by stop and every transport action, which manager-foray/stop-during-a-beat and the transport-during-a-beat cases pin with timersLive 0."
    },
    "the beat is observable in telemetry, both when it fires and when it is cut": {
      "reason": "text-pin",
      "why": "Pins the English telemetry lines seam.gap.armed / hold / cut. The beat's arming, hold and cut are fixtured by their effects (manager-foray/an-unbridged-seam-holds-the-full-beat, pause-during-a-beat); the engine's rows use the diag-tokens vocabulary."
    },
    "the manager adds no macrotask between a settled load and play()": {
      "reason": "webview-only",
      "why": "WebKit's user-gesture indicator is task-scoped, so a timer between the load and play() strands an autoplay start. The native engine starts audio from native code, where no autoplay gesture exists to lose."
    }
  },
  "queue-state": {
    "an unknown event is ignored with telemetry rather than throwing": {
      "reason": "js-module-shape",
      "why": "Pins reduce()'s default branch for an event object whose type is outside the closed set. Swift's Event is a closed enum switched exhaustively, so an unknown event is unrepresentable; the engine's own unknown-input path is the contract family's unknown-cmd (NE-11j)."
    },
    "reduce never mutates the state it is given": {
      "reason": "js-module-shape",
      "why": "Pins that the JS reducer leaves its argument object untouched. The Swift PlayerQueueState is an enum value type passed by value, so a reducer that mutates its input cannot be written; every transition's output is pinned by the queue-state family instead."
    }
  },
  "seam-gap": {
    "every refusal explains itself differently": {
      "reason": "text-pin",
      "why": "Pins that describeSeam's four refusal sentences are distinct English strings; the engine's refusal reasons are closed tokens (the diag-tokens family, NE-04)."
    },
    "the beat's log line names both sides and the length": {
      "reason": "text-pin",
      "why": "Pins describeSeam's English telemetry sentence. The engine writes packed seam rows from the diag-tokens vocabulary, not this string."
    }
  },
  "seek-policy": {
    "a padded timestamp still renders as an estimate, never as a clock time": {
      "reason": "text-pin",
      "why": "Pins formatTimestamp's wording for a PADDED time. That a padded segment is playable but never exact is fixtured (seek-policy/can-play-padded-not-exact). The page renders every timestamp a listener sees (chapter lists, the scrubber, spoken answers). The engine carries seconds and a precision token (seekPrecision, fixtured) and never formats one, so this wording has no Swift meaning."
    },
    "approximate rendering contains no banned copy words": {
      "reason": "text-pin",
      "why": "A copy-rules scan (CLAUDE.md) over rendered strings. The page renders every timestamp a listener sees (chapter lists, the scrubber, spoken answers). The engine carries seconds and a precision token (seekPrecision, fixtured) and never formats one, so this wording has no Swift meaning."
    },
    "describeTimestamp never implies precision it does not have": {
      "reason": "text-pin",
      "why": "Pins describeTimestamp's prose (\"at 1:07:30\", \"around minute 68\"). The page renders every timestamp a listener sees (chapter lists, the scrubber, spoken answers). The engine carries seconds and a precision token (seekPrecision, fixtured) and never formats one, so this wording has no Swift meaning."
    },
    "formatTimestamp marks approximate times and rounds away false precision": {
      "reason": "text-pin",
      "why": "Pins formatTimestamp's \"~N min\" wording. The page renders every timestamp a listener sees (chapter lists, the scrubber, spoken answers). The engine carries seconds and a precision token (seekPrecision, fixtured) and never formats one, so this wording has no Swift meaning."
    },
    "formatTimestamp refuses junk instead of rendering NaN": {
      "reason": "text-pin",
      "why": "Pins the \"--:--\" placeholder string. The page renders every timestamp a listener sees (chapter lists, the scrubber, spoken answers). The engine carries seconds and a precision token (seekPrecision, fixtured) and never formats one, so this wording has no Swift meaning."
    },
    "formatTimestamp renders exact times to the second": {
      "reason": "text-pin",
      "why": "Pins formatTimestamp's clock strings. The page renders every timestamp a listener sees (chapter lists, the scrubber, spoken answers). The engine carries seconds and a precision token (seekPrecision, fixtured) and never formats one, so this wording has no Swift meaning."
    }
  },
  "transport-policy": {
    "client.js asks these rules and keeps no private copy of one": {
      "reason": "js-module-shape",
      "why": "Pins that player/client.js imports the transport rules and declares no copy of them; the Swift engine has no client.js, and its TransportPolicy port is checked by the transport family instead."
    }
  },
  "transport-reconcile": {
    "AUDIT / ROUND 2: a FINISHED episode leaves Jump back in and the ribbon; a part-heard one stays": {
      "reason": "dom-only",
      "why": "Jump back in and the ribbon for a finished episode are the page's decision (plan §3 A-1: the page owns the 'Jump back in' math). Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "AUDIT: the head unit's STOP pauses and leaves every other control working": {
      "reason": "webview-only",
      "why": "The head unit's STOP reaching client.js's remoteStop through navigator.mediaSession; natively stopCommand is registered and disabled and a remote stop is a pause (plan §4.5, T-7), NE-12s's commandAvailability. In native mode the page writes no navigator.mediaSession (plan §3 A-2); NowPlayingPublisher and RemoteSurface write from the engine's own state through the NE-18 seams, whose recording fakes carry their own XCTests, and the mapping rule itself is fixtured in media-episode."
    },
    "AUDIT: the lock screen says PLAYING while sound is coming out": {
      "reason": "webview-only",
      "why": "The lock screen's playbackState/rate while sound is coming out, as the page writes it. In native mode the page writes no navigator.mediaSession (plan §3 A-2); NowPlayingPublisher and RemoteSurface write from the engine's own state through the NE-18 seams, whose recording fakes carry their own XCTests, and the mapping rule itself is fixtured in media-episode."
    },
    "AUDIT: ↺ and ↻ move a restored ribbon too, and ↻ never crosses the end": {
      "reason": "dom-only",
      "why": "The restored ribbon's ↺/↻ nudges move a PENDING position the page holds with nothing loaded in any engine; the press that follows carries it as playEpisode's startSec (native-facades.test.js). Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "REPORT 1: a LATE interruption never pauses audio the listener has since resumed": {
      "reason": "webview-only",
      "why": "A suspended WebView handles the plugin's session event when it wakes, so an interruption can reach the page after the listener resumed, and the page must ask the element rather than obey. The engine observes AVAudioSession.interruptionNotification live (plan §4.4), where began means the session was taken now; the reconcile that does run natively is fixtured in manager-episode (an-unseen-stop-is-corrected-once-and-one-press-resumes)."
    },
    "REPORT 1: an element resumed from OUTSIDE the reducer still has its position written": {
      "reason": "webview-only",
      "why": "WebKit's own media session can resume an <audio> element without asking the page, so the page writes the position from the element's timeupdate. Natively every lock-screen and car press is an EngineInput (plan §4.5, the remote seams), so the deck never plays behind the core's back; the core's position cadence while playing is fixtured in manager-episode (position-events-*)."
    },
    "REPORT 1: the machine is corrected TOWARDS playing when the element plays": {
      "reason": "webview-only",
      "why": "The same out-of-band resume: WebKit's media session starting the element with the reducer saying interrupted. Natively no press bypasses the core (plan §4.5), so the deck is only ever started by a core command; the core's foreground reconcile still corrects towards playing on the deck's own word."
    },
    "REPORT 2: an unexplained pause says what state the element was in": {
      "reason": "webview-only",
      "why": "The fingerprint is HTMLMediaElement's own fields (currentTime, readyState, networkState, error.code). AVPlayer has none of them: the deck reports an uncommanded pause with its playhead (DeckEvent.pausedUncommanded atSec, AVDeckTests' testAnExternalPauseBecomesAReconcileInput) and the core writes the stop row with its cause."
    },
    "REPORT 3: a booted shell writes BOTH halves of the build into the record it will copy out": {
      "reason": "js-module-shape",
      "why": "The page's diagnostic record stamping both halves of the build (web and CFBundleVersion). The page keeps doing it in native mode; the engine writes its own header line into the Copy (NE-26), so there is no page rule for Swift to reimplement."
    },
    "REVIEW: a Foray page opened over the restored bar hears the bar's press": {
      "reason": "dom-only",
      "why": "A Foray page opened over the restored bar hearing the bar's press is page wiring between two page surfaces. Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "REVIEW: a play the browser held back keeps its 'Press play again' line after app.js reports the refusal": {
      "reason": "webview-only",
      "why": "Safari holding back a play() for want of a gesture ('Press play again'). An AVPlayer needs no gesture — the backend facade's notePlayGesture is a no-op — so native mode has no held-back play to keep a line for."
    },
    "REVIEW: the Android notification's own Stop (`close`) still closes a PAUSED player": {
      "reason": "webview-only",
      "why": "Android's notification close. Android keeps the JS player and the native engine is iOS only (plan §11, J-1), so there is no Swift rule here."
    },
    "REVIEW: the page's neighbours are asked about the episode that is NOW current": {
      "reason": "js-module-shape",
      "why": "Which episode client.js asks its continuation neighbours about. In native mode the page still computes the chain itself (continuation.js, a JS-only family, plan §5.5 C-2) and hands it over as setContinuation; the engine only walks it, so it has no neighbour rule to reimplement."
    },
    "ROUND 2 a11y-7: a slider that has FOCUS is not rewritten every tick": {
      "reason": "dom-only",
      "why": "A focused slider is not rewritten every tick (a11y). Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "ROUND 2 copy-5 / player-10: a RESTORED Foray reads the same on the bar and the lock screen, and its clip follows a scrub": {
      "reason": "js-module-shape",
      "why": "Text scan: the page's mediaViewFields reads the restored Foray and the clip under the pending position; native mode replaces this page wiring wholesale (plan §5.6: syncMediaSession returns early and the lock-screen surfaces are not installed), so there is no rule here for the engine to reimplement."
    },
    "ROUND 2 copy-5: the live Foray bar's second line is the show and the clip, with no 'Now:'": {
      "reason": "dom-only",
      "why": "The live Foray bar's second line. Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "ROUND 2 honesty-13: elapsed and remaining add up, and the countdown keeps its sign until it shows nothing": {
      "reason": "dom-only",
      "why": "Elapsed and remaining add up and the countdown keeps its sign. Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "ROUND 2 p-car-3: reconciling an interruption does not tell an already-paused element to pause again": {
      "reason": "webview-only",
      "why": "WebKit records a script pause() made during an interruption as the state to restore, so the OS's own resume then resumes nothing. AVPlayer keeps no such memory: the engine resumes by its own command after interruptionEnded (manager-episode interruption-resume-steps-back-in-place)."
    },
    "ROUND 2 p-car-6: a finished EPISODE stays on the lock screen paused; a finished Foray still clears it": {
      "reason": "webview-only",
      "why": "A finished episode left on the lock screen paused and a finished Foray cleared, as the page writes it; natively NowPlayingPublisher clears only on a finished Foray, close or data deletion (plan §4.5). In native mode the page writes no navigator.mediaSession (plan §3 A-2); NowPlayingPublisher and RemoteSurface write from the engine's own state through the NE-18 seams, whose recording fakes carry their own XCTests, and the mapping rule itself is fixtured in media-episode."
    },
    "ROUND 2 p-car-8: a stall is reported to the OS at rate 0, with the state still playing": {
      "reason": "webview-only",
      "why": "A stall reported to the OS at rate 0 with the state still playing, as the page writes it. In native mode the page writes no navigator.mediaSession (plan §3 A-2); NowPlayingPublisher and RemoteSurface write from the engine's own state through the NE-18 seams, whose recording fakes carry their own XCTests, and the mapping rule itself is fixtured in media-episode."
    },
    "ROUND 2 perf-7: a control's accessible name is not rewritten to itself on every tick": {
      "reason": "dom-only",
      "why": "A control's accessible name is not rewritten to itself every tick. Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "ROUND 2 perf-7: the OS position is written once a second, not four times": {
      "reason": "webview-only",
      "why": "The OS position written once a second by the page's dedupe. In native mode the page writes no navigator.mediaSession (plan §3 A-2); NowPlayingPublisher and RemoteSurface write from the engine's own state through the NE-18 seams, whose recording fakes carry their own XCTests, and the mapping rule itself is fixtured in media-episode."
    },
    "ROUND 2 player-6: dragging the scrubber moves the clocks with the thumb, not the audio": {
      "reason": "dom-only",
      "why": "Dragging the scrubber moves the clocks with the thumb. Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "ROUND 2 player-7: a Foray clip that will not load says so on the bar and in the sheet": {
      "reason": "dom-only",
      "why": "The copy on the bar and in the sheet for a Foray clip that will not load; the engine's side is its skipped event and lastError (manager-foray). Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "ROUND 2 player-9: the speed button is named for the menu it opens": {
      "reason": "dom-only",
      "why": "The speed button's accessible name. Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "ROUND 2 review (touch-10): the sheet draws a chapter token as a 44px .ep-chapter-row that seeks, not an inline stamp": {
      "reason": "dom-only",
      "why": "The sheet's chapter token drawn as a 44px row. Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "ROUND 2 review: a slider focused by a POINTER keeps following the audio": {
      "reason": "dom-only",
      "why": "A slider focused by a pointer keeps following the audio. Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "SWEEP: Jump back in's Foray rows read the LIVE runtime, as the Foray page's resume does": {
      "reason": "dom-only",
      "why": "Jump back in's Foray rows read the live runtime: the page's A-1 math (plan §3), fed by the page's own resolve. Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "SWEEP: a speed the listener chose before hydration landed is not overruled by the store": {
      "reason": "js-module-shape",
      "why": "Which speed wins when the listener chose one before cp_rate hydrated. The page still owns cp_rate (plan §5.2) and sends setRate with the winner; the engine has no store of its own to be overruled by."
    },
    "SWEEP: the lock screen shows a RESTORED bar's position, not the empty element's 0:00": {
      "reason": "webview-only",
      "why": "The lock screen's position for a RESTORED bar as the page writes it; natively the cold path paints Now Playing from the engine's restore record without activation (plan §4.5, NE-24). In native mode the page writes no navigator.mediaSession (plan §3 A-2); NowPlayingPublisher and RemoteSurface write from the engine's own state through the NE-18 seams, whose recording fakes carry their own XCTests, and the mapping rule itself is fixtured in media-episode."
    },
    "THE #267 WINDOW IS REAL: every reconcile guard passes inside the refusal recovery": {
      "reason": "webview-only",
      "why": "#267: the two-element HtmlAudioBackend's autoplay-refusal recovery (a WebKit per-element NotAllowedError) with the parked prefetch on. AVPlayer has no autoplay policy, so native mode has no refusal recovery for a reconcile to fall inside (plan §4.3); the engine's own prepare is DeckPair's (NE-32)."
    },
    "VISUAL PASS: previous/next clip are their own labelled row, shown only while a Foray is loaded": {
      "reason": "dom-only",
      "why": "The previous/next clip row's label and visibility. Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "VISUAL PASS: the mini bar's ↺15 is the same nudge, for an episode and for a Foray": {
      "reason": "dom-only",
      "why": "The mini bar reusing the sheet's ↺15 handler is page wiring; the nudge itself is a seekBy the engine performs (manager-foray / transport families). Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "a transition in flight is not a lie: reconciling mid-effect cannot stop a start": {
      "reason": "js-module-shape",
      "why": "JS `_handle` sets the state and then awaits its effects, so a reconcile can arrive between `playing` and startPlayback (the `_applying` guard). EngineCore.handle is one synchronous turn: no input can land between a state and the commands it emits."
    },
    "an ESTIMATED Foray runtime is hedged in the Now Playing countdown and the slider's 'of' (states-11)": {
      "reason": "dom-only",
      "why": "The hedge on an ESTIMATED Foray runtime in the countdown and the slider's 'of'. Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "an element that does not model paused reads as NOT paused, never as paused": {
      "reason": "webview-only",
      "why": "Duck-typing an HTMLMediaElement that does not model `paused`. The native deck is an AVPlayer whose timeControlStatus always answers (NE-15), so there is no element that can fail to model it."
    },
    "audio must NOT start after that reconcile, and the queue must not strand (#267)": {
      "reason": "webview-only",
      "why": "#267: a reconcile racing the WebKit autoplay-refusal recovery of the two-element HtmlAudioBackend. AVPlayer has no autoplay policy and native mode has no page-side reconcile at all (attach only reads, native-facades.test.js), so the race does not exist there."
    },
    "loading an already-paused element arms nothing either": {
      "reason": "webview-only",
      "why": "The `_expectPause` token exists because an <audio> element fires `pause` only when it was playing, so a pause() on a paused element (a redundant pause, a load) must arm nothing. AVDeck keeps no token: an uncommanded stop is read from intendsToPlay, readiness, the end and the player's rate (AVDeckTests' testTheUncommandedPauseRule), so there is nothing a redundant pause or a load could leak."
    },
    "re-pausing an already-paused element arms nothing, so the NEXT real pause survives": {
      "reason": "webview-only",
      "why": "The `_expectPause` token exists because an <audio> element fires `pause` only when it was playing, so a pause() on a paused element (a redundant pause, a load) must arm nothing. AVDeck keeps no token: an uncommanded stop is read from intendsToPlay, readiness, the end and the player's rate (AVDeckTests' testTheUncommandedPauseRule), so there is nothing a redundant pause or a load could leak."
    },
    "the sheet shows ⏭, Up Next (N) and Save from the page's episode surface, and each does the page's own thing (audit round 2, p-impatient-7 / p-switcher-5)": {
      "reason": "dom-only",
      "why": "The sheet's ⏭, Up Next (N) and Save read the page's own episode surface. Page painting that client.js does identically in both modes from what the manager surface answers (in native mode, the facades); the engine never draws the page, so there is nothing here for Swift to reimplement."
    },
    "AUDIT: \"Next clip\" is disabled on the last segment instead of silently doing nothing": {
      "reason": "dom-only",
      "why": "client.js render() disables the sheet's \"Next clip\" button when the playhead's index is the Foray's last; in native mode the index is the engine snapshot's (NE-35) and the page paints it the same way. The engine's own refusal of a next past the end is ForayTapeTests/testAForayNeverChains; the button's state is page painting with nothing for Swift to reimplement."
    },
    "PERSONA: an episode played AFTER the Foray keeps the bar": {
      "reason": "dom-only",
      "why": "lastPlayedForay() is the page choosing which bar to paint at boot, by comparing the updated_at stamps of the cp_foray and cp_last_episode rows. In native mode the page makes the same choice over the rows the engine writes in the same format (the rows family, cp-foray.json); the engine never decides what the page's bar shows."
    }
  },
  "tts-bridge": {
    "inShell: false in a plain browser tab, true when Capacitor injected a bridge": {
      "reason": "webview-only",
      "why": "Whether the page runs inside the Capacitor shell (window.Capacitor present), read from the live window. The engine is the shell; it never asks whether it is one."
    },
    "inShell: reads the ambient window when called with no argument": {
      "reason": "webview-only",
      "why": "Whether the page runs inside the Capacitor shell (window.Capacitor present), read from the live window. The engine is the shell; it never asks whether it is one."
    },
    "the shell tries the bundle's flattened copy first; the website tries the plugin's own path first": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration."
    },
    "the candidate order is chosen from the live window, not baked in at import": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration."
    },
    "speak: the module is resolved once per bridge, however many lines it speaks": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration."
    },
    "speak: a candidate that 404s falls through to the next one": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration."
    },
    "speak: nothing loadable at all resolves { ok: false } and never rejects": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration. What the manager does with a line nothing can speak is fixtured (manager-foray/a-script-with-no-synthesiser-fails-cleanly)."
    },
    "speak: a module with no speak() is a miss, not a crash": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration."
    },
    "a throwing logger must not break the failure path": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration."
    },
    "onFinished: returns an unsubscribe function immediately, before the module resolves": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration. The subscription races the module's load; natively the narrator's delegate is wired at construction."
    },
    "onFinished: a module with no onFinished() is a silent miss, not a crash": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration. What the manager does without a finished event is fixtured (manager-foray/a-bridge-without-finished-never-auto-advances)."
    },
    "onFinished: unsubscribing before the module resolves prevents the late subscription": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration. The subscription races the module's load; natively the narrator's delegate is wired at construction."
    },
    "onFinished: unsubscribing after the module resolves removes the real listener": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration. The subscription races the module's load; natively the narrator's delegate is wired at construction."
    },
    "onFinished: a non-function argument is a no-op, not a crash": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration. A typed Swift callback cannot be a non-function."
    },
    "listVoices: forwards options to the module and returns its answer": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration. The voice list itself is SpeechRules' (NE-33: resolveVoice, bestVoice, candidates) and the default-voice family's."
    },
    "listVoices: an older module without the method reports that, rather than throwing": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration."
    },
    "listVoices: an unreachable module resolves { ok: false }, never rejects": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration."
    },
    "client.js actually passes a TTS bridge into the queue manager": {
      "reason": "js-module-shape",
      "why": "client.js's wiring of the page's shared bridge instance (into the queue manager, and the Kokoro probe through the same instance). In native mode the engine owns its narrator; the page's wiring is pinned here and by native-mode.test.js, not reimplemented."
    },
    "kokoroProbe delegates to the module, through the same memoised load": {
      "reason": "js-module-shape",
      "why": "The developer-only Kokoro probe's delegate on the page's bridge (kokoroProbe through the memoised module). It is a ForayTts plugin method the page calls; the engine neither probes nor exposes it."
    },
    "an older shell build — a module with no kokoroProbe — answers engine-absent": {
      "reason": "js-module-shape",
      "why": "The developer-only Kokoro probe's delegate on the page's bridge (kokoroProbe through the memoised module). It is a ForayTts plugin method the page calls; the engine neither probes nor exposes it."
    },
    "no module at all is no-bridge, and the probe still resolves": {
      "reason": "js-module-shape",
      "why": "The developer-only Kokoro probe's delegate on the page's bridge (kokoroProbe through the memoised module). It is a ForayTts plugin method the page calls; the engine neither probes nor exposes it."
    },
    "client.js runs the probe through the SAME shared bridge instance": {
      "reason": "js-module-shape",
      "why": "client.js's wiring of the page's shared bridge instance (into the queue manager, and the Kokoro probe through the same instance). In native mode the engine owns its narrator; the page's wiring is pinned here and by native-mode.test.js, not reimplemented."
    },
    "a transport call before anything has spoken does NOT load the module": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration."
    },
    "an older shell's module — a speak() and no pause() — is reported, not thrown": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration. What the manager does with a bridge that has no transport is fixtured (manager-foray/l05-no-transport-still-pauses)."
    },
    "state() is idle, without a fetch, until something has spoken": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration."
    },
    "a module that will not load leaves a transport call resolved, never rejected": {
      "reason": "js-module-shape",
      "why": "tts-bridge.js is the page's loader for the ForayTts plugin's web module (a dynamic import over candidate paths, memoised per bridge, answering { ok: false } rather than rejecting). The native engine narrates through its own SpeechNarrator in the same binary (plan §4; NE-33): there is no module to find, load, memoise or version-skew, so this is a rule about the JS module, not about narration."
    }
  }
}
