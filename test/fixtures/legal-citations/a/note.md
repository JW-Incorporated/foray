# Fixture: sibling-resolution note (a)

Exists only so `resolveFile`'s sibling-document-relative strategy has two
same-named, non-ambiguous-by-directory files to disambiguate between. See
`test/legal-citations.test.js`'s "sibling-document-relative" fixture test.
