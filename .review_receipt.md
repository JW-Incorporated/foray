[review-receipt] APPROVE reviewer=claude-opus base=origin/main findings=2 fixed

Fresh-context Opus review (~/.codex/review-claude.sh, run twice - once before and once after fixes):

Round 1 findings (both fixed):
- P2: Playlists for you read state.cardSlots before it was guaranteed built on first Home v2 paint (fixed: hoisted buildCards() call to the top of renderHomeV2(), matching v1's own renderHome()).
- P3, addressed: playlistsForYouHtml's sort could throw on a playlist missing both last_played_at and created (defensive fallback added).
- P3, fixed as a real convention violation: the foray Jump back in card was wired through bindPickLogging's episode-shaped pick channel (data-ev/data-ep/data-ctx) with a foray id - the flag-off jumpBackInHtml/forayListHtml never do this; removed to match.

Round 2 (final diff): No blocking findings. Remaining P3 nits (fragile regex-based bridge-line insertion in miniCardV2, id-encoding asymmetry across route types, and the documented always-at-least-one-tier-needed limit of pickWithStretchFloor) are either already matching pre-existing repo convention (verified against renderHome/bannerHtml's own esc()-only vs esc(encodeURIComponent())-per-route pattern) or intentional/documented behavior with test coverage.
