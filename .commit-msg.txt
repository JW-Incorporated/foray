Stage 2 fix: guard episode-page fallback against dead links

Codex review caught: the "Listen in your podcast app" fallback in
renderEpisode() rendered unconditionally for any item with no
audio_url, so a resolved episode/snapshot lacking apple_collection_id
also lacking apple_episode_url produced a link to
".../idundefined" -- a visibly actionable dead link.

Guard it exactly like archivedRow already does: only render the
fallback when item.apple_collection_id exists. Added a test pinning
that no dead link (or any play affordance) renders in that case.

node --check app.js: pass
node --test test/*.test.js: 425/425 pass
