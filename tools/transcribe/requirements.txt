# Foray transcription environment — T1 spike (issue #116)
#
# Verified 2026-08-11 on Windows 11 / Python 3.14.5 / CPU-only.
# Install with:  python -m pip install -r requirements.txt
#
# GPU note: this file is device-agnostic for NVIDIA. faster-whisper's
# `ctranslate2` wheel on PyPI ships BOTH the CPU and the CUDA runtime for
# Windows/Linux x86_64 — you do NOT install a different package for CUDA,
# you pass device="cuda". You do need NVIDIA cuBLAS + cuDNN present; see
# README.md.
#
# AMD GPU note: CTranslate2 (the engine faster-whisper is built on) has NO
# AMD backend at all — no ROCm, no Vulkan. None of the packages below will
# use an AMD GPU no matter what --device flag you pass. `bench.py` on an
# AMD card silently runs CPU-speed math while still installing and
# "working" — nothing here errors to tell you. See README.md §3b for the
# separate AMD/Vulkan path (`bench_whispercpp.py`, a different engine
# entirely, no new Python packages needed beyond `av` which is already
# pinned below).

faster-whisper==1.2.1

# --- pinned transitive deps, exactly what was verified ---
# (installing faster-whisper alone will resolve these; pinned here so a
#  rebuild six months from now reproduces the measured numbers)
av==18.0.0
ctranslate2==4.8.1
huggingface_hub==1.27.0
onnxruntime==1.28.0
tokenizers==0.23.1
tqdm==4.70.0

# Second-order deps present in the verified environment:
# anyio==4.14.2  certifi==2026.7.22  click==8.4.2  colorama==0.4.6
# filelock==3.32.2  flatbuffers==25.12.19  fsspec==2026.7.0  h11==0.16.0
# hf-xet==1.6.0  httpcore==1.0.9  httpx==0.28.1  idna==3.18  numpy==2.5.2
# packaging==26.3  protobuf==7.35.1  PyYAML==6.0.3  setuptools==84.0.0
# typing_extensions==4.16.0
