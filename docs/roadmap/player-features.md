# Package PQ — player features queued behind the native engine (#762 Up Next tools, #691 autoplay tail, #30 bookmarks, #29 downloads, #761 new-episode alerts) — revision 2

Written 2026-09-25 against `origin/main` @ `ecb6bfa3` (main moved since revision 1's `0b2b8f92`; every path, function, line and count below was re-verified at `ecb6bfa3` with `git show origin/main:<path>` / `git grep`). The local checkout on the planning machine is BEHIND origin/main, so every executor must `git fetch origin` and branch from `origin/main`, never from the local `main`. Line numbers are anchors at `ecb6bfa3`; if a cited function is not within ±40 lines, `git grep -n "function <name>"` and continue — only stop if the function does not exist.

**Reviewer problems addressed in this revision (all 15 accepted, none rejected):** (1) DENIED_PATTERNS make PQ-20/22/28/29 human-merge PRs — §1 and Wave 4 rewritten, six founder merges; (2) PQ-17 event subscription now cites `player/native-engine.js` `listen()` verbatim; (3) PQ-13 names the row2 append test and the "six controls" comment; (4) PQ-02's Play-next disabled rule is one expression with declared inputs; (5) floors are minimums — every count restated from origin/main; (6) PQ-18 Library insertion point named; (7) PQ-18 deletion anchor and ledger test named; (8) PQ-26 names the `visibilitychange` listener; (9) PQ-28 adds the notification-tap route as new work with a JS half and test; (10) PQ-09 verifies `$defs.hop` first, ContractDecoding quote dropped; (11) PQ-11 names the one caller; (12) PQ-16 defines the `positions` shape; (13) count line corrected; (14) HUMAN-ACTIONS heading shape quoted; (15) PQ-13 hedge replaced with verified lines.

## 1. Goal, done-definition, dependencies, open founder questions

**Goal.** Ship the five player features the founder queued behind the native engine, on BOTH the JS player (web + Android, the parity reference) and the iOS native engine, without a second queue model: Up Next tools (drag reorder, Play next, swipe-to-remove, Clear), the "then more of what fits" tail of continuous playback, bookmarks, a download manager with an offline store, and new-episode alerts for followed shows.

**Done when:** (1) every task's suite is green and floored, `node tools/ci/run-suites.mjs` passes, and each PR opened as DRAFT has been merged (the six human-merge PRs by the founder); (2) on the founder's iPhone, with the native engine on, all five behave as the JS player does (the device checks named in PQ-24 and PQ-30 are recorded in HUMAN-ACTIONS.md); (3) the three privacy-policy §1 rows the new `cp_` keys need are in `docs/legal/privacy-policy.md` and `test/data-deletion.test.js` counts them (its "the shipped source names exactly the N cp_ key families" test at line 443 is red until they are — that is the mechanism, not a nuisance).

**The one architectural fact that shapes everything.** Under the shipped contract (`docs/native-engine-plan.md` §3 A-1, §5.5; `player/engine-contract.js` `COMMANDS`; `player/continuation.js` header), the PAGE owns Up Next (`cp_queue`), lists, bookmarks and every *decision*; the engine only walks the K=8 `setContinuation` hops the page precomputes. So #762's "once the native engine owns the queue" is already answered: the engine never will. Up Next tools, the tail and bookmarks are JS-only work with ONE iOS obligation — every edit re-sends the plan (already automatic: `saveQueueIds` (app.js 2879) → `refreshEpisodeNavigation` (3374) → `sendContinuation` (3408)), pinned by new tests. Only downloads (a `file://` URL into `AVDeck.load`) and alerts (background fetch + local notifications) need Swift/Java, and those are the opus tasks.

**Dependencies.**
- Native engine deck M2–M4 (`docs/native-engine-plan.md`): none of these tasks depends on M2+. PQ-19 (play a local file on the native lane) depends on M1 being the iOS default (it is: `mobile/ENGINE_DEFAULT.json`, DECISIONS 2026-09-24 NE-27d).
- Credentials/spend: none for the default path. A push provider (APNs/FCM) is a founder call (Q1) and is NOT planned; the default is on-device background refresh + local notifications, keyless.
- Devices: PQ-24 (downloads) and PQ-30 (alerts) end with a founder phone check, recorded as HUMAN-ACTIONS items.
- **Human-merge paths touched (from `tools/ci/path-policy.mjs`, verified):** DENIED_PREFIXES include `mobile/package.json`, `mobile/package-lock.json`, `tools/mobile/inject-background-audio.mjs`, `tools/mobile/prepare-webdir.mjs`; and DENIED_PATTERNS deny BY FILE NAME at any depth under `mobile/`: `/\.gradle(\.kts)?$/`, `/^Package\.swift$/`, `/\.podspec$|^Podfile$/`, `/^package(-lock)?\.json$/`, `/^\.npmrc$/`, `/^capacitor\.config\.(js|ts|…)$/`. A new plugin cannot exist without its `package.json` (+ `Package.swift` on iOS, `build.gradle` on Android), so **every plugin-creating PR is a human-merge PR: PQ-20, PQ-22, PQ-28, PQ-29, plus the two declaration PRs PQ-21 and PQ-30 — six founder merges in total.** Each opens a HUMAN-ACTIONS item. The `hold` label rule (native-engine-plan §12 line 675: every PR touching `mobile/plugins/*/ios/**` opens with `hold`, removed only after a green `ios-kit` run on the head SHA) still applies to PQ-20 and PQ-28 as a signal; the HUMAN-ACTIONS item tells the founder to merge only after `hold` is off.
- `index.html`'s CSP is `media-src https:`: fine for Android (`https://localhost/_capacitor_file_/…`) and iOS native (`file://` never touches the WebView); a web build never downloads (CORS, #29).

**Open founder questions (only real product/spend/legal calls; each has a default the tasks implement unless overruled).**
1. **Push provider?** Default: NO provider and no server. Alerts come from the phone itself (iOS `BGAppRefreshTask` + `UNUserNotificationCenter`; Android `WorkManager` periodic + a notification channel). Cost: iOS decides when refresh runs (typically hours, never guaranteed); the web only badges. Minutes-fresh push = APNs/FCM keys + a feed-watching server — new spend and credentials, out of this plan.
2. **Download policy default?** Default: manual per-episode downloads only, Wi-Fi only (a "Download over cellular" switch, off), 2 GB cap with LRU eviction that never evicts an episode with an in-progress position, no automatic prefetch of Up Next.
3. **Bookmarks stay on the device?** Default: yes — `cp_bookmarks` is local-only and logs NO new event type (a new `logEvent` type is a privacy-policy §2 change; `test/legal-citations.test.js` enforces it). Sync through `cp_events` is a later card.
4. **Tail mix?** Default: every third tail pick is the stretch subject (≥ 33 %, above the ~30 % floor), the rest round-robin the top subjects of today's deal; the existing "Continuous playback" switch turns the whole chain (list AND tail) off — no second switch.
5. **Alerts default per followed show?** Default: on when you follow, with a per-show "New-episode alerts" switch on the show page.
6. **Privacy-policy wording** for the three new §1 rows and the changed `cp_starred_shows` row: exact text in PQ-14/PQ-23/PQ-27; one HUMAN-ACTIONS `[DECIDE]` item asks him to approve the four sentences (the 2026-09-24 Q7 pattern).
7. **Android stays on the JS player** (DECISIONS 2026-09-25 Q5 leaves it open). Assumed yes; the Java halves (PQ-22, PQ-29) never touch playback so they survive either answer.

## 2. Task table

| id | title | executor | why-opus | depends-on | size |
|---|---|---|---|---|---|
| PQ-01 | `player/queue-order.js`: pure Play-next / Clear / move-to arithmetic + tests | qwen | | — | S |
| PQ-02 | app.js: Play next + Clear Up Next on #/queue and #/episode, wired to PQ-01 | qwen | | PQ-01 | S |
| PQ-03 | `player/queue-drag.js`: pure drag-to-reorder gesture arithmetic (commit on release) | qwen | | — | S |
| PQ-04 | app.js + styles.css: drag handle on Up Next rows bound to PQ-03; `test/up-next-gestures.test.js` | qwen | | PQ-01, PQ-03 | S |
| PQ-05 | `player/queue-swipe.js`: pure swipe-to-remove arithmetic | qwen | | — | S |
| PQ-06 | app.js + styles.css: swipe-to-remove on Up Next rows bound to PQ-05 | qwen | | PQ-04, PQ-05 | S |
| PQ-07 | iOS parity pin: every Up Next tool re-sends `setContinuation` (`test/engine-continuation.test.js`) | qwen | | PQ-02, PQ-04, PQ-06 | XS |
| PQ-08 | Ledger + plan docs for #762; close #762 | qwen | | PQ-07 | XS |
| PQ-09 | `player/continuation.js`: the `tail` input (fixture cases, JS-only family) | qwen | | — | S |
| PQ-10 | `player/tail-fill.js`: build the tail from today's subject deal with the stretch share | qwen | | — | S |
| PQ-11 | app.js: hand the tail to the rules, why-line + announcement, engine plan carries it; comment on #691 | qwen | | PQ-09, PQ-10 | S |
| PQ-12 | `player/bookmarks.js`: `cp_bookmarks` store + precision | qwen | | — | S |
| PQ-13 | client.js sheet "Bookmark" button → page-owned `EPISODE_NAVIGATION.addBookmark` | qwen | | PQ-12 | S |
| PQ-14 | Legal row for `cp_bookmarks` + `test/data-deletion.test.js` count (same branch, before push) | opus | legal wording; auto-merge path with no review window | PQ-13 | XS |
| PQ-15 | app.js episode page: bookmarks list (tap → play-then-seek), remove, approximate label | qwen | | PQ-13 | S |
| PQ-16 | `player/download-store.js`: `cp_downloads` record, transitions, LRU eviction plan, `playSource()` | qwen | | — | S |
| PQ-17 | `player/download-bridge.js`: the web half of ForayDownloads (nativePromise + events), fake-bridge tests | qwen | | PQ-16 | S |
| PQ-18 | app.js: Download control, Library "Downloads" section, cellular switch, Delete-my-data purge | qwen | | PQ-16, PQ-17 | S |
| PQ-19 | client.js + facades: play from the local file, `isLocalFile` seek precision, missing-file degrade | qwen | | PQ-16, PQ-17 | S |
| PQ-20 | iOS `mobile/plugins/foray-downloads` (background URLSession store) — human-merge PR | opus | Swift, device behaviour, URL security, DENIED_PATTERNS (`package.json`, `Package.swift`) | PQ-17 | M |
| PQ-21 | Declare `foray-downloads` in `mobile/package.json` + shell-invariants 3→4 + docs + AppDelegate hook — human-merge PR | opus | DENIED prefixes (`mobile/package.json`, injector) | PQ-20, PQ-22 | XS |
| PQ-22 | Android half of `foray-downloads` (system `DownloadManager`) — human-merge PR | opus | Java, device, DENIED_PATTERNS (`build.gradle`) | PQ-17, PQ-20 | M |
| PQ-23 | Legal rows for `cp_downloads` + the on-disk store (§1, §7) + data-deletion count (on PQ-16's branch) | opus | legal wording | PQ-18, PQ-19 | XS |
| PQ-24 | `docs/downloads-device-check.md` + HUMAN-ACTIONS item: the #29 acceptance drives on a phone | opus | device measurement, founder-facing | PQ-21, PQ-23 | S |
| PQ-25 | `player/show-alerts.js`: new-since watermark, per-show opt-out flag inside the `cp_starred_shows` record | qwen | | — | S |
| PQ-26 | app.js: check followed shows on Library/resume, "N new" badge, show-page alerts switch | qwen | | PQ-25 | S |
| PQ-27 | Legal: `cp_starred_shows` row rewrite (it promises "no notifications") (on PQ-25's branch) | opus | legal wording | PQ-26 | XS |
| PQ-28 | iOS `mobile/plugins/foray-notify` + the notification-tap route (JS half) — human-merge PR | opus | Swift, DENIED_PATTERNS, permission UX, founder Q1 | PQ-26, founder Q1 | M |
| PQ-29 | Android half of `foray-notify`: WorkManager periodic check + channel — human-merge PR | opus | Java, device, DENIED_PATTERNS | PQ-26, PQ-28, founder Q1 | M |
| PQ-30 | Declare `foray-notify`, inject the plist keys, shell-invariants 4→5, alerts device check — human-merge PR | opus | DENIED prefixes, founder-facing | PQ-28, PQ-29, PQ-21 | XS |

**30 tasks: 10 opus (PQ-14, 20, 21, 22, 23, 24, 27, 28, 29, 30), 20 qwen; human-merge PRs: PQ-20, 21, 22, 28, 29, 30 (six founder merges).**

## Standard conventions (every task applies these verbatim)

- Repo: `C:/Users/wjduv/Desktop/Vibe Coding/foray`. Start: `git -C "<repo>" fetch origin` then `git -C "<repo>" -c core.autocrlf=false worktree add ../foray-wt-<task> -b <branch> origin/main` and work ONLY in that worktree (LF tree; never run `format:write` repo-wide; never `git restore`/`git clean`).
- Never commit `deploy-manifest.json` or `data/forays-directory.json` (gitignored build outputs). `sw.js` `BUILD_ID` stays the literal `"unstamped"`.
- **Floors are minimums.** `test/suite-integrity.test.js` asserts `count >= floor` (lines 2125, 2841, 2990). A NEW suite (`player/*.test.js`, `test/*.test.js`, `tools/**/*.test.mjs`) needs a `FLOORS` entry with its top-level `test()` count, in the same PR. When you add N tests to an existing suite, raise its floor by N and append `; <old> -> <new> // PQ-xx (#issue): <what>` to that line's comment. Expect `(current count + N) pass`, where the current counts at `ecb6bfa3` are: `test/up-next-queue.test.js` 21 (floor 20), `test/starred-shows.test.js` 10 (floor 9), `test/episode-page.test.js` 11 (floor 8), `test/engine-continuation.test.js` 6 (floor 6), `test/up-next-autoadvance.test.js` 21 (floor 21), `player/continuation.test.js` 11 (floor 11), `test/library-screen.test.js` 20 (floor 20), `test/drawer-settings-toggle.test.js` 13 (floor 13), `player/now-playing-sheet.test.js` 30 (floor 30). Never delete a test to hit a number.
- Tests that name a Foray read `tools/foray/fixtures/frozen/` (`data/forays.json` there), never live `data/`.
- No new `logEvent("<type>")` strings anywhere (privacy §2 inventory). No new `cp_` key except the three this plan names (`cp_bookmarks`, `cp_downloads`; `cp_starred_shows` only gains fields), each with its legal row. `test/data-deletion.test.js` `keyFamiliesInSource()` (line 76) scans `app.js` (`APP_SRC`, line 44) — so each new key's literal must appear in `app.js`.
- One test process at a time: run `node --test <files>` calls sequentially, never in parallel (the founder has asked for a light machine — no parallel test runs, no sub-agents). Root suites: `node --test "test/*.test.js"`; the CI-equivalent full run is `node tools/ci/run-suites.mjs` (slow; once before the PR).
- Commit messages end with the two trailer lines exactly: `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_017FFM7M73d6sEYQiHroVesz`. Branch `feature/<slug>`; PR opened as DRAFT (`gh pr create --draft --title "<given>" --body-file <file>`), body opens with a 1–2 sentence TL;DR then `---`, ends with `🤖 Generated with [Claude Code](https://claude.com/claude-code)` and the session URL line. Open the PR and STOP.
- Every new test's comment names the one-line mutation that turns it red, and you run that mutation once before committing.
- `app.js` is a classic script tested in `node:vm` harnesses; new page tests copy the harness of the suite named in the task (never import across suites).
- **HUMAN-ACTIONS.md** is `<!-- ha-format: 2 -->`; an item's heading is exactly `## #<N> <emoji> [KIND] <title> (~<time>)` with an uppercase bracketed kind — existing headings use `[BLOCKING]` (🔴), `[UPGRADE]` (🟢) and `[DECIDE]` (🟡) — followed by `**Why:**`, `**Steps:**`, `**Worked if:**`. Use `[DECIDE]` for approvals, `[BLOCKING]` for a merge the founder must do, `[UPGRADE]` for a device check. Run `node --test test/human-actions-integrity.test.js` after adding one; if it rejects the kind, use one it accepts (its source lists them) and say so in the PR.
- **Human-merge PRs** (PQ-20/21/22/28/29/30): `node tools/ci/path-policy.mjs` reporting DENIED paths is expected; the PR body's TL;DR says "human merge — DENIED path: <which>"; a `[BLOCKING]` HUMAN-ACTIONS item asks the founder to merge it (~2 min) and names the order in Wave 4.

## 3. Tasks

### PQ-01 · `player/queue-order.js` — pure Up Next list arithmetic (qwen, S)

**Executor:** qwen. Pure JS, no DOM.

**Context (read first).** `app.js` 2862 `queueIds()`, 2879 `saveQueueIds(ids)` (THE one writer of `cp_queue`), 2960 `addToQueue`, 2978 `removeFromQueue`, 2988 `moveQueueItem(id, dir)`; 3313 `playedFromUpNext(id)` and the Up Next model comment above it (the row you play jumps to the top, nothing else moves — founder 2026-09-24); `player/continuation.js` lines 1–120 (`planAfterEnded`); `player/sheet-drag-dismiss.js` (exported-constants-and-functions module shape with a test beside it); `test/suite-integrity.test.js` `FLOORS`.

**Exact change.**
1. Create `player/queue-order.js` (ES module, header comment citing #762 and the 2026-09-24 model) exporting:
   - `moveTo(ids, id, toIndex)` → new array with `id` at `clamp(toIndex, 0, ids.length-1)`, others in original order; returns `ids` itself (same reference) when `id` is absent or the index is unchanged.
   - `playNextOrder(ids, id, currentId)` → the list with `id` placed directly AFTER `currentId` when `currentId` is in `ids`, else at index 0; `id` is removed from its old place first; if `id === currentId` returns `ids` unchanged (same reference). Inputs are arrays of non-empty strings; anything else is filtered out first (same filter as `queueIds()`).
   - `clearOrder(ids, currentId)` → `[currentId]` when `currentId` is in `ids`, else `[]`.
2. Create `player/queue-order.test.js` (node:test, `assert/strict`).

**Tests to add** (`player/queue-order.test.js`, 8): "moveTo puts the row at the index and keeps every other row in order" (`moveTo(["a","b","c","d"],"d",0)` → `["d","a","b","c"]`; mutation: return `ids`); "moveTo clamps past either end and is a no-op for an unknown id" (`moveTo(list,"d",99)` → same order; same reference for unknown id; mutation: drop the clamp); "Play next inserts after the playing row" (`playNextOrder(["a","b","c"],"c","a")` → `["a","c","b"]`; mutation: insert at 0); "Play next with nothing playing goes to the head" (→ `["c","a","b"]`); "Play next on an id not yet queued adds it" (`playNextOrder(["a"],"z","a")` → `["a","z"]`; mutation: require membership); "Play next on the playing row changes nothing" (same reference); "Clear keeps only the playing row" (`clearOrder(["a","b","c"],"b")` → `["b"]`; `clearOrder(["a"],null)` → `[]`; mutation: return `[]` always); "non-string entries are dropped before any rule runs" (`playNextOrder(["a",7,""],"b","a")` → `["a","b"]`).

**Commands:** `node --test player/queue-order.test.js` → 8 pass; add `"player/queue-order.test.js": 8, // PQ-01 (#762): Play next, Clear and move-to arithmetic` to `FLOORS`; `node --test test/suite-integrity.test.js` → pass; `node --check app.js`.

**Do not touch:** `app.js`, `player/continuation.js`, `styles.css`, `mobile/`.

**Stop and escalate if:** suite-integrity fails for a suite you did not add (stale tree — re-fetch), or a rule cannot be a pure function of arrays.

**Definition of done:** commands green; branch `feature/pq-01-queue-order`; DRAFT PR `feat(up-next): queue-order rules for Play next, Clear and move-to (PQ-01, #762)`.

### PQ-02 · app.js: Play next + Clear Up Next (qwen, S)

**Executor:** qwen.

**Context.** `app.js` 2879 `saveQueueIds`; 2960 `addToQueue` (the `liveEpisode(id)` (3106) refusal, `rememberEpisode` (3069), `logEvent("queued")`), 2978 `removeFromQueue`; 3313 `playedFromUpNext`, 3322 `UP_NEXT_CTX`; 11427 `renderQueue`; 11457 `upNextRow(r, idx, total)` (row markup incl. `data-reorder-up/down`, `data-dequeue`); 11514 `queueButtonFor`, 11526 `afterQueueMove(id, dir, topBefore)`, 11541 `afterQueueRemove(index)`; 11548 `bindUpNextReorder`; 11362 `renderEpisode` (`.ep-actions` holds the play/star/`upNextBtn` controls); 1773 `UP_NEXT_TOGGLE`, 1849 `upNextBtn(id, item)`, 1740 `setToggleLabel`; 6262 `bindUpNext(scope)`; 7146 `announce`; 6659 `focusQuietly`; `player/client.js` 4594 `currentEpisodeId()` and 147 `window.forayContinuation = continuation;` (the bridge pattern). `test/up-next-queue.test.js` (node:vm harness, `mount({seed})`; 21 tests, floor 20; it already stubs `currentEpisodeId` once — reuse that stub shape). `test/modal-and-focus.test.js` line 578 "the Up Next binders run the after-steps (the helpers are not orphans)" (a source scan of the binder shape).

**Exact change.**
1. `player/client.js`: `import * as queueOrder from "./queue-order.js";` beside line 141's continuation import and `window.forayQueueOrder = queueOrder;` directly under line 147. Nothing else in client.js.
2. `app.js`, after `moveQueueItem` (2988):
   - `function queueOrderRules() { const r = window.forayQueueOrder; return r && typeof r.playNextOrder === "function" ? r : null; }`
   - `function currentPlayingId() { try { return window.ForayPlayer?.currentEpisodeId?.() || null; } catch (_) { return null; } }`
   - `function playNextInQueue(id)`: `if (!id || !liveEpisode(id)) return false; const rules = queueOrderRules(); if (!rules) return false; const before = queueIds(); const next = rules.playNextOrder(before, id, currentPlayingId()); if (next === before) return false; const wasQueued = before.includes(id); saveQueueIds(next); rememberEpisode(id); if (!wasQueued) logEvent("queued", { episode_id: id }); return true;` (existing event type only).
   - `function clearQueue()`: `const rules = queueOrderRules(); const before = queueIds(); const left = rules ? rules.clearOrder(before, currentPlayingId()) : []; const removed = before.filter((x) => !left.includes(x)); saveQueueIds(left); removed.forEach((x) => logEvent("unqueued", { episode_id: x })); return removed.length;`.
3. `upNextRow(r, idx, total)`: compute inside the function `const ids = queueIds(); const cur = currentPlayingId(); const curIdx = ids.indexOf(cur); const isCurrent = r.id === cur; const playNextDisabled = isCurrent || (curIdx >= 0 ? ids[curIdx + 1] === r.id : idx === 0);` (if the row already has an `isCurrent`-style variable, reuse it instead of redeclaring). Add, inside `.up-next-reorder` before the ↑ button: `<button type="button" class="reorder playnext" data-playnext="${esc(r.id)}" ${playNextDisabled ? "disabled" : ""} aria-label="Play next">Next</button>`.
4. `renderQueue`: in the page head when `rows.length > 1`, add `<button type="button" class="up-next-clear" id="up-next-clear">Clear</button>`. Empty-state text unchanged.
5. `bindUpNextReorder`: bind `[data-playnext]` → `const top = window.scrollY; if (playNextInQueue(id)) afterQueueMove(id, -1, top);`; bind `#up-next-clear` → `const n = clearQueue(); announce(n === 1 ? "Removed 1 episode from Up Next." : \`Removed ${n} episodes from Up Next.\`); repaintQueuePage(); focusQuietly($("#view").querySelector("h2"));` (`repaintQueuePage` is at 2901; if `afterQueueMove` already repaints, do not repaint twice — read 11526–11540). Keep every existing binder line intact so modal-and-focus's scan at 578 still matches.
6. Add `"data-playnext"` to `QUEUE_FOCUS_ATTRS` (2918) so a Play-next tap keeps focus like the arrows.
7. `renderEpisode` `.ep-actions`: after `upNextBtn(item.id, item)` add `${item.audio_url ? \`<button type="button" class="up-next playnext" data-playnext="${esc(item.id)}" aria-label="Play next">Play next</button>\` : ""}`; in `bindUpNext(scope)` (6262) also bind `[data-playnext]` inside `scope` → `playNextInQueue(id)` then repaint the sibling `[data-upnext]` buttons with `setToggleLabel(b, isQueued(id), UP_NEXT_TOGGLE)` exactly as the existing handler does.
8. `styles.css`: `.playnext` shares `button.reorder` sizing (the selector lists at 351/360 and `button.reorder` at 1446); add `button.up-next-clear { … }` next to `button.up-next-remove` (1460) using the same tokens.

**Tests to add** (`test/up-next-queue.test.js`, +5 → 26 pass; floor 20→25): "Play next from a row lands the episode right after the playing one" (seed `cp_queue=["a","b","c"]`, stub `currentEpisodeId()` → "a"; `playNextInQueue("c")` → `cp_queue` = `["a","c","b"]`; mutation: call `addToQueue` instead); "Play next on an unqueued playable episode adds it and refuses an unplayable one" (`playNextInQueue("z-unplayable")` → false, `cp_queue` unchanged); "Clear keeps the playing row and announces the count" (current "b" → `cp_queue=["b"]`, announcement contains "2 episodes"); "#/queue renders a Clear control only with two or more rows, and a Play next control per row, disabled on the row after the playing one" (count `[data-playnext]` = rows; `#up-next-clear` present/absent; the row after current has `disabled`; mutation: drop `ids[curIdx+1] === r.id`); "#/episode renders Play next beside + Up Next, and its click goes through playNextInQueue".

**Commands:** `node --test test/up-next-queue.test.js` → 26 pass; update the floor line; `node --test test/suite-integrity.test.js`; `node --test test/up-next-autoadvance.test.js`; `node --test test/engine-continuation.test.js`; `node --test test/modal-and-focus.test.js`; `node --test test/episode-page.test.js`; `node --check app.js`.

**Do not touch:** `player/continuation.js`, `player/queue-order.js`, `mobile/`, `index.html`, `docs/legal/`.

**Stop and escalate if:** `test/modal-and-focus.test.js` line 578 fails after your change and fixing it would mean editing that test; or `currentEpisodeId` is missing in a harness you did not write.

**DoD:** commands green; branch `feature/pq-02-play-next-clear`; DRAFT PR `feat(up-next): Play next and Clear Up Next (PQ-02, #762)`.

### PQ-03 · `player/queue-drag.js` — pure drag-to-reorder arithmetic (qwen, S)

**Executor:** qwen.

**Context.** `player/sheet-drag-dismiss.js` (exports `startDrag`, `moveDrag`, `endDrag`, `dragOffset`, `claimsTouch`, `DIRECTION_LOCK_PX = 8`, `CLAIM_SLOP_PX = 3` — copy its state-object style); `player/strip-scrub-gesture.js`; DECISIONS 2026-09-23 lane L2: "A gesture commits on `pointerup`, never on the click that may follow"; `player/sheet-drag-dismiss.test.js` for test style.

**Exact change.** Create `player/queue-drag.js` exporting:
- `HANDLE_LOCK_PX = 6`, `AUTOSCROLL_EDGE_PX = 48`.
- `startRowDrag({ index, y, t, rowTops })` → `{ index, fromIndex: index, startY: y, y, t, rowTops: [...rowTops], claimed: false, over: index }`. `rowTops` = top edge (px) of every row in list order, N ≥ 1; height(i) = `rowTops[i+1]-rowTops[i]`; the last row's height = the average of the others (56 when N=1).
- `moveRowDrag(state, y, t)` → new state; `claimed` becomes true once `|y - startY| >= HANDLE_LOCK_PX`; `over` = the largest `i` with `y > rowTops[i] + height(i)/2`, clamped to `[0, N-1]`, from the ORIGINAL layout; `offsetPx = y - startY`.
- `endRowDrag(state)` → `{ commit: state.claimed && state.over !== state.fromIndex, from: state.fromIndex, to: state.over }`.
- `autoscrollDelta(y, viewportHeight)` → `-8` when `y < AUTOSCROLL_EDGE_PX`, `+8` when `y > viewportHeight - AUTOSCROLL_EDGE_PX`, else 0.
- `claimsTouch(state)` → `state.claimed`.

**Tests** (`player/queue-drag.test.js`, 9): claim after 6 px not 5 (mutation `>=`→`>`); `over` follows midpoints downward (tops `[0,56,112,168]`, from 0 to y=90 → over 1; y=141 → over 2); upward; clamps at both ends; `endRowDrag` commits only when claimed and moved (mutation: drop the `claimed` test); release at the origin index does not commit; `autoscrollDelta` three branches; uneven heights use each row's own midpoint; a single-row list never commits.

**Commands:** `node --test player/queue-drag.test.js` → 9; floor `"player/queue-drag.test.js": 9, // PQ-03 (#762)`; `node --test test/suite-integrity.test.js`.

**Do not touch:** `app.js`, `styles.css`, other player modules. **Stop and escalate if:** a rule needs DOM measurement. **DoD:** green; branch `feature/pq-03-queue-drag`; DRAFT PR `feat(up-next): drag-to-reorder gesture arithmetic (PQ-03, #762)`.

### PQ-04 · app.js + styles.css: drag handle on Up Next rows (qwen, S)

**Executor:** qwen.

**Context.** `app.js` `upNextRow` (11457), `bindUpNextReorder` (11548), `afterQueueMove` (11526), `repaintQueuePage` (2901), `QUEUE_FOCUS_ATTRS` (2918); `player/client.js` 147 (bridge pattern); `test/engine-continuation.test.js` lines 1–120 (harness whose elements REMEMBER listeners — `listeners: {}` at line 53 in `makeEl`; copy it); `styles.css` 1433–1462 (`.up-next-row`, `button.reorder`, `button.up-next-remove`); DECISIONS 2026-09-23 L2 (commit on pointerup; "the arrows remain").

**Exact change.**
1. `player/client.js`: `import * as queueDrag from "./queue-drag.js"; window.forayQueueDrag = queueDrag;` beside the PQ-02 line.
2. `upNextRow`: prepend to `.up-next-reorder` a handle `<button type="button" class="reorder drag-handle" data-drag-handle="${esc(r.id)}" aria-label="Drag to reorder" aria-describedby="up-next-drag-hint">⋮⋮</button>`; `renderQueue` adds once under the heading `<p class="sub visually-hidden" id="up-next-drag-hint">Hold and drag to move. Arrow keys still move one step.</p>`.
3. New `function bindUpNextDrag(scope)` called from `renderQueue` after `bindUpNextReorder`: for each `[data-drag-handle]` (guard `_bound`): `pointerdown` (primary button only) → `try { btn.setPointerCapture(e.pointerId); } catch (_) {}`, `rowTops = [...scope.querySelectorAll(".up-next-row")].map((r) => r.getBoundingClientRect().top)`, `state = rules.startRowDrag({ index, y: e.clientY, t: e.timeStamp, rowTops })`, add class `is-dragging` to the row, `topBefore = window.scrollY`; `pointermove` → `state = rules.moveRowDrag(state, e.clientY, e.timeStamp)`; when `rules.claimsTouch(state)` set `row.style.transform = \`translateY(${state.offsetPx}px)\`` and toggle `.drop-before` on the row at `state.over`; `const d = rules.autoscrollDelta(e.clientY, window.innerHeight); if (d) window.scrollBy(0, d);`; a non-passive `touchmove` listener on the handle calls `preventDefault()` only while `claimsTouch(state)`; `pointerup`/`pointercancel` → `const r = rules.endRowDrag(state)`; clear transforms/classes; if `r.commit` → `saveQueueIds(window.forayQueueOrder.moveTo(queueIds(), id, r.to)); afterQueueMove(id, r.to < r.from ? -1 : 1, topBefore);` else nothing.
4. `styles.css`: `.up-next-row.is-dragging { opacity: .85; z-index: 2; position: relative; touch-action: none; }`, `.up-next-row.drop-before { box-shadow: inset 0 2px 0 var(--accent); }`, `button.drag-handle { cursor: grab; touch-action: none; }`, plus the `body.ui-v2` variants beside the existing Up Next ui-v2 rules (grep `ui-v2 .up-next-row`).

**Tests** — new `test/up-next-gestures.test.js` (harness copied from `test/engine-continuation.test.js`, with `getBoundingClientRect` on rows returning `{top: 56*i}` and the REAL `player/queue-drag.js` + `player/queue-order.js` published on `window` as client.js would), 6: (1) every row has a drag handle and the hint paragraph exists once; (2) pointerdown + move of 6 px claims and paints `is-dragging`; (3) release at y=141 from row 0 commits and `cp_queue` = `["b","c","a"]` (seed `["a","b","c"]`) — mutation: commit on `click` instead of `pointerup`; (4) release without movement writes nothing; (5) pointercancel writes nothing and clears the class; (6) after a commit the announcement is "Moved to position 3 of 3.". Floor `"test/up-next-gestures.test.js": 6, // PQ-04 (#762)`.

**Commands:** `node --test test/up-next-gestures.test.js` → 6; `node --test test/suite-integrity.test.js`; `node --test test/up-next-queue.test.js`; `node --test test/modal-and-focus.test.js`; `node --test test/no-horizontal-scroll.test.js`; `node --test test/tap-targets.test.js`; `node --check app.js`.

**Do not touch:** `player/queue-drag.js` internals, `sw.js`, `index.html`, `mobile/`. **Stop and escalate if:** tap-targets or no-horizontal-scroll needs a token change outside the Up Next rules. **DoD:** green; branch `feature/pq-04-queue-drag-wiring`; DRAFT PR `feat(up-next): drag handle reorders rows, commit on release (PQ-04, #762)`.

### PQ-05 · `player/queue-swipe.js` — swipe-to-remove arithmetic (qwen, S)

**Executor:** qwen. **Context:** as PQ-03 (`sheet-drag-dismiss.js` constants, its `releaseVelocity`, L2 "commit on release").

**Exact change.** `player/queue-swipe.js` exports `SWIPE_LOCK_PX = 8`, `REMOVE_DISTANCE_PX = 96`, `REMOVE_VELOCITY_PX_PER_MS = 0.6`, `FLICK_MIN_PX = 32`; `startSwipe(x, y, t)`; `moveSwipe(state, x, y, t)` (direction lock: the first axis to pass 8 px wins; vertical first → `rejected: true` forever; only leftward drags count, rightward clamps to 0); `endSwipe(state)` → `{ remove, offsetPx }` where `remove = claimed && !rejected && (|offset| >= 96 || (velocity >= 0.6 && |offset| >= 32))`, velocity from the last two samples; `swipeOffset(state)` for the paint (rubber-banded past 160: `160 + (d-160)/3`).

**Tests** (`player/queue-swipe.test.js`, 8): vertical-first rejects; horizontal-first claims; rightward never claims/removes; distance threshold exact at 96 (mutation `>=`→`>`); flick below distance removes; slow short release does not; rubber-band past 160; a rejected gesture reports `remove:false` whatever follows. Floor `"player/queue-swipe.test.js": 8, // PQ-05 (#762)`.

**Commands:** `node --test player/queue-swipe.test.js`; `node --test test/suite-integrity.test.js`. **Do not touch:** app.js/styles. **Escalate if:** any rule needs DOM. **DoD:** green; branch `feature/pq-05-queue-swipe`; DRAFT PR `feat(up-next): swipe-to-remove gesture arithmetic (PQ-05, #762)`.

### PQ-06 · app.js + styles.css: swipe-to-remove on Up Next rows (qwen, S)

**Executor:** qwen. **Context:** PQ-04's `bindUpNextDrag`, `removeFromQueue` (2978), `afterQueueRemove(index)` (11541), `test/up-next-gestures.test.js`, `styles.css` `.up-next-row` (1433) and the existing `prefers-reduced-motion` block (grep).

**Exact change.** (1) client.js: `import * as queueSwipe from "./queue-swipe.js"; window.forayQueueSwipe = queueSwipe;`. (2) `bindUpNextSwipe(scope)` from `renderQueue`: pointer listeners on each `.up-next-row > .info` (not the buttons): pointerdown → `startSwipe`; pointermove → `moveSwipe`, paint `row.style.transform = \`translateX(-${swipeOffset}px)\`` and `.swiping`; non-passive `touchmove` preventDefault only while claimed and not rejected; pointerup → `endSwipe`; if `remove` → `const index = queueIds().indexOf(id); removeFromQueue(id); afterQueueRemove(index);` else reset transform. A row that is current may be removed (matches today's ✕). (3) styles: `.up-next-row.swiping { transition: none; }`, `.up-next-row .swipe-under { … }` ("Remove" label revealed on the right, existing tokens); inside the reduced-motion block add `.up-next-row { transition: none; }`.

**Tests** (`test/up-next-gestures.test.js` +5 → 11; floor 6→11): a 100 px leftward release removes and `cp_queue` loses the id (mutation: commit on click); vertical-first leaves the queue unchanged; rightward never removes; the announcement after a removal is "Removed from Up Next."; the ✕ button still works.

**Commands:** `node --test test/up-next-gestures.test.js` → 11; `node --test test/suite-integrity.test.js`; `node --test test/up-next-queue.test.js`; `node --test test/modal-and-focus.test.js`; `node --check app.js`. **Do not touch:** `player/queue-swipe.js`, `mobile/`. **Escalate if:** the `.info` click-to-open binding conflicts with pointer capture in a way that needs `bindPickLogging` changes. **DoD:** green; branch `feature/pq-06-queue-swipe-wiring`; DRAFT PR `feat(up-next): swipe a row left to remove it (PQ-06, #762)`.

### PQ-07 · iOS parity pin: every Up Next tool re-sends the plan (qwen, XS)

**Executor:** qwen. **Context:** `app.js` `saveQueueIds` (2879) → `refreshEpisodeNavigation()` (3374) → `sendContinuation()` (3408); `test/engine-continuation.test.js` (its fake player records `setContinuation({planSeq, autoAdvance, chain})` calls — header line 11; 6 tests, floor 6); `docs/native-engine-plan.md` §5.5.

**Exact change.** Tests only: add 3 to `test/engine-continuation.test.js` — (1) `playNextInQueue("c")` with current "a" and queue `["a","b","c"]` → the LAST `setContinuation` call's `chain[0].nextId === "c"` and `chain[1].nextId === "b"`, `planSeq` greater than the previous call's (mutation: remove `refreshEpisodeNavigation()` from `saveQueueIds`); (2) `saveQueueIds(window.forayQueueOrder.moveTo(...))` re-plans likewise; (3) `clearQueue()` re-plans to a chain that continues with the play list (seed `state.playList`) and no queued hops. Floor 6→9 with `// PQ-07 (#762): Play next, drag reorder and Clear each re-send the engine's plan`.

**Commands:** `node --test test/engine-continuation.test.js` → 9; `node --test test/suite-integrity.test.js`. **Do not touch:** any non-test file. **Escalate if:** a test needs a code change (a PQ-02/04 bug: report, do not patch). **DoD:** green; branch `feature/pq-07-up-next-engine-pin`; DRAFT PR `test(up-next): the engine's plan follows every Up Next tool (PQ-07, #762)`.

### PQ-08 · Ledger + plan docs for #762 (qwen, XS)

**Executor:** qwen. **Context:** `docs/audit/round-2/status.tsv` line 162 (`p-impatient-8`, disposition `deferred-founder`); `test/audit-status.test.js` (`DISPOSITIONS` at line 23: `fixed`, `already-fixed`, `refuted`, `deliberate`, `refuted-now`, `deferred-founder`, `deferred-device`, `open`; line 12: changing a refuted row to fixed turns a second test red — read 1–60); `docs/listening-queue-plan.md` (§5 "Staged breakdown" line 151, §8 addendum line 205); `gh issue view 762`.

**Exact change.** (1) status.tsv: `p-impatient-8` disposition → `fixed`, `where` → `PQ-01..PQ-07 (feature/pq-0x-* PRs)`, note: "Drag handle (commit on pointerup), Play next (after the playing row), swipe-left to remove, Clear (keeps the playing row); arrows kept; every edit re-sends the engine's plan. Tests: player/queue-order, queue-drag, queue-swipe suites, test/up-next-gestures.test.js, test/engine-continuation.test.js." (2) listening-queue-plan.md: append `## 9. Stage 4 — the tools (2026-09, #762)` with one paragraph per behaviour and the rule that the engine never owns `cp_queue` (cite native-engine-plan §5.5). (3) `gh issue close 762 --comment "<one paragraph naming the PRs>"` only after PQ-02/04/06/07 are merged (`gh pr view <n> --json state` first).

**Commands:** `node --test test/audit-status.test.js`; `node --test test/human-actions-integrity.test.js`. **Do not touch:** `docs/DECISIONS.md`, `CLAUDE.md`, `docs/adr/` (DENIED). **Escalate if:** audit-status refuses the disposition. **DoD:** green; branch `docs/pq-08-762-ledger`; DRAFT PR `docs(up-next): #762 ledger and plan addendum (PQ-08)`.

### PQ-09 · `player/continuation.js`: the tail — after the list, more of what fits (qwen, S)

**Executor:** qwen. Pure rules with fixture cases (JS-only family; no Swift owes it).

**Context.** `player/continuation.js` (all: `planAfterEnded`, `nextAfterEnded`, `continuationChain`, `continuationPlan`, `canNext`, THE INJECTED STATE comment); `player/continuation.test.js` (fixture-driven; 11 tests, floor 11); `player/parity/fixtures/continuation/continuation.json` (`{id, covers, call, args, expect}`); `player/parity/coverage.js` `COVERED_SUITES` and `player/parity/coverage.test.js`; `tools/parity/record.mjs` header (`--check`, `--family`); `player/parity/floors.json` (families.continuation 49 at line 5, suites.continuation 11 at line 25 — raise-only, written by record.mjs); `player/engine-contract.js` 572–577 (`$defs.hop`: `required: ["planSeq","hopSeq","nextId"]`, `properties: {planSeq, hopSeq, nextId}`, and — verified — NO `additionalProperties: false`, so extra hop fields pass the schema); DECISIONS 2026-09-14 (the tail is wanted; the floor governs what is queued).

**Exact change.**
0. First: `git grep -n additionalProperties player/engine-contract.js` — the only hit must be the `rows` map at line 496. If `$defs.hop` (572) or any object containing `chain` has `additionalProperties: false`, STOP and escalate (a contract change is not qwen work).
1. `planAfterEnded(state, finishedId)`: after the list branch finds nothing, read `state.tail` (`idList`), pick the first `isPlayable(id)` that is not `finishedId`; return `{ nextId, rest, fromList: false, fromTail: true }`. When the tail also has nothing: `{ nextId: null, rest, fromList: false, fromTail: false }`. Existing returns gain `fromTail: false`.
2. `nextAfterEnded`: `after.tail = idList(state.tail).filter((id) => id !== plan.nextId)` when `fromTail`, else unchanged; a tail pick moves `playChainId` to it but never `playListCursor`.
3. `continuationChain`: each hop gains `fromTail` (boolean); the loop carries `tail` in the stepped state.
4. `continuationPlan`/`canNext` unchanged in shape.
5. Fixture cases: add ≥ 6 to `continuation.json` with ids `continuation/tail-after-list-exhausted`, `continuation/tail-only-when-queue-and-list-empty`, `continuation/tail-skips-unplayable-and-finished`, `continuation/tail-empty-is-the-end`, `continuation/next-after-ended-tail-pick-moves-chain-not-cursor`, `continuation/chain-walks-into-the-tail`, each `covers` one of two NEW test names: `"then more of what fits: the tail plays after Up Next and the list, never before"` and `"a tail pick moves the chain, leaves the cursor, and the chain carries fromTail"`. Every EXISTING case whose `expect.return` spells a full return object gains `"fromTail": false` (the comparator is exact; `--check` tells you which).
6. Header comment: THE INJECTED STATE gains `tail`.

**Tests:** the two new fixture-driven `test()`s; extend the existing "the chain is nextAfterEnded applied K times" walked tuple with `fromTail`. Mutations: return `fromTail: true` for a list pick; search the tail before the list.

**Commands** (in order, one at a time): `node --test player/continuation.test.js` → 13; `node tools/parity/record.mjs --check --family continuation` → exit 0; `node tools/parity/record.mjs --family continuation` (refreshes `player/parity/manifest.json` and raises `floors.json` — commit both); `node --test player/parity/coverage.test.js`; `node --test player/parity/run.test.js`; `node --test player/engine-contract.test.js`; raise `"player/continuation.test.js": 11` → 13 (`// PQ-09 (#691): the tail`) and `node --test test/suite-integrity.test.js`; `node --test test/up-next-autoadvance.test.js`; `node --test test/engine-continuation.test.js`.

**Do not touch:** `app.js`, Swift, `swift-pending.json`/`unported.json` (if the recorder writes to them, stop).

**Stop and escalate if:** step 0 finds `additionalProperties: false`; `player/engine-contract.test.js` or `player/parity/run.test.js` goes red because of the extra hop field; `coverage.test.js` reports the new tests unmapped after your `covers[]` entries and the guard's rule differs from the description; `record.mjs` changes any fixture outside `continuation/`; `--check` fails on a case you did not edit.

**DoD:** green; branch `feature/pq-09-continuation-tail`; DRAFT PR `feat(continuation): the tail — more of what fits after the list (PQ-09, #691)`.

### PQ-10 · `player/tail-fill.js`: build the tail from today's deal (qwen, S)

**Executor:** qwen. **Context:** `app.js` 2036 `buildCards({ reserve = [] } = {})` — read it fully and confirm `state.cardSlots = [{ slot, branch, role: "stretch"|"top", item, items: [...] }]` (`items` are pool items with `id` and `show`; the stretch slot is first when one exists); `subjectLabel(branch)` (2109); DECISIONS 2026-09-14; CLAUDE.md principle 1 (~30 % floor).

**Exact change.** `player/tail-fill.js` exports `STRETCH_EVERY = 3`, `TAIL_LENGTH = 10`, `buildTail({ slots, exclude = [], length = TAIL_LENGTH })` → array of ids: positions 1..length; position `p` with `p % STRETCH_EVERY === 0` takes the next unused id from the stretch slot's `items` (skipping `exclude` and taken ids); other positions round-robin the `top` slots in `slot` order; an empty slot is skipped; an empty stretch slot leaves its positions unfilled (never borrowed); no duplicate `item.show` in two consecutive positions when an alternative exists; stops early when nothing is left. Also `tailReason(slots, id)` → `{ role, branch }` or null.

**Tests** (`player/tail-fill.test.js`, 8): positions 3, 6, 9 are stretch ids (mutation: `%`→`!==`); top slots alternate; `exclude` honoured; exhausted stretch slot leaves gaps; consecutive same-show avoided; length respected; `tailReason` both roles + null; deterministic. Floor `"player/tail-fill.test.js": 8, // PQ-10 (#691)`.

**Commands:** `node --test player/tail-fill.test.js`; `node --test test/suite-integrity.test.js`. **Do not touch:** app.js. **Escalate if:** the slot shape in `buildCards` differs from the one above. **DoD:** green; branch `feature/pq-10-tail-fill`; DRAFT PR `feat(continuation): tail-fill builds the "more of what fits" list with the stretch share (PQ-10, #691)`.

### PQ-11 · app.js: hand the tail to the rules; say what is coming (qwen, S)

**Executor:** qwen. **Context:** `app.js` `continuationState(currentId = null)` (3270), `nextAfterEnded(finishedId)` (3295, ONE caller: `playNextAfter` at 3510–3514), `startChained(nextId, ctx)` (3520: `why: whyFor(...)`, `logEvent("play_started", …)`), `applyEngineAdvance(hop)` (3436), `sendContinuation` (3408), `autoAdvanceOn` (3174) and the continuous-playback comment ending "not built here" (3173), `buildCards` (2036), `stretchBridgeLine(subjectLabelText)` (10041), `subjectLabel` (2109), `pickedHistory` (1973), `queueIds` (2862), `announce` (7146); `test/up-next-autoadvance.test.js` (21 tests, floor 21); `test/engine-continuation.test.js` (9 after PQ-07).

**Exact change.**
1. client.js: `import * as tailFill from "./tail-fill.js"; window.forayTailFill = tailFill;`.
2. app.js `function tailIds(currentId)`: `const tf = window.forayTailFill; if (!tf) return []; if (!state.cardSlots?.length) { try { buildCards(); } catch (_) { return []; } } const exclude = new Set([...pickedHistory(), ...queueIds(), ...(state.playList || []), currentId].filter(Boolean)); return tf.buildTail({ slots: state.cardSlots, exclude: [...exclude] });`. `continuationState(currentId)` gains `tail: tailIds(currentId)`.
3. `nextAfterEnded(finishedId)` (3295) returns `{ nextId: step.nextId, fromTail: Boolean(step.fromTail) }` instead of a bare id. Its ONE caller `playNextAfter` (3510) becomes `const step = nextAfterEnded(id); if (!step.nextId) …(existing no-next path)…; startChained(step.nextId, ctx, { fromTail: step.fromTail });`. `startChained(nextId, ctx, { fromTail = false } = {})`: when `fromTail` and `window.forayTailFill.tailReason(state.cardSlots, nextId)?.role === "stretch"` → `why: stretchBridgeLine(subjectLabel(branch))`, else `whyFor(nextId, nextItem)`; after a successful tail start `announce(\`Up next: ${nextItem.title}\`)`.
4. `applyEngineAdvance(hop)` (3436): a hop with `fromTail` is applied like a list hop (chain moves, cursor untouched); `logEvent("play_started", …, "autoadvance")` unchanged.
5. Replace the "not built here" sentence at 3173 with the tail rule and its share.
6. After merge: `gh issue comment 691` with what shipped and the Q4 default; do not close.

**Tests** — `test/up-next-autoadvance.test.js` (+4 → 25 pass; floor 21→25): with Up Next empty and the list finished, the next play is `tail[0]` (seed `state.cardSlots` directly with two top slots and a stretch slot; mutation: drop `tail` from `continuationState`); the third tail play is the stretch pick and its `why` is the bridge line naming the subject; a tail pick never repeats history/queue ids; the switch off stops the tail too. `test/engine-continuation.test.js` (+1 → 10; floor 9→10): the plan sent after a list end carries hops with `fromTail: true` beyond the list.

**Commands:** `node --test test/up-next-autoadvance.test.js` → 25; `node --test test/engine-continuation.test.js` → 10; floors; `node --test test/suite-integrity.test.js`; `node --test test/home-v2.test.js`; `node --test test/home-play.test.js`; `node --check app.js`.

**Do not touch:** `player/continuation.js`, `CLAUDE.md`/`docs/DECISIONS.md`. **Escalate if:** `buildCards()` throws in the harness in a way that needs fixtures beyond seeding `state.cardSlots` (seed the slots; never run the dealer in tests); if a `backend/` copy rule would be needed.

**DoD:** green; branch `feature/pq-11-tail-wiring`; DRAFT PR `feat(continuation): keep playing into more of what fits, and say so (PQ-11, #691)`.

### PQ-12 · `player/bookmarks.js` — the `cp_bookmarks` store (qwen, S)

**Executor:** qwen. **Context:** `player/seek-policy.js` `seekPrecision(item, { isLocalFile, source, observedDuration, recordedDuration })` (`OWN`, `EXACT`, `APPROXIMATE`, `DRIFT_TOLERANCE_SEC = 30`), `describeTimestamp(seconds, precision)`, `formatTimestamp`; `player/position-store.js` `positionRow`; issue #30 §1; `app.js` `lsGet` (147)/`lsSet` (157) — the store is injected as `{ get(key, fallback), set(key, value) }`.

**Exact change.** `player/bookmarks.js` exports `KEY = "cp_bookmarks"`, `PER_EPISODE_CAP = 50`, `TOTAL_CAP = 500`, and: `readAll(store)` → `{ [episodeId]: Bookmark[] }` (non-object → `{}`; entries not `{sec: finite ≥ 0, created_at: string}` dropped); `addBookmark(store, { episodeId, sec, durationSec = null, label = null, now = Date.now() })` → `{ sec: Math.round(sec), label, created_at: new Date(now).toISOString(), duration_sec: durationSec }` appended sorted by `sec`; within 5 s of an existing one → not added (returns the existing); over `PER_EPISODE_CAP` drop the oldest `created_at` on that episode; over `TOTAL_CAP` drop the oldest overall; returns `null` and writes nothing when `store.set` returns false; `removeBookmark(store, episodeId, createdAt)` → boolean, removes the episode key when empty; `listBookmarks(store, episodeId)` → sorted copy; `bookmarkPrecision(item, bookmark, observedDurationSec)` → `seekPrecision(item, { source: OWN, recordedDuration: bookmark.duration_sec ?? undefined, observedDuration: observedDurationSec ?? undefined })`; `bookmarkLabel(bookmark, precision)` → `bookmark.label || describeTimestamp(bookmark.sec, precision)`.

**Tests** (`player/bookmarks.test.js`, 10): add/sort; 5-s dedupe (mutation: drop it); per-episode cap; total cap; remove + key cleanup; refused write → null; corrupt rows dropped; EXACT for a static enclosure, APPROXIMATE for `dai_suspected` with 45 s drift, EXACT with 10 s drift (mutation: swap `>`); label falls back. Floor `"player/bookmarks.test.js": 10, // PQ-12 (#30)`.

**Commands:** `node --test player/bookmarks.test.js`; `node --test test/suite-integrity.test.js`. `test/data-deletion.test.js` stays green here (it scans `app.js`, not `player/`); it goes red at PQ-13 and green at PQ-14, all on this branch. **Do not touch:** app.js, client.js, docs/legal. **DoD:** suite green; branch `feature/pq-12-bookmarks` (shared with PQ-13/14; no PR yet).

### PQ-13 · Sheet "Bookmark" button, page-owned add (qwen, S)

**Executor:** qwen. **Context:** `player/client.js` `buildUI` (945; row2 controls 1196–1213: comment "`.fp-row2` wraps, so six controls fit a 320px phone" at 1196, `const saveBtn` 1204, `row2.append(stopBtn, rateBtn, nextBtn, saveBtn, queueLink, openLink, forayLink);` 1213), `paintEpisodeSurface` (3010; reads `episodeNavigation` getters), `episodePositionSec()` (1819), `episodeDurationSec()` (1862), `paintControl(btn, text, label)` (931), `setEpisodeNavigation(nav)` (4499), `currentEpisodeId()` (4594), line 147 bridges; `app.js` `EPISODE_NAVIGATION` (3344: `get upNextCount` 3367, `isSaved` 3368, `toggleSaved` 3369), `lsGet/lsSet`; `player/now-playing-sheet.test.js` (30 tests, floor 30; line 429 `const m = /row2\.append\(([^)]*)\)/.exec(CODE);` — read 420–450: the test that pins the row2 order and the `.fp-row2` wrap assertion); `test/transport-controls.test.js` and `test/now-playing-keyboard.test.js` (read their sheet-control expectations first); `styles.css` `.fp-row2`, `.fp-save`.

**Exact change.**
1. client.js: `import * as bookmarks from "./bookmarks.js"; window.forayBookmarks = bookmarks;`. In `buildUI`: `const bookmarkBtn = el("button", "fp-btn fp-bookmark", "Bookmark");` and change line 1213 to `row2.append(stopBtn, rateBtn, nextBtn, saveBtn, bookmarkBtn, queueLink, openLink, forayLink);` (Stop still leads); change the 1196 comment from "six controls" to "seven controls"; `ui.bookmarkBtn = bookmarkBtn`; click → `const nav = episodeNavigation; const id = ForayPlayer.currentEpisodeId(); if (!nav || !id || typeof nav.addBookmark !== "function") return; const bm = nav.addBookmark(id, episodePositionSec(), episodeDurationSec()); paintControl(bookmarkBtn, bm ? "Bookmarked ✓" : "Bookmark", bm ? "Bookmarked at this point" : "Bookmark this point"); if (bm) setTimeout(() => paintControl(bookmarkBtn, "Bookmark", "Bookmark this point"), 1500);`. In `paintEpisodeSurface`: `ui.bookmarkBtn.hidden = !(showEpisode && typeof nav?.addBookmark === "function")` (a Foray hides it, like Save).
2. app.js `EPISODE_NAVIGATION`: `addBookmark(id, sec, durationSec) { const b = window.forayBookmarks; if (!b) return null; const bm = b.addBookmark({ get: lsGet, set: lsSet }, { episodeId: id, sec, durationSec }); if (bm) announce("Bookmarked."); return bm; }` and `bookmarksFor(id) { const b = window.forayBookmarks; return b ? b.listBookmarks({ get: lsGet, set: lsSet }, id) : []; }`. Add a comment line in app.js containing the literal `"cp_bookmarks"` next to these (e.g. `const BOOKMARKS_KEY = "cp_bookmarks"; // owned by player/bookmarks.js KEY`) so `keyFamiliesInSource()` counts it.
3. styles.css: `.fp-bookmark` beside `.fp-save`, same tokens.

**Tests:** `player/now-playing-sheet.test.js`: update the row2-order test near line 429 so its expected list includes `bookmarkBtn` after `saveBtn` (same test, same intent), re-check the `.fp-row2` wrap assertion, and add +2 (floor 30→32): the row2 append list contains `bookmarkBtn` directly after `saveBtn` (mutation: remove it); the click handler reads `episodePositionSec()` not `backend.currentTime` (mutation: swap). New `test/bookmarks.test.js` (harness from `test/engine-continuation.test.js`, publishing the real `player/bookmarks.js` as `window.forayBookmarks`), 4: `addBookmark` writes `cp_bookmarks` `{ "ep-1": [{sec: 125, …}] }`; a second add at 127 s is deduped; `bookmarksFor` sorted; the announcement fires. Floor `"test/bookmarks.test.js": 4, // PQ-13 (#30)`.

**Commands:** `node --test player/now-playing-sheet.test.js` → 32; `node --test test/bookmarks.test.js` → 4; `node --test test/transport-controls.test.js`; `node --test test/now-playing-keyboard.test.js`; `node --test test/tap-targets.test.js` (if any enumerates row2 controls by count, extend by one and say so in the PR); `node --test test/suite-integrity.test.js`; `node --check app.js`.

**Do not touch:** `player/bookmarks.js` rules, `docs/legal/` (PQ-14), `mobile/`. **Escalate if:** transport-controls/now-playing-keyboard need more than a count change. **DoD:** green except `test/data-deletion.test.js` (PQ-14 fixes it on this branch); same branch; no PR until PQ-14.

### PQ-14 · Legal row for `cp_bookmarks` (opus, XS — same branch, before push)

**Executor:** opus. Why: legal wording on an auto-merge path.

**Context.** `docs/legal/privacy-policy.md` §1 table (`cp_queue` row at 135, `cp_starred_shows` at 138 — the model rows); `test/data-deletion.test.js` line 443 `test("the shipped source names exactly the N cp_ key families …")` and the dated history comment under it (last entries around 521–538; read to the end of that comment to find the current N), and the code ↔ policy tests both directions (grep `policy` near 565–600); `docs/legal/data-safety.md` §A2 (follow what `cp_queue` does); `test/legal-citations.test.js`.

**Exact change.** (1) Insert after the `cp_queue` row: `| \`cp_bookmarks\` | Bookmarks you set inside episodes — for each episode id, the second you marked, when you set it, an optional label you typed, and the episode's length at that moment (so a bookmark on an ad-stitched show can be shown as approximate if its copy changes). Set from the Now Playing sheet; listed on the episode page. Never sent, never synced | **No** |`. (2) data-deletion.test.js: bump the pinned N by one in the title and assertion, add `<N> -> <N+1> on 2026-09-25: \`cp_bookmarks\`, bookmarks inside episodes (issue #30, PQ-12..14). Same mechanism.` to the history comment. (3) Confirm `keyFamiliesInSource()` (line 76) sees the `"cp_bookmarks"` literal PQ-13 put in app.js.

**Tests:** none new; the existing go red → green. **Commands:** `node --test test/data-deletion.test.js`; `node --test test/legal-citations.test.js`; `node --test "test/*.test.js"`; `node --test player/bookmarks.test.js`; `node --test player/now-playing-sheet.test.js`; `node tools/ci/run-suites.mjs` once. **Do not touch:** §2, §7, store listings. **Escalate if:** a test demands a §7 or data-safety change (say which). **DoD:** green; push `feature/pq-12-bookmarks`; DRAFT PR `feat(bookmarks): bookmark a moment from the sheet, stored on the device (PQ-12..14, #30)`; add a HUMAN-ACTIONS.md `## #<N> 🟡 [DECIDE] Approve four privacy-policy sentences for bookmarks, downloads and followed-show alerts (~5 min)` item quoting this row (PQ-23/PQ-27 append theirs), `node --test test/human-actions-integrity.test.js`.

### PQ-15 · Episode page: bookmarks list, jump, remove (qwen, S)

**Executor:** qwen. **Context:** `app.js` `renderEpisode` (11362), `episodeChaptersHtml(item)` (11262: `<button class="ep-chapter-row" data-ts=…>` rows), `bindEpisodeSeeks(scope, item)` (11297: play-then-seek on `data-ts`), `EPISODE_NAVIGATION.bookmarksFor`, `window.forayBookmarks`, `ForayPlayer.episodeProgress(id, durationSec = null)` (client.js 4420 — read its return shape and use its duration field), `test/bookmarks.test.js`, `test/episode-page.test.js` (11 tests, floor 8), `styles.css` `.ep-chapter-row` (2290).

**Exact change.** (1) `function bookmarksHtml(item)`: `const list = EPISODE_NAVIGATION.bookmarksFor(item.id); if (!list.length) return "";` observed duration from `episodeProgress(item.id, item.duration_sec ?? null)`'s duration field (else `null`); each row `<li><button type="button" class="ep-chapter-row ep-bookmark" data-ts="${bm.sec}"><span class="ep-chapter-time">${esc(stamp)}</span><span class="ep-chapter-title">${esc(bookmarkLabel(bm, precision))}</span></button><button type="button" class="ep-bookmark-remove" data-bookmark-remove="${esc(bm.created_at)}" aria-label="Remove bookmark">✕</button></li>` inside `<section class="ep-bookmarks"><h3>Bookmarks</h3><ul>…</ul></section>`. (2) Insert `${bookmarksHtml(item)}` after `.ep-actions`, before the description. (3) `bindBookmarkRemove(scope, item)`: click → `window.forayBookmarks.removeBookmark({ get: lsGet, set: lsSet }, item.id, createdAt)`, replace `.ep-bookmarks` outerHTML with `bookmarksHtml(item)`, rebind seeks and removes, `announce("Bookmark removed.")`. (4) Jump = existing `bindEpisodeSeeks`.

**Tests** (`test/bookmarks.test.js` +4 → 8; floor 4→8): two bookmarks listed in `sec` order as `data-ts` rows (mutation: skip sorting); an approximate bookmark's title contains "roughly" (seed `dai_suspected` + 45 s drift); remove writes `cp_bookmarks` and re-renders without the row; no section when empty.

**Commands:** `node --test test/bookmarks.test.js` → 8; `node --test test/episode-page.test.js` → 11; `node --test test/episode-page-publish-date-description-chapters.test.js`; `node --test test/suite-integrity.test.js`; `node --check app.js`. **Do not touch:** client.js, seek-policy. **Escalate if:** `episodeProgress()` lacks a duration field (pass `null`, say so). **DoD:** green; branch `feature/pq-15-bookmarks-page`; DRAFT PR `feat(bookmarks): the episode page lists, jumps to and removes bookmarks (PQ-15, #30)`.

### PQ-16 · `player/download-store.js` — records, transitions, eviction, play source (qwen, S)

**Executor:** qwen. **Context:** issue #29 (Store/Policy sections); `player/position-store.js` (`NEAR_END_SEC = 30`, `MIN_RESUME_SEC = 10`); `player/seek-policy.js`; `player/episode-progress.js` `makeLastEpisode`; `player/engine-contract.js` `COMMAND_ARGS.playEpisode`; `AVDeck.swift` (any absolute URL with a scheme loads).

**Exact change.** `player/download-store.js` exports `KEY = "cp_downloads"`, `DEFAULT_CAP_BYTES = 2 * 1024**3`, `STATUSES = ["queued","downloading","done","failed","unplayable-here","missing"]`, and pure functions over `{ settings: { cellular: false, capBytes }, items: { [id]: { status, bytes, total, path, webSrc, observed_duration_sec, updated_at, last_played_at, reason, source_url } } }`:
- `readDownloads(store)` (normalises; unknown statuses dropped), `writeDownloads(store, value)`.
- `applyProgress(value, { id, status, bytes, total, path, webSrc, reason, now })` → new value (`done` requires `path`; `failed`/`unplayable-here` keep `reason`).
- `markPlayed(value, id, now)`; `markMissing(value, id)`.
- `evictionPlan(value, positions)` → ids to remove, oldest `last_played_at` first, until `sum(bytes of done) ≤ capBytes`. **Input shape:** `positions = { [id]: { sec: number, durationSec: number|null } }`. Never evict an id where `sec >= MIN_RESUME_SEC && (dur == null || sec <= dur - NEAR_END_SEC)` with `dur = positions[id].durationSec ?? record.observed_duration_sec ?? null`; an id absent from `positions` is evictable.
- `playSource(item, record, { platform })` → `done` + `path` → `{ audio_url: platform === "ios" ? "file://" + path : record.webSrc, isLocalFile: true }`; anything else → `{ audio_url: item.audio_url, isLocalFile: false }`.
- `localPlayable(item, src)` → `src.isLocalFile ? { ...item, audio_url: src.audio_url, source_audio_url: item.audio_url, isLocalFile: true } : item` (PQ-19 uses it).
- `usageLine(value)` → `"1.2 GB of 2 GB used · 7 episodes"` (binary GB, one decimal; `"0 episodes downloaded"` when none).

**Tests** (`player/download-store.test.js`, 13): normalisation drops junk; `done` without path refused; eviction oldest-first (mutation: newest-first); an in-progress episode (sec 600, dur 3600) is never evicted (mutation: drop the guard); a finished one (sec 3580, dur 3600) is evictable; an id with no position is evictable; `playSource` ios `file://`, android `webSrc`, remote otherwise; `markMissing` flips; `usageLine`; `localPlayable` keeps `source_audio_url` and sets `isLocalFile`; `localPlayable` returns the same item when remote. Floor `"player/download-store.test.js": 13, // PQ-16 (#29)`.

**Commands:** `node --test player/download-store.test.js`; `node --test test/suite-integrity.test.js`. **Do not touch:** client.js/app.js/mobile. **Escalate if:** any rule needs the bridge. **DoD:** green; branch `feature/pq-16-download-store` (shared with PQ-17/18/19/23 → one PR at PQ-23).

### PQ-17 · `player/download-bridge.js` — the web half of ForayDownloads (qwen, S)

**Executor:** qwen. **Context:** `player/durable-store.js` `vaultTier(bridge, …)` (393: `bridge.nativePromise(plugin, method, options)`, every call wrapped); `mobile/plugins/foray-audio/web/foray-audio-shell.js` 126–145 (shell detection: `typeof capacitor.nativePromise === "function"`, note "`isNativePlatform()` is NOT" the test — read that comment) and 266 (`capacitor.nativePromise(PLUGIN_NAME, method, {})`) — the `nativePromise` example ONLY; **native → JS events come from `player/native-engine.js` lines 116–124, the `listen(fn)` helper**, spelled: `try { if (typeof cap?.addListener === "function") return cap.addListener(PLUGIN, EVENT, fn); if (typeof cap?.nativeCallback === "function") { cap.nativeCallback(PLUGIN, "addListener", { eventName: EVENT }, fn); return null; } } catch (_) {} return null;` — copy exactly that, per event name; `mobile/plugins/foray-vault/package.json` (`//no-js-entry` — the web half lives in `player/`).

**Exact change.** `player/download-bridge.js` exports `DOWNLOADS_PLUGIN = "ForayDownloads"`, `USER_AGENT` (`"4a/<build> (+https://jw-incorporated.github.io/foray/)"`, build from `window.__forayBuild` else `"dev"`), `createDownloadBridge({ bridge, onEvent, setTimeoutFn = setTimeout })` → `null` when `typeof bridge?.nativePromise !== "function"` (the shell rule from foray-audio-shell.js); else `{ enqueue({ id, url, userAgent, allowCellular }), cancel({ id }), remove({ id }), removeAll(), list(), usage(), fileSrc({ path }) }` — each `bridge.nativePromise(DOWNLOADS_PLUGIN, method, options)` raced against a 10 s deadline, resolving `{ ok: false, reason: "timeout" | String(err) }` on timeout/throw, never rejecting; `fileSrc` returns `bridge.convertFileSrc(path)` when that function exists. Events: for each of `downloadProgress {id, bytes, total}`, `downloadDone {id, path, bytes}`, `downloadFailed {id, reason, status}` subscribe with the `listen` shape above (`bridge.addListener(DOWNLOADS_PLUGIN, name, fn)` else `bridge.nativeCallback(DOWNLOADS_PLUGIN, "addListener", { eventName: name }, fn)`), forwarding to `onEvent(name, payload)`.

**Tests** (`player/download-bridge.test.js`, 9, fake bridge): null without `nativePromise`; `enqueue` passes the exact options; a throwing bridge → `{ok:false}`; timeout → `{ok:false, reason:"timeout"}` (injected `setTimeoutFn`); events forwarded by name via `addListener`; falls back to `nativeCallback` when `addListener` is absent (mutation: drop the fallback); `fileSrc` uses `convertFileSrc`; `removeAll` exists; UA carries the site URL. Floor `"player/download-bridge.test.js": 9, // PQ-17 (#29)`.

**Commands:** `node --test player/download-bridge.test.js`; `node --test test/suite-integrity.test.js`. **Do not touch:** `mobile/`, `tools/mobile/`. **Escalate if:** the mechanism needs `@capacitor/core`'s ES import (use only `window.Capacitor`). **DoD:** green on `feature/pq-16-download-store`.

### PQ-18 · app.js: Download control, Library Downloads, cellular switch, purge (qwen, S)

**Executor:** qwen. **Context:** `app.js` `renderEpisode` (11362), `renderLibrary` (11678; the section list at 11743–11748: Forays, Followed shows, Saved, Playlists, Up Next, History; `libSection(title, bodyHtml)` 11641), `drawerToggle(id, label, read, write, { words, repaint, into })` (14780), `bindDrawerToggles` (14837), `async function deleteMyData({ deviceOnly = false } = {})` (15788; the player stop `await player.stopForDataDeletion()` ~15814 inside a `try { … } catch (_) { /* an unstoppable player is not a reason to refuse a deletion */ }` ending at 15817; read 15788–15960 and name the ledger object that records each store as deleted/kept before editing), `clearStoredKeys()` (grep — the `cp_` purge loop), `init()` (18128), `lsGet/lsSet`, `announce`; `player/client.js` 147 bridges; `test/drawer-settings-toggle.test.js` (13); `test/library-screen.test.js` (20); `test/data-deletion.test.js` line 1482 `test("every store the app opens is in the deletion ledger, deleted or kept for a stated reason")` — read its guidance comment.

**Exact change.**
1. client.js publishes `window.forayDownloads = { store: downloadStore, createBridge: createDownloadBridge, USER_AGENT }`; app.js in `init()` calls `state.downloadBridge = window.forayDownloads?.createBridge({ bridge: window.Capacitor, onEvent: onDownloadEvent }) ?? null` and publishes `window.forayDownloads.bridge = state.downloadBridge`, `window.forayDownloads.recordFor = (id) => downloadsValue().items[id] ?? null`, `window.forayDownloads.onMissing = (id) => { saveDownloads(store.markMissing(downloadsValue(), id)); announce("Downloaded copy missing — streaming instead."); }`.
2. `downloadsValue()`/`saveDownloads(v)` over `lsGet("cp_downloads", null)` (the literal must appear in app.js); `onDownloadEvent(name, payload)` → `applyProgress` (on `downloadDone`, also `webSrc: bridge.fileSrc({ path })`), save, repaint the episode page / Library if showing.
3. Episode page: after `upNextBtn` add `downloadBtn(item)` ONLY when `state.downloadBridge` is non-null; label by status: `Download` → `enqueue({ id, url: item.audio_url, userAgent: USER_AGENT, allowCellular: settings.cellular })` (ORIGINAL `audio_url`), `Downloading 43%` (disabled), `Downloaded ✓` (click → `remove`, delete the record), `Not available for download here` (disabled + `<p class="note">${esc(reason)}</p>`), `Download failed — try again`.
4. Library: insert `${libSection("Downloads", downloadsHtml)}` between line 11747 `libSection("Up Next", queueHtml)` and 11748 `libSection("History", historyHtml)`, only when `state.downloadBridge` is non-null; `downloadsHtml` = `usageLine` + rows for `done` items (`rowsForIds(...)` as the other sections do) + a "Remove all" button (`removeAll`, clears items).
5. Drawer: `drawerToggle("downloads-cellular-toggle", "Download over cellular", () => downloadsValue().settings.cellular, (on) => saveDownloads({ ...v, settings: { ...v.settings, cellular: on } }))` — appended only when the bridge exists.
6. Eviction: after every `downloadDone`, build `positions = Object.fromEntries(Object.keys(items).map((id) => [id, progressOf(id)]))` where `progressOf(id)` reads `window.ForayPlayer.episodeProgress(id, items[id].observed_duration_sec ?? null)` into `{ sec, durationSec }` (map its fields), then `evictionPlan(value, positions)` → `remove` each.
7. `deleteMyData` (15788): immediately BEFORE the `try { … } catch (_) { /* an unstoppable player … */ }` block that ends at 15817, insert `const downloadsPurge = state.downloadBridge ? await state.downloadBridge.removeAll() : { ok: true, reason: "no bridge" };` and record it in the same ledger object the vault's refused purge uses (the name you read in step 0): `ok` → deleted, `{ok:false}` → kept with `reason`.

**Tests** — new `test/downloads.test.js` (harness from `test/engine-continuation.test.js` + a fake `window.Capacitor` `{ nativePromise: fake, addListener: fake, convertFileSrc }` and the REAL `player/download-store.js`/`download-bridge.js`), 8: no Download button off the shell; `enqueue` gets the original `audio_url`; a `downloadProgress` event repaints "Downloading 43%"; `downloadDone` writes `done` with `path`; `unplayable-here` renders the note + disabled control; Library shows the usage line and rows after Up Next and before History; the cellular switch writes `settings.cellular`; Delete my data calls `removeAll` before purging. Floor `"test/downloads.test.js": 8, // PQ-18 (#29)`. `test/data-deletion.test.js`: extend the ledger per the comment above line 1482 (+1 test that the downloads store is in the ledger; raise its floor by 1).

**Commands:** `node --test test/downloads.test.js`; `node --test test/drawer-settings-toggle.test.js`; `node --test test/library-screen.test.js`; `node --test test/episode-page.test.js`; `node --test test/data-deletion.test.js` (only the key-count test may be red — PQ-23); `node --test test/suite-integrity.test.js`; `node --check app.js`. **Do not touch:** `mobile/`, `index.html`, `docs/legal`. **Escalate if:** the ledger test needs a policy §7 sentence beyond PQ-23's; if `episodeProgress` cannot give positions (evict only `played` ones and say so). **DoD:** green except the key count; branch `feature/pq-16-download-store`.

### PQ-19 · Play from the local file (JS backend + native engine), exact seek, degrade (qwen, S)

**Executor:** qwen. **Context:** `player/client.js` `ForayPlayer.play(item, opts)` (grep `^  play(item` near 4200–4240: the lane gate, `setNowPlaying(item, why)`, `const lastRec = makeLastEpisode(item)` 4230), `setNowPlaying(item, why)` (2206: the `seekPrecision(item, { isLocalFile: false, source: OWN })` note), `reportPlayFailure(err)` (4397), `episodeProgress` (4420); `player/native-facades.js` `NativeManagerFacade.play` (295: `args = { item, lastEpisodeRow: this._lastEpisodeRow(item) ?? { id: item.id } }`); `player/html-audio-backend.js` `load(item, {startOffset})`; `player/download-store.js` `playSource`, `localPlayable`; `player/parity/coverage.js` (do NOT add tests to covered suites `transport-reconcile`, `html-audio-backend`, `queue-manager`).

**Exact change.** (1) `play()`: after `canPlay` and the lane gate, `const platform = window.Capacitor?.getPlatform?.() || "web"; const dl = window.forayDownloads; const src = (!opts?.noLocal && dl?.store && dl.recordFor) ? dl.store.playSource(item, dl.recordFor(item.id), { platform }) : { audio_url: item.audio_url, isLocalFile: false }; const playable = dl?.store ? dl.store.localPlayable(item, src) : item;` hand `playable` to the manager/queue and to the native facade; `setNowPlaying(item, why)` and `makeLastEpisode(item)` keep the ORIGINAL item. (2) `setNowPlaying`: precision reads `isLocalFile: Boolean(current?.isLocalFile)` (set `current` from `playable`'s flag in the one place `play()` stores the current item; read 2206–2240 first). (3) Missing-file degrade: in `reportPlayFailure(err)` (4397) / the load rejection path, when `current?.isLocalFile` → `window.forayDownloads.onMissing(item.id)` then `ForayPlayer.play(originalItem, { ...opts, noLocal: true })` once. (4) Native: the facade receives `playable`; verify `this._lastEpisodeRow(item)` (295) is computed from the item passed — pass the original item for that row and `playable` for `item` (change the facade's `play(item, ...)` signature to `play(item, { lastEpisodeItem = item } = {})` or equivalent, keeping `player/native-facades.test.js` green).

**Tests** — new `player/download-play.test.js` (source-text over client.js with the `codeOnly()` stripper copied from `player/now-playing-sheet.test.js`), 4: `play()` calls `playSource` before the manager; `setNowPlaying` is called with the original item; the precision call reads `isLocalFile` from the current item; the missing path calls `onMissing` and retries with `noLocal`. Floor `"player/download-play.test.js": 4, // PQ-19 (#29)`.

**Commands:** `node --test player/download-store.test.js`; `node --test player/download-play.test.js`; `node --test player/transport-reconcile.test.js`; `node --test player/native-facades.test.js`; `node --test player/media-session.test.js`; `node --test player/parity/coverage.test.js`; `node --test test/suite-integrity.test.js`. **Do not touch:** `AVDeck.swift`, `html-audio-backend.js`, covered suites. **Escalate if:** the manager's queue-set copies only whitelisted item fields (name the function and stop); if `native-facades.test.js` needs the item shape changed beyond an added option. **DoD:** green; same branch (inert until PQ-20/21 ship: `createDownloadBridge` is null without the plugin).

### PQ-20 · iOS `foray-downloads` plugin — human-merge PR (opus, M)

**Executor:** opus. Why: Swift, background transfer, URL/redirect security, Application Support vs Caches; and the PR creates `mobile/plugins/foray-downloads/package.json` + `Package.swift`, both DENIED_PATTERNS → human merge.

**Context.** `mobile/plugins/foray-vault/` (files: `package.json`, `Package.swift`, `ios/Sources/ForayVaultPlugin/ForayVaultPlugin.swift`, `KeychainVault.swift` — the smallest plugin); `mobile/plugins/foray-audio/ios/Sources/ForayAudioPlugin/Engine/EngineDiagnostics.swift` line 53 (`.applicationSupportDirectory` + named subdirectory — reuse; directory `foray-downloads`); `docs/native-engine-plan.md` §12 line 675 (`hold` label); `tools/mobile/foray-vault.test.mjs` (source-fact pins — write the same kind); issue #29; `player/download-bridge.js` (method/event names are the contract).

**Exact change.** `mobile/plugins/foray-downloads/{package.json, Package.swift, ios/Sources/ForayDownloadsPlugin/ForayDownloadsPlugin.swift, DownloadStore.swift, DownloadPolicy.swift, ios/Tests/ForayDownloadsPluginTests/DownloadPolicyTests.swift}`. `jsName = "ForayDownloads"`; methods `enqueue`, `cancel`, `remove`, `removeAll`, `list`, `usage`, `fileSrc` (absolute path). `DownloadPolicy` (pure): redirect cap 8 → `failed reason=too-many-redirects`; `Range: bytes=0-0` GET probe (never HEAD); `User-Agent` from the call; `403` → `unplayable-here`; file name = SHA-256(episode id) + `.bin`; `allowCellular` → `allowsCellularAccess`. Store: `URLSessionConfiguration.background(withIdentifier: "ai.jwlabs.foura.downloads")`, files under `Application Support/foray-downloads/`, `isExcludedFromBackup = true`, JSON index beside. Events via `notifyListeners`. `removeAll` deletes the directory. XCTests for `DownloadPolicy`. Node pins in `tools/mobile/foray-downloads.test.mjs` (jsName/method set = `player/download-bridge.js`; `applicationSupportDirectory` present, `cachesDirectory` absent; `isExcludedFromBackup`; `allowsCellularAccess` from the call; no `HEAD` literal) + a floor. The `handleEventsForBackgroundURLSession` AppDelegate hook is injected by `tools/mobile/inject-background-audio.mjs` (DENIED) — that patch goes in PQ-21; this plugin must tolerate its absence (foreground completion still works).

**Commands:** `cd tools/mobile && npm ci && npm test -- foray-downloads.test.mjs`; `npm test -- shell-invariants.test.mjs` (still 3 plugins declared — unchanged); `node --test test/suite-integrity.test.js`; `node tools/ci/path-policy.mjs` (DENIED expected). Swift is CI-executed only: quote the `ios-kit` run URL and head SHA in the PR. **Do not touch:** `mobile/package.json`, `tools/mobile/prepare-webdir.mjs`, the injector, `foray-audio`. **Escalate if:** the plugin cannot function at all without the AppDelegate hook (then PQ-21 must merge first and the HUMAN-ACTIONS order says so). **DoD:** CI green; DRAFT PR `feat(ios): foray-downloads plugin — background downloads into Application Support (PQ-20, #29) — human merge` with `hold`; HUMAN-ACTIONS `[BLOCKING]` item "Merge the six plugin PRs in order" created here (PQ-21/22/28/29/30 append to it).

### PQ-21 · Declare `foray-downloads` + AppDelegate hook — human-merge PR (opus, XS)

**Executor:** opus. Why: `mobile/package.json` and `tools/mobile/inject-background-audio.mjs` are DENIED prefixes.

**Context.** `mobile/package.json` (`file:plugins/…` entries and their `//` notes); `tools/mobile/shell-invariants.test.mjs` 1015–1025 (pinned at exactly three local plugins: `["foray-audio", "foray-tts", "foray-vault"]`, with the comment telling you to update it here); `docs/mobile-shell.md` §2.4 (the plugin list); `tools/mobile/inject-background-audio.mjs` (the `Info.plist`/AppDelegate patcher; `--check`) and `tools/mobile/inject-background-audio.test.mjs`.

**Exact change.** (1) `mobile/package.json`: `"foray-downloads": "file:plugins/foray-downloads"` + a `//foray-downloads` note. (2) shell-invariants 1022/1025: three→four, add `"foray-downloads"`, comment names PQ-20/#29. (3) docs/mobile-shell.md: one paragraph. (4) Injector: add the `handleEventsForBackgroundURLSession(identifier:completionHandler:)` AppDelegate patch (forwarding to the plugin) with test cases in `inject-background-audio.test.mjs`. **Commands:** `cd tools/mobile && npm test -- shell-invariants.test.mjs`; `npm test -- inject-background-audio.test.mjs`; `node tools/ci/path-policy.mjs` (DENIED expected). **Do not touch:** plugin sources. **DoD:** DRAFT PR `chore(mobile): declare the foray-downloads plugin and its background-session hook (PQ-21, #29) — human merge`; append to the HUMAN-ACTIONS `[BLOCKING]` item.

### PQ-22 · Android half of `foray-downloads` — human-merge PR (opus, M)

**Executor:** opus. Why: Java + device; creates `android/build.gradle` (DENIED_PATTERNS). **Context:** `mobile/plugins/foray-vault/android/**` (`build.gradle`, `AndroidManifest.xml`, `ForayVaultPlugin.java`, `DeviceOnlyVault.java`), `mobile/plugins/foray-audio/android/build.gradle` dependency style, `docs/android-native-code.md`, issue #29, `tools/mobile/foray-downloads.test.mjs` (PQ-20's — rebase on PQ-20). **Exact change.** `mobile/plugins/foray-downloads/android/...` `ForayDownloadsPlugin` (`@CapacitorPlugin(name = "ForayDownloads")`): `android.app.DownloadManager` `Request` with `addRequestHeader("User-Agent", ua)`, `setAllowedOverMetered(allowCellular)`, destination `getFilesDir()/foray-downloads/<sha256(id)>.bin` via `setDestinationUri`; a `BroadcastReceiver` for `ACTION_DOWNLOAD_COMPLETE` → `downloadDone`/`downloadFailed` (403 via `COLUMN_REASON` → `unplayable-here`); progress polled every 2 s while any download runs; `fileSrc` returns the path; `removeAll`. Extend `tools/mobile/foray-downloads.test.mjs` with the Android pins (manifest/gradle namespace agreement like shell-invariants' existing plugin test) and raise its floor. **Commands:** `cd tools/mobile && npm test -- foray-downloads.test.mjs`; `npm test -- shell-invariants.test.mjs`; Java compiles in `android-build.yml` — quote the run. **Do not touch:** `mobile/package.json`. **Escalate if:** `DownloadManager` cannot honour the 8-redirect cap (document that the cap is iOS-only and why). **DoD:** CI green; DRAFT PR `feat(android): foray-downloads — DownloadManager-backed offline store (PQ-22, #29) — human merge`; append to the HUMAN-ACTIONS item.

### PQ-23 · Legal rows for `cp_downloads` and the on-disk store (opus, XS — on PQ-16's branch)

**Executor:** opus. **Context:** as PQ-14; `docs/legal/privacy-policy.md` §7 ("How to delete your data") and §1's native-engine paragraph (the `ForayEngine.` keys and `Application Support/foray-engine/diag.jsonl`); `test/legal-citations.test.js` 966–1000 (names the native player's `ForayEngine.` keys — the downloads directory must NOT be listed as a `ForayEngine.` key); `test/data-deletion.test.js` 443 and 1482. **Exact change.** §1 row after `cp_bookmarks`: `| \`cp_downloads\` | Which episodes you downloaded for offline listening, each one's download state and size, the file's location on this device, the episode's length as downloaded, and your "download over cellular" setting. The audio files themselves sit in the app's own storage on the device (Application Support on iPhone, the app's files directory on Android), are never backed up, and are deleted by "Delete my data" and by removing the download | **No** |`; §7: one sentence that Delete my data removes the downloaded audio files; data-safety §A2/§B2: follow the existing on-device pattern, no new category. data-deletion pinned count +1 with a dated history line. **Commands:** `node --test test/data-deletion.test.js`; `node --test test/legal-citations.test.js`; `node --test test/downloads.test.js`; `node --test "test/*.test.js"`; `node --test player/download-store.test.js`; `node --test player/download-bridge.test.js`; `node --test player/download-play.test.js`; `node tools/ci/run-suites.mjs`. **DoD:** green; push `feature/pq-16-download-store`; DRAFT PR `feat(downloads): download manager and offline store, JS side (PQ-16..19, PQ-23, #29)`; append the row to the PQ-14 `[DECIDE]` item.

### PQ-24 · Downloads device check (opus, S)

**Executor:** opus. Why: device measurement, founder-facing. **Context:** `docs/native-engine-m1-car-test.md` (numbered device script shape), HUMAN-ACTIONS.md format (heading shape in Standard conventions), issue #29 acceptance list. **Exact change.** `docs/downloads-device-check.md`: (1) download a `dai_suspected` episode on Wi-Fi, background the app mid-transfer, confirm completion; (2) airplane mode → play, scrub, ↺15/30↻, lock screen; (3) bookmark at 1:07:30 on the downloaded copy → exact; streamed another day → "roughly"; (4) delete the file via Xcode container, press play → "Downloaded copy missing — streaming instead."; (5) cellular off → enqueue on LTE → stays queued; (6) verify the request URL is the enclosure URL (Proxyman/Charles, or the plugin's diag row). HUMAN-ACTIONS `## #<N> 🟢 [UPGRADE] On a phone, check the download manager against the #29 acceptance list (~20 min)`. **Commands:** `node --test test/human-actions-integrity.test.js`. **DoD:** DRAFT PR `docs(downloads): device check script + HUMAN-ACTIONS item (PQ-24, #29)`.

### PQ-25 · `player/show-alerts.js` — new-since watermark and per-show opt-out (qwen, S)

**Executor:** qwen. **Context:** `app.js` `starredShowsMap()` (1875), `toggleShowStar(id)` (1878; `editStored("cp_starred_shows", …)` at 1894 — the writer; record `{ show_id, title, artwork_url, starred_at }`), `fetchShowEpisodes(show_id, cursor)` (4850) and `api/shows/[show_id]/episodes.ts` (row `published_at` at 202, `next_cursor` 259; page 1 newest-first); issue #761.

**Exact change.** `player/show-alerts.js` exports `CHECK_INTERVAL_MS = 6 * 3600 * 1000` and pure functions over a followed-show record: `alertsOn(record)` (`record.alerts !== false`), `setAlerts(record, on)`, `dueForCheck(record, now)` (`!record.checked_at || now - Date.parse(record.checked_at) >= CHECK_INTERVAL_MS`), `newSince(rows, record)` → rows with `published_at > record.seen_published_at` (ISO string compare; rows without `published_at` ignored; when `seen_published_at` is absent → `[]`), `afterCheck(record, rows, now)` → record with `checked_at`, `latest_published_at` (max), `unseen_count` (= `newSince(rows, record).length`), `seen_published_at` seeded to `latest_published_at` when absent; `markSeen(record)` → `unseen_count: 0`, `seen_published_at: latest_published_at`; `alertText(record, newest)` → `{ title: record.title, body: newest.title }`.

**Tests** (`player/show-alerts.test.js`, 9): default on; opt-out; due at exactly 6 h (mutation `>=`→`>`); a fresh follow seeds and reports 0; a later row counted; equal timestamp not new; `markSeen` zeroes; `alertText`; missing `published_at` ignored. Floor `"player/show-alerts.test.js": 9, // PQ-25 (#761)`.

**Commands:** `node --test player/show-alerts.test.js`; `node --test test/suite-integrity.test.js`. **Do not touch:** app.js. **DoD:** green; branch `feature/pq-25-show-alerts` (shared with PQ-26/27).

### PQ-26 · app.js: check followed shows, badge, show-page switch (qwen, S)

**Executor:** qwen. **Context:** `app.js` `starredShowRow(entry)` (1935), `renderStarredShows` (1950), `showStarBtn`/`bindShowStars` (1908/1914), `renderShow(show_id, initialQuery)` (5269), `fetchShowEpisodes` (4850 — read what it resolves to), `renderLibrary`/`libraryFollowedHtml` (11678/11670), the writer `editStored("cp_starred_shows", …)` (1894 — add `saveStarredShows(map)` beside it and keep one writer), the two `visibilitychange` listeners: **16429 is the voice-list refresh — leave it alone; 18316 is the foreground refresh (`refreshForayDirectory("foreground"); refreshGreeting();` at 18319) — add the check there**; `test/starred-shows.test.js` (10 tests, floor 9; header says "no notifications"), `test/library-screen.test.js` (20).

**Exact change.** (1) client.js: `import * as showAlerts from "./show-alerts.js"; window.forayShowAlerts = showAlerts;`. (2) app.js `async function checkFollowedShows({ force = false } = {})`: for each record with `alertsOn` and (`force` || `dueForCheck`), oldest `checked_at` first, at most 6 per call: `fetchShowEpisodes(show_id)` → on success `afterCheck(record, rows, Date.now())` and `saveStarredShows`; failures leave the record untouched. Call it from `renderLibrary`, `renderStarredShows`, and inside the listener at 18316 directly after `refreshGreeting();`. (3) Badge: `starredShowRow` renders `<span class="show-new-badge" aria-label="${n} new episodes">${n} new</span>` when `unseen_count > 0`; Library's Followed section title shows the total. (4) `renderShow`: if followed → `markSeen` + save; a switch `<button type="button" class="alerts-toggle" data-show-alerts="${esc(id)}" aria-pressed="${on}">New-episode alerts: ${on ? "on" : "off"}</button>` beside Follow, shown only when followed; click → `setAlerts` + save + repaint the label. (5) Field names are frozen after this lands (PQ-28/29 read `CapacitorStorage.cp_starred_shows`).

**Tests** — `test/starred-shows.test.js` (+5 → 15 pass; floor 9→14, `// PQ-26 (#761)`): a check after a fake fetch with two newer rows writes `unseen_count: 2` (mutation: never save); a fresh follow seeds and shows no badge; the badge renders "2 new"; opening the show page zeroes it; the switch writes `alerts:false` and a later check skips that show. `test/library-screen.test.js` +1 → 21 (floor 20→21): Followed shows the badge.

**Commands:** `node --test test/starred-shows.test.js` → 15; `node --test test/library-screen.test.js` → 21; `node --test test/show-page.test.js`; `node --test test/show-episodes-cache.test.js`; `node --test test/suite-integrity.test.js`; `node --test test/data-deletion.test.js` (no new key — must stay green; if red, stop); `node --check app.js`. **Do not touch:** `api/`, `docs/legal` (PQ-27). **Escalate if:** `fetchShowEpisodes` resolves rows without `published_at` for `pi:` shows (skip those shows and say so). **DoD:** green; branch `feature/pq-25-show-alerts`.

### PQ-27 · Legal: the `cp_starred_shows` row no longer says "no notifications" (opus, XS — same branch)

**Executor:** opus. **Context:** `docs/legal/privacy-policy.md` line 138 (the row); `test/starred-shows.test.js` header and `app.js` 1930 comment ("exactly what cp_starred_shows holds -- no fetch, no ranking, no …") — update the comment and the header sentence, not the tests' intent; data-safety.md (search "Follow"). **Exact change.** Rewrite the row: `| \`cp_starred_shows\` | A per-device map of shows you followed (the key keeps its older "starred" name): title, artwork address, when you followed, whether new-episode alerts are on for that show (on unless you turn it off on the show's page), when 4a last checked the show's feed, and the newest publish date you have seen. Checking asks our API on Vercel for the show's latest episodes, the same request the show's page makes (§4.3); nothing about you is sent. Alerts appear on this device only; no auto-download, and following never changes what 4a surfaces elsewhere | **No** |`. **Commands:** `node --test test/data-deletion.test.js`; `node --test test/legal-citations.test.js`; `node --test test/starred-shows.test.js`; `node --test "test/*.test.js"`; `node tools/ci/run-suites.mjs`. **DoD:** green; push; DRAFT PR `feat(follow): new-episode badges for followed shows, per-show alerts switch (PQ-25..27, #761)`; append the row to the `[DECIDE]` item.

### PQ-28 · iOS `foray-notify` + the notification-tap route — human-merge PR (opus, M)

**Executor:** opus. Why: Swift, permission prompts, `BGTaskScheduler`, creates `package.json`/`Package.swift` (DENIED_PATTERNS), founder Q1. Proceed on the Q1 default unless overruled.

**Context.** `mobile/plugins/foray-vault/` (plugin shape); `mobile/plugins/foray-audio/ios/Sources/ForayAudioPlugin/Engine/EngineBoot.swift` (how the engine boots from `AppDelegate` — the same injected hook is where `BGTaskScheduler.shared.register` must run; the plist keys and the register call go into the injector in PQ-30); `mobile/plugins/foray-audio/ios/Tests/ForayAudioPluginTests/CapacitorStoragePrefixTests.swift` (the `CapacitorStorage.` prefix); `player/show-alerts.js` (rules to mirror); `player/native-engine.js` 116–124 (`listen` shape for the JS half); `app.js` `API_ORIGIN` (552 = `https://foray-web-seven.vercel.app`). **Verified: there is NO `appUrlOpen` handler in app.js or client.js; `?foray=` is a cold-load query parameter. The tap route is NEW work, decided here:** the plugin implements `UNUserNotificationCenterDelegate` `didReceive` and calls `notifyListeners("alertOpened", { showId })`; the JS half navigates.

**Exact change.** (1) Swift: `mobile/plugins/foray-notify/{package.json, Package.swift, ios/Sources/ForayNotifyPlugin/ForayNotifyPlugin.swift, AlertsRefresh.swift, ios/Tests/ForayNotifyPluginTests/AlertsRefreshTests.swift}`: methods `requestPermission` (`requestAuthorization([.alert,.sound])`, answers `{granted}`), `status`, `scheduleRefresh` (`BGAppRefreshTaskRequest("ai.jwlabs.foura.alerts")`, earliest 6 h), `notifyNow({title, body, showId})` (`threadIdentifier = showId`, `userInfo.showId`); the `didReceive` delegate emits `alertOpened {showId}` (and buffers the last one until a listener attaches, for cold starts). `AlertsRefresh`: read `CapacitorStorage.cp_starred_shows` from `UserDefaults`, for each show with `alerts != false` and due, GET `<API_ORIGIN>/api/shows/<id>/episodes` (10 s), apply the same `afterCheck` rule, write back, post one notification per show with new rows, re-schedule. Swift unit tests mirror PQ-25's 9 cases. (2) JS half, new `player/alert-open.js`: `routeForAlert(payload)` → `"#/show/" + encodeURIComponent(payload.showId)` or null when absent; `bindAlertOpen(bridge, navigate)` subscribes to `alertOpened` with the `listen` shape (addListener else nativeCallback) and calls `navigate(route)`. client.js: `bindAlertOpen(window.Capacitor, (h) => { location.hash = h; })` when `typeof window.Capacitor?.nativePromise === "function"`; app.js show page: when followed and `window.Capacitor?.nativePromise` exists, the PQ-26 switch's turn-on first `await nativePromise("ForayNotify","requestPermission",{})` and, if not granted, leaves the switch off and announces "Notifications are off for 4a in Settings." (3) Node pins `tools/mobile/foray-notify.test.mjs`: jsName/method set; the task identifier literal `ai.jwlabs.foura.alerts` (PQ-30's plist key must match); `requestAuthorization` only inside `requestPermission`; `didReceive` emits `alertOpened`.

**Tests:** `player/alert-open.test.js` (4: route for a showId; null without; subscribes via `addListener`; falls back to `nativeCallback` — mutation: drop the fallback) with a floor; the Swift tests; the node pins with a floor. **Commands:** `node --test player/alert-open.test.js`; `node --test test/suite-integrity.test.js`; `cd tools/mobile && npm test -- foray-notify.test.mjs`; `node tools/ci/path-policy.mjs` (DENIED expected); Swift via CI (`ios-kit`), quote the run. **Do not touch:** injector, `mobile/package.json` (PQ-30). **Escalate if:** the Preferences plugin stores the key under a different form than `CapacitorStorage.<key>`. **DoD:** CI green; DRAFT PR `feat(ios): foray-notify — local new-episode alerts via background refresh, tap opens the show (PQ-28, #761) — human merge` with `hold`; append to the `[BLOCKING]` item.

### PQ-29 · Android `foray-notify`: WorkManager periodic check + channel — human-merge PR (opus, M)

**Executor:** opus. Why: Java, device, `build.gradle` (DENIED_PATTERNS). **Context:** `mobile/plugins/foray-audio/android/.../ForayAudioPlugin.java` (`requestNotifications`, the `POST_NOTIFICATIONS` alias — reuse the pattern), its `AndroidManifest.xml`, `mobile/plugins/foray-vault/android/**`, `player/show-alerts.js`, `player/alert-open.js` (PQ-28: the `alertOpened` event is the same contract). **Exact change.** `mobile/plugins/foray-notify/android/...`: `build.gradle` adds `androidx.work:work-runtime:2.9.x`; `ForayNotifyPlugin` methods as PQ-28; `AlertsWorker extends Worker` (periodic 6 h, `NetworkType.CONNECTED`), reads the Preferences plugin's SharedPreferences file (verify its name in `@capacitor/preferences` under `mobile/node_modules` after `npm ci`), same rule as PQ-25, posts on channel `foray_alerts` ("New episodes"); the notification's `PendingIntent` carries `showId` and the plugin emits `alertOpened {showId}` from `handleOnNewIntent`. Node pins in `tools/mobile/foray-notify.test.mjs` (channel id literal, worker registered once, permission alias reused, `alertOpened` emitted). **Commands:** `cd tools/mobile && npm test -- foray-notify.test.mjs`; `npm test -- shell-invariants.test.mjs`; Java via `android-build.yml`. **DoD:** CI green; DRAFT PR `feat(android): foray-notify — WorkManager new-episode alerts (PQ-29, #761) — human merge`; append to the `[BLOCKING]` item.

### PQ-30 · Declare `foray-notify`, inject the plist keys, device check — human-merge PR (opus, XS)

**Executor:** opus. Why: DENIED prefixes, founder-facing. **Context:** PQ-21 (identical mechanics), `tools/mobile/inject-background-audio.mjs` (`UIBackgroundModes` handling at 197 `arrayStrings`), `tools/mobile/inject-background-audio.test.mjs`, `docs/ios-lock-screen.md` §8.5 (device-check style). **Exact change.** `mobile/package.json` entry + note; shell-invariants 4→5 (`"foray-notify"`); injector: `BGTaskSchedulerPermittedIdentifiers = ["ai.jwlabs.foura.alerts"]`, `UIBackgroundModes` gains `fetch`, `AppDelegate` registers the task before `didFinishLaunching` returns; injector test cases; `docs/mobile-shell.md` paragraph; HUMAN-ACTIONS `## #<N> 🟢 [UPGRADE] On the next TestFlight build, check new-episode alerts overnight (~10 min + a night)`: follow a show that publishes daily, turn alerts on (permission prompt appears once), lock the phone overnight → a notification names the show and the episode; tap → the show page; the Library badge matches; switch off → no further alerts. **Commands:** `cd tools/mobile && npm test -- shell-invariants.test.mjs`; `npm test -- inject-background-audio.test.mjs`; `node --test test/human-actions-integrity.test.js`; `node tools/ci/path-policy.mjs` (DENIED expected). **DoD:** DRAFT PR `chore(mobile): declare foray-notify and inject its background-refresh keys (PQ-30, #761) — human merge`; append to the `[BLOCKING]` item as the last merge.

## 4. Sequencing

**Throttle note (founder, 2026-09-25: the machine is bogged down):** run at most TWO executors at a time, never more; each executor runs one `node --test` process at a time; no sub-agents; skip `node tools/ci/run-suites.mjs` on intermediate tasks (only the PR-opening tasks run it).

**Wave 1 (pairwise parallel, no shared files):** PQ-01, PQ-03, PQ-05, PQ-09, PQ-10, PQ-12, PQ-16, PQ-25 — each touches only its own two files plus one `FLOORS` line (rebase conflicts in `test/suite-integrity.test.js` are one-line; merge PRs one at a time).

**Wave 2 (SERIAL — every task edits `app.js` and/or `player/client.js` line 147, rebase each on the previous merge):** PQ-02 → PQ-04 → PQ-06 → PQ-07 → PQ-08 (Up Next lane) → PQ-11 (tail) → PQ-13+PQ-14 (one PR) → PQ-15 → PQ-17 → PQ-18 → PQ-19 → PQ-23 (one PR for PQ-16..19+23) → PQ-26+PQ-27 (one PR).

**Wave 3 (native, opus, one at a time alongside Wave 2 — they touch only `mobile/plugins/<new>/`, `tools/mobile/<new>.test.mjs` and, for PQ-28, `player/alert-open.*` + one client.js line):** PQ-20 → PQ-22 (rebases on PQ-20; shared `tools/mobile/foray-downloads.test.mjs`) → PQ-28 (after PQ-26 merges; field names frozen) → PQ-29 (rebases on PQ-28). At most two Swift PRs open at once (plan §12).

**Wave 4 (human merges — six PRs, the founder merges in this order, one `[BLOCKING]` HUMAN-ACTIONS item lists them):** PQ-20, PQ-22, PQ-21 (declaration + AppDelegate hook), PQ-28, PQ-29, PQ-30 (declaration + plist keys; rebases on PQ-21 for `shell-invariants` 3→4→5 and `mobile/package.json`). Each waits for its `ios-kit`/`android-build` run to be green and, for PQ-20/28, for `hold` to be removed first.

**Wave 5:** PQ-24 (device script + `[UPGRADE]` item) after PQ-21 and PQ-23 merge; the alerts device check is inside PQ-30. The founder's approval of the four legal sentences (Q6) is one `[DECIDE]` item created by PQ-14 and appended by PQ-23 and PQ-27.

**What is deliberately not in this package:** the `#/episode/<id>?t=` deep link and chapters (#30 §2–3), Up Next auto-download, a push provider, syncing bookmarks through `cp_events`, and any change to `docs/DECISIONS.md`/`CLAUDE.md` (denied; every rule here already has its ruling: 2026-09-14, 2026-09-23 L2/L3, 2026-09-24 items 6/12/13, 2026-09-25 Q5).
