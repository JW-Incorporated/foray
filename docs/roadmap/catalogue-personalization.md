# Package plan: Catalogue and personalization quality (revision 2)

Verified against `origin/main` = `ecb6bfa3` (2026-09-25, PR #839 "save a generated playlist"). The reviewer's baseline `9442bba7` (#835) is now one commit behind; #839 touched `app.js` (+294, all in playlists/save code), `docs/legal/privacy-policy.md`, `docs/legal/data-safety.md`, `styles.css`, `test/save-playlist.test.js`. Every path, function and line number below was re-checked this session with `git show origin/main:<path>` / `git grep`. Line numbers drift by the hour; function names do not. **Relocation rule (binding for every executor):** before editing, re-locate by name with `git grep -n "^function <name>" origin/main -- app.js` (or `^const <NAME>`), and treat a number here as a hint, never as an address.

**Reviewer problems: disposition.** All 16 are accepted and applied except two partial rejections, stated inline with evidence: (a) PKG-09/10 — the show page does NOT read `api/shows/[show_id]/episodes.ts`'s `showHeader` for its show row; it reads `api/shows/search?id=` (`app.js:5259-5264`), whose `?id=` branch answers from `showByIdFromCatalog()` → `loadBreadthCatalog()` (`api/shows/search.ts:161-168, 200-213`), so `breadthCatalog.ts:134` IS the one line that puts nodes on the client row. The real gap is different and is now PKG-10's job: an index-listed show is seeded with `taxonomy_node_ids: []` and returns before the API fetch (`app.js:5219-5229`). (b) PKG-12 — no `--query` flag is added; the recipe is a scratch script that copies the harness's own `fullPool()` (kept read-only, per the task's "do not modify the harness").

## 1. Goal, done-definition, dependencies, founder questions

**Goal.** The listener's Home, generated playlists, "Similar shows" and breadth show pages stop being driven by wrong or missing labels, and the personalization plumbing that already exists (personas, depth ladders, first-party events) is wired to the client. Done when: (a) every discover item records whether its topics were judged per episode or inherited from its show, and no generated playlist or Similar-shows list is built from a broad show's inherited label; (b) breadth-tier shows carry taxonomy nodes and their pages show chips and Similar shows; (c) `data/personas.json` and `data/ladders.json` are read by the client with tests; (d) `card_shown` and observed `finished`/`skipped_at` reach the `events` table under the anonymous identity; (e) the smaller #558/#560 defects still open are closed.

**Already fixed on main, do not redo.** #558 items 1-4; #560 items 1 (`api/_test/vercel-bundle.test.mjs` walks every `api/**` reader — note the directory is `api/_test/`, not `api/test/`), 2, 5, 7, 10's `Cache-Control`. **Family Mode fails closed is DONE** (#835, `docs/audit/round-3-code/status.tsv:170` data-integrity-4 = fixed): `familySafe()` at `app.js:1480` implements founder Q1's default (explicit `true` hidden, comedy hidden, `false` shown, unrated inherits the show rating and additionally requires no explicit pool episode on that show — `showHasExplicitEpisodes`); `familyAllows()` (1523) is applied in `poolFiltered` (1446), `episodesForShow` (3729), the show page's full-catalogue rows (5453, with `FAMILY_HIDES_NOTE` at 5454) and Library (11684); `test/boot-path.test.js` pins it (floor 43). `build-catalog-client.mjs` already projects `explicit` (`CLIENT_SHOW_FIELDS` at line 46-50; `data/catalog-client.json` carries it on all 220 rows). The old PKG-20 is deleted; PKG-22 still records the Q1 default. Event sync per-chunk marking (audit app-1-10) is done: `syncEventsOnce` (`app.js:762`) marks EACH chunk synced when its POST lands.

**Dependencies.**
- Founder decisions below (each has a default; tasks say which default they assume).
- `backend/src/`, `docs/DECISIONS.md`, `docs/adr/`, `.github/`, `tools/ci/`, `CLAUDE.md` are DENIED (`tools/ci/path-policy.mjs:62-83`); `api/` is a human-look path. PKG-09, PKG-18 and PKG-22 wait for a founder merge click.
- Supabase `app_users` RLS (HUMAN-ACTIONS.md #116, repo root) is NOT applied: nothing here writes `app_users.persona_seed`; the persona pick stays a client-side prior.
- No credentials, no devices. iOS Now Playing for playlists (#558 item 7) is excluded: the native engine owns Now Playing since M1.
- **Legal-copy coupling (binding for PKG-13, 14, 18, 19):** `test/legal-citations.test.js:730-760` EXECUTES `toEventRow` and scans every `logEvent("<type>")` literal in `app.js`; it asserts `docs/legal/privacy-policy.md:224` ("**Nineteen of the twenty-three event types the app records never leave the device.**", words, parsed via the table at 734), `docs/legal/data-safety.md:44` ("**Exactly 4 of 23 event types are transmitted**") and that the §2 "Not sent" list (policy 237+) is exactly the local-only set, both directions. Any task that adds a `logEvent` type or a `toEventRow` case updates all three sentences/lists in the same PR.

**Open founder questions (defaults proposed; tasks assume the default).**
1. **Family Mode and unrated episodes (round-3 Q1).** The default is already shipped by #835 (see above); 516 of 2,167 discover items still lack the `explicit` key (PKG-01 stamps `null`, no behaviour change). *Default:* confirm the shipped rule; PKG-22 records it. If overruled, that is a new task, not this package.
2. **The "general" show marker (#547 fix 2, suggested-shows §6.13).** *Default:* add `label_scope: "general"` to broad shows' `data/catalog.json` rows; generators refuse to inherit from a general show; the nightly requires a per-episode topic for a general show's new episodes. `Lex Fridman Podcast` already has `taxonomy_node_ids: []` and `explicit: null`; `scan.mjs:135` seeds topics from that array, so its new episodes are dropped by `resolve.mjs:262` today — the marker makes that explicit.
3. **Do breadth shows get subjects (§6.13 #5)?** `data/breadth-classification.json` classifies all 19,787 breadth shows. *Default:* yes — fold topics where `confidence !== "low"` and `needs_review !== true` (16,736 shows on main; 3,051 get `[]`), and the API stops zeroing them.
4. **`data/catalog-breadth-intl.json.gz` (13.8 MB, read by no code).** *Default:* delete it and correct the living docs; leave dated records as they are. Reversible via git.
5. **#217 Gershwin `music/jazz` tag.** *Default:* re-measure (PKG-12); tag only if "the history of jazz" keeps 3 results.
6. **`card_shown` archetype.** The client deals by role `top`/`stretch`; the contract requires one of five archetypes (`backend/src/types/events.ts:44`). *Default:* extend `ArchetypeSlotSchema` with `"top"` (the `0009_events.sql` check at line 12 is on `type`; `archetype` at line 20 is unconstrained `text`).
7. **Publish the `fusion-101` ladder?** `status: "draft"`; nothing in PKG-15/16 shows until it flips. *Default:* keep draft until PKG-07's relabel lands.

## 2. Task table

| id | title | executor | why-opus | depends-on | size |
|---|---|---|---|---|---|
| PKG-01 | Topic provenance: `topics_source` on every discover item; `explicit` key always present | qwen | — | — | S |
| PKG-02 | Client catalogue carries `label_scope` (one field, one assertion) | qwen | — | — | XS |
| PKG-03 | The `label_scope: "general"` marker: choose the shows, set it, Similar shows refuses general labels | opus | curation judgement | PKG-02 | S |
| PKG-04 | Generated playlists: one leaf-items helper for both generators, per-show cap 2, no inherited general labels, snapshot keeps provenance | qwen | — | PKG-01, PKG-02 | S |
| PKG-05a | `merge.mjs`: a general show's episode without its own topic is refused (`MERGE_CATALOG_PATH`) | qwen | — | PKG-03 | S |
| PKG-05b | Nightly prompt: the edits author must supply `topics` for a general show's episodes | opus | prompt/LLM design | PKG-05a | XS |
| PKG-06 | `tools/refresh/relabel.mjs`: validated relabel of committed discover items | qwen | — | PKG-01 | XS |
| PKG-07 | Relabel pass: the 26 `engineering/energy-fusion` items and general shows' inherited leaf labels | opus | per-episode curation | PKG-03, PKG-06 | S |
| PKG-08 | Fold breadth topics into `data/catalog-breadth.json` | qwen | — | — | S |
| PKG-09 | Backend stops zeroing breadth `taxonomy_node_ids` (one line + test; DENIED path) | opus | `backend/src/` DENIED | PKG-08 | XS |
| PKG-10 | Breadth show page: index-seeded shows upgrade to the API row; chips and Similar shows render | qwen | — | none for tests; PKG-09 for live effect | S |
| PKG-11 | Playlist parts keep `release_date`; stale generator comment | qwen | — | — | XS |
| PKG-12 | #217: re-measure the Gershwin tag with a scratch copy of the harness pool | qwen | — | — | XS |
| PKG-13 | Personas: load `data/personas.json`, `applyPersonaPick()` as a decaying prior | qwen | — | — | S |
| PKG-14 | Persona row in the first-run Preferences pane, copy, local instrumentation events | opus | product/UX judgement, copy | PKG-13 | S |
| PKG-15 | Depth ladders: load published ladders, port `inferLadderProgress`, `cp_ladder_progress` | qwen | — | — | S |
| PKG-16 | Ladder rung line on a Home card | qwen | — | PKG-15 | S |
| PKG-17 | Event-sync mapping tests (`toEventRow`, per-chunk batching, local-only types) | qwen | — | — | XS |
| PKG-18 | `card_shown` end to end (contract, emit, map, policy rows) | opus | `backend/src/types/events.ts` DENIED; Q6; legal copy | PKG-17 | S |
| PKG-19 | Observed `finished` / `skipped_at` from in-app playback | opus | player/native-engine seams; provenance rule | PKG-17 | S |
| PKG-21 | Remove the international breadth catalogue and its "138k" claims (gated on Q4) | qwen | — | founder Q4 | XS |
| PKG-22 | Record the rulings and corrections in `docs/DECISIONS.md` | opus | DENIED path | PKG-03, 09, 18 | XS |

Repo conventions every task follows (stated once, binding for all): worktree with `git -C "C:/Users/wjduv/Desktop/Vibe Coding/foray" -c core.autocrlf=false worktree add ../foray-<id> -b <branch> origin/main`; never commit `deploy-manifest.json` or `data/forays-directory.json`; `sw.js` keeps `BUILD_ID = "unstamped"`; a NEW test file gets a floor entry in `test/suite-integrity.test.js` `FLOORS` (existing files: raise the floor by exactly the number of tests added, with a dated note in the same line style); any test that names a Foray reads it from `tools/foray/fixtures/frozen/`; PRs open as DRAFT (`gh pr create --draft`); commit messages end with the two trailer lines from the session reminder; run one test process at a time (`node --test <one file>`), never `format:write` repo-wide; a test is not done until its named mutation was applied and seen red; pass counts below are "at least the floor, plus exactly the N added" — a suite may already run above its floor (e.g. `first-time-onboarding` has 34 static tests against a floor of 33).

## 3. Task sections

### PKG-01 — Topic provenance: `topics_source` on every discover item; `explicit` key always present
**Executor:** qwen.
**Context (read first):** `tools/refresh/topics.mjs` (`topicKey` 50, `MAX_TOPICS` 55, `episodeTopics` 77-121, `uniformTopicShows` 134); `tools/refresh/merge.mjs` (imports at 42, `envPath` 49, env paths 51-58, `nodeIds` 64, preflight `episodeTopics` 100, item literal `topics:` 146 and `explicit: ep.explicit` 148); `tools/refresh/merge-topics.test.mjs` header (subprocess harness with `RESOLVED_PATH`/`EDITS_PATH`/`MERGE_DISCOVER_PATH`/`MERGE_TAGS_PATH`); `tools/refresh/backfill-show.mjs` (an existing one-shot writer to copy the write-back style from); `test/data-topic-integrity.test.js` (12 static tests; discover test at 115); `data/discover.json` item keys: `id, show, title, apple_collection_id, apple_track_id, apple_episode_url, release_date, duration_min, artwork_url, topics, hook, audio_url, audio_type, audio_bytes, duration_sec, dai_suspected` plus `explicit` on 1,651 of 2,167; `data/catalog.json` show keys include `title, taxonomy_node_ids, explicit`.
**Exact change:**
1. `topics.mjs`: `export function topicSource(editTopics) { return editTopics === undefined || editTopics === null ? "show" : "episode"; }` under `episodeTopics`.
2. `merge.mjs` item literal: after `topics:` add `topics_source: topicSource(edit.topics),`; change `explicit: ep.explicit,` → `explicit: ep.explicit ?? null,`. Import `topicSource` next to `episodeTopics`.
3. New `tools/refresh/backfill-provenance.mjs` (idempotent): reads `data/discover.json` and `data/catalog.json` (env overrides `BACKFILL_DISCOVER_PATH`, `BACKFILL_CATALOG_PATH` via the same `envPath` idiom); for each item lacking `topics_source`: show = catalog show with `show.title === item.show`; `topics_source = (show && topicKey(item.topics) === topicKey(show.taxonomy_node_ids)) ? "show" : "episode"` (no catalog show → `"episode"`); for each item lacking the `explicit` key set `explicit: null`. Writes back 2-space JSON + trailing newline; prints `{stamped_source, stamped_explicit, unchanged}`. `--check` exits 1 if any item would change.
4. Run it once; commit `data/discover.json`. Expected `stamped_source` 2,167, `stamped_explicit` 516 (report actuals in the PR).
**Tests to add:**
- `tools/refresh/merge-topics.test.mjs` (+3, floor 18 → 21): `"topicSource is 'show' for an absent override and 'episode' for a present one"` (mutation: return `"episode"` always); `"merge writes topics_source 'episode' when edits.json carries topics, 'show' otherwise"` via the subprocess harness (mutation: drop the `topics_source:` line); `"merge writes explicit: null when the resolved episode has no flag"` (mutation: revert to `ep.explicit`).
- `test/data-topic-integrity.test.js` (+2, floor 12 → 14): `"every discover item carries topics_source in {show, episode}"`, `"every discover item carries an explicit key (true, false or null)"` — real-data tests; kill documented in the header: remove one key locally, run `backfill-provenance.mjs --check`, expect exit 1.
- `tools/refresh/backfill-provenance.test.mjs` (NEW, floor 3): `"--check exits 1 when an item lacks topics_source"`, `"stamps 'show' when topics equal the show's nodes and 'episode' otherwise"`, `"is idempotent: a second run changes nothing"` — temp dir, 3-item fixture, env overrides.
**Commands:** `node --test tools/refresh/merge-topics.test.mjs`; `node --test tools/refresh/backfill-provenance.test.mjs` → 3 pass; `node tools/refresh/backfill-provenance.mjs` then `... --check` → exit 0; `node --test test/data-topic-integrity.test.js` → 14; `node --test test/suite-integrity.test.js`.
**Do not touch:** `app.js`, `search-engine.js`, `scan.mjs`, `resolve.mjs`, `data/catalog.json`, `backend/`.
**Stop and escalate if:** existing merge-topics tests go red for a reason other than the two new fields; items lack `show` or `topics`; `git log origin/main -1 -- data/discover.json` is under 2 hours old (rebase first).
**Definition of done:** suites green; both keys on every item; DRAFT PR `data(refresh): topics_source provenance and an always-present explicit key (#547, #560 §6.3)`.

### PKG-02 — Client catalogue carries `label_scope`
**Executor:** qwen.
**Context:** `tools/build-catalog-client.mjs` (Usage line 20: `[--out path] [--check]`; `CLIENT_SHOW_FIELDS` 46-50 already holds `explicit`; `main()` guard 112); `test/show-page.test.js:667` `"catalog-client.json is derived from catalog.json via the committed build script"` (runs `--check`; its documented mutation is "add a field to CLIENT_SHOW_FIELDS"); `test/suite-integrity.test.js:679` (show-page floor 44).
**Exact change:** 1. Append `"label_scope"` to `CLIENT_SHOW_FIELDS` with a one-line comment (`projectShow` writes `show[f] ?? null`, so every row gets `label_scope: null` until PKG-03). 2. `node tools/build-catalog-client.mjs`; commit `data/catalog-client.json`. 3. In the existing drift test at `show-page.test.js:667` add one assertion after the `--check` call: `assert.ok(CLIENT_SHOW_FIELDS.includes("label_scope"))` (import via `await import(pathToFileURL(...))`; the module's `main()` guard at 112 keeps it side-effect-free). Floor stays 44 (no new `test()`).
**Tests to add:** none new; mutation for the assertion: remove `"label_scope"` → red; mutation for the drift: edit one `editorial_note` in the client file → the existing test red.
**Commands:** `node tools/build-catalog-client.mjs --check` → 0; `node --test test/show-page.test.js` → ≥ 44 pass.
**Do not touch:** `data/catalog.json`, `app.js`, `test/explicit-badge.test.js`, `vercel.json`, workflows.
**Stop and escalate if:** `--check` is non-zero before your change (report; do not "fix" data).
**Definition of done:** DRAFT PR `data(catalog-client): ship label_scope to the client`.

### PKG-03 — The `label_scope: "general"` marker; Similar shows refuses general labels
**Executor:** opus — which shows are "general" is curation judgement.
**Context:** `tools/refresh/topic-uniformity.mjs` (`--min` default 8 at line 35; `--show "<title>"` prints one show's episodes); `tools/refresh/merge-topics.test.mjs` test `"REAL REPO: the nine shows judged to RANGE now vary per episode"` (~line 167-200) — its table names Stuff You Should Know, The Rest Is History, Huberman Lab, Ologies with Alie Ward, Unexplainable, Freakonomics Radio, Software Engineering Daily, Being an Engineer and one more: these nine are the expected candidate seed; issue #547; `docs/product/suggested-shows-requirements.md` §6.6 (899-912) and §6.13 (1069+); `app.js:4936` `similarShows(show, limit = 6)` (filters `state.catalog.shows` by shared nodes), `4954` `similarShowsSection`, called at `5368`; `data/catalog.json` `engineering/energy-fusion` shows: Titans of Nuclear, omega tau, CleanTechies Podcast, Catalyst with Shayle Kann, Lab to Market Leadership, CBC Ideas, TechSurge: Deep Tech Podcast; `test/show-page.test.js` similarShows tests at 813-919.
**Exact change:**
1. For the nine RANGE shows, the seven fusion-labelled shows and every show `node tools/refresh/topic-uniformity.mjs --min 12` lists, run `--show` and decide: a show ranging across roots gets `"label_scope": "general"` in `data/catalog.json` (expected: the nine, plus CBC Ideas, Lex Fridman Podcast, TechSurge, Catalyst; Titans of Nuclear and omega tau expected NOT). All others: no field. PR body: one line of reason per show, and an explicit reconciliation against the nine-show list (any of the nine NOT marked needs a stated reason). Keep `taxonomy_node_ids` unchanged; Lex Fridman keeps `[]`.
2. `node tools/build-catalog-client.mjs`; commit the client file.
3. `similarShows`: (a) `if (show?.label_scope === "general") return [];` before the node check; (b) candidate filter adds `&& s.label_scope !== "general"`.
**Tests to add:** `test/show-page.test.js` (+3, floor 44 → 47): `"similarShows returns [] for a general show"` (mutation: drop (a)); `"similarShows never lists a general show as a candidate"` (mutation: drop (b)); `"catalog.json: every label_scope value is 'general' or absent"` (mutation: set one to "broad").
**Commands:** `node tools/refresh/topic-uniformity.mjs --min 12`; `node --test test/show-page.test.js` → ≥ 47; `node tools/build-catalog-client.mjs --check` → 0; `node --test test/data-topic-integrity.test.js`.
**Do not touch:** `showsForCategory`, `renderCategory`, `generatedPlaylists` (PKG-04), `backend/`, `docs/DECISIONS.md`.
**Stop and escalate if:** more than ~15 shows look general (taxonomy problem — write it up instead); a show has < 3 episodes in `discover.json`.
**Definition of done:** suites green; DRAFT PR `catalogue: label_scope "general" on broad shows; Similar shows stops inheriting their label (#547, §6.6)`.

### PKG-04 — Generated playlists: one helper for both generators, per-show cap, no inherited general labels
**Executor:** qwen.
**Context:** `app.js:1103` `snapshot()` (`topics:` ~1115, `explicit:` ~1132, `release_date:` ~1140 — the whitelist); `2167-2169` the three `GENERATED_*` consts; `2170` `generatedPlaylists()` (per-leaf chain: `.filter(!slotItemIds)` → sort by `release_date` desc, id → `.slice(0, GENERATED_PLAYLIST_SIZE)` → `MIN` check); `2212` `generatedPlaylistById(id)` (audit app-1-6: rebuilds `gen-<leaf>` for the route with the same slice/MIN logic, falling back to `onLeaf` when unslotted < MIN); `search-engine.js:1661` `PER_SHOW_CAP = 2`, `1674` `diversify` (the rule, not to be called); `docs/product/playlists-requirements.md` §6.13 (949); `test/explicit-badge.test.js:28-70` `loadApp()` and `_state`; `test/home-v2-real-data.test.js:213` (asserts "Generated for you" renders over real data).
**Exact change:**
1. `snapshot()`: `topics_source: src.topics_source ?? null,` after `topics:`.
2. New `const GENERATED_PER_SHOW_CAP = 2;` beside the other three, comment citing `search-engine.js PER_SHOW_CAP`.
3. New `function leafPlaylistItems(candidates, { generalShowTitles })`: input an array of pool items already on the leaf; drops `it.topics_source === "show" && generalShowTitles.has(it.show)`; sorts as today (release_date desc, then id); takes items in order skipping any whose `show` already has `GENERATED_PER_SHOW_CAP`, until `GENERATED_PLAYLIST_SIZE`; returns the array (caller applies the `MIN` check). `generalShowTitles` = `new Set((state.catalog?.shows||[]).filter(s => s.label_scope === "general").map(s => s.title))`, computed by a small `generalShowTitleSet()` called once per caller.
4. `generatedPlaylists()`: replace the filter/sort/slice chain with `leafPlaylistItems(onLeaf.filter(it => !slotItemIds.has(it.id)), { generalShowTitles })`; keep the `MIN` check after.
5. `generatedPlaylistById()`: keep its unslotted-vs-onLeaf choice, then pass the chosen array through the same helper; keep the `MIN` check after.
**Tests to add:** `test/generated-playlists.test.js` (NEW, floor 6; `loadApp` copied from explicit-badge; seed `state.taxonomy`, `state.discover`, `state.catalog`, `state.session = {episodes:{}, cards:[]}`, `state.interests` via `_state`): `"snapshot keeps topics_source"` (mutation: remove the field); `"a generated playlist holds at most 2 items from one show"` (6 of one show + 2 of another → 4 items; mutation: cap 2 → 6); `"an item whose only claim to the leaf is a general show's label is excluded"` (mutation: drop the general filter); `"an item from a general show with a per-episode label is kept"` (mutation: filter on show alone); `"a leaf under GENERATED_PLAYLIST_MIN after the cap produces no playlist"` (mutation: MIN check before the cap); `"generatedPlaylistById returns the same ids as the Home card for the same leaf"` (mutation: apply the helper in one caller only).
**Commands:** `node --test test/generated-playlists.test.js` → 6; `node --test test/home-v2-real-data.test.js`; `node --test test/search-playlists.test.js`; `node --test test/save-playlist.test.js` (#839 saves generated playlists — must stay green); `node --test test/boot-path.test.js` (pins app-1-6); `node --test test/suite-integrity.test.js`.
**Do not touch:** `search-engine.js`, `buildPlaylist`, `topicSearchStatus`, `similarShows`, data files.
**Stop and escalate if:** `home-v2-real-data.test.js` or `boot-path.test.js` fails because real data now yields fewer generated playlists (report the count; product outcome, not a bug to hide).
**Definition of done:** suites green; DRAFT PR `home: generated playlists cap 2 per show and refuse inherited general labels (#547, #558 item 5)`.

### PKG-05a — `merge.mjs`: a general show's episode without its own topic is refused
**Executor:** qwen.
**Context:** `tools/refresh/topics.mjs:77-121` (`episodeTopics({ showTopics, editTopics, nodeIds, id })`, `TopicError` codes); `tools/refresh/merge.mjs:49-64` (`envPath`, the four env paths, `nodeIds`), 100 (preflight), 146 (item literal); `tools/refresh/merge-topics.test.mjs` header (harness); `tools/refresh/README.md`.
**Exact change:** 1. `episodeTopics` gains `labelScope` in its options object: when `labelScope === "general"` and `editTopics` is `undefined`/`null`, throw `new TopicError("TOPICS_REQUIRED_GENERAL", \`${id}: this show is marked label_scope "general"; its episodes need their own topics (1-3 taxonomy node ids in edits.json)\`)`. 2. `merge.mjs`: `const MERGE_CATALOG_PATH = envPath("MERGE_CATALOG_PATH", "data/catalog.json");` next to the others; build `const labelScopeByShow = new Map(catalog.shows.map(s => [s.title, s.label_scope]))`; pass `labelScope: labelScopeByShow.get(ep.show)` at both call sites (preflight 100 and literal 146). Document the env var in the header block (lines 13-14 style). 3. README: one paragraph under the topics rule naming the new error code.
**Tests to add:** `tools/refresh/merge-topics.test.mjs` (+2; floor 21 → 23 after PKG-01, else 18 → 20): `"a general show's episode without an override is refused with TOPICS_REQUIRED_GENERAL"` (fixture catalog via `MERGE_CATALOG_PATH`; mutation: drop the throw); `"a general show's episode with an override merges with topics_source 'episode'"` (mutation: pass `labelScope` undefined).
**Commands:** `node --test tools/refresh/merge-topics.test.mjs`; `node --test tools/refresh/candidates.test.mjs`; `node --test test/suite-integrity.test.js`.
**Do not touch:** `.github/`, `backend/`, `data/discover.json`, `scan.mjs`, `resolve.mjs`, the prompt file (PKG-05b).
**Stop and escalate if:** adding the catalog read breaks the existing harness tests that do not set `MERGE_CATALOG_PATH` (then default to `data/catalog.json` at repo root — it exists — and report if that still fails).
**Definition of done:** suites green; DRAFT PR `refresh: a general show's episode must carry its own topic (#547 fix 1, merge half)`.

### PKG-05b — Nightly prompt: the edits author must supply `topics` for a general show's episodes
**Executor:** opus — prompt/LLM design.
**Context:** `docs/agents/runner-prompts/foray-nightly.md:16` ("You produce `edits.json` (hooks, tags, and `topics` where an episode differs…") and 91-121 (the `topics` OPTIONAL rule, #292); `docs/nightly-refresh-cloud.md:17`; PKG-05a's error code.
**Exact change:** under the `topics` bullet (line ~102) add one paragraph: for an episode whose show has `"label_scope": "general"` in `data/catalog.json`, `topics` is REQUIRED — 1-3 ids from `data/taxonomy.json`, chosen from the episode's own description, primary first; `merge.mjs` refuses the run with `TOPICS_REQUIRED_GENERAL` otherwise. Add the error code to the failure list at 140-141. `docs/nightly-refresh-cloud.md:17`: no change unless the line's wording becomes wrong. Lex Fridman (`taxonomy_node_ids: []`): note in the same paragraph that its episodes are dropped by `resolve.mjs` until topics are authored — default stays "leave empty" (PR body asks the founder).
**Tests to add:** none (prose); `node --test tools/refresh/publish-digest.test.mjs` and `node --test tools/refresh/watch-nightly.test.mjs` must stay green (they may read the prompt file).
**Commands:** the two suites above.
**Do not touch:** `.github/workflows/*`, `merge.mjs` (PKG-05a).
**Stop and escalate if:** the prompt file is not the one the cloud routine reads (check `docs/nightly-refresh-cloud.md` for the routine's prompt source; if it names another location, edit that instead and say so).
**Definition of done:** DRAFT PR `refresh(prompt): general shows need per-episode topics in edits.json (#547 fix 1, prompt half)`.

### PKG-06 — `tools/refresh/relabel.mjs`
**Executor:** qwen.
**Context:** `tools/refresh/topics.mjs:77-121`; `data/discover.json` item shape (PKG-01); `data/taxonomy.json` (`nodes[].id`); `tools/refresh/backfill-provenance.mjs` (PKG-01) for the writer idiom.
**Exact change:** CLI `node tools/refresh/relabel.mjs --id <episode id> --topics a/b,c/d [--dry-run]` and `--from <json>` where the file is `[{ "id", "topics": [] }]`; env override `RELABEL_DISCOVER_PATH`. For each entry: find the item by `id` (exit 1 naming every missing id before writing anything); `episodeTopics({ showTopics: item.topics, editTopics: topics, nodeIds, id })` validates; set `item.topics` and `item.topics_source = "episode"`; write once at the end; print `id: [old] -> [new]`; `--dry-run` prints only.
**Tests to add:** `tools/refresh/relabel.test.mjs` (NEW, floor 4): `"relabels one item and stamps topics_source 'episode'"` (mutation: drop the stamp), `"refuses an unknown taxonomy id and writes nothing"` (mutation: skip validation), `"refuses when any id in a batch is missing, before writing"` (mutation: write per item), `"--dry-run changes nothing on disk"` (mutation: ignore the flag).
**Commands:** `node --test tools/refresh/relabel.test.mjs` → 4; `node --test test/suite-integrity.test.js`.
**Do not touch:** `data/discover.json` (PKG-07 runs it), `app.js`, `episodeTopics`'s signature.
**Stop and escalate if:** `episodeTopics` needs a signature change (wrap it instead).
**Definition of done:** DRAFT PR `refresh: relabel.mjs, a validated per-episode relabel for the committed pool`.

### PKG-07 — Relabel pass on the contaminated leaves
**Executor:** opus — curation.
**Context:** issue #547; `topic-uniformity.mjs --show`; the 26 `engineering/energy-fusion` items on main: CBC Ideas 10, Catalyst 6, Lex Fridman 3, TechSurge 3, Lab to Market Leadership 2, Stuff You Should Know 1, CleanTechies 1; `data/taxonomy.json` leaf `terms`; PKG-06's tool.
**Exact change:** batch file `docs/curation/relabel-2026-09-fusion.json` relabelling every item with `topics_source === "show"` whose show is `label_scope: "general"` and whose current leaf has ≥ 3 pool items (list with a one-off `node -e`). 1-3 topics per item from title + hook; an item genuinely about fusion keeps the leaf. Run `node tools/refresh/relabel.mjs --from docs/curation/relabel-2026-09-fusion.json` (`--dry-run` first). Record before/after `topic-uniformity.mjs` headline numbers in the PR.
**Tests to add:** `test/data-topic-integrity.test.js` (+1, floor 14 → 15): `"no engineering/energy-fusion item comes from a general show's inherited label"` (mutation: relabel one item back).
**Commands:** the relabel run; `node tools/refresh/topic-uniformity.mjs`; `node --test test/data-topic-integrity.test.js` → 15; `node --test test/home-v2-real-data.test.js`; `node tools/test-search.mjs` → pass.
**Do not touch:** `data/catalog.json`, `app.js`, the probe set in `tools/test-search.mjs`.
**Stop and escalate if:** `tools/test-search.mjs` goes red (report the probe); > ~60 items need relabelling (split by show).
**Definition of done:** DRAFT PR `data: relabel the fusion-contaminated episodes of general shows (#547 F19)`.

### PKG-08 — Fold breadth topics into `data/catalog-breadth.json`
**Executor:** qwen.
**Context:** `data/breadth-classification.json`: top level `{version, built_at, provenance, entries}` where **`entries` is an OBJECT keyed by the Apple collection id as a string** — `entries["73329284"] = { topics: [...], confidence: "high"|"medium"|"low", source, topic_confidences: [{node, confidence}], needs_review: bool, rationale, ... }`, 19,787 keys; distribution on main: high 17,599 / medium 1,838 / low 350; `needs_review` 3,042; fold set (`confidence !== "low" && !needs_review`) = 16,736. `data/catalog-breadth.json` is 12.47 MB, 2 lines, `shows[]` with `apple_collection_id` (number), `title`, `feed_url`, `artwork_url`, `apple_genre`, `apple_genre_ids`, `episode_count`, `explicit`, `chart_*`, `in_curated`; `backend/src/catalog/breadthCatalog.ts:74` (`taxonomy_node_ids?: string[]` already typed on the raw row); `test/data-topic-integrity.test.js:59-85` (the classification tests, for the reader idiom).
**Exact change:** new `tools/refresh/fold-breadth-topics.mjs` (env overrides `FOLD_BREADTH_PATH`, `FOLD_CLASS_PATH`, `FOLD_TAXONOMY_PATH`): for each show, `entry = entries[String(show.apple_collection_id)]`; if entry and `entry.confidence !== "low"` and `entry.needs_review !== true`, `show.taxonomy_node_ids = [...new Set(entry.topics)].filter(id => nodeIds.has(id))`, else `[]`. Write back single-line JSON exactly as the file is (`JSON.stringify(doc)` + `"\n"` — check the trailing newline on main first and match it). Print `{ folded, empty, unknown_id }`; expected ≈ `{ folded: 16736, empty: 3051, unknown_id: 0 }` — report actuals. `--check` exits 1 if a run would change anything.
**Tests to add:** `tools/refresh/fold-breadth-topics.test.mjs` (NEW, floor 5; fixture `entries` as an object with three keys): `"folds topics for a high/medium entry"`, `"leaves [] for a low-confidence or needs_review entry"` (mutation: drop either condition), `"drops an id the taxonomy does not know"` (mutation: skip the filter), `"--check is 0 after a run and 1 after an edit"`, `"REAL DATA: every taxonomy_node_ids in data/catalog-breadth.json resolves"` (mutation: inject a bad id in a temp copy → red). Nothing is added to `test/data-topic-integrity.test.js` (PKG-01/07 own its floor).
**Commands:** `node tools/refresh/fold-breadth-topics.mjs`; `... --check` → 0; `node --test tools/refresh/fold-breadth-topics.test.mjs` → 5; `cd api && npm ci && npm test` (`api/_test/vercel-bundle.test.mjs` and search tests read this file); `node --test test/suite-integrity.test.js`.
**Do not touch:** `backend/src/**`, `api/**`, `data/breadth-classification.json`.
**Stop and escalate if:** the file exceeds 16 MB after folding (Vercel `includeFiles` budget); api tests fail on the new field.
**Definition of done:** DRAFT PR `data(breadth): fold classified topics into catalog-breadth.json (§6.4, founder Q3 default)`.

### PKG-09 — Backend stops zeroing breadth `taxonomy_node_ids`
**Executor:** opus — `backend/src/` is DENIED; needs a founder merge.
**Context:** `backend/src/catalog/breadthCatalog.ts:116` (curated branch: `show.taxonomy_node_ids ?? []`), `:134` (breadth branch: `[]`); the breadthCatalog test (`git grep -l loadBreadthCatalog origin/main -- backend/test`); `api/shows/search.ts:161-168` (`showByIdFromCatalog` reads `loadBreadthCatalog()`; the `?id=` branch at 200-213 answers `{ id, show, degraded:false }` with that row) — this IS the endpoint the show page uses for a breadth row (`app.js:5259-5264`, `state.breadthShowCache[show_id] = data.show`). Reviewer note rejected in part: `api/shows/[show_id]/episodes.ts:232`'s `showHeader` feeds the episode list's header, not `breadthShowCache`; no `api/` change is needed.
**Exact change:** line 134 → `taxonomy_node_ids: show.taxonomy_node_ids ?? [],`. Backend test: a breadth raw row with `taxonomy_node_ids: ["science"]` loads with that array; one without loads `[]` (mutation: revert). Confirm by reading `api/shows/search.ts:196-213` that the row is passed through unmodified (if it is projected, add the field to the projection — same PR, api/ is a human-look path; say so).
**Commands:** `cd backend && npm ci && npm run typecheck && npm test`; `cd api && npm ci && npm run typecheck && npm test`.
**Do not touch:** `app.js` (PKG-10), data files, `searchBreadthShows.ts`.
**Stop and escalate if:** the shard index (`api/shows/index/[...path].ts`) is what the page reads for some tier — note it, keep this PR to the loader.
**Definition of done:** both packages green; DRAFT PR `backend(catalog): breadth shows keep their folded taxonomy nodes (§6.4)`; body says it waits for a human merge.

### PKG-10 — Breadth show page: index-seeded shows upgrade to the API row
**Executor:** qwen.
**Context:** `app.js:5215-5266` — the function that resolves a show missing from the curated catalogue (`git grep -n "const fromIndex = showIndex" origin/main -- app.js`; `bindRetry(v, () => resolveMissingShow(show_id))` at ~5257 names it): the `fromIndex` branch (5219-5229) seeds `state.breadthShowCache[show_id] = { ..., taxonomy_node_ids: [], tier }`, renders and RETURNS, so the `api/shows/search?id=` fetch (5237-5266) never runs for an index-listed show — after PKG-09 its chips would still never appear; `renderShow` 5269 (`chips` from `show.taxonomy_node_ids` ~5298; `similarShowsSection(show)` 5368); `showById` 3656-3661; `test/show-page.test.js:508` (breadth-tier test) and 813-919 (similarShows tests); `test/show-pages-3b-full-catalogue.test.js`.
**Exact change:** 1. In the `fromIndex` branch, after `renderShow(...)`, do not return before starting the same `fetchApiJson(\`api/shows/search?id=...\`)` used below (extract the fetch-and-cache part into `function upgradeBreadthShowRow(show_id)` used by both paths); on a row with `Array.isArray(row.taxonomy_node_ids) && row.taxonomy_node_ids.length`, set `state.breadthShowCache[show_id] = { ...state.breadthShowCache[show_id], ...row }` and call `renderShow(show_id, ...)` again only if `parseShowRoute()?.show_id === show_id` (read how the existing branch guards "navigated away" at ~5240 and reuse that check). A row with empty nodes or a failed fetch changes nothing (no flash, no error). 2. No change to `renderShow`.
**Tests to add:** `test/show-page.test.js` (+3; floor 47 → 50 after PKG-03, else 44 → 47): `"a breadth row in the cache with taxonomy_node_ids renders chips and a Similar shows section"` (seed `state.breadthShowCache["123"] = { show_id:"123", title:"X", taxonomy_node_ids:["science"], tier:"breadth" }` plus a curated show on `["science"]`; assert `#/category/science` and the heading; mutation: `similarShows` returns `[]` for `tier === "breadth"`); `"an index-seeded show is upgraded when the API row carries nodes"` (stub `fetchApiJson` to resolve `{ show: { ..., taxonomy_node_ids: ["science"] } }`; assert the cache row and a second render with chips; mutation: keep the early return); `"an API row without nodes leaves the seeded row and does not re-render"` (mutation: drop the length guard).
**Commands:** `node --test test/show-page.test.js`; `node --test test/show-pages-3b-full-catalogue.test.js`; `node --test test/suite-integrity.test.js`.
**Do not touch:** `similarShows` (PKG-03), `api/`, `backend/`.
**Stop and escalate if:** the show route parser does not expose the current show id (name what it exposes).
**Definition of done:** suites green; DRAFT PR `show page: breadth shows show their chips and Similar shows once the API row carries nodes (§6.4 client half)`; body says the live effect waits for PKG-09's merge.

### PKG-11 — Playlist parts keep `release_date`; stale generator comment
**Executor:** qwen.
**Context:** `app.js:2451` `PLAYLIST_PART_FIELDS`, `2456` `playlistPart`, `10825` `archivedRow` (`fmtDate(item.release_date)`), `2240-2260` header comment of `generatedPlaylistCandidatesForQuery` (2261) describing the pre-F14 `cardSlots`/`subjectQueueById` mechanism; `docs/product/playlists-requirements.md` §6.14 (957); `test/playlist-durability.test.js` (`loadApp`, part fixtures; the byte-budget test at 386).
**Exact change:** 1. Append `"release_date"` to `PLAYLIST_PART_FIELDS`. 2. Rewrite the comment to describe today's code: it filters `generatedPlaylists()` (leaf-based, `state.interests`-ordered) by title substring; delete the `cardSlots`/`subjectQueueById` sentences.
**Tests to add:** `test/playlist-durability.test.js` (+1, floor 39 → 40): `"a playlist part stores release_date, so an archived part still shows its date"` (build from an item with `release_date`, remove it from the pool, render the detail page, assert the date; mutation: remove the field).
**Commands:** `node --test test/playlist-durability.test.js` → ≥ 40; `node --test test/search-playlists.test.js`; `node --test test/save-playlist.test.js`.
**Do not touch:** `snapshot()`, storage keys, `docs/DECISIONS.md`.
**Stop and escalate if:** the byte-budget test at 386 fails (report the numbers).
**Definition of done:** DRAFT PR `playlists: parts keep their publish date; generator comment matches the code (#558 items 8, 10)`.

### PKG-12 — #217: re-measure the Gershwin tag
**Executor:** qwen.
**Context:** issue #217; `search-engine.js:1456` `STRONG_RATIO = 0.5` (the bar logic follows at ~1488/1547), `1661` `PER_SHOW_CAP`, `1674` `diversify`; `tools/test-search.mjs:93` (Usage: the only flag is `--tiering`, line 1143 — there is NO single-query mode), `119-143` `fullPool()` (session.json episodes + validated-links overlay, then discover items deduped), `160-168` `search()` (`SE.interpretQuery(q, ctx)` → `SE.searchWithRelaxation(pool, interp, 2, itemTags, () => 0.5)` → `SE.classifyResults(results)`), `145` `freshCtx()`; `data/discover.json` item `sticky-notes--gershwin-rhapsody`.
**Exact change (no repo change):** write `<scratchpad>/jazz-probe.mjs` that copies lines 100-168 of the harness verbatim (imports, the five data reads, `fullPool`, `freshCtx`, `search`) but does NOT import the battery, then prints for `"the history of jazz"`: `status`, and each pick's `id`, `show`, score sum. Run it against the worktree pool as is; then apply `music/jazz` to the Gershwin item locally (PKG-06's tool, or a direct edit in the worktree), run again, revert. Post both lists, sums, `STRONG_RATIO` bar and the command lines as a comment on #217 with the recommendation (3 kept → tag; 2 → hold). Open a DRAFT PR `data: music/jazz on Gershwin's Rhapsody In Blue (#217)` only if the numbers say tag, with `node tools/test-search.mjs` green in the body.
**Tests to add:** none unless the PR opens.
**Commands:** `node <scratchpad>/jazz-probe.mjs` from the worktree root (the script resolves `ROOT` from its own path — set `ROOT` to the worktree explicitly); `node tools/test-search.mjs` → pass.
**Do not touch:** `search-engine.js`, `tools/test-search.mjs`, any other item.
**Stop and escalate if:** `classifyResults`'s signature differs from the harness's use (report; do not guess).
**Definition of done:** the #217 comment with both measurements; PR only if warranted.

### PKG-13 — Personas: load `data/personas.json`; `applyPersonaPick()` as a decaying prior
**Executor:** qwen.
**Context:** `data/personas.json` (`{version, notes, default_persona_id, personas[]}`; each `{id, label, description, seed_confidence, weights:[{node_id, weight}]}`; ids curious-engineer-maker, history-and-ideas, health-and-performance, true-crime-and-story, comedy-and-culture (0.35, 5-8 weights), generalist (0.15, 41 weights)); `app.js:18165-18178` `documentsP` and the destructure at 18225-18228; `6445` `expandTaxonomyPick`, `6455` `ONBOARDING_SEED_LIFT`, `6487-6503` `applyOnboardingPicks` (clamp is inline `Math.max(0, Math.min(1, ...))` at ~6497; returns root ids), `858` `setInterest`, `883` `saveInterests`, `931` `nudgeTopics`; `674` `toEventRow` (`default: return null; // unsaved / playlist_* / family_mode / refreshed_all — local only`); `test/data-topic-integrity.test.js:134`; issue #70; the legal-copy coupling in §1 (`legal-citations` counts `logEvent` types).
**Exact change:**
1. `fetchJson("data/personas.json")` appended to `documentsP`; `state.personas` appended to the destructure; null-safe reads `state.personas?.personas || []`.
2. `function personaById(id)`.
3. `function applyPersonaPick(id)`: null persona → `false`; for each `{node_id, weight}` and every `n` of `expandTaxonomyPick(node_id)` present in `state.interests`: `setInterest(n, Math.max(0, Math.min(1, state.interests[n] + persona.seed_confidence * weight)))` (the inline clamp, exactly as `applyOnboardingPicks` uses — there is no `clamp01` helper; do not add one); `saveInterests()`; `state._interestsGen = (state._interestsGen || 0) + 1`; return the touched root ids (same shape as `applyOnboardingPicks`).
4. `logEvent("persona_picked", { persona_id: id })` inside; add `persona_picked` to the `default:` comment in `toEventRow`. **Legal copy in the same PR:** `docs/legal/privacy-policy.md:224` words become "Twenty of the twenty-four"; add `persona_picked` to the §2 "Not sent" list (237+); `docs/legal/data-safety.md:44` becomes "Exactly 4 of 24".
**Tests to add:** `test/personas-client.test.js` (NEW, floor 5; `loadApp` from explicit-badge; seed real `data/taxonomy.json` and `data/personas.json`, then `loadInterests()`): `"applyPersonaPick lifts the persona's roots and their leaves by seed_confidence × weight"` (mutation: use `weight` alone); `"an unknown persona id returns false and writes nothing"`; `"the pick is a prior: four thumbs-down nudges on the lifted root bring it below the unlifted default"` (mutation: lift × 10); `"persona_picked is logged and stays local (toEventRow returns null)"`; `"init's document batch names data/personas.json"` (source regex, as `first-time-onboarding.test.js:191` does; mutation: remove the fetch).
**Commands:** `node --test test/personas-client.test.js` → 5; `node --test test/first-time-onboarding.test.js` → ≥ 33; `node --test test/interests-roots.test.js`; `node --test test/legal-citations.test.js` → ≥ 20; `node --test test/suite-integrity.test.js`.
**Do not touch:** the first-run sheet (PKG-14), `backend/`, `data/personas.json`.
**Stop and escalate if:** `legal-citations` still fails after the three copy edits (paste its message).
**Definition of done:** DRAFT PR `personas: the client loads data/personas.json and can seed a decaying prior (#70, part 1)`.

### PKG-14 — Persona row in the first-run Preferences pane
**Executor:** opus — product/UX judgement and copy.
**Context:** `app.js:7229` `showFirstTimeExplainerOnce`; step 2 near `7420` (`const go = ddEl("button", "fy-sheet-go", "Show my picks")`) and `7448` (`const applied = applyOnboardingPicks([...picked], typed)`), the chips/typed/Skip handling around them; `6532` `redealAfterOnboardingPicks`; `docs/ux/foray-mockup.jsx` (`PrefsScreen`); `docs/marketing/09-product-feature-review.md` §4; issue #70; `test/first-time-onboarding.test.js` (34 static tests, floor 33); `test/listener-copy.test.js`; `test/onboarding-sheet-once.test.js` (floor 6); `test/no-horizontal-scroll.test.js`; the legal-copy coupling in §1.
**Exact change:** above the chip grid in step 2, five persona pills (all but `generalist`), single-select, labelled `persona.label`; tapping lights a pill only; "Show my picks" runs `applyPersonaPick(selected)` first (if any), then `applyOnboardingPicks([...picked], typed)`, then one `redealAfterOnboardingPicks` with the union of roots. Skip stays a skip. Local-only events: `onboarding_seen` (step 1 render), `interests_seeded {chips: n, persona: id|null}`, `onboarding_skipped {step: 1|2}` — each added to `toEventRow`'s default comment, the policy §2 "Not sent" list, and the two count sentences (policy 224 → "Twenty-three of the twenty-seven"; data-safety 44 → "4 of 27", after PKG-13's 24). Copy: one line above the pills, ≤ 12 words, no "we". Screenshots of both panes (`node tools/web/prepare-dist.mjs`, static server on `dist/`).
**Tests to add:** `test/first-time-onboarding.test.js` (+4, floor 33 → 37): one pill per non-generalist persona; persona + Show my picks lifts that persona's top root more than a chip pick alone (mutation: skip `applyPersonaPick`); Skip with a lit pill writes nothing; the three events fire with the stated payloads and map to null.
**Commands:** `node --test test/first-time-onboarding.test.js` → ≥ 37; `node --test test/onboarding-sheet-once.test.js` → ≥ 6; `node --test test/listener-copy.test.js`; `node --test test/personas-client.test.js`; `node --test test/legal-citations.test.js`; `node --test test/no-horizontal-scroll.test.js`.
**Do not touch:** `backend/`, `data/personas.json`, the Welcome pane's existing copy.
**Stop and escalate if:** five pills do not fit at 320 px even as a wrapping grid (stop, attach the screenshot).
**Definition of done:** suites green, screenshots attached; DRAFT PR `onboarding: pick a starting profile (personas surfaced, #70 part 2)`.

### PKG-15 — Depth ladders: load, port `inferLadderProgress`, `cp_ladder_progress`
**Executor:** qwen.
**Context:** `docs/curation/ladders-client-spec.md` §1 Fetching (33), §2 inference (47), §3 cache (77), §5 hard constraints (114); `backend/src/curation/ladderProgress.ts:62` `inferLadderProgress` (exported) and `:95` `topologicalOrder` (module-private — port both anyway); `backend/test/ladderProgress.test.ts` (8 `it()` cases); `data/ladders.json` (`{version, built_at, ladders:[{id:"fusion-101", status:"draft", ..., rungs:[{id, level, subtopic, goal, episode_ids, prerequisites}]}]}`, 7 rungs); `app.js:1973` `pickedHistory()`, `18165` `documentsP`, `157` `lsSet`; `test/data-deletion.test.js:443` (pins the cp_ family count — the title says 22 but the assertion at 561 is **31**; a new `cp_` literal makes it 32) and `:572` (every family in code must be a row in policy §1 and vice versa); `docs/legal/privacy-policy.md:59` §1 table, row format at 129 (`| \`cp_history\` | … | **No** |`); `deleteMyData` purges by prefix (`app.js:15556`: `k.startsWith("cp_")`), so no key list changes.
**Exact change:** 1. `fetchJson("data/ladders.json")` in `documentsP`; `state.ladders = (doc?.ladders || []).filter(l => l.status === "published")` (a fetch failure → `[]`, nothing thrown). 2. `inferLadderProgress(ladder, pickedEpisodeIds)` and `ladderTopologicalOrder(rungs)` ported line for line. 3. `ladderProgressAll()` → recompute from `pickedHistory()` on every call and `lsSet("cp_ladder_progress", Object.fromEntries(list.map(p => [p.ladderId, p])))` (derived cache, never read as truth). 4. Policy §1 row for `cp_ladder_progress` (derived from `cp_history`, local, never sent). 5. `test/data-deletion.test.js:443/561`: 31 → 32 with a dated note in the comment block (same pattern as the `cp_diag` note); data-deletion floor unchanged (no new test).
**Tests to add:** `test/ladders-client.test.js` (NEW, floor 11): the 8 ported cases with the TS fixture ladder, plus `"draft ladders are filtered out at load"` (mutation: drop the filter), `"cp_ladder_progress is rewritten from cp_history on every call"` (mutation: read the cache), `"a fetch failure leaves state.ladders [] and throws nothing"`.
**Commands:** `node --test test/ladders-client.test.js` → 11; `node --test test/data-deletion.test.js` → ≥ 88; `node --test test/legal-citations.test.js`; `node --test test/suite-integrity.test.js`.
**Do not touch:** card rendering (PKG-16), `backend/`, `data/ladders.json`.
**Stop and escalate if:** `data-deletion.test.js` needs a change outside `app.js`/`test/`/`docs/legal/`.
**Definition of done:** DRAFT PR `ladders: the client reads published ladders and infers the entry rung (spec §1-§3)`.

### PKG-16 — Ladder rung line on a Home card
**Executor:** qwen.
**Context:** spec §4 (91) and §5 (114); `app.js:6319` `miniCard(slot)` — the card body renderer (the Suggested composer at 10562-10569 says "the card body is exactly miniCard(slot)"); locate the why-line inside `miniCard` by grepping `why` within 6319-6440; `9961` `renderHome`; `test/card-anatomy.test.js` (floor 18); `test/listener-copy.test.js`.
**Exact change:** in `miniCard`, after the why-line: if `state.ladders?.length`, use a per-render memo (`state._ladderProgressRender`, set by `renderHome` before dealing cards and cleared after) of `ladderProgressAll()`; for a card whose `slot.item.id` is in the `episode_ids` of the entry rung (`entryRungId`, or any `rootRungIds` rung when it is null and nothing is reached) of a published ladder, append `<p class="card-ladder">Step {k} of {n} — {ladder.title}</p>`, `k` = 1-based position in topological order. No re-ranking, no reserved slot. One `.card-ladder` rule in `styles.css`.
**Tests to add:** `test/ladders-client.test.js` (+3, floor 11 → 14): `"a card whose item is in the entry rung of a published ladder shows 'Step k of n — title'"` (fixture `status: "published"` in the test only; mutation: render for any rung); `"a draft ladder never produces the line"`; `"the line never changes which items are dealt"` (deal with and without ladders, equal ids; mutation: push the ladder item into slot 1).
**Commands:** `node --test test/ladders-client.test.js` → 14; `node --test test/card-anatomy.test.js` → ≥ 18; `node --test test/home-v2.test.js`; `node --test test/listener-copy.test.js`; `node --test test/tap-targets.test.js`.
**Do not touch:** `buildCards`, `data/ladders.json`, `styles.css` beyond one rule.
**Stop and escalate if:** `card-anatomy.test.js` pins an exact child count for the card.
**Definition of done:** DRAFT PR `ladders: a rung line under the why-line of a laddered pick (spec §4)`.

### PKG-17 — Event-sync mapping tests
**Executor:** qwen.
**Context:** `app.js:668` `SB_ARCHETYPES`, `674-712` `toEventRow` (picked/saved/thumbs/session_shown → rows; default null), `736` `trySyncEvents`, `762-805` `syncEventsOnce` (500-row chunks; **each chunk is marked synced the moment its POST is ok**; `if (!res.ok) return;` leaves that chunk and the rest for next time — audit app-1-10); `docs/curation/events-client-integration-spec.md` §2 (46), §3 (68), §5 (115); `backend/src/types/events.ts` (mirror as literal expectations, no import); `test/data-deletion.test.js` (its `window.forayEventLog`/`fetch` harness and its app-1-10 tests — cite them, do not duplicate); `docs/legal/privacy-policy.md:237` (the "Not sent" list).
**Exact change:** tests only.
**Tests to add:** `test/event-sync-mapping.test.js` (NEW, floor 8): exact rows for `picked` (context in `SB_ARCHETYPES` carried; `"upnext"` → `null` archetype), `saved`, `thumbs` (with `node_id` → row, without → `null`, `episode_slug` absent when missing), `session_shown` → `session_built {session_key, builder}`; every type in the policy's "Not sent" list → `null`; `"syncEventsOnce posts ≤ 500 rows per request and marks each chunk as it lands"` (1,001 rows → 3 POSTs; third returns 500 → `markSynced` called exactly twice, with chunks 1 and 2's ids including any local-only ids between them, never chunk 3's; a second run POSTs only the remaining rows; mutation: mark all ids before the loop → red); `"a batch of only local-only rows marks them synced without a POST"`.
**Commands:** `node --test test/event-sync-mapping.test.js` → 8; `node --test test/data-deletion.test.js`; `node --test test/suite-integrity.test.js`.
**Do not touch:** `app.js` logic (report defects in the PR), `backend/`.
**Stop and escalate if:** the mapping disagrees with `events.ts` for an existing type — a finding for PKG-18/19.
**Definition of done:** DRAFT PR `tests: pin the event-sync mapping and per-chunk batching (ADR-0005 client half)`.

### PKG-18 — `card_shown` end to end
**Executor:** opus — DENIED contract file, Q6, legal copy.
**Context:** spec §4 (88); `backend/src/types/events.ts:26-42` `EventTypeSchema` (has `card_shown`), `44` `ArchetypeSlotSchema`, `54` `CardShownPayloadSchema`; `backend/migrations/0009_events.sql:12` (check on `type` only), `:20` (`archetype text`, unconstrained); `backend/src/curation/interestLearning.ts:204` (how `card_shown` is read); `backend/test/events.test.ts`; `app.js:2036` `buildCards`, `6319` `miniCard`, `9961` `renderHome`, `6532-6545` `redealAfterOnboardingPicks` (undoes `cp_seen` for the pre-pick deal at ~6541); `674` `toEventRow`; policy `224`, §2 table `230-236`; `data-safety.md:44`; `test/legal-citations.test.js:730-760`.
**Exact change:** 1. `ArchetypeSlotSchema` gains `"top"` with a comment; `backend/test/events.test.ts` accepts it. 2. Client, one rule: emit `logEvent("card_shown", { episode_id, topics, archetype: slot.role })` once per `(deal, episode_id)` when `renderHome` renders a slot — a `Set` on `state` (`state._cardShownDeal`), cleared in `buildCards` and in `redealAfterOnboardingPicks` next to the `cp_seen` undo. 3. `SB_ARCHETYPES` gains `"top"`; `toEventRow`: `case "card_shown": return SB_ARCHETYPES.has(p.archetype) ? row("card_shown", { episode_slug: p.episode_id, topics: p.topics || [], archetype: p.archetype }, p.archetype) : null`. 4. Legal copy: a `card_shown` row in the §2 "Sent" table; the 224 sentence's words and `data-safety.md:44` numbers updated (sent +1, total +1 relative to whatever main holds at merge time — compute from the test's message); `test/legal-citations.test.js` green. 5. `test/event-sync-mapping.test.js` +2 (floor 8 → 10): the mapping; one emission per dealt card per deal (mutation: emit per render).
**Commands:** `cd backend && npm test`; `node --test test/event-sync-mapping.test.js` → 10; `node --test test/home-v2.test.js`; `node --test test/legal-citations.test.js`; `node --test test/first-time-onboarding.test.js`; `node --test test/suite-integrity.test.js`.
**Do not touch:** migrations, `interestLearning.ts`.
**Stop and escalate if:** any migration constrains `archetype` (then it is a migration — founder territory).
**Definition of done:** suites green; DRAFT PR `events: card_shown reaches the events table (spec §4; archetype "top")`; body notes the human merge for `backend/src/`.

### PKG-19 — Observed `finished` / `skipped_at` from in-app playback
**Executor:** opus — player/native-engine seams and the provenance rule.
**Context:** spec §1 (23) and §4 (88); `backend/src/types/events.ts:65` `SkippedAtPayloadSchema {elapsed_seconds, duration_seconds?}`, `:75` `FinishedPayloadSchema {percent_complete 0..1, source}`; `player/client.js:1354` `_episodeEndedListeners`, `1360` `_announceEpisodeEndedIfNeeded` (Foray → return; native mode with `engineWalksAtEnd` → return), `4642` `onEpisodeEnded(fn)`; `app.js:18352-18353` (wires `advanceQueueOnEnded`), `3485-3500` `advanceQueueOnEnded` doc, `3436` `applyEngineAdvance` (logs `play_started` with the hop's `step.ts`), `3465` `drainEngineEvents`; `player/episode-progress.js`; policy `224`, §2 table, "Not sent" list; `data-safety.md:44`. (The old "line 128 stale `finished` row" instruction is dropped: line 128 is the `cp_interests` row; `finished` appears in the policy only in prose at line 37.)
**Exact change:** emit `finished {episode_id, topics, percent_complete: 1, source: "observed"}` (web) from the one `onEpisodeEnded` consumer and (native) once per replayed `advanced` hop in `applyEngineAdvance`, with the hop's `ts`; emit `skipped_at {episode_id, topics, elapsed_seconds, duration_seconds}` when a different episode starts while the current one has `elapsed > 0` and `< 0.85 × duration` — from the single function that owns "the current episode changed" (find it; do not add a second owner). Never for a Foray clip. Map both in `toEventRow`. Legal copy: two "Sent" rows; the 224 words; `data-safety.md:44`; prose at 37 if it now misstates.
**Tests to add:** `test/event-sync-mapping.test.js` +3 (floor accordingly); one test that native mode emits `finished` exactly once per advance replay (mutation: emit on both paths → 2); a `player/` test only if a new hook is added.
**Commands:** `node --test test/event-sync-mapping.test.js`; `node --test test/up-next-autoadvance.test.js`; `node --test test/engine-continuation.test.js`; `node --test player/transport-reconcile.test.js`; `node --test test/legal-citations.test.js`; `node tools/ci/engine-ci.mjs engine-paths` (if it runs without Swift; else say so).
**Do not touch:** `mobile/`, Swift, `backend/src/`.
**Stop and escalate if:** the native hop record lacks elapsed/duration (then `skipped_at` is web-only and the PR says so).
**Definition of done:** DRAFT PR `events: observed finished and skipped_at from in-app playback (spec §1.2)`.

### PKG-21 — Remove the international breadth catalogue (gated on founder Q4)
**Executor:** qwen. Runs only after the founder answers Q4 "yes".
**Context:** `data/catalog-breadth-intl.json.gz` (read by no code); every reference on main: `.gitignore:9` (`data/catalog-breadth-intl.json`), `tools/mobile/prepare-webdir.test.mjs:153` (the "no pipeline input is in the plan" list), `tools/shows/identity.mjs:3` (comment), and the "138,470 / 121,786" claims in `docs/CATALOG-PIPELINE.md:187`, `docs/curation/catalogue-broadening.md:69,129`, `docs/product/suggested-shows-requirements.md:97,193-198,891`, `docs/catalog-growth-plan.md:32`, `docs/search-plan.md:741,1256`, `docs/show-pages-plan.md`, `docs/curation/grilling-foray-sourcing.md:212`, `docs/curation/alcohol-forms-coverage.md:161`, `docs/curation/breadth-classification-methodology-plan.md:295`, `docs/curation/search-coverage-gaps.md:26`, `docs/curation/cataloguing-lessons-for-corpus.md`, `docs/agents/fleet-review-2026-08.md:213`, `docs/codebase-audit-2026-09-14.md`, `docs/adr/0006`, `docs/DECISIONS.md`.
**Exact change:** `git rm data/catalog-breadth-intl.json.gz`; remove `.gitignore:9`; remove the list entry at `prepare-webdir.test.mjs:153` (the test still guards the other four); fix the comment in `tools/shows/identity.mjs:3`; **edit the living docs only**: `docs/CATALOG-PIPELINE.md`, `docs/curation/catalogue-broadening.md`, `docs/product/suggested-shows-requirements.md` — replace the claim with "19,787 US shows (the international file was retired on <date>; DECISIONS <entry>)". **Leave as dated records, unedited:** `docs/catalog-growth-plan.md`, `docs/search-plan.md`, `docs/show-pages-plan.md`, `docs/curation/grilling-foray-sourcing.md`, `docs/curation/alcohol-forms-coverage.md`, `docs/curation/breadth-classification-methodology-plan.md`, `docs/curation/search-coverage-gaps.md`, `docs/curation/cataloguing-lessons-for-corpus.md`, `docs/agents/fleet-review-2026-08.md`, `docs/codebase-audit-2026-09-14.md`; `docs/adr/0006` and `docs/DECISIONS.md` are PKG-22's.
**Tests to add:** none. Final check: `git grep -n "catalog-breadth-intl" -- . ':!docs/adr' ':!docs/DECISIONS.md' ':!docs/catalog-growth-plan.md' ':!docs/search-plan.md' ':!docs/show-pages-plan.md' ':!docs/curation/grilling-foray-sourcing.md' ':!docs/curation/alcohol-forms-coverage.md' ':!docs/curation/breadth-classification-methodology-plan.md' ':!docs/curation/search-coverage-gaps.md' ':!docs/curation/cataloguing-lessons-for-corpus.md' ':!docs/agents/fleet-review-2026-08.md' ':!docs/codebase-audit-2026-09-14.md'` → empty.
**Commands:** `node --test tools/mobile/prepare-webdir.test.mjs`; `node tools/ci/generate-manifest.mjs --check` → 0; `node tools/ci/run-suites.mjs`.
**Do not touch:** `docs/adr/`, `docs/DECISIONS.md`, `.github/`.
**Stop and escalate if:** any file outside the enumerated list references the gz (report it; do not edit).
**Definition of done:** DRAFT PR `data: retire the unread international breadth catalogue (§6.5, founder Q4)`.

### PKG-22 — Record the rulings and corrections in `docs/DECISIONS.md`
**Executor:** opus — DENIED path, human merge.
**Context:** `docs/DECISIONS.md:1-15` (format, newest first); the D5 entry (`git grep -n "D5" origin/main -- docs/DECISIONS.md`); `docs/adr/0006` Context (121,786 intl shows); PKG-03/09/18 PR bodies; `status.tsv:170` (Q1 default shipped).
**Exact change:** one dated entry with four sub-headings: Family Mode unrated policy (the shipped #835 default, or the founder's ruling verbatim); `label_scope: "general"` and what the generators do with it; breadth shows carry classified topics (confidence ≠ low, not needs_review); `card_shown` archetype `"top"`. A one-line correction under D5 pointing at the leaf-based generator. If PKG-21 ran, one line in ADR-0006's Context that the intl file was retired.
**Tests:** `node --test test/legal-citations.test.js`; `node --test test/audit-status.test.js`.
**Do not touch:** code.
**Stop and escalate if:** the founder has answered any question differently from the default — record his words.
**Definition of done:** DRAFT PR `docs(decisions): catalogue and personalization package rulings`; body says human merge.

## 4. Sequencing

**Throttle (founder, 2026-09-25: the box is memory-bound):** at most TWO agents at once during wave 1, ONE during any wave that touches `app.js`; one `node --test <file>` per machine at a time; no agent runs `cd api && npm test` or `cd backend && npm test` while another test process is running.

**Wave 1 (disjoint files; run in pairs):** PKG-01 (`tools/refresh/topics.mjs`, `merge.mjs`, new backfill, `data/discover.json`, `test/data-topic-integrity.test.js`, `merge-topics.test.mjs`) with PKG-08 (`data/catalog-breadth.json`, new fold tool + suite); then PKG-02 (`build-catalog-client.mjs`, client file, `show-page.test.js` one assertion) with PKG-17 (new test only); then PKG-11 (`app.js` PLAYLIST_PART_FIELDS + comment) with PKG-12 (no repo change); then PKG-13 (`app.js` personas + policy counts) alone; then PKG-15 (`app.js` ladders + `data-deletion` pin + policy row) alone. `app.js` merge order 11 → 13 → 15 (rebase each).

**Wave 2 (after wave 1 merges):** PKG-03 (needs 02; `data/catalog.json`, `similarShows`, `show-page.test.js`) alone; then PKG-04 (needs 01 + 02; generators, `snapshot`, new suite) alone; PKG-06 (needs 01; new tool + suite) may pair with either; PKG-09 (needs 08; backend, human merge) may pair with any `app.js` task; PKG-14 (needs 13; sheet) alone; PKG-16 (needs 15; `miniCard`) alone; PKG-18 (needs 17; backend contract + `toEventRow` + `renderHome`) alone, after 16. All three of 13/14/18 edit the same policy count sentences — rebase and recompute the numbers from `legal-citations`' failure message each time.

**Wave 3:** PKG-05a (needs 03; `merge.mjs`) then PKG-05b (needs 05a; prompt prose); PKG-07 (needs 03 + 06; data relabel) alone (it rewrites `data/discover.json`); PKG-10 (needs 03's floor; live effect needs 09 deployed) alone; PKG-19 (needs 17 and 18's mapping/copy merged; player seams — run alone, the riskiest); PKG-21 (needs founder Q4).

**Wave 4:** PKG-22 (after 03, 09, 18 merge; human merge).

Never run two `app.js` tasks in the same worktree; never run two data-writing tools (01, 07, 08) against the same branch.
