# Corpus supply package — foray-db / transcript-farm → R2 → foray (G-03, G-10…G-16, G-19, #578, #73, #279)

Planned 2026-09-25 against `origin/main` 0b2b8f92; revised 2026-09-25 after review (all 14 reviewer problems accepted; three of the reviewer's own line numbers were re-verified and corrected: `showDir` is `transcriptArchiveLookup.ts:449-463` and `locate` `:464-471`; `tools/foray/check-forays.test.mjs` floor is 168; `test/generateForaysArgs.test.ts` floor is 13). Every foray path, function and line range below was checked with `git show origin/main:<path>` / `git grep`. foray-db is a separate private repo a qwen agent cannot read, so every foray-db fact a qwen task needs is INLINED in that task; foray-db citations remain only as provenance for opus tasks.

## 1. Goal, done-definition, dependencies, open founder questions

**Goal.** Forays stop drawing tape from one PC's folder and one show per topic. Joey's corpus (`foraycorpus`, PostgreSQL on the tailnet) becomes the source of *what exists*; the R2 bucket `foray-transcriptions` (already written by the transcript farm in foray's own layout, and soon by foray-db's publisher/Apple engine per #831) becomes the source of *transcript bodies*; foray gains (a) an exporter that turns the corpus into versioned catalogue artifacts, (b) a sync that mirrors R2 into `data-local/transcripts/` and rebuilds the searchable index, (c) per-episode ad-load tiers the pipeline and `check-forays` enforce, (d) per-episode topic terms so episodes never inherit a show label, and (e) an app catalogue derived from the corpus.

**Done when.** (1) `node tools/foraycorpus-export/export.mjs --source jsonl:<fixture>` reproduces `counts.json` exactly and a second run emits an empty delta; a live dry run from hermes-vm lands within ±2 % of the corpus brief's 2026-09-10 figures. (2) `node tools/foraycorpus-export/sync-r2.mjs` followed by `node tools/generation/warm-transcript-index.mjs` makes every R2 body searchable on the generation machine, with a written count the run-start guard checks. (3) `data/dai-measurements.json` carries N ≥ 2 probes per curated-catalogue episode with audio; SYSK reads `locate-required`; Gastropod's spread reproduces ADR-0008's 33.4 s; `mintSegmentSource` refuses `locate-required` tape and `check-forays` refuses a source row the player would skip. (4) All 194 taxonomy nodes carry `terms[]`, `data/episode-topics.json` exists, and the resolver resolves the run-2 prompt from corpus terms alone. (5) `api/shows/search` answers from a corpus-derived artifact through a pointer. (6) #279 and #73 have candidate lists and applied waves; #578/#831 have status comments.

**Dependencies.**
- *Other packages:* none hard. G-00/generation code is on main. The native-engine decks (M2–M4) are untouched. G-40 (ad-pad tier in the player) already consumes `ad_pad_sec` (`player/foray-queue.js:379-381`), so PKG-21's `ad_pad_sec` on minted rows is readable by the web player today.
- *Founder decisions:* Q1–Q5 below. D10 is largely answered (Apple transcripts WILL be harvested, DECISIONS 2026-09-25; bodies already sit in a private bucket).
- *Credentials:* a read-only R2 S3 key pair for `foray-transcriptions` (Q1); `wyatt_readonly` on `foraycorpus` at 100.79.104.9 (exists, tailnet-only); `gh` auth on the export host for release publishing. Nothing new in GitHub Actions secrets.
- *Devices/hosts:* hermes-vm 100.96.16.99 (tailnet) for the live export and the DAI probes; the founder's generation PC for the first sync (it holds today's `data-local/`).
- *Facts the plan rests on (read in foray-db by the planner; inlined where a qwen task needs them):* R2 keys are `transcripts/normalized/<safeKey(show_id)>/<safeKey(guid)>.json` and `transcripts/raw/<safeKey(show_id)>/<safeKey(guid)>.vtt` (`transcript-farm/farm/r2.py:660-689`), body sha256 in `x-amz-meta-sha256`; the body carries `show_id`, `guid`, `source_url` (enclosure), `cues[]`, `warnings[]`, `transcript_source`, optional `asr{}`. **The farm's `show_id` is `String(podcastindex_feed_id)`, else a title slug (`farm/run.py:654-665`) — not foray's `show_id`.** 7,531 legacy whisper JSONs sit at the bucket ROOT as `<show-slug>/<title-slug>-<sha1(guid)[:10]>.json` and are NOT in foray layout (`legacy_asr_mapper.py`); the sync must ignore everything outside `transcripts/`. foray-db does not yet write publisher/Apple bodies to R2; #831's contract is forward-looking, so the sync treats a directory named by a foray `show_id` as already mapped.
- *Schema facts that are ASSUMED, not in the brief:* brief §1 (`docs/curation/corpus-integration-brief.md:29-35`) lists columns only for `podcasts`, `podcast_feeds`, `episodes`, `episode_source_records`, `assets`. The column lists this plan uses for `podcast_source_records`, `podcast_source_links`, `feed_host_policies`, and the column `episodes.updated_at` are assumptions from foray-db's models; PKG-02 records them as ASSUMED and PKG-03 (opus, has the DB) confirms or corrects them and PKG-07's `SQL` map in its PR. If `episodes.updated_at` does not exist, the delta keys on `assets.id` alone and PKG-03 says so.

**Open founder questions** (only product/spend/legal/credential calls; each has a default the plan proceeds on):
- **Q1 — R2 read credential.** foray needs an S3-API key pair (Access Key ID + Secret) scoped *Object Read* on bucket `foray-transcriptions`. Default: Joey issues it from the Cloudflare account that owns the bucket; Wyatt stores it OUTSIDE the tree (`~/.foray/r2-credentials`, same key names the farm reads: `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_S3_ENDPOINT`, `R2_BUCKET`) on the generation PC and hermes-vm. The existing `data-local/.cf-token` is a Cloudflare API token, not an S3 pair, and is not used.
- **Q2 — Export host and publish target (D2).** Default: the exporter runs weekly by cron on hermes-vm as `wyatt_readonly`; catalogue artifacts (show/episode metadata only, no transcript text) publish as GitHub Releases with a committed pointer, exactly like `tools/shows/` (keyless read, `gh` write); transcript bodies stay in R2. No Actions+Tailscale.
- **Q3 — Rights flags (D10 remainder).** Default: shows whose feed carries `itunes:block` or `podcast:locked` are exported with the flag and EXCLUDED from the app catalogue adapter and from sourcing until a ruling says otherwise; Apple-sourced bodies are used (2026-09-25 ruling).
- **Q4 — Outbound probes.** The DAI work sends ~2 × (500 + ~8,000) two-byte ranged GETs to publisher CDNs from hermes-vm/the PC under `ForayBot` at ≥ 1.2 s per host. Zero spend, but it is foray traffic against hosts Joey's crawler has paused. Default: go, from hermes-vm, never concurrently with the crawler's IP.
- **Q5 — The two catalogue waves change the curated tier (#279 drinks ~80–440 shows, #73 true-crime to 80–100 + computing).** Default: agents prepare candidate lists and PRs; the founder's review is the PR itself; Joey's open "comedy that fits" definition stays out of scope.

## 2. Task table

| id | title | executor | why-opus | depends-on | size |
|---|---|---|---|---|---|
| PKG-01 | Package scaffold `tools/foraycorpus-export/` | qwen | — | — | S |
| PKG-02 | Synthetic corpus fixture + JSONL row source + `mimes.mjs` | qwen | — | PKG-01 | S |
| PKG-03 | G-03 real fixtures: scrubbed corpus rows, Practical AI bodies, truncated archive; confirm assumed columns | opus | credentials (tailnet DB), PII scrubbing judgement, schema confirmation | PKG-02 | S |
| PKG-04 | `catalogue.mjs`: shows.jsonl builder | qwen | — | PKG-02 | S |
| PKG-05 | `episodes.mjs`: per-show episodes.jsonl builder | qwen | — | PKG-02 | S |
| PKG-06 | `manifest.mjs` + `delta.mjs`: high-water mark, sha256, atomic pointer | qwen | — | PKG-04, PKG-05 | S |
| PKG-07 | `pg-row-source.mjs`: read-only Postgres row source | qwen | — | PKG-02 | S |
| PKG-08 | `export.mjs` CLI end to end on the fixture | qwen | — | PKG-06, PKG-07 | S |
| PKG-09 | `overlap.mjs`: itunes_id ↔ apple_collection_id overlap count | qwen | — | PKG-02 | XS |
| PKG-10 | Live dry run from hermes-vm; run doc | opus | credentials, tailnet host, diagnosing drift | PKG-08, PKG-09, PKG-03 | S |
| PKG-11 | `r2-client.mjs`: S3-API list/get with fake-client tests | qwen | — | PKG-01 | S |
| PKG-12 | `show-map.mjs`: R2 show directory → foray `show_id` | qwen | — | PKG-01 | S |
| PKG-13 | `sync-r2.mjs`: mirror R2 into `data-local/transcripts/` | qwen | — | PKG-11, PKG-12 | S |
| PKG-14 | `rebuild-index.mjs`: sync then warm launcher | qwen | — | PKG-13 | XS |
| PKG-15 | First live sync + warm on the generation PC; sync doc | opus | credentials, field diagnosis on the PC | PKG-14, Q1 | S |
| PKG-16 | `dai-tier.mjs`: pure per-episode tiering | qwen | — | PKG-01 | S |
| PKG-17 | `probe-runner.mjs` + `dai-sample.mjs`: G-19 stratified sample + probe runner | qwen | — | PKG-16 | S |
| PKG-18 | Run the G-19 sample over two days; shares doc | opus | outbound egress/politeness, interpreting results | PKG-17, PKG-10, Q4 | S |
| PKG-19 | `dai-measure.mjs`: curated-catalogue probes → `data/dai-measurements.json` + derive | qwen | — | PKG-17 | S |
| PKG-20 | Run dai-measure twice ≥ 24 h apart; commit data; verify SYSK/Gastropod | opus | egress, field verification, data judgement | PKG-19, Q4 | S |
| PKG-21 | Tier gate + `ad_pad_sec` in `audioSourceLookup.ts` | opus | `backend/src/` is DENIED | PKG-20 (data), PKG-16 (shape) | S |
| PKG-22 | `check-forays`: `ad_pad_sec` shape + locate-required refusal | qwen | — | PKG-16 | S |
| PKG-23 | Run-start integrity guard against the sync state | opus | `backend/src/` DENIED | PKG-13, PKG-03 | S |
| PKG-24 | Cache-through R2 cue provider (optional) | opus | `backend/src/` DENIED, cross-cutting seam | PKG-11, PKG-13 | S |
| PKG-25 | Floor recalibration on the run-2 fixture with corpus df | opus | diagnosis from records, backend tests | PKG-26, PKG-15 | S |
| PKG-26 | `backend/src/cli/buildCorpusTerms.ts`: df table + per-episode terms | opus | `backend/src/` DENIED, tokenizer reuse | PKG-15 | S |
| PKG-27 | `build-terms.mjs` launcher | qwen | — | PKG-26 | XS |
| PKG-28 | Topic-assignment rule + constants | opus | product/data judgement (thresholds, `general`) | PKG-26 | S |
| PKG-29 | `topics.mjs`: node `terms[]`, `data/episode-topics.json`, integrity test | qwen | — | PKG-28, PKG-27 | S |
| PKG-30 | G-13b resolver reads node terms + df; #547 regression | opus | `backend/src/` DENIED | PKG-29 | S |
| PKG-31 | `catalog-adapter.mjs`: shows.jsonl → breadth-shaped rows + diff | qwen | — | PKG-04 | S |
| PKG-32 | `publish-release.mjs`: corpus artifacts as a GitHub Release + pointer | qwen | — | PKG-06, PKG-31 | S |
| PKG-33 | `api/shows/search` + `breadthCatalog` read through the pointer | opus | `api/` needs a human merge click; `backend/src/` DENIED | PKG-32 | S |
| PKG-34 | `corpus-freshness.yml` advisory workflow | opus | `.github/` DENIED | PKG-32 | XS |
| PKG-35 | #279 drinks-wave candidate list + report | qwen | — | PKG-04 (optional), PKG-19 | S |
| PKG-36 | #279 apply wave to `data/catalog.json`; re-score the alcohol spine | opus | product judgement on the curated tier | PKG-35, #275 merged | S |
| PKG-37 | #73 true-crime / computing candidate lists (`node-wave.mjs`) | qwen | — | PKG-35 | S |
| PKG-38 | #73 select, add, classify; non-DAI share | opus | product judgement, LLM classification pass | PKG-37 | S |
| PKG-39 | G-16 HUMAN-ACTIONS cards + STATE.md announcement | opus | credentials/host decision routing | — | XS |
| PKG-40 | #578 / #831 status comments and close-out mapping | opus | reading foray-db, judgement | PKG-15 | XS |

40 tasks; 18 opus, 22 qwen.

### Conventions every task follows (referenced as "standard conventions")
- Worktree per task, LF: `git -C "C:/Users/wjduv/Desktop/Vibe Coding/foray" -c core.autocrlf=false worktree add ../foray-wt-<id> -b <branch> origin/main`. Never run `format:write` repo-wide.
- Never commit `deploy-manifest.json` or `data/forays-directory.json`; `sw.js` keeps `BUILD_ID = "unstamped"`; `node tools/ci/generate-manifest.mjs --check` must stay green.
- Every new `tools/**/*.test.mjs` suite gets a floor in `FLOORS` in `test/suite-integrity.test.js` (`test/` is on `ALLOWED_PREFIXES`; the floor is the test count, and the file asserts every floored suite is in `tools/ci/run-suites.mjs`'s plan — a new `tools/<dir>/` is discovered automatically, see `run-suites.mjs:40-55`). New `backend/test/*.test.ts` suites get a `BACKEND_FLOORS` entry (`test/suite-integrity.test.js:2159`). When a task adds tests to an EXISTING suite it bumps that suite's floor by the exact count and says so in the PR body.
- Tests that name a Foray use `tools/foray/fixtures/frozen/data/` or `tools/foray/fixtures/boundary/data/`, never `data/forays.json`.
- One test process at a time; run the named suite, then `node --test "test/suite-integrity.test.js"`.
- Every test comment names the one-line mutation that turns it red (CLAUDE.md § "A green test is not evidence").
- Commit trailers: `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_017FFM7M73d6sEYQiHroVesz`. PR body ends with `🤖 Generated with [Claude Code](https://claude.com/claude-code)` and the session URL. PRs open as DRAFT (`gh pr create --draft`). Never babysit a PR.
- DENIED paths (`tools/ci/path-policy.mjs:61-230`): `.github/`, `.claude/`, `CLAUDE.md`, `docs/DECISIONS.md`, `docs/adr/`, `docs/roles.md`, `backend/src/`, `tools/ci/`, `tools/release/`, listed `tools/mobile/*` files, `test/release-gates.test.js`. `api/` and `index.html` are unlisted (human merge). A qwen task that would need any of these stops.
- No network in any test. No credential is ever read, printed or written by a test.
- No task edits a module another task created unless its depends-on lists that task; shared files are only `test/suite-integrity.test.js` (one line each) and `tools/foraycorpus-export/README.md` (append one paragraph).

## 3. Tasks

### PKG-01 · Package scaffold `tools/foraycorpus-export/` — qwen · S
**Executor:** qwen.
**Context (read first):** `tools/shows/package.json` (whole file; the scoped-package pattern), `tools/corpus/package.json` (the `//test` note on argument forwarding), `tools/ci/run-suites.mjs:20-60` (how a package with its own `test` script is discovered and installed), `test/suite-integrity.test.js:54-60` (FLOORS shape) and `:1935` (a `tools/shows/` floor line), `tools/shows/config.mjs` (a config module that imports `../segments/politeness.mjs`), `tools/segments/fetch-transcripts.mjs:204-229` (`safeKey`, `transcriptPath`), `docs/curation/foray-to-spec-roadmap.md:485-511` (G-10 card: the directory is `tools/foraycorpus-export/`, NOT `tools/corpus/`).
**Exact change:**
1. Create `tools/foraycorpus-export/package.json`: `{"name":"foray-corpus-export","private":true,"type":"module","engines":{"node":">=22.5"},"scripts":{"test":"node --test"},"dependencies":{"pg":"^8.13.1","@aws-sdk/client-s3":"^3.600.0"}}` plus a `"//test"` string copied in spirit from `tools/corpus/package.json` (the script MUST end in `node --test` and forward positionals). `pg-cursor` is NOT a dependency (PKG-07 paginates by keyset with plain `query`). Run `npm install --prefix tools/foraycorpus-export --package-lock-only` then `npm ci --prefix tools/foraycorpus-export` and commit `package-lock.json`; verify `git check-ignore tools/foraycorpus-export/node_modules` prints the path (if not ignored, stop).
2. Create `tools/foraycorpus-export/config.mjs` exporting exactly: `ROOT` (repo root, resolved like `tools/shows/config.mjs`), `NORMALIZED_PREFIX = "transcripts/normalized"`, `RAW_PREFIX = "transcripts/raw"`, `FINGERPRINT_PREFIX = "fingerprints"`, `R2_ENV = Object.freeze({accessKeyId:"R2_ACCESS_KEY_ID", secretAccessKey:"R2_SECRET_ACCESS_KEY", endpoint:"R2_S3_ENDPOINT", bucket:"R2_BUCKET", credentialsFile:"R2_CREDENTIALS_FILE"})`, `DEFAULT_BUCKET = "foray-transcriptions"`, `TRANSCRIPTS_DIR = join(ROOT,"data-local","transcripts")`, `EXPORT_OUT_DIR = join(ROOT,"data-local","corpus-export")`, `SYNC_STATE_FILE = join(TRANSCRIPTS_DIR,"r2-sync-state.json")`, `PG_ENV = "FORAYCORPUS_DATABASE_URL"`, `PG_STATEMENT_TIMEOUT_MS = 300000`, `PG_PAGE_SIZE = 5000`, `RELEASE_TAG_PREFIX = "corpus-export-"`, `POINTER_PATH = join(ROOT,"data","corpus-catalogue-pointer.json")`; re-export `safeKey` from `../segments/fetch-transcripts.mjs` and `CORPUS_UA`, `AUDIO_UA`, `CONTACT` from `../segments/politeness.mjs` (no new identity strings anywhere in this package).
3. Create `tools/foraycorpus-export/README.md` (≤ 80 lines): what the package is (G-10/G-11 foray side), the module list as they land, the env names, and the sentence "credentials are read from the environment or `R2_CREDENTIALS_FILE`, never from the tree".
4. Create `tools/foraycorpus-export/config.test.mjs` with tests: (a) `NORMALIZED_PREFIX`/`RAW_PREFIX` equal the farm's strings above (mutation: change one character); (b) `safeKey("https://a/b?c=1")` equals the value `tools/segments/fetch-transcripts.mjs` produces (import both, compare; mutation: re-implement `safeKey` locally); (c) `package.json`'s `scripts.test` ends with `node --test` (mutation: append a file glob); (d) identity-string scan over EXACTLY the file set `execFileSync("git", ["ls-files", "tools/foraycorpus-export"])` filtered to paths ending `.mjs` and not under `fixtures/` (this excludes `node_modules/`, which is untracked and contains `User-Agent:` inside `@aws-sdk/*`): no file contains the substring `User-Agent:` or `Mozilla/` (mutation: add one to `config.mjs`). The test comment states why the file set is `git ls-files` and not a directory walk.
5. Add `"tools/foraycorpus-export/config.test.mjs": 4,` to `FLOORS` in `test/suite-integrity.test.js` next to the `tools/shows/` lines, with a one-line comment naming this task.
**Tests to add:** the four above in `config.test.mjs`.
**Commands:** `npm ci --prefix tools/foraycorpus-export` → exit 0. `npm test --prefix tools/foraycorpus-export -- config.test.mjs` → 4 pass. `node tools/ci/run-suites.mjs --list` → the plan lists `tools/foraycorpus-export/config.test.mjs` under its own package group. `node --test "test/suite-integrity.test.js"` → green. `node tools/ci/generate-manifest.mjs --check` → green.
**Do not touch:** anything outside `tools/foraycorpus-export/` except the one FLOORS line; no `tools/ci/`, no `backend/`.
**Stop and escalate if:** `run-suites.mjs --list` does not pick up the package; `npm ci` needs a registry token; `node_modules` is not ignored; Node < 22.5 on the machine; `git ls-files` is unavailable in the test environment (do not fall back to a directory walk).
**Definition of done:** the commands above green; DRAFT PR titled `feat(corpus-export): package scaffold (PKG-01)`; PR body lists the env names.

### PKG-02 · Synthetic corpus fixture + JSONL row source + `mimes.mjs` — qwen · S
**Executor:** qwen.
**Context:** `docs/curation/corpus-integration-brief.md:23-36` (§1: the five tables with column lists — the column names for those five are the contract), `:40-79` (§2: timed vs plain mime lists, including the leading-slash variants and `application/pdf` as plain), `:81-110` (§3: audio rows are `asset_type='alternate'`), `tools/shows/dump-reader.mjs:1-30` (a generator row reader to mirror in shape), `tools/segments/sweep-transcripts.mjs:107-112` (`TIMED_TRANSCRIPT_TYPES` — the 4-entry list; the brief's 8-entry list is a superset) and `:172-200` (`normalizeMimeType` — import it), `tools/foraycorpus-export/config.mjs` (PKG-01).
**Exact change:**
1. Create `tools/foraycorpus-export/fixtures/synthetic/` with one JSONL file per table, one JSON object per line. Column lists — FROM THE BRIEF: `podcasts.jsonl` (id, public_id, canonical_title, description, declared_language, english_candidate_status, explicit, author, site_url, image_url, status), `podcast_feeds.jsonl` (id, podcast_id, current_url, normalized_url, url_hash, status, last_success_at, last_attempt_at, crawl_rank, crawl_tier, categories, itunes_block_raw, podcast_locked_raw), `episodes.jsonl` (id, public_id, podcast_id, canonical_title, description, source_published_at, duration_seconds, explicit, status, updated_at — `updated_at` is ASSUMED, see below), `episode_source_records.jsonl` (episode_id, feed_id, guid_raw, guid_hash, title_raw, description_raw, pub_date_parsed, duration_parsed_seconds), `assets.jsonl` (id, owner_type, owner_id, asset_type, url, mime_type, language, relation, declared_byte_length). ASSUMED (not in the brief; taken from foray-db models by the planner): `podcast_source_records.jsonl` (id, source_record_id, itunes_id, extra_source_json), `podcast_source_links.jsonl` (podcast_source_record_id, feed_id, podcast_id), `feed_host_policies.jsonl` (host, state, reason), and `episodes.updated_at`. Content: 4 podcasts — A: `english_candidate_status:"yes"`, 3 episodes each with a `text/vtt` transcript asset AND an `alternate` audio asset (`audio/mpeg`, url on `traffic.omny.fm`), PLUS one `asset_type:"chapters"` asset (`application/json+chapters`) on A's first episode, itunes_id 111; B: yes, 2 episodes with `text/plain`-only transcripts, `itunes_block_raw:"yes"`, itunes_id null; C: `english_candidate_status:"no"`, 2 episodes with vtt; D: yes, 1 episode with `application/srt` + `application/json` transcripts and no audio, `podcast_locked_raw:"true"`, itunes_id 444. Feeds: exactly A's and C's feed rows have non-null `last_success_at`; B's and D's are null. Make `updated_at` and asset ids monotonic so PKG-06's delta has something to key on.
2. Create `fixtures/synthetic/counts.json`: `{"podcasts":4,"podcasts_english":3,"episodes":8,"episodes_english_timed":4,"episodes_english_timed_audio":3,"assets":13,"feeds":4,"feeds_crawled":2}`. Derivation (state it in the README): assets = A 3×(vtt+audio) 6 + A chapters 1 + B 2 plain + C 2 vtt + D srt+json 2 = 13; episodes_english_timed = A 3 + D 1 = 4 (B is plain, C is not English); episodes_english_timed_audio = A 3; feeds_crawled = A, C = 2.
3. Create `fixtures/synthetic/README.md`: "synthetic, hand-written 2026-09-25; column lists for podcasts/podcast_feeds/episodes/episode_source_records/assets are brief §1; the lists for podcast_source_records, podcast_source_links, feed_host_policies and the column episodes.updated_at are ASSUMED and confirmed by PKG-03; replaced for validation by `fixtures/scrubbed-2026-09/` (PKG-03)".
4. Create `tools/foraycorpus-export/mimes.mjs` exporting `TIMED_MIMES = Object.freeze(["text/vtt","application/srt","application/x-subrip","application/json","text/srt","application/vtt","/application/srt","/application/vtt"])` (brief §2), `PLAIN_MIMES = Object.freeze(["text/plain","text/html","application/pdf","text/markdown"])` (brief §2 — copy the brief's plain list exactly; if the brief lists more, include them), `classifyTranscriptMime(raw)` → `"timed"|"plain"|"other"`: `null`/non-string → `"other"`; lowercase and trim; if the raw string is in `TIMED_MIMES` (the leading-slash variants match here, before normalization) → `"timed"`; else `normalizeMimeType(raw)` and check `TIMED_MIMES` then `PLAIN_MIMES`; else `"other"`. `isTimedMime(raw)` = `classifyTranscriptMime(raw) === "timed"`.
5. Create `tools/foraycorpus-export/row-source.mjs` exporting `TABLES = Object.freeze([...8 names])`, `jsonlRowSource(dir)` → object `{kind:"jsonl", async *rows(table){...}, async counts(){...}, describe(){return "jsonl:<dir>"}}`. `rows(table)` streams lines with `readline` (never `readFileSync` whole), skips blank lines, throws `RowSourceError("MALFORMED_ROW", "<file>:<line>")` on a JSON parse error, throws `RowSourceError("UNKNOWN_TABLE")` for a table not in `TABLES`. `counts()` returns `{[table]: n}` for all tables.
6. Create `mimes.test.mjs` (3): (a) `classifyTranscriptMime("/application/srt")` → timed, `"text/plain"` → plain, `"application/pdf"` → plain, `"image/png"` → other (mutation: drop the leading-slash branch); (b) `"TEXT/VTT "` → timed (mutation: drop lowercase/trim); (c) `TIMED_MIMES` is a superset of `TIMED_TRANSCRIPT_TYPES` from `tools/segments/sweep-transcripts.mjs` (mutation: remove `application/x-subrip`).
7. Create `row-source.test.mjs` (5): (a) every table yields the README's row count (mutation: drop a line); (b) a malformed line names file and line number (write a tmp dir with a bad line; mutation: swallow the error); (c) unknown table throws (mutation: return empty); (d) recompute `episodes_english_timed` and `episodes_english_timed_audio` from the rows using `isTimedMime` and `asset_type === "alternate" && mime_type.startsWith("audio/")`, and equal counts.json (mutation: count assets instead of distinct episodes); (e) `assets`, `episodes`, `feeds_crawled` totals equal counts.json (mutation: edit counts.json).
8. Floor lines: `mimes.test.mjs` (3), `row-source.test.mjs` (5).
**Commands:** `npm test --prefix tools/foraycorpus-export -- mimes.test.mjs` → 3 pass; `-- row-source.test.mjs` → 5 pass; `node --test "test/suite-integrity.test.js"` green.
**Do not touch:** other packages; no real corpus data (nothing copied from the DB).
**Stop and escalate if:** a column OUTSIDE the lists above (brief + ASSUMED) is needed to build the fixture (do not invent one; write the need in the PR); `normalizeMimeType` is not exported from `sweep-transcripts.mjs` (report; do not fork it).
**Definition of done:** commands green; DRAFT PR `feat(corpus-export): synthetic fixture, JSONL row source, mime classifier (PKG-02)`.

### PKG-03 · G-03 real fixtures + schema confirmation — opus · S
**Executor:** opus. Why: needs the tailnet DB role (credentials), scrubbing judgement (author emails, descriptions), `data-local/` on the founder's PC, and it is the one task that can confirm the ASSUMED column lists.
**Context:** `docs/curation/foray-to-spec-roadmap.md:397-410` (G-03 card), `docs/curation/corpus-integration-brief.md:23-36` and `:246-` (Appendix A method notes), PKG-02's fixture layout and README (the ASSUMED lists), `backend/test/helpers/rawVttCueProvider.ts:1-30` (how tests reach `data-local` bodies today), `backend/src/generation/transcriptCorpus.ts:68-88` (`corpusSafeKey`), `backend/test/fixtures/` (existing run-2 fixtures).
**Exact change:**
1. First, confirm the schema: `\d podcast_source_records`, `\d podcast_source_links`, `\d feed_host_policies`, `\d episodes` (or `information_schema.columns`). Record the actual column lists in `fixtures/scrubbed-2026-09/SCHEMA.md`. If any ASSUMED list differs from PKG-02's, update in THIS PR: PKG-02's fixture files and README, `row-source.mjs`'s `TABLES` (if a table is renamed), and PKG-07's `SQL` map (if PKG-07 has merged; else post the corrected lists as a comment on PKG-07's PR and in this PR body). If `episodes.updated_at` does not exist, say so here; PKG-06 then keys the delta on `assets.id` only and PKG-08 test (c) remains valid.
2. Against `foraycorpus` (read-only, `SET TRANSACTION READ ONLY`, `statement_timeout = 300000`), select ~20 podcasts: the 10 with the most English timed transcripts, 5 mid-size (20–99), 3 with `itunes_block_raw`/`podcast_locked_raw` set, 2 with `english_candidate_status='no'`; pull their feeds, source records, links, episodes (cap 60 per podcast, newest first), episode_source_records, assets and the full `feed_host_policies` table into `tools/foraycorpus-export/fixtures/scrubbed-2026-09/<table>.jsonl` using PKG-02's (confirmed) column lists. Scrub: `description`/`description_raw` truncated to 200 chars; `author` replaced by `"author-<n>"`; no `content_encoded_raw`. Write `counts.json` with the same keys as PKG-02, computed by SQL and recorded with the query text in `README.md` (date, role, host redacted to "tailnet").
3. Copy three Practical AI normalized bodies from `data-local/transcripts/normalized/practical-ai-*/` (smallest three) into `backend/test/fixtures/transcripts/practical-ai/<file>.json` unchanged, with `sha256.json` `{ "<file>": "<hex>" }` computed by `sha256sum`; record which show directory they came from.
4. Create `backend/test/fixtures/transcripts/truncated-archive/`: a copy of a two-show archive (two directories, 3 bodies each) plus `r2-sync-state.json` claiming `bodies_expected: 9` (three more than present) with `shows{}` listing both directories at `bodies: 4` and `5` — the guard fixture PKG-23 uses.
5. Total size < 5 MB (`du -sh`); add `tools/foraycorpus-export/fixtures.test.mjs` (2) asserting the scrubbed counts.json matches a recount of the JSONL rows via `jsonlRowSource` + `isTimedMime` (mutation: edit counts.json) and that no scrubbed file contains an `@` inside `author` (mutation: put one). Floor line (2).
**Commands:** `npm test --prefix tools/foraycorpus-export -- fixtures.test.mjs` → 2 pass; `-- row-source.test.mjs` still 5; `du -sh tools/foraycorpus-export/fixtures backend/test/fixtures/transcripts` < 5 MB.
**Do not touch:** the DB (reads only); `backend/src/`.
**Stop and escalate if:** the role cannot read a table; a confirmed table lacks the join column the plan relies on (`podcast_source_links.podcast_id` or `.feed_id`) — record the actual join path in SCHEMA.md and stop before changing PKG-04's algorithm.
**Definition of done:** fixtures + SCHEMA.md committed with READMEs; DRAFT PR `test(corpus-export): G-03 scrubbed corpus fixture, schema confirmation, archive fixtures (PKG-03)`; the PR body lists every ASSUMED column that was confirmed or corrected.

### PKG-04 · `catalogue.mjs`: shows.jsonl builder — qwen · S
**Executor:** qwen.
**Context:** brief `:23-36` (§1 join keys: feed URL is the join; `podcast_source_records.itunes_id` on ~64 %), `:112-124` (§4 categories json shape), `:125-146` (§5 rights flags), roadmap `:485-511` (G-10 ask), `tools/shows/shard-build.mjs:132-160` (`normalizeFeedUrl` usage — import `normalizeFeedUrl` from wherever `shard-build.mjs` imports it; verify with `git grep -n "normalizeFeedUrl" origin/main -- tools/shows`), `data/catalog.json` (`shows[].show_id`, `feed_url`, `apple_collection_id`), PKG-02 `row-source.mjs` and `mimes.mjs` (`isTimedMime` — import it; do not define a mime list here).
**Exact change:**
1. Create `tools/foraycorpus-export/catalogue.mjs` exporting `async function buildShows(source, { catalog })` → `{ rows, stats }`. Algorithm: load all `podcasts` where `english_candidate_status === "yes"` into a Map by id (≤ 20k rows expected after the filter; the brief says 19,104 English podcasts with episodes); stream `podcast_feeds` and attach the feed with the latest `last_success_at` (else the first) per podcast; stream `podcast_source_links` + `podcast_source_records` to attach `itunes_id`, `source_record_id` (= PodcastIndex feed id), `extra_source_json.category1..10`; stream `episodes` (English podcasts only) to aggregate per podcast: `episodes_total`, `newest_published_at`; stream `assets` (`owner_type === "episode"`) to aggregate `timed_transcript_episodes` (distinct episode ids with `isTimedMime(mime_type)` from `mimes.mjs`) and `audio_episodes` (distinct episode ids with an `alternate` asset whose mime starts with `audio/`).
2. Row shape (one line per podcast, keys in this order): `corpus_podcast_id` (public_id), `podcastindex_feed_id` (source_record_id or null), `itunes_id` (or null), `title`, `feed_url` (current_url), `feed_url_normalized`, `language` (declared_language), `explicit`, `image_url`, `site_url`, `categories` (array of `{category, subcategory}` from feed json, else `[]`), `pi_categories` (array of the non-null category1..10 strings), `rights: {itunes_block: bool, podcast_locked: bool}` (truthy raw values `"yes"|"true"|"1"` → true), `crawl: {tier, rank, last_success_at, status}`, `episodes_total`, `timed_transcript_episodes`, `audio_episodes`, `newest_published_at`, `foray_show_id` (catalog.json `show_id` whose normalized `feed_url` matches, else `String(itunes_id)` when itunes_id is not null, else null).
3. Sort rows by `corpus_podcast_id`. `stats` = `{podcasts_read, podcasts_english, rows, with_itunes_id, blocked, locked}`.
4. Create `catalogue.test.mjs` over the synthetic fixture: (a) exactly 3 rows (C excluded; mutation: drop the English filter); (b) A has `timed_transcript_episodes 3`, `audio_episodes 3`, `foray_show_id "111"`; B has `rights.itunes_block true` and `foray_show_id null`; D has `timed_transcript_episodes 1` (srt+json count once; mutation: count assets instead of distinct episodes); (c) a catalog.json stub whose `feed_url` equals A's feed with a trailing slash difference still maps to its `show_id` (mutation: compare raw urls); (d) rows are sorted (mutation: remove sort). Floor (4).
**Commands:** `npm test --prefix tools/foraycorpus-export -- catalogue.test.mjs` → 4 pass.
**Do not touch:** `data/`; `mimes.mjs`, `episodes.mjs` (PKG-05's).
**Stop and escalate if:** `normalizeFeedUrl` is not importable from `tools/shows/` (do not copy it; report).
**Definition of done:** DRAFT PR `feat(corpus-export): shows.jsonl builder (PKG-04)`.

### PKG-05 · `episodes.mjs`: per-show episodes.jsonl — qwen · S
**Executor:** qwen.
**Context:** brief `:40-79` (§2 "2–3 formats per episode"), `:81-110` (§3 audio rows), PKG-02 `mimes.mjs` (`TIMED_MIMES`, `classifyTranscriptMime` — import; no mime list is defined here), `tools/segments/sweep-transcripts.mjs:172-200` (`normalizeMimeType`, `hasTimestamps`, `pickTranscript` — read for the preference idiom; import `normalizeMimeType` only), `tools/segments/fetch-transcripts.mjs:204-229` (`safeKey`, `transcriptPath`), PKG-04 row shape.
**Exact change:**
1. Create `tools/foraycorpus-export/episodes.mjs` exporting `pickTranscriptAsset(assets)` → the timed asset preferring `text/vtt` > `application/srt` > `application/x-subrip` > `application/json` > other timed, else the first plain, else null (classification via `classifyTranscriptMime`); `async function buildEpisodes(source, { podcastIds, chosenFeedByPodcast })` → async generator of `{ podcast_id, rows }` per podcast, rows sorted by `source_published_at` desc.
2. Row shape: `corpus_episode_id` (public_id), `guid` (episode_source_records.guid_raw; if several source records, the one whose `feed_id` equals `chosenFeedByPodcast.get(podcast_id)`, else the first), `title`, `published_at` (source_published_at ISO), `duration_sec`, `explicit`, `transcript: {url, mime, timed: bool} | null`, `transcript_alternates: [{url, mime}]` (all other transcript assets, excluding the chosen one), `chapters_url` (asset with `asset_type === "chapters"` if present else null — the synthetic fixture has one on A's first episode), `audio: {url, mime, declared_bytes} | null` (first `alternate` audio asset; `declared_bytes` = `declared_byte_length` or null), `asset_ids` (sorted ints), `updated_at` (null when the column is absent).
3. Writer: `writeEpisodesFile(outDir, showKey, rows)` → `episodes/<safeKey(showKey)>.jsonl` written via a tmp file + rename; refuses when the resolved path does not start with `join(outDir, "episodes") + sep` (the two-line guard from `transcriptPath`, cited); `showKey` = `foray_show_id ?? corpus_podcast_id` (documented in the README).
4. `episodes.test.mjs` (5): (a) `pickTranscriptAsset` prefers vtt over srt over json (mutation: reorder); (b) A yields 3 rows with `audio.url` set and A's first episode has `chapters_url` set, the other two null (mutation: pick any json asset as chapters); (c) D yields 1 row with `transcript.mime "application/srt"` and `transcript_alternates` length 1 (mutation: return alternates including the chosen one); (d) the writer refuses a `showKey` of `"../x"` (mutation: remove the check); (e) rows sorted newest first (mutation: asc). Floor (5).
**Commands:** `npm test --prefix tools/foraycorpus-export -- episodes.test.mjs` → 5 pass.
**Do not touch:** `tools/segments/` (imports only); `mimes.mjs`, `catalogue.mjs`.
**Stop and escalate if:** the fixture's `assets.jsonl` has no `chapters` row (PKG-02 must add it; do not add it here).
**Definition of done:** DRAFT PR `feat(corpus-export): per-show episodes.jsonl (PKG-05)`.

### PKG-06 · `manifest.mjs` + `delta.mjs` — qwen · S
**Executor:** qwen.
**Context:** roadmap `:485-511` ("Delta by `assets.id` / `episodes.updated_at` high-water mark", manifest with sha256, feeds-crawled/known, 30-day insert rate, `latest.json` written last atomically), brief `:125-146` (§5 freshness numbers), `tools/shows/state.mjs:12-30` (`alreadyBuilt`, `nextState` — the state-file idiom), `tools/shows/import-dump.mjs:186` (`writeBuildOutput` shape), `tools/segments/sweep-transcripts.mjs:359-382` (`writeJsonAtomic` — import it), PKG-03's SCHEMA.md if merged (whether `episodes.updated_at` exists).
**Exact change:**
1. `delta.mjs`: `readState(file)` → `{version:1, last_export_version|null, high_water:{max_asset_id:0, max_episode_updated_at:null}}` (defaults when absent); `computeHighWater(source)` streams `assets` and `episodes` for the max values (`max_episode_updated_at` stays null when rows carry no `updated_at`); `isChanged(episodeRow, hw)` → `(episodeRow.updated_at != null && hw.max_episode_updated_at != null && episodeRow.updated_at > hw.max_episode_updated_at) || episodeRow.asset_ids.some(id => id > hw.max_asset_id)`; `writeState(file, state)` atomically.
2. `manifest.mjs`: `buildManifest({ exportVersion, builtAt, source, files, counts, highWater, delta, overlap })` → `{version:1, export_version, built_at, source:{kind, identity}, counts:{shows, episodes, timed_transcript_episodes, audio_episodes, feeds_known, feeds_crawled, inserts_30d}, high_water, delta:{episodes_added, episodes_changed, shows_changed}, overlap, files:[{path, bytes, sha256, rows}]}`; `hashFile(path)` (sha256 streaming); `writeLatest(outRoot, {export_version, manifest_path, built_at})` → writes `latest.json` via tmp+rename and ONLY after every file in `files` exists with matching sha256 (re-hash; throw `ManifestError("FILE_MISMATCH")` otherwise). `feeds_known` = count of `podcast_feeds` rows; `feeds_crawled` = rows with non-null `last_success_at`; `inserts_30d` = `episodes` rows whose `updated_at` (else `source_published_at`) ≥ built_at − 30 d.
3. `manifest.test.mjs` (5): (a) a manifest lists every file with a sha256 that equals `sha256sum` of the file (mutation: hash the path string); (b) `writeLatest` refuses when a listed file's bytes changed after hashing (mutation: skip the re-hash); (c) `latest.json` is absent if the write throws (tmp file cleaned; mutation: write directly); (d) `isChanged` is true for a newer `updated_at`, true for a newer asset id, false otherwise, and false-not-throw when `updated_at` is null (mutation: `>=`); (e) `inserts_30d` counts only the window (mutation: `>` vs `≥` on the boundary row). Floor (5).
**Commands:** `npm test --prefix tools/foraycorpus-export -- manifest.test.mjs` → 5 pass.
**Do not touch:** `tools/shows/` (import only).
**Stop and escalate if:** `writeJsonAtomic`'s import pulls network code at import time (verify by importing in the test).
**Definition of done:** DRAFT PR `feat(corpus-export): manifest, delta high-water mark, atomic pointer (PKG-06)`.

### PKG-07 · `pg-row-source.mjs` — qwen · S
**Executor:** qwen.
**Context:** brief `:36` (role can SELECT, statement timeout respected), `:23-35` (table names), `backend/package.json:29` (`pg ^8.13.1` is the version the repo already uses), `tools/foraycorpus-export/config.mjs` (`PG_ENV`, `PG_STATEMENT_TIMEOUT_MS`, `PG_PAGE_SIZE`), `row-source.mjs` (the interface to implement), PKG-02's fixture README (which column lists are ASSUMED).
**Exact change:**
1. Create `pg-row-source.mjs` exporting `pgRowSource({ connectionString = process.env[PG_ENV], clientFactory })` returning the same interface as `jsonlRowSource`. On first use: `BEGIN; SET TRANSACTION READ ONLY; SET LOCAL statement_timeout = <PG_STATEMENT_TIMEOUT_MS>;`. `rows(table)` uses keyset pagination with plain `client.query` (no `pg-cursor`): `SELECT <columns> FROM <table> WHERE id > $1 ORDER BY id LIMIT $2` for the six tables that have `id` (`podcasts`, `podcast_feeds`, `podcast_source_records`, `episodes`, `assets`; `episode_source_records` — paginate on `(episode_id, feed_id)` with a tuple comparison `WHERE (episode_id, feed_id) > ($1, $2)`); `podcast_source_links` paginate on `(podcast_source_record_id, feed_id)`; `feed_host_policies` read whole. Column lists from PKG-02 (`SQL` map exported as frozen strings so tests can pin them). The `SQL` map carries a header comment: "column lists for podcast_source_records, podcast_source_links, feed_host_policies and episodes.updated_at are ASSUMED until PKG-03 confirms". `counts()` runs `SELECT reltuples::bigint FROM pg_class WHERE relname=$1` per table (estimates; documented). `describe()` returns `"pg:<host redacted>"` — never the connection string.
2. Never log the connection string; on connection error rethrow `RowSourceError("PG_CONNECT", <message with password stripped by regex /:[^:@/]+@/ → ":***@">)`.
3. `pg-row-source.test.mjs` with a fake client (`{query: async (text, params) => …}` recording calls) (5): (a) the first three statements are the BEGIN/READ ONLY/timeout trio (mutation: drop READ ONLY); (b) `rows("episodes")` issues `id > $1 ORDER BY id LIMIT $2` pages until a short page and yields all rows in order (mutation: stop after the first page); (c) every SQL string for the 8 tables contains only the PKG-02 columns (parse the SELECT list; mutation: add `*`); (d) `describe()` never contains a password from a `postgres://u:p@h/db` string (mutation: return the string); (e) `rows("nope")` throws `UNKNOWN_TABLE`. Floor (5).
**Commands:** `npm test --prefix tools/foraycorpus-export -- pg-row-source.test.mjs` → 5 pass.
**Do not touch:** no live DB call anywhere; no `.env` reading beyond `process.env`.
**Stop and escalate if:** a column OUTSIDE PKG-02's lists (brief + ASSUMED) is needed; PKG-03's SCHEMA.md (if merged) contradicts a list — then adopt SCHEMA.md and say so in the PR.
**Definition of done:** DRAFT PR `feat(corpus-export): read-only Postgres row source (PKG-07)`.

### PKG-08 · `export.mjs` CLI end to end — qwen · S
**Executor:** qwen.
**Context:** PKG-04..07 modules, `tools/shows/run-and-publish.mjs:37-80` (`runBuild` idiom, `--dry-run`), `tools/generation/warm-transcript-index.test.mjs:1-60` (launcher test style), `tools/segments/fetch-transcripts.mjs:343-360` (`numericArg` — import for flag parsing).
**Exact change:**
1. Create `export.mjs` (`#!/usr/bin/env node`, `main()` guarded by `import.meta.url === pathToFileURL(process.argv[1]).href`) exporting `parseExportArgs(argv)` → `{source:"jsonl:<dir>"|"pg", out, dryRun, catalogPath, breadthPath|null}` (defaults: `out = EXPORT_OUT_DIR`, `catalogPath = data/catalog.json`; unknown flag → throw) and `async function runExport(args, { now = () => new Date(), sourceFactory })`.
2. `runExport`: resolve source; `exportVersion = now().toISOString()`; `outRoot/<exportVersion>/`; build shows (PKG-04) → `shows.jsonl`; for each show build episodes (PKG-05, passing the chosen feed map from PKG-04) → `episodes/<key>.jsonl`; compute high-water and delta vs `outRoot/state.json`; write `manifest.json` (with `overlap` from PKG-09 when `--breadth` is given, else null); write `latest.json` last; write state. `--dry-run` computes everything into a tmp dir and prints the manifest, writing nothing under `outRoot`. Print one summary line: `shows=<n> episodes=<n> delta_added=<n> delta_changed=<n> out=<path>`.
3. `export.test.mjs` (5, end to end on the synthetic fixture, tmp out dir): (a) files exist and `manifest.counts.shows === counts.json.podcasts_english`, `counts.episodes` = English episodes (mutation: count C); (b) a second run with the same fixture emits `delta.episodes_added 0`, `delta.episodes_changed 0` and a new version directory whose `shows.jsonl` sha256 equals the first (mutation: include `built_at` inside shows rows); (c) after appending one asset row with a higher id to a copied fixture, the third run reports `episodes_changed 1` (mutation: ignore asset ids); (d) `--dry-run` leaves `outRoot` absent (mutation: write state); (e) `latest.json.export_version` equals the newest directory (mutation: write latest first). Floor (5).
**Commands:** `npm test --prefix tools/foraycorpus-export -- export.test.mjs` → 5 pass; `node tools/foraycorpus-export/export.mjs --source jsonl:tools/foraycorpus-export/fixtures/synthetic --out "$TMP/out"` → summary line with `shows=3`.
**Do not touch:** `data/`; nothing network.
**Stop and escalate if:** the fixture-run output exceeds 1 MB (something is duplicating rows).
**Definition of done:** DRAFT PR `feat(corpus-export): export CLI (PKG-08)`.

### PKG-09 · `overlap.mjs` — qwen · XS
**Executor:** qwen.
**Context:** brief `:125-146` end ("overlap not measured … `podcast_source_records.itunes_id` ↔ `apple_collection_id` for the 19,787 breadth ids"), roadmap `:501-502`, `data/catalog-breadth.json` (minified; `shows[].apple_collection_id`, `feed_url`), PKG-04 rows (`itunes_id`, `feed_url_normalized`).
**Exact change:** `overlap.mjs` exporting `computeOverlap(showRows, breadthDoc)` → `{breadth_shows, matched_by_itunes_id, matched_by_feed_url_only, unmatched}` (a breadth show counts once, itunes first). CLI `node overlap.mjs --shows <shows.jsonl> [--breadth data/catalog-breadth.json]` prints the JSON. Test (`overlap.test.mjs`, 3): a breadth stub of 3 shows vs the synthetic rows → `{3, 1, 1, 1}` (mutation: count the feed match twice); missing `apple_collection_id` never matches `null` itunes_id (mutation: `==`); output keys stable. Floor (3).
**Commands:** `npm test --prefix tools/foraycorpus-export -- overlap.test.mjs` → 3 pass.
**Do not touch:** `data/`; `export.mjs` (PKG-08 wires the `--breadth` flag).
**Stop and escalate if:** `catalog-breadth.json` rows lack `feed_url` (they have it; verified).
**Definition of done:** DRAFT PR `feat(corpus-export): breadth overlap count (PKG-09)`.

### PKG-10 · Live dry run from hermes-vm — opus · S
**Executor:** opus. Why: tailnet host and DB credentials; reading drift against the brief is judgement.
**Context:** brief `:1-20` (§0 TL;DR numbers), `:40-79`, `:125-146`, `:202-211` (§7.2 hermes-vm row), roadmap `:505-511` (Done-when: ±2 % from a NON-founder host), PKG-08 CLI, PKG-09, PKG-03's SCHEMA.md.
**Exact change:** on hermes-vm (repo checked out at the PKG-08 merge commit, `npm ci --prefix tools/foraycorpus-export`), export `FORAYCORPUS_DATABASE_URL` from the RO role's env file (never echoed), run `node tools/foraycorpus-export/export.mjs --source pg --out /var/lib/foray-corpus-export --breadth data/catalog-breadth.json`, then a second run. Write `docs/curation/corpus-export-run-2026-09.md`: date, host, role name, wall time, peak RSS (`/usr/bin/time -v`), manifest counts vs brief figures (442,145 timed English episodes / 1,967 podcasts / 323,721 with audio / 19,104 English podcasts) with the % drift, the overlap numbers, and the second run's delta (must be 0/0). If any count drifts > 2 %, diagnose (crawl recovered? column semantics?) and say which before closing. Add the doc to `docs/curation/README.md` if that file indexes docs (check first).
**Tests to add:** none (a field run). Re-run PKG-03's `fixtures.test.mjs` only if the scrubbed fixture is regenerated here (only if the schema drifted).
**Commands:** as above; `node --test "test/suite-integrity.test.js"` before the PR.
**Do not touch:** `backend/src/`, DB writes, `tools/ci/`.
**Stop and escalate if:** the role cannot connect from hermes-vm (HUMAN-ACTIONS card via PKG-39); a query exceeds the 300 s timeout (paginate smaller, then report).
**Definition of done:** the doc lists counts within ±2 % or a diagnosed reason; DRAFT PR `docs(corpus-export): live export run 2026-09 (PKG-10)`.

### PKG-11 · `r2-client.mjs` — qwen · S
**Executor:** qwen.
**Context:** `tools/foraycorpus-export/config.mjs` (`R2_ENV`, `DEFAULT_BUCKET`), `tools/segments/politeness.mjs:41-67` (no new identity strings; the S3 SDK's own UA is fine and must not be overridden), `@aws-sdk/client-s3` README in `tools/foraycorpus-export/node_modules/@aws-sdk/client-s3/README.md` (`ListObjectsV2Command`, `GetObjectCommand`, `Body.transformToByteArray`). **Inlined foray-db facts (the transcript farm's `_client_config`, `transcript-farm/farm/r2.py:411-445`, which a qwen agent cannot read):** the farm constructs its S3 client with region `"auto"`, the account's S3 endpoint `https://<account>.r2.cloudflarestorage.com`, path-style addressing, and — the ONE R2 gotcha — request checksum calculation and response checksum validation set to "when required" (R2 rejects the SDK's default flexible checksums on PUT and mis-validates on GET). The farm's credential file accepts these key aliases, case-insensitive, `key = value` one per line: access key `access_key_id | r2_access_key_id | aws_access_key_id | access_key_id`; secret `secret_access_key | r2_secret_access_key | aws_secret_access_key`; endpoint `s3_api_endpoint | endpoint | endpoint_url | r2_s3_endpoint`; bucket `bucket | r2_bucket`. The Cloudflare dashboard spelling `Access_Key_ID` and `Secret_Access_Key` must therefore resolve after lowercasing. Objects written by the farm carry user metadata `sha256` (the SDK exposes it as `Metadata.sha256`, lowercased key) holding the hex sha256 of the body bytes.
**Exact change:**
1. `r2-client.mjs` exporting `loadR2Credentials({ env = process.env, readFile })` → `{accessKeyId, secretAccessKey, endpoint, bucket}`: env vars (`R2_ENV`) first; else parse `env.R2_CREDENTIALS_FILE` (or `~/.foray/r2-credentials`) as `key = value` lines with the alias table above (keys lowercased before matching); `bucket` defaults to `DEFAULT_BUCKET`; throw `R2Error("NO_CREDENTIALS", "<which sources were tried>")` naming sources (`env`, the file path), never values. The returned object has a `toJSON()` returning `{accessKeyId:"<redacted>", endpoint, bucket}`.
2. `createR2Client(creds, { S3Client = (await import("@aws-sdk/client-s3")).S3Client })` → `new S3Client({region:"auto", endpoint, credentials:{accessKeyId, secretAccessKey}, forcePathStyle:true, requestChecksumCalculation:"WHEN_REQUIRED", responseChecksumValidation:"WHEN_REQUIRED"})`.
3. `async function* listPrefix(client, bucket, prefix)` paginating `ListObjectsV2Command` on `NextContinuationToken`, yielding `{key, size, etag, lastModified}`; `async function getObject(client, bucket, key)` → `{body: Buffer, sha256Meta: metadata["sha256"] ?? metadata["x-amz-meta-sha256"] ?? null, etag, size}` (read the stream fully via `Body.transformToByteArray()`; cap at 64 MB → `R2Error("TOO_LARGE")`); `sha256(buffer)` helper.
4. `r2-client.test.mjs` (5) with a fake client `{send(cmd)}` switching on `cmd.constructor.name`: (a) two pages are concatenated and the token is passed back (mutation: ignore the token); (b) `getObject` returns the metadata sha256 and a Buffer whose `sha256` matches a known string (mutation: return the etag as sha256); (c) credentials from a file with the dashboard spelling `Access_Key_ID = …` / `Secret_Access_Key = …` / `S3_API_Endpoint = …` resolve, and `JSON.stringify(creds)` never contains the secret (mutation: include it); (d) missing everything throws naming `R2_CREDENTIALS_FILE` (mutation: throw a bare message); (e) the client is constructed with `requestChecksumCalculation:"WHEN_REQUIRED"` and `responseChecksumValidation:"WHEN_REQUIRED"` (capture the ctor args via an injected `S3Client` class; mutation: drop one). Plus, in the same file as a 6th test: `git ls-files` contains no path ending `r2-credentials` (mutation: add such a file). Floor (6).
**Commands:** `npm test --prefix tools/foraycorpus-export -- r2-client.test.mjs` → 6 pass.
**Do not touch:** no network; no credential file in the tree.
**Stop and escalate if:** `@aws-sdk/client-s3` cannot be installed offline in CI (`npm ci` fails) — report the exact error; `Body.transformToByteArray` is not present in the installed SDK version (report the version; do not hand-roll a stream reader without saying so).
**Definition of done:** DRAFT PR `feat(corpus-export): R2 S3 client with fake-client tests (PKG-11)`.

### PKG-12 · `show-map.mjs` — qwen · S
**Executor:** qwen.
**Context:** `data/transcription-queue.json` (rows: `rank, title, feed_url, podcastindex_feed_id, apple_collection_id` — the queue the farm consumed; verified on row 1), `data/catalog.json` (`show_id`, `feed_url`, `apple_collection_id`), `data/breadth-transcript-yield.json` (`shows[].show_id === String(apple_collection_id)` — the breadth convention), `backend/src/generation/transcriptArchiveLookup.ts:449-463` (`showDir`: a directory matches `showId` exactly or `showId-*`) and `:464-471` (`locate`), `tools/segments/fetch-transcripts.mjs:204-212` (`safeKey`), `normalizeFeedUrl` (same import as PKG-04). **Inlined foray-db facts (a qwen agent cannot read foray-db; these ARE the spec):** (i) the farm's `_show_id` (`transcript-farm/farm/run.py:654-665`) is `String(podcastindex_feed_id)` when the queue row has one, else `slugify(title)`. (ii) `slugify` (`transcript-farm/farm/forayfmt.py:79-112`) is: NFKD-normalize; drop every code point that is not ASCII; lowercase; replace every run of characters not in `[a-z0-9]` with a single `-`; strip leading and trailing `-`; return `"show"` if the result is empty. Pinned examples: `slugify("Darkness Radio!")` → `"darkness-radio"`; `slugify("Café Société — Épisodes")` → `"cafe-societe-episodes"`; `slugify("  The   Odd Lots Podcast ")` → `"the-odd-lots-podcast"`; `slugify("日本語")` → `"show"`. (iii) The farm's `safe_key` is a byte-for-byte port of foray's `safeKey`, so R2 directories are `safeKey(<farm show_id>)`. (iv) Legacy whisper objects live at the bucket ROOT as `<show-slug>/<title-slug>-<10 hex>.json` (no `transcripts/` prefix) and are NOT bodies in foray layout.
**Exact change:**
1. `show-map.mjs` exporting `slugify(title)` (ported exactly as above), `buildShowMap({ queue, catalog, breadth })` → `Map<r2Dir, {show_id, via}>` where for each queue row: `dirFarm = safeKey(String(podcastindex_feed_id))` (when present) and `dirSlug = safeKey(slugify(title))`; `show_id` = catalog show whose `normalizeFeedUrl(feed_url)` equals the row's (via `"catalog-feed"`), else `String(apple_collection_id)` when present (via `"breadth-apple"`), else `slugify(title)` (via `"queue-title"`); both `dirFarm` and `dirSlug` map to it. Every catalog `show_id` and every breadth `String(apple_collection_id)` also maps to itself under `safeKey(show_id)` and under the raw `show_id` (via `"identity"`, the #831 forward contract). First writer wins on a key collision; collisions are returned in a second value: the function returns `{map, collisions:[{dir, kept, dropped}]}`.
2. `resolveShowDir(map, r2Dir)` → `{show_id, via} | null`; `localDirFor(show_id)` → `safeKey(show_id)` (matches `transcriptPath`).
3. `show-map.test.mjs` (7): (a) a queue row with PI id 470582 and a catalog show with the same feed_url maps `safeKey("470582")` → that `show_id` (mutation: map to the PI id); (b) a queue row absent from catalog and with apple id maps to `String(apple_collection_id)`; (c) a slug directory resolves too; (d) the four pinned `slugify` examples above (mutation: skip NFKD → the café case fails); (e) unknown dir → null; (f) `localDirFor("being-an-engineer")` equals `safeKey("being-an-engineer")` (mutation: return the raw id); (g) two queue rows whose titles slugify identically produce one `collisions` entry and the first mapping is kept (mutation: last writer wins). Floor (7).
**Commands:** `npm test --prefix tools/foraycorpus-export -- show-map.test.mjs` → 7 pass.
**Do not touch:** the data files (read only).
**Stop and escalate if:** `data/transcription-queue.json` rows lack `podcastindex_feed_id` (they have it); `normalizeFeedUrl` is not importable from `tools/shows/`.
**Definition of done:** DRAFT PR `feat(corpus-export): R2 show directory → foray show_id map (PKG-12)`.

### PKG-13 · `sync-r2.mjs` — qwen · S
**Executor:** qwen.
**Context:** PKG-11, PKG-12, `tools/segments/fetch-transcripts.mjs:221-229` (`transcriptPath` — it refuses escapes; copy its two-line `startsWith(base + sep)` guard), `:396-425` (the on-disk body shape the fetcher writes: `show_id, guid, source_url, cues, warnings`), `tools/segments/merge-segments.mjs` (`TRANSCRIPT_SOURCES` — import; verify with `git grep -n "export const TRANSCRIPT_SOURCES" origin/main -- tools/segments/merge-segments.mjs`), `backend/src/generation/transcriptArchiveLookup.ts:464-471` (`locate`) and `:472-` (`locateUncached`: exact `corpusSafeKey(guid).json`, then slug prefix, then the body's own `guid` — so file names may stay as R2 names). Inlined: bucket-root legacy objects `<show-slug>/<title-slug>-<10 hex>.json` and anything under `fingerprints/` are not bodies.
**Exact change:**
1. `sync-r2.mjs` exporting `parseSyncArgs(argv)` → `{out = TRANSCRIPTS_DIR, raw = false, dryRun = false, prefix = NORMALIZED_PREFIX, limit = Infinity, showFilter = null}` and `async function syncR2({ client, bucket, out, raw, dryRun, prefix, limit, showFilter, showMap, now })`.
2. For each object under `transcripts/normalized/` (and `transcripts/raw/` when `--raw`): parse `<dir>/<file>` from the key (anything with more or fewer path segments, or outside `transcripts/`, is counted `ignored`); `mapped = resolveShowDir(showMap, dir)`; `localDir = mapped ? localDirFor(mapped.show_id) : dir` (unmapped keeps the R2 name and is listed); `localPath = join(out, kind, localDir, file)` guarded by the `transcriptPath`-style `startsWith(base + sep)` check (do NOT call `transcriptPath` itself: it re-applies `safeKey` and would rename the file). If the local file exists and its sha256 equals the object's metadata sha256 (or, when that metadata is absent, its size equals `size`), count `skipped_same` and continue. Else GET, verify sha256 against metadata when present (mismatch → `quarantined` with reason `SHA_MISMATCH`, nothing written), parse JSON, validate: `typeof show_id === "string"`, `typeof guid === "string"`, `Array.isArray(cues)`, every cue has numeric `start_sec`, `end_sec` and string `text`, `transcript_source` absent/null or in `TRANSCRIPT_SOURCES` (else `quarantined` `BAD_SHAPE:<field>`); write bytes byte-identical via tmp+rename (never re-serialize); count `written`. `--raw` objects are written without parsing.
3. After the walk write `SYNC_STATE_FILE` (`data-local/transcripts/r2-sync-state.json`): `{version:1, synced_at, bucket, prefix, objects_seen, ignored, written, skipped_same, quarantined:[{key, reason}], unmapped_dirs:[{dir, objects}], shows:{[localDir]: {show_id, via, bodies}}, bodies_expected: <sum of bodies over shows>}` where `bodies` = normalized bodies present on disk in that directory after the run. `--dry-run` prints the same summary and writes nothing (no state file either). Print one summary line.
4. `sync-r2.test.mjs` (8, fake client + tmp dir): (a) a normalized object lands at `<out>/normalized/<safeKey(show_id)>/<file>` byte-identical (mutation: `JSON.stringify` the parsed body); (b) a second sync writes nothing (`skipped_same 1`; mutation: compare mtime); (c) a bucket-root legacy key `some-show/title-abcdef1234.json` and a `fingerprints/...` key are `ignored` (mutation: treat any `.json` as a body); (d) a body with `transcript_source: "not-a-source"` is quarantined and absent on disk (mutation: write anyway); (e) sha mismatch quarantines (mutation: skip the check); (f) an unmapped dir keeps its name and appears in `unmapped_dirs` (mutation: drop it); (g) the state file's `bodies_expected` equals the number of normalized bodies on disk after the run (mutation: count objects seen); (h) `--dry-run` leaves `out` absent. Floor (8).
**Commands:** `npm test --prefix tools/foraycorpus-export -- sync-r2.test.mjs` → 8 pass.
**Do not touch:** never delete local files (a body absent from R2 stays); never rewrite `show_id` inside a body; `r2-client.mjs`, `show-map.mjs`.
**Stop and escalate if:** `TRANSCRIPT_SOURCES` is not exported from `merge-segments.mjs` (report; do not copy the list).
**Definition of done:** DRAFT PR `feat(corpus-export): mirror R2 transcripts into data-local (PKG-13)`.

### PKG-14 · `rebuild-index.mjs` launcher — qwen · XS
**Executor:** qwen.
**Context:** `tools/generation/warm-transcript-index.mjs:1-60` (why a launcher, `REPO_ROOT`, `WARM_ENTRY`, `warmArgs`, spawns `node <tsx> <entry>`) and its test, `backend/src/cli/warmTranscriptIndex.ts:20-70` (reconcile then build; `--show`, `--offline`, `--reconcile-only`, `--build-only`) and `:72-86` (`parseWarmArgs`: repeated `--show` IS collected into `shows[]` — verified, so one spawn with many `--show` pairs is correct), PKG-13's state file.
**Exact change:** `rebuild-index.mjs` exporting `warmArgsFor(state, { offline })` → `["--show", <localDir>, "--show", <localDir>, …]` for every key of `state.shows` whose `bodies > 0`, sorted, plus `--offline` when set; `main()` runs `sync-r2.mjs`'s `syncR2` (same process) then spawns `tools/generation/warm-transcript-index.mjs` once with those args, exit code propagated. Test (3): args list one `--show` pair per synced show in sorted order (mutation: emit one `--show` with all ids joined); `--offline` passthrough (mutation: drop it); the spawned launcher path exists (`fs.existsSync`). Floor (3).
**Commands:** `npm test --prefix tools/foraycorpus-export -- rebuild-index.test.mjs` → 3 pass.
**Do not touch:** `tools/generation/` (spawn only), `backend/`.
**Stop and escalate if:** `warm-transcript-index.mjs`'s `warmArgs` drops or reorders repeated `--show` pairs (read its test; report).
**Definition of done:** DRAFT PR `feat(corpus-export): sync-then-warm launcher (PKG-14)`.

### PKG-15 · First live sync + warm on the generation PC — opus · S
**Executor:** opus. Why: uses the R2 credential (Q1) on the founder's PC; field diagnosis of what maps and what does not.
**Context:** PKG-13/14, `backend/src/cli/warmTranscriptIndex.ts:20-70` (memory note: 16 GB machine, one show at a time), `backend/src/generation/transcriptCorpus.ts:236-280` (`corpusCoverage` line the driver prints), `docs/curation/foray-to-spec-roadmap.md:513-551` (G-11 Done-when b: SYSK/Odd Lots bodies present and tier-marked — the tier mark is PKG-21).
**Exact change:** with credentials in `~/.foray/r2-credentials`, run `node tools/foraycorpus-export/sync-r2.mjs --dry-run` (record the summary), then the real sync, then `node tools/foraycorpus-export/rebuild-index.mjs`; record wall time, peak RSS, bodies written, unmapped dirs (and what they are: farm shows absent from `data/transcription-queue.json`?), quarantined keys with reasons, collisions from `buildShowMap`. Verify one farm body is reachable: run `node tools/generation/warm-transcript-index.mjs --reconcile-only` and confirm `corpus-digest.json` gained rows for a synced show; note how many are `searchableOnly`. Write `docs/curation/corpus-sync-2026-09.md` with all of it, and a HUMAN-ACTIONS line if a credential problem appeared. If unmapped farm directories exceed 10 % of objects, add the missing queue-row → show mapping rule to PKG-12 (a follow-up PR, not this one).
**Commands:** as above; suites unaffected.
**Do not touch:** `backend/src/`; the bucket (reads only).
**Stop and escalate if:** the bucket listing shows a layout other than the documented one; the sha256 metadata is absent on most objects (then PKG-13's size fallback is doing the work — say so).
**Definition of done:** doc merged; DRAFT PR `docs(corpus-export): first R2 sync on the generation machine (PKG-15)`.

### PKG-16 · `dai-tier.mjs` — qwen · S
**Executor:** qwen.
**Context:** `docs/adr/0008-ad-tolerance-and-timestamp-precision.md:214-252` (the two tiers; pad = `delta_max` + margin ≤ 120 s) and `:86-90` (the Gastropod arithmetic: `534,574 × 8 ÷ 128,200 = 33.4 s` — NOTE 128,200 there is BITS per second, i.e. 128.2 kbps = 16,025 bytes/s; the `× 8` converts bytes to bits), `tools/transcribe/ad-inflation.mjs:85-125` (`inflationRatio`, `AD_FREE_THRESHOLD 1.01`, `AD_FREE_FLOOR 0.99`), `:149-170` (`classify`, `MIN_SAMPLES_FOR_AD_FREE 2`), `player/seek-policy.js:109` (`AD_PAD_CEILING_SEC = 120`, pure ESM, no browser globals — verified), roadmap `:553-586` (G-12 field names: `delta_max_sec`, `spread_sec`, `n_probes`, `pad_sec`, `tier`).
**Exact change:**
1. `dai-tier.mjs` exporting `PAD_MARGIN_SEC = 30`, `PAD_CEILING_SEC` imported from `../../player/seek-policy.js` (`AD_PAD_CEILING_SEC`), `TIERS = Object.freeze(["ad-free","paddable","locate-required","unmeasured","unmeasurable"])`, `bytesPerSec({declared_bytes, feed_duration_sec})` → `declared_bytes / feed_duration_sec` (BYTES per second) or null when either is not > 0, `probeDeltaSec(probe, ctx)` → `(probe.delivered_bytes − ctx.declared_bytes) / bytesPerSec(ctx)` (null when no bitrate), `tierFor({probes, declared_bytes, feed_duration_sec})` → `{n_probes, delta_max_sec, spread_sec, pad_sec, tier, ratios}` with: `probes` filtered to `status 206 && delivered_bytes > 0`; `n_probes < 2` → `unmeasured` (all numbers null); `bytesPerSec` null (declared 0/null, e.g. Megaphone `length="0"`, or no duration) → `unmeasurable`; every ratio in `[AD_FREE_FLOOR, AD_FREE_THRESHOLD)` → `ad-free` with `delta_max_sec` = max delta (may be ≤ 0), `pad_sec 0`; else `delta_max_sec = max(delta_i)`, `spread_sec = max(delta_i) − min(delta_i)`, `pad_sec = round(delta_max_sec + PAD_MARGIN_SEC)`, tier `paddable` if `pad_sec ≤ PAD_CEILING_SEC` else `locate-required`. All seconds rounded to 0.1.
2. `dai-tier.test.mjs` (7): (a) Gastropod: `feed_duration_sec: 3600`, `declared_bytes: 57_690_000` (= 16,025 B/s × 3,600 s), probes `delivered_bytes` = `57_690_000 + 2_000_000` and `57_690_000 + 2_534_574` (both well above the ad-free band) → `spread_sec 33.4`; the test comment states that the ADR's 128,200 is bits/s and that this fixture encodes 16,025 bytes/s (mutation: multiply `bytesPerSec` by 8 → spread 4.2); (b) one probe → `unmeasured`; (c) declared 0 → `unmeasurable`; (d) two probes at ratio 1.000 → `ad-free`, `pad_sec 0`; (e) delta_max 90 → `paddable` (pad 120), delta_max 90.5 → `locate-required` (mutation: `<`); (f) `PAD_CEILING_SEC === 120` and the source of `dai-tier.mjs` contains the import specifier `player/seek-policy.js` (read the file; mutation: hardcode 120 locally); (g) a 404 probe is ignored (mutation: count it). Floor (7).
**Commands:** `npm test --prefix tools/foraycorpus-export -- dai-tier.test.mjs` → 7 pass.
**Do not touch:** `player/`, `tools/transcribe/` (imports only).
**Stop and escalate if:** `player/seek-policy.js` cannot be imported from Node (report the error verbatim).
**Definition of done:** DRAFT PR `feat(corpus-export): per-episode DAI tiering (PKG-16)`.

### PKG-17 · `probe-runner.mjs` + `dai-sample.mjs` (G-19) — qwen · S
**Executor:** qwen.
**Context:** roadmap `:470-483` (G-19: ~500 episodes from the timed+audio set, stratified by feed host and show size, two 2-byte GETs ≥ 24 h apart, HEAD lies), `tools/transcribe/ad-inflation.mjs:221-262` (`probeEpisode(url, declaredBytes, {fetchImpl, gate, sleep, now, …})`) and `:262-336` (the exact return keys `{ratio, delivered_bytes, status, error, …}`), `:195-202` (timeouts/retry), `tools/segments/politeness.mjs:142-152` (`AUDIO_PROBE_HEADERS`, `MIN_HOST_INTERVAL_MS`), PKG-05 episode rows (`audio.url`, `audio.declared_bytes` null in the corpus today), PKG-16.
**Exact change:**
1. `probe-runner.mjs` (shared by PKG-17 and PKG-19; created HERE) exporting `runProbes(picks, stateFile, { fetchImpl, now, minAgeMs = 24 * 3600 * 1000, sleep })`: state = `{version:1, picks:{[pickKey]: {…pick, probes:[{at, status, delivered_bytes, ratio, error}]}}}` with `pickKey = "<show_key>|<guid>"`; for each pick, append a probe from `probeEpisode(pick.audio_url, pick.declared_bytes ?? null, {fetchImpl, now, sleep, …})` ONLY if the last probe for that key is ≥ `minAgeMs` old or absent; persist the state file after every probe (tmp+rename); resume on rerun; return `{attempted, skipped_recent, errors}`. `readProbeState(file)` returns the default when absent.
2. `dai-sample.mjs` exporting `stratifiedSample(episodeRowsByShow, { n = 500, seed = 20260925 })`: frame = rows with `transcript.timed && audio`; strata = `hostOf(audio.url)` × show-size bucket (`1-4`, `5-19`, `20-99`, `100+` timed+audio episodes); allocation: (i) largest-remainder proportional allocation of `n` over strata; (ii) within a stratum pick at most 3 per show, deterministic PRNG (mulberry32 on `seed`); (iii) if a stratum cannot fill its quota under the cap, its shortfall is re-allocated over the strata that still have capped-available rows by one more largest-remainder pass, repeated until no shortfall or no capacity; `frame_capped` = the total number of rows pickable under the ≤ 3-per-show cap; result `{frame_size, frame_capped, strata:[{host, bucket, frame, quota, picked}], picks:[{show_key, guid, audio_url, declared_bytes, feed_duration_sec}]}` with `picks.length === min(n, frame_capped)`.
3. `report(state)`: per pick `tierFor` (PKG-16) — when `declared_bytes` is null the ratio is undefined, so ALSO compute the declared-free classes: `stable` (two 206 probes with equal `delivered_bytes`), `varying` with `spread_sec` from `bytesPerSec ≈ min(delivered)/feed_duration_sec`, `paddable-candidate` (`spread_sec + PAD_MARGIN_SEC ≤ PAD_CEILING_SEC`), `locate-required` (above), `unmeasurable` (no duration or < 2 usable probes). Output `{N, sampling_frame, shares:{ad-free, paddable, locate-required, unmeasured, unmeasurable, stable, varying}, by_host:[…]}`. CLI: `--sample <episodes dir> --out sample.json`, `--probe --state <file>`, `--report --state <file> --out <json>`.
4. `probe-runner.test.mjs` (3): (a) a second `runProbes` within 24 h issues zero fetches (fake `fetchImpl` counter; mutation: drop the age check); (b) the state file is rewritten after each probe (count writes via a spy on the tmp+rename helper or by reading between two fakes; mutation: write once at the end); (c) a rerun after a simulated crash (state with one pick probed, one not) probes only the unprobed pick (mutation: ignore existing state). `dai-sample.test.mjs` (5): (a) sampling is deterministic for a seed and ≤ 3 per show (mutation: `Math.random`); (b) with a fixture where one stratum has only 2 pickable rows and quota 5, `picks.length === min(n, frame_capped)` and the shortfall lands in other strata (mutation: floor without remainders, or skip the re-allocation); (c) two equal 206 probes → `stable`; probes 1,282,000 bytes apart at 128,200 B/s-equivalent (`min(delivered) = 128,200 × feed_duration_sec`) → `varying` `spread_sec 10.0` (mutation: use max instead of min for the bitrate); (d) the report's shares sum to N; (e) `hostOf` strips `www.` and lowercases (mutation: raw hostname). Floors: probe-runner (3), dai-sample (5).
**Commands:** `npm test --prefix tools/foraycorpus-export -- probe-runner.test.mjs` → 3 pass; `-- dai-sample.test.mjs` → 5 pass.
**Do not touch:** no real fetch in tests; `tools/transcribe/` imports only.
**Stop and escalate if:** `probeEpisode` throws (rather than returning `ratio null` + `error`) when `declaredBytes` is `null` (read `:262-336`; report the exact behaviour in the PR and wrap only if it throws).
**Definition of done:** DRAFT PR `feat(corpus-export): probe runner and G-19 DAI tier sampler (PKG-17)`.

### PKG-18 · Run the G-19 sample — opus · S
**Executor:** opus. Why: outbound traffic to publisher CDNs (Q4), politeness with Joey's crawler, interpretation.
**Context:** roadmap `:470-483`, `:513-536` (politeness: the crawler's egress vs ours), PKG-17, PKG-10's export output on hermes-vm.
**Exact change:** on hermes-vm: `dai-sample.mjs --sample /var/lib/foray-corpus-export/<version>/episodes --out sample.json`; `--probe` day 1; `--probe` day 2 (≥ 24 h); `--report`. Commit `docs/curation/dai-sample-2026-09.json` and `docs/curation/dai-sample-2026-09.md` with N, the frame (`frame_size`, `frame_capped`), the shares, by-host table, and the sentence answering §1.2's "playable tape supply" row; propose G-11's backfill order (ad-free/stable hosts first). Add a line in `docs/curation/foray-to-spec-roadmap.md` §1.2 row "Playable tape supply after DAI tier" pointing to the doc (docs/ is allowed).
**Commands:** as above; `node --test "test/suite-integrity.test.js"` (deck-claims: run `node tools/ci/deck-claims.mjs` if it checks that deck — read `tools/ci/deck-claims.mjs:1-40` first; do not edit it).
**Do not touch:** `tools/ci/`.
**Stop and escalate if:** any host returns 429 twice (stop that host, note it); the crawler's IP is the same egress (do not run).
**Definition of done:** the two docs merged; DRAFT PR `docs(corpus): G-19 DAI tier sample (PKG-18)`.

### PKG-19 · `dai-measure.mjs` (G-12 curated-catalogue slice) — qwen · S
**Executor:** qwen.
**Context:** roadmap `:553-586` (G-12: per-episode artifact `data/dai-measurements.json`; `data/dai-classification.json` is DERIVED, its readers stay), `data/transcript-availability.json` (`shows[]`: `show_id, apple_collection_id, title, feed_url, dai_suspected, enclosure_host`; nested `episodes[]`: `guid, title, enclosure_url, enclosure_bytes, duration_sec, transcript_url, transcript_type, has_timestamps`) — this is the ONLY committed episode-level artifact; `data/breadth-transcript-yield.json` is show-rows-only (verified: its `policy` field says so) and is OUT OF SCOPE here — breadth episodes get measured only once an episode-level breadth artifact exists (the corpus export's `episodes/*.jsonl` lacks `enclosure_bytes` today), `tools/transcribe/ad-inflation.mjs:379-405` (`applyVerdicts(classification, results, {measuredAt})` writes `shows[apple_id].ad_inflation`), `:170-193` (`summariseShow`), `tools/refresh/dai.mjs:131` (`resolveChain` — for `resolved_host`; NOT called in tests), PKG-16, PKG-17's `probe-runner.mjs` (import `runProbes`/`readProbeState`; do not modify it).
**Exact change:**
1. `dai-measure.mjs` exporting `selectCatalogueTargets({availability, perShow = Infinity})` → picks every episode with an `https:` `enclosure_url` and `has_timestamps === true`, carrying `show_key: show_id, show_id, apple_collection_id, show_title, guid, audio_url, declared_bytes: enclosure_bytes, feed_duration_sec: duration_sec`; probing via `runProbes` from `probe-runner.mjs` (no probe loop in this file); `buildMeasurements(state, { builtAt })` → `data/dai-measurements.json` = `{version:1, built_at, method:"2-byte ranged GET, N ≥ 2 ≥ 24 h apart; tools/foraycorpus-export/dai-measure.mjs (ADR-0008)", pad_margin_sec:30, pad_ceiling_sec:120, scope:"data/transcript-availability.json (curated)", episodes:{ "<show_id>|<guid>": {show_id, apple_collection_id, guid, audio_url, resolved_host, declared_bytes, feed_duration_sec, probes:[{at, status, delivered_bytes, ratio}], n_probes, delta_max_sec, spread_sec, pad_sec, tier} } }` with keys sorted; `built_at` = the newest probe `at` (NOT now) so a rebuild with no new probes is byte-identical.
2. `deriveShowVerdicts(measurements)` → per `apple_collection_id`: `{verdict: "ad-free"|"injected"|"unknown", median, n, samples}` via `summariseShow(ratios)` where ratios come only from probes with a numeric ratio; `--derive` applies them to `data/dai-classification.json` through `applyVerdicts` with `measuredAt = measurements.built_at` and writes the file with `JSON.stringify(x, null, 2) + "\n"` (the file's existing style) — `dai`/`reason` untouched (that is `classify-dai.mjs`'s).
3. `dai-measure.test.mjs` (5): (a) targets exclude episodes without timestamps and `http:` enclosures (mutation: drop the https check); (b) a fixture state with two probes yields a `locate-required` row for a `+10 min` SYSK-like delta (declared 60,000,000 B, duration 3,000 s → 20,000 B/s; probes +12,000,000 and +12,100,000 bytes → `delta_max_sec 605.0`; mutation: swap min/max); (c) `built_at` equals the newest probe time and two builds from the same state are byte-identical (mutation: `new Date()`); (d) derive writes `ad_inflation.measured_at === built_at` and leaves `dai` untouched (mutation: set `dai`); (e) keys sorted. Floor (5).
**Commands:** `npm test --prefix tools/foraycorpus-export -- dai-measure.test.mjs` → 5; `-- probe-runner.test.mjs` still 3; `-- dai-sample.test.mjs` still 5.
**Do not touch:** `tools/refresh/classify-dai.mjs` (unchanged: derived data flows through `applyVerdicts`), `tools/ci/`, `probe-runner.mjs`, `dai-sample.mjs`.
**Stop and escalate if:** `applyVerdicts`'s result shape expects fields (`undersized_samples`, `note`) you cannot compute — pass them through from `summariseShow` and report; `runProbes`'s state shape does not carry `feed_duration_sec` (PKG-17 must add it; do not fork the runner).
**Definition of done:** DRAFT PR `feat(corpus-export): per-episode DAI measurements and derived show verdicts (PKG-19)`.

### PKG-20 · Run dai-measure twice; commit the data — opus · S
**Executor:** opus. Why: egress (Q4), field verification against ADR-0008's numbers, judging anomalies.
**Context:** roadmap `:577-585` (Done-when: SYSK `locate-required` N ≥ 2; Gastropod reproduces 33.4 s; derive byte-stable; CI DAI invariant passes), `.github/workflows/ci.yml:403-541` (data-and-site: the "DAI flags" invariant — read which files it checks), PKG-19.
**Exact change:** run `--probe` on day 1 and day 2 from hermes-vm (or the PC), `--build`, `--derive`; commit `data/dai-measurements.json` and the changed `data/dai-classification.json`; run `node tools/refresh/classify-dai.mjs --reclassify` afterwards to prove `dai/reason` are unchanged (byte-diff); record SYSK's and Gastropod's rows in the PR body; run `node tools/foraycorpus-export/dai-measure.mjs --build` twice and `git diff --stat` shows nothing. Note episodes with `length="0"` (unmeasurable) count.
**Commands:** as above; `node tools/ci/run-suites.mjs` (full) before the PR since `data/` changed.
**Do not touch:** `tools/refresh/` code.
**Stop and escalate if:** Gastropod's spread is not within 33.4 ± 5 s (check first that the fixture bitrate arithmetic in PKG-16 test (a) matches: bytes/s = declared ÷ duration, no ×8); more than 20 % of probes error.
**Definition of done:** data committed; DRAFT PR `data(dai): per-episode ad-load measurements, first two probes (PKG-20)`.

### PKG-21 · Tier gate + `ad_pad_sec` in `audioSourceLookup.ts` — opus · S
**Executor:** opus. Why: `backend/src/` is DENIED.
**Context:** `backend/src/generation/audioSourceLookup.ts:46-59` (`MintedSegmentSource`), `:107-141` (`loadShowAudioMeta`), `:144-152` (`createDigestAudioSourceResolver`), `:172-205` (`mintSegmentSource`), `backend/src/generation/sourceBeats.ts:1736-1738` (a null resolver result passes the candidate over), `backend/test/audioSourceLookup.test.ts:1-40`, `player/foray-queue.js:379-402` (consumes `ad_pad_sec` on a segment; `dai_suspected` gate), PKG-19's file shape, `tools/foray/check-forays.mjs:289-303` (`ACCEPTED_SHAPES`), `test/suite-integrity.test.js:2547` (`mintedSegmentRow.test.ts` floor 24) and `:2592` (`audioSourceLookup.test.ts` floor 9).
**Exact change:** add `loadEpisodeTiers(root)` reading `data/dai-measurements.json` (absent → empty Map) keyed `"<show_id>|<guid>"`; extend `mintSegmentSource(entry, itemId, showMeta, tiers?)`: look up the tier; `locate-required` → return null; `paddable` → add `ad_pad_sec: pad_sec` to the row; `ad-free`/`unmeasured`/`unmeasurable`/absent → unchanged (the host prior `dai_suspected` still applies). Thread `tiers` through `createDigestAudioSourceResolver` (cache with the meta map). Tests in `audioSourceLookup.test.ts` (+3): locate-required → null (mutation: ignore the tier); paddable → `ad_pad_sec 95` (mutation: drop the field); absent measurements → identical to before (mutation: return null when the file is missing). Also the minted source-row zod schema (`backend/src/types/tapeSourcing.ts` — find the source-row schema `finalizeForay.ts`/`mintedSegmentRow` use): allow optional `ad_pad_sec` number ≥ 0 with one test in `backend/test/mintedSegmentRow.test.ts` (a row with `ad_pad_sec: 95` passes, `ad_pad_sec: -1` fails; mutation: drop the field from the schema). Floors: `BACKEND_FLOORS` `test/audioSourceLookup.test.ts` 9→12 and `test/mintedSegmentRow.test.ts` 24→25.
**Commands:** `cd backend && npm run typecheck && npm test -- test/audioSourceLookup.test.ts test/mintedSegmentRow.test.ts`; `node --test "test/suite-integrity.test.js"`.
**Do not touch:** `tools/foray/check-forays.mjs` (PKG-22), `player/`.
**Stop and escalate if:** `sourceBeats` needs a reason string for a refused candidate that the current null contract cannot carry (add nothing; note it for a follow-up).
**Definition of done:** DRAFT PR `feat(generation): per-episode DAI tier gates minted tape (PKG-21)`.

### PKG-22 · `check-forays`: `ad_pad_sec` shape + locate-required refusal — qwen · S
**Executor:** qwen.
**Context:** `tools/foray/check-forays.mjs:43-84` (its imports: it ALREADY imports sibling ESM — `../../backend/src/copy/rules.js` at `:51`, `../segments/merge-segments.mjs` at `:77`, `./check-narration.mjs` at `:84` — so adding one more import is in keeping), `:289-303` (`ACCEPTED_SHAPES`), `:650-690` (segment-sources validation: `duration_sec`, `audio_url`, `dai_suspected`), `:629` (`checkForays(files)`), `:1644` (`loadFiles(root)`), `tools/foray/check-forays.test.mjs:1-80` (how `--root` fixtures are built), `tools/foray/fixtures/boundary/data/segment-sources.json`, `player/seek-policy.js:109` (`AD_PAD_CEILING_SEC`; pure ESM, no browser globals — verified), `player/foray-queue.js:379-381` (a DAI segment with `ad_pad_sec` above the ceiling is SKIPPED by the player), `test/suite-integrity.test.js:1364` (`check-forays.test.mjs` floor is 168).
**Exact change:** import `AD_PAD_CEILING_SEC` from `../../player/seek-policy.js`. In the segment-sources loop add: if `s.ad_pad_sec !== undefined && s.ad_pad_sec !== null` then it must be a finite number ≥ 0 (else `err`); if `s.dai_suspected === true && typeof s.ad_pad_sec === "number" && s.ad_pad_sec > AD_PAD_CEILING_SEC` → `err(\`segment-sources "${s.id}": ad_pad_sec ${n} exceeds the player's ${AD_PAD_CEILING_SEC} s ceiling — a published Foray would skip this tape (ADR-0008 locate-required)\`)`. Tests in `check-forays.test.mjs` (+4) using a tmp copy of the boundary fixture: a row with `ad_pad_sec: 121` + `dai_suspected: true` fails with the message (mutation: `>=` off-by-one at 120 passes); `ad_pad_sec: 120` passes; `ad_pad_sec: "x"` fails; `ad_pad_sec: 200` with `dai_suspected:false` passes with no error (the pad only matters where the host injects; mutation: drop the `dai_suspected` guard). Floor 168→172. Verify the frozen fixture still passes (`node tools/foray/check-forays.mjs --root tools/foray/fixtures/frozen`).
**Commands:** `node --test tools/foray/check-forays.test.mjs` → 172 pass; `node tools/foray/check-forays.mjs` (live data) green; `cd backend && npm test -- test/finalizeForay.test.ts` (it dynamically imports `check-forays.mjs` at `finalizeForay.ts:578`) green; `node --test "test/suite-integrity.test.js"`.
**Do not touch:** `data/`, `backend/`, `player/`.
**Stop and escalate if:** `cd backend && npm test -- test/finalizeForay.test.ts` fails after the import is added (report the error; do not inline the constant).
**Definition of done:** DRAFT PR `feat(check-forays): refuse tape the player would skip (PKG-22)`.

### PKG-23 · Run-start integrity guard — opus · S
**Executor:** opus. Why: `backend/src/`.
**Context:** `backend/src/cli/generateForays.ts:783-806` (provider, index, `corpusCoverage` at run start), `backend/src/generation/transcriptCorpus.ts:179-200` (`scanNormalizedCorpus`), `:236-280` (`corpusCoverage`), roadmap `:612-640` (G-14 guard: refuse when the archive's count fell below the manifest's; mutation test on the truncated fixture), PKG-03's `truncated-archive/` fixture, PKG-13's state file (`bodies_expected`, `shows{}`), `test/suite-integrity.test.js:2634` (`generateForaysArgs.test.ts` floor is 13).
**Exact change:** `backend/src/generation/archiveIntegrity.ts` exporting `readSyncState(root)` and `archiveIntegrity({ state, shows: CorpusShowOnDisk[] })` → `{ok, expected, found, missingShows:[…], shortShows:[{show, expected, found}]}` (`found` = sum of bodies over the state's `shows` keys present on disk; `ok` iff `found >= expected`); in `generateForays.ts` after `corpusCoverage`, when a sync state exists and `!ok`, print the shortfall and exit 2 unless `--allow-short-archive` (add the flag to args). Tests `backend/test/archiveIntegrity.test.ts` (4): fixture archive with matching state → ok; the truncated fixture → `!ok`, names the short show (mutation: compare directory counts instead of bodies); absent state → ok with `expected null`; a show in state but missing on disk is listed. One test in `generateForaysArgs.test.ts`: `--allow-short-archive` parses to `allowShortArchive: true`, default false (mutation: drop the flag). Floors: `BACKEND_FLOORS` new entry `test/archiveIntegrity.test.ts: 4`; `test/generateForaysArgs.test.ts` 13→14.
**Commands:** `cd backend && npm run typecheck && npm test -- test/archiveIntegrity.test.ts test/generateForaysArgs.test.ts`; `node --test "test/suite-integrity.test.js"`.
**Do not touch:** `tools/`.
**Stop and escalate if:** `generateForays.ts` has a test that spawns the CLI end to end and now needs `data-local` state (skipping the guard when `NODE_ENV === "test"` is NOT acceptable — use the flag).
**Definition of done:** DRAFT PR `feat(generation): refuse to source against a short archive (PKG-23)`.

### PKG-24 · Cache-through R2 cue provider — opus · S (optional; nothing depends on it)
**Executor:** opus. Why: `backend/src/`, a cross-cutting seam.
**Context:** `backend/src/generation/transcriptArchiveLookup.ts:239-253` (`TranscriptCueProvider` incl. `transcriptSource`), `:291-315` (`FileTranscriptCueProvider` fields: `dirByShow`, `cuesByKey`, `guidIndexByDir`, `filesByDir` at `:312`, `locatedByKey` at `:315` — all `private`), `:436-447` (`filesIn`: per-dir file cache populated on first read), `:449-463` (`showDir`, private), `:464-471` (`locate`, private, memoised in `locatedByKey`), `:472-` (`locateUncached`), `backend/src/generation/transcriptTextIndex.ts:149-182` (`TranscriptBodyStat`, `TranscriptBodySource` — the index needs `bodyStat`), `backend/src/generation/mjs-modules.d.ts` (backend may import `.mjs`), PKG-11 (`getObject`), PKG-12 (`localDirFor`).
**Exact change:** A pure subclass cannot work today: `showDir`/`locate` are `private`, and `filesByDir`/`locatedByKey`/`dirByShow` memoise a miss, so a body written after the miss stays invisible. This PR therefore (1) changes `showDir`, `locate`, `locateUncached` and `filesIn` to `protected`, and adds `protected invalidateShow(showId: string): void` to `FileTranscriptCueProvider` that deletes the show's entries from `dirByShow`, `filesByDir` (by the resolved dir), `guidIndexByDir`, and every `locatedByKey`/`cuesByKey` key prefixed `<showId>|`; (2) adds `R2TranscriptCueProvider extends FileTranscriptCueProvider` taking `{client, bucket, showMap, getObject}` (the S3 call is injected as a function — NO `@aws-sdk` import in backend); on a `getCues` miss, GET `transcripts/normalized/<r2Dir>/<corpusSafeKey(guid)>.json` for every `r2Dir` mapping to `entry.show_id`, verify sha256 against metadata, write it into the cache dir byte-identical, call `invalidateShow(entry.show_id)`, then delegate to `super.getCues`. `bodyStat` unchanged (cache only). Selected in `generateForays.ts` when `R2_BUCKET` env is set. Tests `backend/test/r2TranscriptCueProvider.test.ts` (4) with a fake `getObject`: miss → fetch → cached → second call no fetch; a body fetched after a miss is returned on the VERY NEXT `getCues` call (mutation: skip `invalidateShow` → stale `filesByDir` → null); a 404 → null and nothing written; wrong-sha object is not cached. `BACKEND_FLOORS` entry (4).
**Commands:** `cd backend && npm run typecheck && npm test -- test/r2TranscriptCueProvider.test.ts test/transcriptArchiveLookup.test.ts`.
**Do not touch:** `tools/foraycorpus-export/` (imports only).
**Stop and escalate if:** `transcriptArchiveLookup.test.ts` breaks from the visibility change (it should not — `protected` widens); a memo map other than the five listed also caches a miss (list it and invalidate it too).
**Definition of done:** DRAFT PR `feat(generation): cache-through R2 cue provider (PKG-24)`.

### PKG-25 · Floor recalibration on the run-2 fixture — opus · S
**Executor:** opus. Why: diagnosing from records; backend tests.
**Context:** roadmap `:626-640` (today's margins 0.257 / 0.349 against a 63-document idf), `backend/test/fixtures/run2-deepen-2026-09-09.json`, `backend/src/generation/transcriptArchiveLookup.ts:658-716` (`TIER2_WINDOW_MIN_TERMS`, `TIER2_DISTINCTIVE_WEIGHT`, `TIER2_WINDOW_MIN_SHARE`, the "fifteen-episode show" comment at `:857`), `:875-903` (`claimTermWeigher(claimTerms, idf)`), `backend/src/generation/transcriptTextIndex.ts:287-303` (per-search idf), PKG-26's `corpus-df.json`.
**Exact change:** add an optional `idfOverride` to `FileTranscriptTextIndex` (`options.corpusDf?: {docs, df}` → idf from the corpus table when a term is present, else the per-show value); re-run the run-2 fixture's offline sourceBeats cases with and without it on the synced archive; record false-positive and nearest-miss margins in `docs/curation/tier2-floor-recalibration-2026-09.md`; change no threshold in this PR (thresholds are a separate decision with the numbers in hand). Test in `transcriptTextIndex.test.ts` (+1): the override changes a known term's idf in a two-show fixture (mutation: ignore the override); bump its `BACKEND_FLOORS` entry by 1.
**Commands:** `cd backend && npm test -- test/transcriptTextIndex.test.ts test/sourceBeats.test.ts`; `node --test "test/suite-integrity.test.js"`.
**Stop and escalate if:** the offline cases skip because `data-local` bodies are missing (run PKG-15 first).
**Definition of done:** doc + override merged; DRAFT PR `feat(generation): corpus-wide idf override and recalibrated margins (PKG-25)`.

### PKG-26 · `buildCorpusTerms.ts` — opus · S
**Executor:** opus. Why: `backend/src/`, reuse of the index's tokenizer (no second tokenizer may exist).
**Context:** `backend/src/generation/transcriptTextIndex.ts:207-260` (how a show's index is built from `TranscriptBodySource`; find the tokenizer it calls), `backend/src/generation/catalogueLookup.ts:121` (`tokenizeForSourcing`), `backend/src/cli/warmTranscriptIndex.ts:20-70` and `:235-300` (walk one show at a time; memory note), `backend/src/generation/transcriptCorpus.ts:179` (`scanNormalizedCorpus`), roadmap `:513-536` (one df table across the corpus, per-show postings kept), `:588-598` (G-13: top-k tf-idf terms per episode, no model call).
**Exact change:** `backend/src/cli/buildCorpusTerms.ts`: for every show dir (respecting `--show`), for every body: tokens = the index's tokenizer over cue text; per-episode tf map; accumulate df (distinct episodes per term) and `docs`; after the pass write `data-local/transcripts/corpus-df.json` `{version:1, built_at, docs, df:{term:n}}` (terms with df ≥ 2 only) and `data-local/transcripts/episode-terms.jsonl` one line per episode `{show_id, guid, n_tokens, terms:[[term, tfidf]…]}` top 30 by `tf × log(1 + (docs − df + 0.5)/(df + 0.5))`, terms sorted by score desc then alpha. Two passes (df first, then scores) so memory holds only the df map. Export `parseTermsArgs`, `scoreTerms(tf, df, docs, k)`. Tests (`buildCorpusTerms.test.ts`, 4): `scoreTerms` ranks a rare term above a common one (mutation: drop the idf); ties break alphabetically; k caps; df excludes singletons. `BACKEND_FLOORS` (4).
**Commands:** `cd backend && npm run typecheck && npm test -- test/buildCorpusTerms.test.ts`.
**Do not touch:** `tools/` (PKG-27 adds the launcher).
**Definition of done:** DRAFT PR `feat(generation): corpus df table and per-episode terms (PKG-26)`.

### PKG-27 · `build-terms.mjs` launcher — qwen · XS
**Executor:** qwen. **Context:** `tools/generation/warm-transcript-index.mjs` (whole file, ~80 lines) and its test; PKG-26's entry path `src/cli/buildCorpusTerms.ts`.
**Exact change:** `tools/foraycorpus-export/build-terms.mjs` cloning the warm launcher's shape: `TERMS_ENTRY = "src/cli/buildCorpusTerms.ts"`, `termsArgs(argv)` (copy, order preserved), spawn `node <tsx> <entry>` from `backend/` resolving `tsx` exactly as `warm-transcript-index.mjs` does. Test (3): entry exists and ends in `.ts`; args pass through unchanged; the returned array is a copy. Floor (3).
**Commands:** `npm test --prefix tools/foraycorpus-export -- build-terms.test.mjs` → 3.
**Stop and escalate if:** the warm launcher resolves `tsx` in a way that differs from `start-run.mjs` (copy the warm launcher's; do not invent one).
**Definition of done:** DRAFT PR `feat(corpus-export): corpus-terms launcher (PKG-27)`.

### PKG-28 · Topic-assignment rule + constants — opus · S
**Executor:** opus. Why: product/data judgement (what makes an episode belong to a node; what "general" means; #547).
**Context:** roadmap `:588-610` (G-13: never inherit the show label; `general: true`; fill `terms[]` on all 194 nodes), `data/taxonomy.json` (194 nodes, 2 with `terms`, `apple_anchor`), `data/discover.json` items (`topics[]`, `show`, `apple_collection_id`), `data/item-tags.json`, `data/genre-taxonomy-map.json`, `backend/src/generation/resolveTopic.ts:133-181` (existing constants and `GENERIC_LABEL_WORDS`), `test/data-topic-integrity.test.js:1-25`, issue #547 (read via `gh issue view 547`).
**Exact change:** write `docs/curation/episode-topics-rule.md` deciding, with numbers: node term list = top-K (K=25) terms by summed tf-idf over episodes whose discover item lists the node, minus terms present in > 30 % of nodes and minus `GENERIC_LABEL_WORDS`; seed every node with its label tokens + `apple_anchor` tokens so no node is empty; episode assignment = cosine(episode top-30 vector, node term vector) ≥ τ (choose τ on a 50-episode hand-checked sample, record the sample), at most 3 nodes, parent added when a child is assigned; `general: true` for a SHOW when its episodes spread over ≥ 6 root nodes with none > 40 %; the #547 definition: "an item's claim to a node is show-only when the node is not in the item's episode-topics entry AND the show is `general`". Encode the constants in `tools/foraycorpus-export/topic-constants.mjs` (`K`, `NODE_TERM_MAX_NODE_SHARE`, `ASSIGN_COSINE_MIN`, `MAX_NODES_PER_EPISODE`, `GENERAL_MIN_ROOTS`, `GENERAL_MAX_SHARE`) with a 1-test suite pinning the doc's numbers (floor 1).
**Commands:** `npm test --prefix tools/foraycorpus-export -- topic-constants.test.mjs`.
**Definition of done:** DRAFT PR `docs(curation): episode topic assignment rule (PKG-28)`.

### PKG-29 · `topics.mjs` — qwen · S
**Executor:** qwen.
**Context:** PKG-28 doc + constants, PKG-26 outputs (`episode-terms.jsonl`, `corpus-df.json`), `data/taxonomy.json` node shape (`id, parent, label, terms?, apple_anchor`), `data/discover.json` items (`id, show, apple_collection_id, topics`), `data/catalog.json` (`shows[].show_id`) and `data/catalog-breadth.json` (`shows[].apple_collection_id`), `test/data-topic-integrity.test.js` (extend), `tools/refresh/topics.mjs` (read to avoid duplicating any helper; import if one fits), `tools/segments/sweep-transcripts.mjs:359` (`writeJsonAtomic`).
**Exact change:**
1. `topics.mjs` exporting `buildNodeTerms({taxonomy, discover, episodeTerms, showMap})`, `assignEpisodes({nodeTerms, episodeTerms})`, `markGeneralShows({assignments, discover})`, `writeTaxonomyTerms(taxonomyPath, nodeTerms)` (rewrites only `terms[]`, preserving key order and 2-space formatting; verify with a byte diff that nothing else changed), `writeEpisodeTopics(path, assignments)` → `data/episode-topics.json` `{version:1, built_at, rule:"docs/curation/episode-topics-rule.md", episodes:{"<show_id>|<guid>": {topics:[node…], scores:{node:cos}}}, general_shows:[show_id…]}` sorted keys. CLI `--terms <jsonl> --df <json> [--write]`.
2. Extend `test/data-topic-integrity.test.js` with two tests that, when `data/episode-topics.json` is ABSENT, call `t.skip("data/episode-topics.json not yet published (PKG-29 data PR)")`, and when present assert: (i) every node id in `episodes[*].topics` exists in `data/taxonomy.json` (mutation: add a bogus id to a tmp copy); (ii) every `general_shows` entry is in the set `data/catalog.json shows[].show_id` ∪ `String(apple_collection_id)` for every `data/catalog-breadth.json` row (mutation: add `"nope"` to a tmp copy). This code-only PR does NOT bump `data-topic-integrity`'s floor (skipped tests do not count); the data PR removes the skip branch and bumps the floor by 2.
3. `topics.test.mjs` (5) on a 6-node/4-episode fixture: node terms exclude a term shared by > 30 % of nodes (mutation: drop the filter); an episode whose vector matches node X at cosine ≥ τ is assigned X and X's parent (mutation: drop the parent); an episode below τ gets `[]` — never the show's label (mutation: fall back to the show's topics); `writeTaxonomyTerms` changes only `terms` (byte diff of a tmp copy; mutation: re-serialize the whole file); keys sorted. Floor: topics (5).
**Commands:** `npm test --prefix tools/foraycorpus-export -- topics.test.mjs` → 5; `node --test test/data-topic-integrity.test.js` (two skips reported); then on the generation machine (opus runs it in PKG-30's PR or the data PR): `node tools/foraycorpus-export/topics.mjs --terms data-local/transcripts/episode-terms.jsonl --df data-local/transcripts/corpus-df.json --write`.
**Do not touch:** `backend/`; `data/` (the write happens in the data PR).
**Stop and escalate if:** `data/taxonomy.json`'s formatting is not plain 2-space JSON (the byte-diff test will show it).
**Definition of done:** DRAFT PR `feat(corpus-export): taxonomy node terms and episode topics (PKG-29)` (code only; the data write is a second PR `data(topics): …` after PKG-30 confirms the resolver reads it, which removes the skip branch and bumps `data-topic-integrity` by 2).

### PKG-30 · G-13b resolver from node terms + df; #547 regression — opus · S
**Executor:** opus. Why: `backend/src/`.
**Context:** `backend/src/generation/resolveTopic.ts:220-300` (`NodeTermWeights`, `loadConceptTermWeights`, `nodeTermWeights`, `TERM_MATCH_WEIGHT`), `:335-445` (`scoreTopics`, `resolveTopic`), `backend/test/resolveTopic.test.ts`, `backend/src/generation/topicSupply.ts:233-260`, roadmap `:599-609` (Done-when: run-2 prompt and the 8-word paraphrase resolve to `engineering/ai-robotics` from corpus terms alone), `docs/curation/generation-findings-tracker.md` (F-67 wording — grep "F-67").
**Exact change:** `nodeTermWeights` weights each node term by `TERM_MATCH_WEIGHT × idf(term)` when `data-local/transcripts/corpus-df.json` (or an injected df) is present, else unchanged; label tokens keep their current weights but are capped so that a node with ≥ 10 terms cannot win on label alone (document the rule in the file header). Fixture test: taxonomy stub with the filled `ai-robotics` terms + the run-2 prompt text + F-67's 8-word paraphrase → both resolve to `engineering/ai-robotics` with `options.terms` built from a df stub (mutation: zero the idf). #547 regression in `test/data-topic-integrity.test.js` per PKG-28's definition (same skip-when-absent pattern as PKG-29 until the data PR lands), with a tmp-copy mutation that makes one general show's item claim a node only by show label → red. `BACKEND_FLOORS` `test/resolveTopic.test.ts` +2 (state the before/after numbers in the PR).
**Commands:** `cd backend && npm run typecheck && npm test -- test/resolveTopic.test.ts test/topicSupply.test.ts`; `node --test test/data-topic-integrity.test.js`.
**Definition of done:** DRAFT PR `feat(generation): topic resolver reads corpus terms (PKG-30)`.

### PKG-31 · `catalog-adapter.mjs` — qwen · S
**Executor:** qwen.
**Context:** `data/catalog-breadth.json` row keys (`apple_collection_id, title, feed_url, artwork_url, apple_genre, apple_genre_ids, episode_count, explicit, chart_genre_id, chart_genre_name, chart_rank, in_curated, podcastindex_id, tier, region, harvest_source, harvested_at`), `backend/src/catalog/breadthCatalog.ts:43-90` (`CatalogueShowEntry`; `BreadthShowRaw` needs `apple_collection_id, title, feed_url, artwork_url?, in_curated?, chart_rank?`), roadmap `:642-663` (G-15: `chart_rank` stays a prior from the old harvest), `data/genre-taxonomy-map.json` (Apple genre → nodes; read its shape), PKG-04 rows, Q3 default (exclude blocked/locked).
**Exact change:** `catalogAdapter(showRows, { breadthOld, harvestedAt })` → `{shows:[…], report}`: one row per show with `itunes_id` (rows without one are counted `no_apple_id` and skipped — the api joins on it), `rights` both false, `language` starting `en` or null: `apple_collection_id = itunes_id`, `title`, `feed_url`, `artwork_url = image_url`, `apple_genre = categories[0]?.category ?? pi_categories[0] (capitalised) ?? null`, `apple_genre_ids: []`, `episode_count = episodes_total`, `explicit`, `chart_genre_id/name/chart_rank` copied from the old breadth row with the same `apple_collection_id` else null, `in_curated = foray_show_id is a catalog.json id`, `podcastindex_id = podcastindex_feed_id`, `tier "breadth"`, `region "us"`, `harvest_source "foraycorpus"`, `harvested_at`, plus `timed_transcript_episodes`, `audio_episodes` (new fields, additive). Report: `{rows, skipped_no_apple_id, skipped_rights, new_vs_old, dropped_vs_old, feed_url_changed}`. CLI writes `data-local/corpus-export/catalog-breadth-corpus.json` (minified like the original) and prints the report. Tests (5): a blocked show is skipped and counted; chart fields come from the old row (mutation: default 0); `in_curated` true only for catalog matches; output keys match the old row's key set plus the two additive fields (mutation: rename one); minified output parses. Floor (5).
**Commands:** `npm test --prefix tools/foraycorpus-export -- catalog-adapter.test.mjs` → 5.
**Do not touch:** `data/catalog-breadth.json` (never overwritten here).
**Definition of done:** DRAFT PR `feat(corpus-export): breadth-shaped catalogue from the corpus (PKG-31)`.

### PKG-32 · `publish-release.mjs` for corpus artifacts — qwen · S
**Executor:** qwen.
**Context:** `tools/shows/publish-release.mjs:34-180` (`releaseTagFor`, `releaseExists`, `assetBaseUrlFor`, `publishRelease`, `buildPointer` — import them; `REPO_SLUG` from `tools/shows/config.mjs`), `tools/shows/run-and-publish.mjs:80-130` (the flow: state → tag → release → pointer), `data/shows-index-pointer.json` (pointer shape), `config.mjs` (`RELEASE_TAG_PREFIX "corpus-export-"`, `POINTER_PATH`), `.github/workflows/shows-import.yml:80-130` (how the shows pointer PR is opened — do NOT edit; the corpus run is cron on hermes-vm which opens the PR with `gh pr create`).
**Exact change:** `publish-release.mjs` exporting `corpusTagFor(exportVersion)` (prefix + the same slug rule as `releaseTagFor`), `assetsFor(versionDir)` = `manifest.json`, `shows.jsonl.gz`, `catalog-breadth-corpus.json.gz` (gzip written here), `publishCorpus({versionDir, exec, repo, pointerPath, dryRun})`: skip if the release exists (idempotent), `publishRelease`, then write the pointer `{version:1, export_version, release_tag, asset_base_url, manifest_url, catalogue_url, shows_url, published_at, counts}` via tmp+rename; print the `gh pr create --draft` command it would run for the pointer branch (the cron wrapper runs it; this module never pushes). Tests (5) with a fake `exec`: existing release → no `gh release create` call; assets uploaded are exactly the three; the pointer's URLs derive from the tag; `--dry-run` writes nothing; gzip round-trips. Floor (5).
**Commands:** `npm test --prefix tools/foraycorpus-export -- publish-release.test.mjs` → 5.
**Do not touch:** `.github/`, `tools/shows/` (imports only).
**Stop and escalate if:** `publishRelease` requires shard-batch arguments (read `:119-160`; pass an empty inventory).
**Definition of done:** DRAFT PR `feat(corpus-export): publish catalogue artifacts as a release with a pointer (PKG-32)`.

### PKG-33 · `api/shows/search` + `breadthCatalog` read through the pointer — opus · S
**Executor:** opus. Why: `api/` is unlisted (human merge click); `backend/src/catalog/` is DENIED.
**Context:** `api/shows/search.ts:1-4`, `backend/src/catalog/breadthCatalog.ts:90-101` (`loadBreadthCatalog` reads the two committed files), `api/episodes/showIdMap.ts:139-177` (the pointer-fetch-with-fallback idiom and its cache reset for tests), `api/test/vercel-bundle.test.mjs:1-25` (why every runtime data file must be in `vercel.json includeFiles`), `vercel.json` (`includeFiles` list), `api/test/shows-search-apple.test.mjs` (test shape, `node --import tsx --test`), PKG-32 pointer.
**Exact change:** `breadthCatalog.ts` gains `loadBreadthCatalog({ corpus?: BreadthShowRaw[] })` merging a corpus-derived row set in place of `catalog-breadth.json` rows when supplied; `api/shows/search.ts` tries `data/corpus-catalogue-pointer.json` → fetch `catalogue_url` (gzip) with a 2 s timeout and a process cache like `showIdMap`, falling back to the committed file on any failure (log one line); add the pointer to `vercel.json includeFiles`. Tests: api test with a stubbed fetch returning a 2-row catalogue → search answers from it; fetch failure → committed rows; `vercel-bundle.test.mjs` passes with the new file; `BACKEND_FLOORS` for `breadthCatalog.test.ts` bumped by the tests added. Note in the PR body: "needs Wyatt's merge click (`api/`)".
**Commands:** `npm test --prefix api`; `cd backend && npm test -- test/breadthCatalog.test.ts`; `node tools/ci/run-suites.mjs`.
**Definition of done:** DRAFT PR `feat(api): show search reads the corpus catalogue through a pointer (PKG-33)`.

### PKG-34 · `corpus-freshness.yml` — opus · XS
**Executor:** opus. Why: `.github/` DENIED; `founder-approved` label.
**Context:** `.github/workflows/nightly-watch.yml` (an advisory that opens/updates an issue — copy its issue-upsert step), roadmap `:652-657` (advisory, never a required check), PKG-32 pointer.
**Exact change:** daily cron reading `data/corpus-catalogue-pointer.json`, fetching `manifest_url`, and opening/updating one issue titled `Corpus catalogue is stale` when `built_at` is > 48 h old; `GITHUB_TOKEN` only. Record one run id in the PR body.
**Definition of done:** DRAFT PR `ci(corpus): freshness advisory (PKG-34)` labelled `founder-approved` per the standing approval.

### PKG-35 · #279 drinks-wave candidate list — qwen · S
**Executor:** qwen.
**Context:** issue #279 (`gh issue view 279`) and #278 (`gh issue view 278`) — read for background only; the test strings below are fixed and do not depend on the issues, `data/catalog-breadth.json` (`title, feed_url, apple_collection_id, in_curated, chart_rank, apple_genre`), `data/dai-classification.json` (host prior by apple id), `data/breadth-transcript-yield.json` (`dai_suspected`, `episodes_with_timed_transcript` per breadth show), PKG-04's shows.jsonl if present (language, timed counts), `tools/segments/rank-breadth.mjs` (read its exports; reuse any host/DAI helper).
**Exact change:** `tools/foraycorpus-export/wave-candidates.mjs` exporting `DRINK_WORDS = Object.freeze(["whisky","whiskey","bourbon","wine","winemaking","beer","brewing","cider","distill","distilling","distillery","spirits","cocktail","cocktails","mead","sake","rum","gin","vodka","tequila","mezcal","sommelier","vineyard","drinks"])`, `DRINKS_RE = new RegExp("(^|[^a-z])(" + DRINK_WORDS.join("|") + ")([^a-z]|$)", "i")` (the boundary rule: a word must be delimited by a non-letter or the string edge on both sides — so `gin` does not match `Imagine`, `rum` does not match `Forum`, `mead` does not match `Meadows`), `drinksFilter(title)` = `DRINKS_RE.test(title)`, and `buildWave({breadth, yields, dai, shows?, filter})` → rows `{apple_collection_id, title, feed_url, in_curated, chart_rank, english: bool|null, timed_transcript_episodes, dai_prior, dai_measured, score}` sorted by (`dai_prior false` first, timed episodes desc, then title); output `data-local/corpus-export/wave-drinks.json` + a Markdown table `docs/curation/wave-drinks-candidates-2026-09.md` (top 120, counts: total matched, English, non-DAI). Tests `wave-candidates.test.mjs` (4): (a) positives `"WhiskyCast"`, `"Basic Brewing Radio"`, `"The Wine Show"` match (mutation: drop `brewing` from `DRINK_WORDS`); (b) negatives `"Imagine This"`, `"Forum Daily"`, `"Meadows of Heaven"` do not match (mutation: replace the boundary groups with plain alternation); (c) sort order: a `dai_prior:false` row with fewer timed episodes sorts before a `dai_prior:true` row with more (mutation: sort by timed only); (d) `english` is `null` when no shows.jsonl row exists, never `true` by default (mutation: default true). Floor (4).
**Commands:** `npm test --prefix tools/foraycorpus-export -- wave-candidates.test.mjs` → 4.
**Do not touch:** `data/catalog.json` (PKG-36 applies).
**Definition of done:** DRAFT PR `docs(curation): #279 drinks wave candidates (PKG-35)`.

### PKG-36 · #279 apply the wave — opus · S
**Executor:** opus. Why: curated-tier composition is product judgement; #275 sequencing.
**Context:** #279 ("label, never exclude"; "must wait for #275"), #275 status (`gh issue view 275`), PKG-35 doc, `data/catalog.json` show row shape (`show_id, title, apple_collection_id, feed_url, taxonomy_node_ids, source`), `data/taxonomy.json` food/drink nodes (grep `"food`), `docs/curation/alcohol-forms-coverage-2.md` (the spine to re-score) and the tool it used (grep `coverage` in `tools/foray/` and `docs/curation/alcohol-forms-spine.md` header).
**Exact change:** confirm #275 merged (else stop); add the top English, non-DAI-prior candidates (target 80) to `data/catalog.json` with `taxonomy_node_ids` under the drink nodes, `source: "wave-279"`; run the nightly's classification path locally if it exists for new shows (`tools/classify/` README); re-run the alcohol coverage scoring and append the before/after table to `docs/curation/alcohol-forms-coverage-2.md`; report the non-DAI share.
**Commands:** `node tools/ci/run-suites.mjs`.
**Definition of done:** DRAFT PR `data(catalog): #279 drinks wave (PKG-36)`.

### PKG-37 · #73 candidate lists (`node-wave.mjs`) — qwen · S
**Executor:** qwen.
**Context:** issue #73 (true-crime 40→80–100 with its secondary shape; computing 9→depth; "non-DAI sources are disproportionately valuable"; watch `explicit`), `data/genre-taxonomy-map.json`, `data/breadth-classification.json` (breadth shows → nodes; read shape), `data/catalog-breadth.json`, `data/dai-classification.json`, PKG-35's `wave-candidates.mjs` (import `buildWave`'s row helpers; do NOT edit that file or its test).
**Exact change:** NEW file `tools/foraycorpus-export/node-wave.mjs` exporting `nodeWave({node, breadthClassification, breadth, dai, yields})` for `true-crime` (nodes matching `/true-crime|crime/` plus the persona secondaries `personal-journals|psychology|relationships|paranormal|fiction` as a second table) and `computing`; same row shape as PKG-35 + `explicit`; outputs `docs/curation/wave-73-candidates-2026-09.md` (two tables, ≤ 120 rows each, non-DAI share stated). NEW test file `node-wave.test.mjs` (3): node filter picks only shows classified under the node (mutation: match on title); `explicit` carried (mutation: drop it); sort order as PKG-35. Floor: `node-wave.test.mjs` (3); `wave-candidates.test.mjs` stays 4.
**Commands:** `npm test --prefix tools/foraycorpus-export -- node-wave.test.mjs` → 3; `-- wave-candidates.test.mjs` still 4.
**Do not touch:** `wave-candidates.mjs`, `wave-candidates.test.mjs`, `data/catalog.json`.
**Definition of done:** DRAFT PR `docs(curation): #73 candidate lists (PKG-37)`.

### PKG-38 · #73 select, add, classify — opus · S
**Executor:** opus. Why: product judgement; the classifier is an LLM pass (`tools/classify/`, ADR-0006).
**Context:** #73 acceptance criteria; PKG-37 doc; `tools/classify/` README; `data/personas.json` (the True Crime persona weights).
**Exact change:** pick ≥ 40 true-crime-shaped and a computing set; add to `data/catalog.json`; classify; record the numbers and the non-DAI share on #73; spot-check one persona session per the issue's last criterion and say what was seen.
**Definition of done:** DRAFT PR `data(catalog): #73 sourcing wave (PKG-38)`; comment on #73 with the counts.

### PKG-39 · G-16 HUMAN-ACTIONS cards + STATE.md — opus · XS
**Executor:** opus. Why: routes credential/host decisions to the founders.
**Context:** `HUMAN-ACTIONS.md:1-21` (format `ha-format: 2`, next id after #116), roadmap `:665-676` (G-16), `:1162-1180` (§9 D2/D10 rows), `STATE.md:1-8` (announcement format).
**Exact change:** add HUMAN-ACTIONS #117 (Q1: R2 read key — steps for Joey/Wyatt, "worked if: `node tools/foraycorpus-export/sync-r2.mjs --dry-run` prints objects_seen > 0"), #118 (Q2: confirm hermes-vm + weekly cron + Release publishing; steps), #119 (Q4: probes go-ahead). Add a STATE.md workstream entry "corpus supply package" listing owned paths (`tools/foraycorpus-export/**`, `data/dai-measurements.json`, `data/episode-topics.json`, `data/corpus-catalogue-pointer.json`).
**Commands:** `node --test test/human-actions-integrity.test.js`.
**Definition of done:** DRAFT PR `docs: corpus supply human actions and STATE entry (PKG-39)`.

### PKG-40 · #578 / #831 status comments — opus · XS
**Executor:** opus. Why: reads foray-db and judges what is answered.
**Context:** #578's six asks; #831; foray-db `docs/TRANSCRIPT_INTEGRATION_HANDOFF.md`, `src/forays_corpus/acquisition/r2_registrar.py` docstring (the `show_id` rule, `transcript_publications`), `alembic/versions/0030_transcript_publications.py`, PKG-15's sync doc, PKG-03's SCHEMA.md.
**Exact change:** comment on #578 mapping each ask to the current state (1: bodies in R2 + foray index — done on foray's side by PKG-13/26; 2: PKG-29/30; 3: PKG-19/20 on foray's side, corpus still lacks `declared_byte_length`; 4: exporter rows carry guid+enclosure+itunes id — PKG-05; 5: pending; 6: still open — enclosure/length capture), and ask Joey for the `show_id` rule foray-db will use for publisher/Apple bodies (foray assumes foray `show_id`; the farm uses PI feed id — PKG-12 handles both). Comment on #831 that the label landed (PR #832), the rights question is answered by the 2026-09-25 ruling, and close it.
**Definition of done:** the two comments posted; #831 closed.

## 4. Sequencing

**Wave 1 (parallel, no shared files):** PKG-01 → then in parallel PKG-02, PKG-11, PKG-12, PKG-16, PKG-39. (PKG-02/11/12/16 each create their own modules; they share only `config.mjs`, which PKG-01 finalised, and each adds its own FLOORS line — merge conflicts there are one-line, resolve by keeping both.)

**Wave 2 (parallel):** PKG-04, PKG-05, PKG-07, PKG-09 (all on PKG-02, which now owns `mimes.mjs`, so 04 and 05 are independent of each other); PKG-13 (on 11+12); PKG-17 (on 16; creates `probe-runner.mjs`); PKG-22 (on 16, touches `tools/foray/` only); PKG-03 (opus; needs DB; may correct PKG-02's ASSUMED lists and, if PKG-07 has merged, its `SQL` map — if PKG-03 and PKG-07 are in flight together, PKG-03 posts the corrected lists on PKG-07's PR instead).

**Wave 3:** PKG-06 (on 4+5) → PKG-08 (on 6+7) → PKG-10 (opus, hermes-vm). PKG-19 (on 17 — strictly after 17 merges; it imports `probe-runner.mjs` and never edits it). In parallel: PKG-14 (on 13) → PKG-15 (opus, needs Q1); PKG-18 (opus, on 17 + 10's export output); PKG-20 (opus, on 19).

**Wave 4 (opus, backend):** PKG-21 (after 20's data), PKG-23 (after 13 + 03), PKG-26 (after 15), PKG-24 (any time after 13; optional). Qwen in parallel: PKG-27 (after 26), PKG-31 (after 04), PKG-35 → PKG-37 (37 strictly after 35 merges; 37 creates its own `node-wave.mjs` + test and does not edit 35's files).

**Wave 5:** PKG-28 (opus) → PKG-29 (qwen, code only; its integrity tests skip until the data PR) → PKG-30 (opus) → the data write PR (removes the skips, bumps `data-topic-integrity` +2); PKG-25 (opus, after 26 + 15); PKG-32 (qwen, after 06 + 31) → PKG-33 (opus, human merge) and PKG-34 (opus).

**Wave 6:** PKG-36 (after 35 and #275), PKG-38 (after 37), PKG-40 (after 15).

Files no two parallel tasks touch: each task owns distinct new modules (the review-driven rule: PKG-02 owns `mimes.mjs`; PKG-17 owns `probe-runner.mjs`; PKG-35 owns `wave-candidates.mjs`; PKG-37 owns `node-wave.mjs`); the shared touch points are `test/suite-integrity.test.js` (one line per suite — trivial conflicts), `tools/foraycorpus-export/README.md` (append-only, one paragraph per module), and `package.json` (PKG-01 only). Backend tasks (21, 23, 24, 25, 26, 30) each touch different `backend/src` files: 21 `audioSourceLookup.ts` + `types/tapeSourcing.ts`; 23 `archiveIntegrity.ts` + `cli/generateForays.ts`; 24 `transcriptArchiveLookup.ts` (visibility + new provider) + a one-line selection in `cli/generateForays.ts` (so 23 and 24 must not run concurrently — run 23 first); 25 `transcriptTextIndex.ts`; 26 `cli/buildCorpusTerms.ts`; 30 `resolveTopic.ts`. PKG-33 alone touches `api/` and `vercel.json`.
