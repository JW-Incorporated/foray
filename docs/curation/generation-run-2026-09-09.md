# Generation run 2026-09-09 — three Forays through the production pipeline, driven by agents

*Learning exercise requested by Wyatt: "kick off the machinery as-designed and monitor
process successfulness, robustness, etc., logging errors and things to fix along the
way. The main outputs are learnings and things to improve." Same architecture, same
prompts, same model tiers as production; the only substitution is the transport — each
Anthropic Messages API call is answered by a Claude Code subagent at the matching tier
instead of by the API. Built in series; anything reasonable is fixed between runs.*

## 0. Method, so the numbers can be trusted

- **Pipeline:** `backend/src/generation/runForayPipeline` via the batch driver
  `npm run generate-forays -- --prompts <one prompt> --duration medium` — "~1 hour" is
  the **medium** tier (§3: ~60 min, 3–4 acts, 5–7 slots, 28–36 items, "the proven
  shape"); *long* is "up to ~180 min, unproven at every layer" and was not what was asked.
- **Transport:** `ANTHROPIC_BASE_URL` points the official SDK at a local relay
  (`scratchpad/relay/relay.mjs`). The relay receives each request **unchanged** (model
  name, `max_tokens`, the prompt text the builder composed), parks it, and returns
  whatever the orchestrator writes back, in the Messages API shape. The orchestrator
  dispatches one subagent per request with the exact prompt and the mapped tier:
  `claude-opus-4-1 → opus`, `claude-sonnet-4-5 → sonnet`, `claude-haiku-4-5 → haiku`.
  `parseWithRetry` / `parseLastJsonBlock` then parse the reply exactly as they would a
  real one. Zero pipeline code changed for the transport.
- **KPIs** come from two taps: the relay's `kpi.jsonl` (per call: model, ~tokens in,
  ~tokens out, wall time) and the driver's `report.json` (per Foray: outcome, wall ms
  from `outcome.timings`, per-stage timings via `measureStage`). Token counts are
  estimates (chars/4) because there is no API meter on this path — recorded as such.
- **"Time until the user can start listening"** = the whole pipeline + publish. §6's
  progressive generation ("playback starts before generation finishes") is not built:
  `runForayPipeline` returns one finished candidate. Recorded honestly below.

- **Transport overhead is the orchestrator, not the model.** Call #1 (Haiku clarity
  check): the subagent answered in 6 s; the relay saw 59.6 s wall because a human-paced
  orchestration turn sat between request and answer. Per-call `agent_ms`/`wall_ms` are
  both recorded so the pipeline's own latency can be read separately from this
  transport's. A keyed run would see roughly the agent time.

## 1. Findings before the first Foray (state of the machinery as found)

| # | Finding | Severity | Action |
|---|---|---|---|
| F-01 | **§4.5 tier-2 beat sourcing could never fire.** `NullTranscriptCueProvider` was the only `TranscriptCueProvider`; its own comment calls itself the "production default" and says wiring a file reader is "future work." So on any machine, a beat that matched an archived episode fell through, and the only tape was the 212-row pre-cut pool — while this machine holds **1,346 transcript bodies** (of 1,718 digest entries) in exactly the shape the anchor resolver needs. | High — the plan's tape-sourcing layer was unwired | **Fixed before run 1:** `FileTranscriptCueProvider` (reads `data-local/transcripts/normalized/<show>/<guid>.json`, cached, returns null when absent) and the batch driver passes it. |
| F-02 | `parseWithRetry` **has no retry** — its body says "no retry available in this build." One malformed JSON reply from a model kills the stage and the Foray. | High for robustness | Logged; watch how often it bites. Candidate fix: one re-ask with the schema error appended. |
| F-03 | **Model ids are last-generation:** `claude-opus-4-1`, `claude-sonnet-4-5`, `claude-haiku-4-5`. The Claude 5 family is current. Production would be calling superseded models. | Medium (cost/quality) | Logged; tier mapping used the same *levels*. Recommend pinning current ids in one config. |
| F-04 | `DAILY_BUDGET_USD` defaults to **$2.00**. The spine call alone estimates Opus at several thousand tokens; a medium Foray's estimated spend would trip the guard mid-run. | Medium — a real keyed run would halt | Set `DAILY_BUDGET_USD=1000 EPISODE_BUDGET_USD=1000` for this run; recommend a per-run budget flag on the driver. |
| F-05 | `AnthropicExternalResearcher` uses the server-side `web_search` tool. Fine with a key; on the relay the subagent does the searching itself. Note the researcher is **Haiku-tier with 800 output tokens** — the cheapest stage decides a topic's "genuine controversies." | Low/observe | Logged. |
| F-06 | Tier-2 **episode matching is by title tokens only** (`show_title + title` vs. the beat's claim, threshold). The transcript *text* is never searched, though `data-local/corpus/corpus.db` has an FTS index over 558 chunks. A beat about a specific point inside an episode can only match if the episode's title happens to share words with the claim. | Medium (recall) | Logged; likely the biggest lever on tape yield. |
| F-07 | The SDK's default **timeout is 10 min, 2 retries**. A relay answer slower than that produces an identical retried request; the relay dedupes by body hash. A real key never hits this, but any future async/queued transport will. | Low | Relay handles it. |
| F-09 | `backend` **does not typecheck**: `npx tsc --noEmit -p .` reports pre-existing errors (`import.meta` under the configured module setting in `publishForay.ts`/`finalizeForay.ts`, a non-exported `NarrationBuildContext` type imported by two builders, missing declarations for the `.mjs` checkers). `tsx` tolerates all of it at runtime, so nothing fails until it does. | Medium (hygiene) | Logged. |
| F-10 | Subagents (like models) **ignore "no markdown fences" ~half the time**: call #1 came back fenced. `parseWithRetry` already strips one fence pair, so this is survivable — but it is the same class of fragility F-02 makes fatal for any *other* formatting slip. | Low | Relay-side strip added; logged. |
| F-11 | **The research map leaks off-topic concepts.** The spine prompt for the disasters Foray listed "Ai (semantic-concept, tape: strong, 761 items)" among eight candidate subtopics — the catalogue lookup matches on shared tokens ("engineering") and the Practical AI corpus dominates the archive by volume. Opus is told there is strong tape for AI on a bridge-collapse documentary. | Medium (steers the spine toward what the archive has, not what the prompt asked) | Logged; the lookup needs topic-node filtering or a relevance threshold. |
| F-12 | **No external research call was made before the spine.** The order is understand → research-shape (catalogue lookup) → spine; `AnthropicExternalResearcher` (web search) was not invoked in the first three calls. Either it runs later per subtopic or the stage is conditional — worth confirming against §4.2, which describes research as establishing SHAPE before the spine. | Observe | Track when/if it fires. |
| M-1 | *(orchestrator, not pipeline)* Attempt 1 of run 1 was aborted at call #3: the em dash in call #2's answer was double-encoded by the answer helper (Windows stdin defaulted to cp1252) and the mojibake was already inside the spine prompt. Fixed (stdin forced to UTF-8, verified round-trip), run restarted from call #1 with the identical first two answers. Cost: two Haiku calls, ~3 min. | — | Fixed. |
| P-01 | *(positive)* **Act deepening is parallel.** After the spine, the pipeline issued the three Sonnet deepen-act calls at once (#4/#5/#6 arrived within the same second), each carrying the full spine for context and owning one act. Wall time for this stage is one call, not three. | Good | Keep; note the relay/orchestrator must answer concurrently to preserve it. |
| F-13 | **No confirmation loop after the spine.** §4.3 ("Build the spine") flows straight into §4.4 deepening with no validation of the spine's counts before three more model calls are spent — a spine outside 28–36 beats or below the 30 % exploration floor would only be caught at finalize, after every downstream stage has paid for it. Run 1's spine happened to land at 3 acts / 6 slots / 31 beats. | Medium (cost of late failure) | Logged; a cheap structural check between §4.3 and §4.4 would fail fast. |
| P-02 | *(positive, and the payoff of F-01)* **Tier-2 tape sourcing fired.** The first narration prompt of run 1 says: *"connective narration around real tape (source item causality-engineered-network--47-hyatt-regency-kansas-city)"* — the Hyatt beat was anchored to a *Causality* episode's transcript cues, a segment the 212-row pool never had. Before `FileTranscriptCueProvider`, that beat would have fallen through. | Good | Count tier-1 vs tier-2 vs unresolved beats in the candidate when it lands. |
| F-14 | **The narration writer is asked for verbatim-quoted sources it has no way to look up.** Sonnet, no tools, must return `sources[].quote` + `publication` for every factual claim. A model without search will *reconstruct* quotations. Whether §4.7's verifier stage (also Sonnet, also tool-less) can tell an invented quote from a real one is the open question this run tests. | High (fabricated citations are the failure `narration-craft.md` most fears) | Observe the verifier's verdicts; likely fix: give the writer the cue text it is framing (it already has the segment id) and require quotes to come from the tape or the archive. |
| F-15 | **Narration is fully serial.** `writeNarration` awaits one beat at a time (nested `for` loops), and each beat is a writer call *and* a verifier call. A 31-beat spine is ~60+ sequential Sonnet calls — the dominant wall-clock cost of a Foray, and the reason §6's "listen before generation finishes" is architecturally far off. Slots (and certainly acts) are independent and could be written in parallel the way deepening already is (P-01). | High (time-to-listen) | Logged; parallelise per slot, keep the per-beat verifier. |
| F-12 → resolved | External research fires only for **catalogue-gap** subtopics (`fanOutExternalResearch` over seeds whose tape is weak). Every seed for run 1 reported strong/moderate tape (because of F-11's inflated concept matches), so the web-search stage never ran. The Foray was shaped entirely from what the archive's titles suggested. | Medium (F-11 and F-12 compound) | Logged. |
| F-16 | **The narration retry was uninformed.** `writePageAndVerify` retried a rejected page with the byte-identical prompt. A model deterministic enough to repeat itself fails identically; a temperature-varied one gets a coin flip. Run 1 died this way on its first Frame page. | High | **Fixed before run 1 attempt 3:** the rejection reasons are appended to the writer's context note on the second attempt (prompt otherwise unchanged). |
| F-17 | **One beat's narration failure kills the whole Foray.** After the (failed) retry, `NarrationWriteError` propagated out of the stage; seven calls of spine and deepening were discarded. No fallback (shorter mode, drop the contested source, narrate-less beat), no partial candidate. | High (cost of late failure; compounds F-13) | Logged; design question for §4.7. |
| F-18 | **The batch driver is not resumable within a Foray.** Its resume key is the prompt's candidate *file*, which a failed run never writes, so a re-run repeats every stage from the clarity check. | Medium | Logged; a per-stage checkpoint would make F-17 cheap. |
| F-19 | **Frame mode's 70–170 character budget and §4.7 rule 3 are close to incompatible.** Rule 3 requires a contested claim to *say so in the script*; a Frame page that names a contested source has no room to voice the contestation. The structural validator (`contested-not-flagged-in-text`) then rejects it. Either Frame pages must not carry contested sources (a writer rule) or the budget must flex when a source is contested. | Medium | Logged. The informed retry (F-16) gives the model a chance to resolve it either way. |
| F-20 | Cosmetic: the driver's own error text double-encodes `§` (`Â§4.7`) on this Windows console — the pipeline has its own encoding edge, separate from I-04. | Low | Logged. |
| P-03 | *(positive, F-16's fix observed)* On attempt 3 the pipeline re-asked for the rejected Frame page with the reason appended verbatim — *"YOUR PREVIOUS ATTEMPT AT THIS PAGE WAS REJECTED for: a source is marked contested but the script does not say so explicitly (§4.7 rule 3)"* — and the writer's prompt was otherwise byte-identical. | Good | Whether the second attempt passes is the next data point. |
| F-21 | `test/writeNarration.test.ts` has one failing case on this machine (the disclosure template round-trip through `check-forays.mjs`), matching the documented Vitest defect for checkouts under a path with a space (`runPipeline.ts`'s own comment). Pre-existing; not re-verified beyond the pattern. | Low | Logged. |
| F-22 | **The verifier cannot catch a fabricated quote — by design.** Its prompt says: *"Read it against ONLY its declared sources below."* It checks that the script is consistent with the quotes the *writer supplied*; it has no way to know whether "Engineering News-Record" ever printed that sentence. So the §4.7 "independent verification" guards against internal inconsistency and contested-not-flagged, not against invented sourcing (F-14). The two stages share the blind spot. | High | Logged; verification against the archive's own cue text (for tape-adjacent pages) or a retrieval step is the real fix. |
| F-23 | **Mis-anchored tape (F-06 in action).** Beat 2 of run 1 — the Hyatt hanger-rod design change — was sourced to `causality-engineered-network--22-chernobyl`: a *Chernobyl* episode, matched on shared title tokens. Nothing between `findTranscriptArchiveMatch` and the narration writer checks that the matched episode is *about* the claim; the writer is told only the item id, so it wrote a Frame handing the listener into tape about the wrong disaster. The M4 gates check show concentration, not topical fit. **Running tally in run 1: beat 1 → Hyatt (correct), beat 2 → Chernobyl, beat 3 → Three Mile Island** — every *Causality* episode qualifies on the show-title tokens alone. | **High** (listener-facing wrong tape) | Logged; tier-2 needs a relevance check (cue-text match against the claim, not title tokens) and the writer needs to see the tape's own words. |
| F-24 | **Why F-23 is structural, not bad luck.** (a) `TIER2_MATCH_THRESHOLD = 2`: an episode matches a claim on two shared tokens between the claim and `show_title + title` — and *Causality — Engineered Network*'s own title tokens ("engineered", "network"…) overlap most engineering claims, so nearly every Causality episode "matches" nearly every beat. (b) `resolveAnchorFromCues` then accepts the **first contiguous run of ≥ 4 claim words (>2 letters) found anywhere in the transcript** — generic runs like "the original design required" or "was not proposed as" occur in almost any hour-long transcript. (c) The minted segment spans **only that anchor window** (`startSec` of its first word to `endSec` of its last) — a few seconds of audio, not a segment. Net: tier-2 as built will confidently mint tiny, off-topic "tape" for most beats once bodies are available. Before F-01's fix it never fired, which is why this was invisible. | **High** | Logged; fix = match on cue *text* (FTS/embeddings over `corpus.db`), require topical anchors (distinctive words, not stopword-adjacent runs), and cut segments to sentence/cue boundaries with a minimum duration. |
| F-25 | **Contested-marking is the writer's default, and Frame pages cannot carry it.** Pages 1 and 2 of run 1 both marked sources `contested: true` (the second marked two of four) with no room in 70–170 characters to say so, and both were rejected on rule 3 and retried. If this holds, most Frame pages cost two writer calls — a ~2× multiplier on the largest stage (F-15). Note the prompt asks the model to declare `contested` for each source with no guidance on what "genuinely contested" means; a model hedging on quote fidelity (F-14) has every incentive to say "contested". | High (cost) | Logged; either define "contested" narrowly in the writer prompt, or let Frame pages reference a contested source without voicing it (rule 3 exemption for connective modes). |
| F-26 | The writer emitted HTML entities (`&amp;`) inside `publication` strings. Harmless to JSON parsing; would be spoken/displayed literally downstream. | Low | Logged; normalise entities in `parseWithRetry` or validate publications. |
| F-27 | **Fabricated citation, induced by the mis-anchor.** Retry #11 (Hyatt hanger-rod page) cited all three quotes to *"New Safe Confinement Construction Engineering Interviews"* — the New Safe Confinement is the Chernobyl reactor shelter. The only Chernobyl signal the writer had was the item id `causality-engineered-network--22-chernobyl` in its context note (F-23); it produced Chernobyl-flavoured "sources" for a Kansas City claim, with verbatim-looking quotes that cannot exist. The script says "That account is disputed", so rule 3 passes, and the verifier reads only the declared sources (F-22), so it will pass too — **and it did: call #12 returned `{"verified": true}` in 5 s.** This is the exact failure mode `narration-craft.md` is written to prevent, and nothing in §4.7 can see it.** | **Critical** (published falsehood with a fake citation) | Logged. Fix stack: F-24 (correct tape), give the writer the tape's actual cue text, and verify quotes against the archive/corpus rather than against the writer's own declarations. |
| F-28 | **Claim-text overreach: the writer's `claimText` asserts more than its own `quote` contains, and the script voices the overreach.** Page 13 (box-beam load) scripted "Nobody at either firm drew one [diagram]" and sourced it to a claim field saying "No one at either firm drew that diagram", whose quote only says the connection was not "checked for adequacy". The verifier (call #14, 20 s) caught it — the first rule-based rejection that was actually about facts rather than the contested flag — but only because the overreach was visible *inside* the writer's own declaration. Combined with F-27 this shows the verifier's true scope: it checks internal consistency between quote and claim, never whether the quote exists. Cost: one more writer call (#15) plus one more verify. | Medium (cost; also a signal that the beat purpose itself carries an unsourceable assertion — 'the record shows no one drew it' was written into the beat by the deepen stage, F-13) | Logged. Purpose text from deepen-acts should be marked as *direction*, not as *fact the page may assert*; the writer prompt should say the beat purpose is not a source. |
| F-29 | **Tier 1 has the same disease as tier 2, plus a fallback that guarantees garbage.** Beat 4 (Kansas City code-load) was anchored to `bfh-griddle-bakestone` — a British food-history segment about hearth cooking. Replicating `scoreSegmentsAgainstClaim` on the beat's claim: the *right* segment (Causality #47 Hyatt Regency) scored 5 (`call, load, one, phone, walkway`) but was excluded by the M3/M4 filters (already used by beat 1); the next candidates were a plate-tectonics segment at 4 (`before, have, likely, would`) and the griddle at 4 (`have, one, people, would`), and 29 of 212 pool segments cleared `TIER1_MATCH_THRESHOLD = 2`. Three causes: (a) the 27-word stopword list in `catalogueLookup.ts` treats `have`, `would`, `one`, `before`, `people`, `had`, `found`, `likely` as signal; (b) no topic gate — the pool carries a `topic` field (`food/food-history`) and the Foray has a resolved taxonomy node, and the scorer never compares them; (c) when the best segment is filtered out, `findTier1Match` falls through to the next candidate over the threshold instead of returning null, so exhaustion of the one relevant episode *guarantees* a cross-domain hit. Net for this Foray: after 4 beats, 3 of 4 tape anchors point at tape unrelated to the beat (Chernobyl, Three Mile Island, griddles). | **Critical** (the product is tape + narration; the tape is wrong) | Logged. Fix before run 2: require topic-prefix agreement (or a shared concept) before counting tokens; raise threshold to 3 and drop function words; on filter-exhaustion return null so §4.5 falls to tier 2 / unresolved rather than to the next-best stranger. |
| F-30 | **The writer treats the tape item id in its context note as a citable publication.** Retry #19 (beat 4) kept a real NBS figure ("about 60 percent of the minimum required") but set `publication: "bfh-griddle-bakestone"` — the segment id from "connective narration around real tape (source item …)". Same root as F-27 (the only concrete noun the writer is given is the item id, so it becomes the citation) but a cleaner demonstration: the quote is genuine and the attribution is a podcast about griddles. Any downstream surface that shows sources will print a slug as a publication. | High | Logged. Fold into the F-27 fix: give the writer the tape's real show/episode title *and* the cue text, and state in the prompt that tape is tape, not a print source; validate `publication` against a slug pattern in `writeNarration`. |
| F-31 | **Under the as-designed retry policy a medium Foray is statistically unreachable.** Attempt 3 wrote 5 pages before dying. First-attempt outcomes: 5 of 5 rejected (3 on rule 3 / contested, 1 on claim overreach, 1 on a number the *beat purpose itself* mis-stated). Second-attempt outcomes: 4 passed, 1 rejected — and `writePageAndVerify` allows exactly 2 attempts, after which `NarrationWriteError` propagates and the batch driver records the whole Foray as `error` (F-17). With a ~20 % second-attempt failure rate per page and ~31 pages, the chance of finishing is (0.8)^31 ≈ 0.1 %. Three attempts at run 1 have now failed at beats 1, 1 and 5 for this reason; nothing about the fourth would differ. | **Critical** (blocks any Foray of the target length) | Fix before attempt 4: 3 attempts; a tape beat whose *connective* page still fails keeps its tape and drops the page (the design's own rule that "a beat's existence never depends on" one stage); define *contested* and *purpose ≠ source* in the writer prompt so first attempts stop failing. |
| F-32 | **Attribution is unstable across attempts — the same quote span moves between publications.** Attempt 4, page 1: the span "killing 114 people and injuring 216 others as they attended a tea dance in the hotel's atrium" was attributed to *Wikipedia* on the first attempt and to *Encyclopædia Britannica* on the informed retry, verbatim. A quote that survives a change of publication is a quote from the writer's memory, not from a document; the publication field is a plausibility label. Nothing in §4.7 checks that a (quote, publication) pair is stable, let alone real. Also seen: with the new *contested* definition in force, the writer reaches for tertiary sources (Wikipedia twice on the first attempt) — there is no source-quality rule in the prompt at all. | High | Logged. The real fix is F-27's: verify quotes against text the pipeline actually holds (transcript cues, corpus, fetched pages), not against the writer's own declaration. Cheap partial: forbid tertiary encyclopaedias as the *only* source for a claim, and make the verifier flag a publication that names no author, title or year. |
| F-33 | **The I-13 sourcing fix was necessary but not sufficient: two shared *content* words are still noise inside one domain.** Attempt 4 anchored beat 2 (hanger-rod fabrication) to Chernobyl again. Replay with the patched scorers: Hyatt #47 scores 3 but is already used by beat 1 (the pool holds one Hyatt segment), so the ranked walk falls through to Chernobyl and Fukushima at 2 — on words like `design`, `problem`, `change`. Tier 2 would have done no better: its best was *Being an Engineer*, "PCB Design, Fabrication & Manufacturing" at 2 (`design`, `fabrication`). Inside a single topic every episode shares the trade's vocabulary, so a two-word bar cannot separate "about this event" from "about engineering". Thresholds raised to 3 for both tiers (a beat with no 3-word match gets narration, which is the design's own guardrail — no tape beats wrong tape). Applies from run 2; attempt 4's sourcing had already run. Deeper fix remains: score against the segment/episode *transcript text*, or against the catalogue's semantic concepts, not against a 20-word why-line or a title. | **Critical** (product-visible: wrong tape) | Threshold 3 applied (I-16). |
| F-34 | **A page with zero sources passes structural validation and goes to the verifier with an empty source list.** Attempt 4, beat 2: told that the purpose is direction not a source (I-13), the writer wrote a question-only Frame — "Before you hear him explain it: was this a shortcut, or the only way…" — with `sources: []`. `validateNarratedBeat` has no minimum-source rule, so the page went to verification as-is. Whether that is a defect is an editorial call: a Frame that only asks a question makes no factual claim, and narration-craft.md's S1/S2 hand-offs are often exactly that. But (a) "him" presupposes who the tape speaker is, and the tape here is the Chernobyl episode (F-33), so the page is wrong in a way no source check can see; (b) a writer that learns "no claims, no rejections" will drift toward content-free pages. Watch the rate. | Medium | Logged. Consider: allow source-less pages only when the script contains no declarative sentence; give the writer the tape's real speaker/episode so pronouns like "him" are grounded. |
| F-35 | **The informed retry carries only the latest rejection, not the history.** `retryNote` is overwritten on each failure, so attempt 3 of beat 2 is told about attempt 2's rejection only; it cannot see that attempt 1 failed for the same reason, or that an earlier attempt failed for a *different* reason it may now be reintroducing (attempt 3 pages regularly fix the last complaint and revive the first). Cheap fix: accumulate notes ("attempt 1 rejected for …; attempt 2 rejected for …"). | Medium (cost) | Logged; accumulate. |
| F-36 | **My I-13 prompt line traded one failure class for another.** "The purpose above is editorial direction, not a source… narrow the script to what can be" was meant to stop the writer voicing the beat's own wording as fact (F-28). Under it the writer's dominant first-attempt output for tape beats became a *source-less* Frame — a question or a tease with `sources: []` (beat 2 ×3, beat 3 ×1; four in a row) — which the verifier rejects every time as unsupported. Before the line: 0/5 first attempts passed, mostly on the contested flag. After: 0/3 so far, now on empty sources. Net page cost unchanged, and beat 2 lost its page entirely. The writer is obeying the letter of "every factual claim must carry a source" by making none, and the verifier reads even "here's the moment before the fix" as a claim. The prompt now says two things that pull apart: *narrow to what you can source* and *every claim needs a source*, with no statement that a Frame must still carry at least one sourced fact. | High (self-inflicted; must be fixed before run 2) | Fix: replace the line with a positive rule — "A Frame states at least one sourced fact from the purpose and may add one question; never zero sources" — (applied to the prompt for run 2). A structural rule was tried and withdrawn: the validator already rejects source-less Patch/Carry pages, and a Frame that only asks a question is legitimately source-free (F-37). |
| F-37 | **The design disagrees with itself about whether a connective page needs sources.** `StubNarrationWriterBuilder` encodes the intent explicitly — `needsSource = mode === "Patch" || mode === "Carry"`, i.e. Frame/Hinge/Marker pages are source-free by design — while the writer prompt says "every factual claim in the script must carry a source" and the verifier fails "any factual claim [with] no supporting source". A Frame that says anything about the world therefore needs a source in practice, and the stub's zero-source Frames would fail the real verifier. Found when the F-36 guard broke three dry-run tests. `validateNarratedBeat` already enforces "at least one source" for Patch/Carry (§4.7 rule 1) and is silent for connective modes — so the code's position is: content pages need sources, connective pages are free, and the verifier then contradicts that for any connective page that says anything. Resolution taken: no new structural rule (the existing one is right for content pages); the writer prompt now says a connective page may have zero sources only if it asserts no fact at all. The tension should be settled in narration-craft.md. | Medium | Logged; decide in the design doc whether "source-free Frame" means "no claims" (then the verifier is right) or "claims about the tape" (then the verifier needs the tape). |
| F-38 | **Analytical beats get tape too.** Beat 6 is a thesis, not an event — "every link in a failure chain gets evaluated against a local question… and almost never against the global question" — yet §4.5 ran the same token scorer over it and anchored it to a *Geology Bites* episode on banded iron formations. Nothing in the deepen output marks a beat as *argument* vs *account*, so sourcing cannot know that no tape could carry it; a spoken argument like this is exactly what narration is for. Under the I-16 threshold it would have gone to narration (score 2 → below 3) — by accident, not by design. | Medium | Logged. Have the deepen stage tag beats `kind: "account" | "argument"` (or reuse `exploration`) and skip tape lookup for arguments. |
| F-39 | **A malformed model reply costs a whole page attempt.** Verifier call #34 (attempt 4) returned a JSON object missing its closing brace. `parseWithRetry` — despite its name — has no repair and no re-ask ("no retry available in this build"), so the parse error surfaced as the page's failure reason, the writer was told its page was rejected for `LLM output failed schema validation`, and one of the beat's three attempts was spent on a transport fault. A real API call can return the same thing. | Medium (cost, and a misleading retry note) | Cheap: try a brace-balance repair, then one re-ask of the *same* call with "your previous reply was not valid JSON" before failing the attempt; never charge a page attempt for a parse failure. |
| F-40 | **The shared parser's anti-drift promise had already lapsed.** `parseWithRetry.ts` says it was extracted so the five Anthropic builders "can no longer silently drift" — but three builders written since (writer, verifier, continuity) each carry a fresh private copy. Fixing F-39 in the shared file would have fixed nothing in the narration stage. The three copies now delegate to the shared function. | Low (maintenance) | Fixed; consider an eslint rule or a test that greps for `function parseWithRetry` outside the shared module. |
| F-41 | **Nothing checks that a page does what its beat is for.** Beat 7 (the first Patch, 340–765 chars) was meant to introduce the deepen stage's named concept, "the assumption of continued verification". The writer — told to narrow to what it can source (I-13) — dropped the concept entirely and re-told the Hyatt collapse from the top: date, tea dance, phone call, doubled load, all already covered by beats 1–5. Four sources, plausibly verifiable, and a page that both repeats the Foray and abandons its own purpose. The verifier checks claims against sources; the structural validator checks length and banned words; no stage asks "does this page accomplish the purpose it was given?" or "does it repeat an earlier page?" | High (listener-visible: repetition, and the act's argument never lands) | Give the verifier the beat purpose and a second question — *does the script accomplish it?* — and give the writer the previous pages' scripts (or a one-line summary each) so it knows what has been said. The stitch/continuity stage may catch some repetition; it runs after every page is paid for. |
| F-42 | **Quote spans degenerate to single words.** Beat 12's three sources quote `"fish screen"`, `"debris"` and `"spillway"` from McCullough and a *Pennsylvania History* paper — one- and two-word spans that any text on the subject contains, so they "support" anything. The structural validator's only quote rule is that claim and quote share a significant word, which a one-word quote satisfies trivially; the verifier accepted three-word spans in beat 5 and five-word spans in beat 11. As the run went on, spans got shorter: the writer has learned that a short quote is never contradicted. This is the writer gaming the verifier, and it makes the grounded-quote metric (plan §veracity) essential — a real quote can be looked up; a one-word one cannot. | High | Minimum quote span (≥ 8 words, or a full clause) in `validateNarratedBeat`; verifier told to reject spans too short to be checkable. |
| F-43 | **Rule 3's detector is a keyword list, and it rejected a page that did say the point was disputed.** Beat 12 scripted "How much it choked the one channel… is something historians still argue over" with the matching source marked contested — exactly what the rule asks for — and `containsContestedLanguage` returned false because "argue over" is not in its vocabulary. The retry said "Historians still dispute" — also not on the list (it has `disputed`, not `dispute`) — and was rejected again, putting a narration beat on its last attempt over a string match. Same shape as F-25's cost, opposite cause: there the flag was wrong, here the detector. | **Critical** (a correct page can end the run) | Broaden the list (argue, argued, debate, dispute, disagree, unsettled, unresolved, open question, no consensus, contested) or, better, let the verifier judge rule 3 with the source list in hand — it already sees both. |
| F-44 | **The verifier is inconsistent on source-less pages.** Calls #12, #14, #16, #18, #20, #22, #32, #34, #36 and #60 all rejected zero-source Frames as unsupported, including pure questions ("was this a shortcut, or the only way…?"). Call #62 passed one ("What if the storm a dam was built for isn't the storm its river has now? Listen for how that gap opens…") with no note. Same prompt, same model, same shape of page — a coin with a 1-in-11 heads. Whichever side the design chooses (F-37), the verifier prompt has to say it; today the outcome depends on sampling. | Medium | State in the verifier prompt whether a claim-free hand-off with no sources passes; ideally decide it structurally so the verifier never sees the case. |
| F-45 | **"Narrow to what you can source" can produce a false statement about the record.** Beat 18 attempt 1 said the staged load sat "right over" the under-designed gussets; the verifier rejected the spatial link as unsourced (correct, given the declared quote). Attempt 2 replaced it with "Exactly where, the record doesn't say" — which is untrue: the NTSB report locates the staged material over the U10 nodes. The retry turned an unsourced true claim into a sourced-looking false one, and the verifier cannot tell, because it only checks declared quotes. This is the failure mode a *veracity* metric must catch and the closed-world verifier structurally cannot (F-22, F-27, F-32). | High (published falsehood, narrator voice) | The grounded-quote metric plus a *negative-claim* rule: a script may not assert what the record does or does not contain unless a source says so. |
| F-46 | **The writer quoted the beat purpose back as a source.** Beat 23, attempt 3: `quote: "welding crews reinforced the tower's joints at night for three months in 1978"` — the deepen stage's own sentence, word for word — attributed to *Engineering News-Record*. The prompt hands the writer one paragraph of text and demands verbatim quotes; on its last attempt, with the purpose unsourceable from memory, the writer quoted the only text it had. Nothing checks that a quote is not a substring of the prompt. | **Critical** (a fabricated citation with a real publisher's name, on a content page) | Structural: reject any quote that overlaps the beat purpose or the prompt (trivial substring check); and the retrieval-first design, which gives the writer real text to quote instead. |
| F-47 | **The model upgrade (F-03) changes what a call costs and whether it fits.** On `claude-opus-5` and `claude-sonnet-5` adaptive thinking is ON by default and its tokens are billed as output and counted against `max_tokens`; no builder sets `thinking` or `output_config.effort`, and the caps are tight — verifier 1,000, continuity 500, understander 400/600, researcher 800. A keyed run on the new ids can therefore truncate a JSON reply inside the thinking budget (F-39's failure, now systematic) and the `BudgetGuard` estimate in `env.ts` undercounts by the thinking tokens. Found while verifying WS-F's ids against the current API reference. | High (keyed runs only; the relay transport does not exercise it) | Set `output_config: { effort: "low" }` on the per-page calls (writer, verifier, continuity, understander) and leave the spine at default effort; raise the small caps by a thinking allowance; make the guard's estimate include it. Card for a WS-F follow-up. |
| F-48 | **WS-A's print evidence is the retrieval model's transcription, not fetched bytes.** `AnthropicExternalResearcher.retrievePassages` runs a web-search call and asks the model to return `{title, url, text}` passages as JSON; the pipeline then holds *that* text as the document and checks the writer's quotes against it. The substring gate therefore proves a quote is consistent with what the retrieval stage wrote, not with the page at the url — one model copying a search result faithfully is likely but is not verified, so `groundedQuoteRate = 1.0` overstates what has been checked. Also still on the basic `web_search_20250305` tool type; the Claude 5 ids support `web_search_20260209`/`web_fetch_20260209`. | Medium (the remaining gap in the veracity chain) | Follow-up card: retrieve with `web_fetch` + `citations: {enabled: true}` and hold `cited_text` spans (server-attested page content) as the document, or store the search tool's own result blocks rather than the model's restatement; move to the `_20260209` tool types with the model upgrade (F-47). |
| F-49 | **Run 2, first exercise of the merged pipeline: sourcing returned ZERO tape beats for 35 beats.** Two causes, both measured from the checkpoint file. (a) The deepen stage (WS-C's new `kind` field) tagged **29 of 35 beats `argument`**, and §4.5 skips tape lookup for arguments by design (F-38's fix) — on an angle-driven spine ("the unglamorous reality of production ML is engineering discipline, not research") nearly every beat reads as a thesis, so the prompt's one-line definition of *account* vs *argument* tips the whole Foray to narration. (b) The **6 `account` beats found no tape either**: "No tape found anywhere in the §4.5 search order" — the 212-row pool has no AI segments, and tier 2 (transcript text ≥3 content words inside the anchored window, lineage gate) found nothing across the 63 *Practical AI* bodies (the run doc first said 337 — that is *Being an Engineer*'s count; corrected 2026-09-09), while the research map itself reported *Ai: 761 items, Machine Learning: 40 items* as strong tape. Net: the pipeline will publish an all-narration Foray on the one subject the archive is richest in. | **Critical** (a Foray plays the tape; run 1 had wrong tape, run 2 has none) | Deepen prompt: define `argument` narrowly (a claim about what something *means* that no recording could carry) and default to `account`; cap arguments per slot; sourcing should log per beat WHY tier 1/tier 2 rejected (best candidate, score, which gate) so the threshold/gate can be tuned against real data rather than guessed; a Foray with < N tape beats should be a validation outcome, not a silent success. |
| I-21 | *(transport)* **Haiku subagents do not perform the delivery step.** Calls #1 and #2 (Haiku tier) returned the correct JSON but made zero tool calls, so `res-N.json` was never written and the orchestrator delivered by hand (call #2 also came back fenced, F-10). From call #4 on the wrapper makes the subagent *start* with a tool call (`python mk.py N` prints its own instructions), which every Sonnet/Opus agent has followed. | Harness | Orchestrator copies Haiku replies into the relay; not a pipeline deviation — the reply text is unchanged. |
| I-22 | *(transport)* Evidence-retrieval calls (§WS-A, Haiku with the server-side `web_search` tool) are answered by subagents that are ALLOWED WebSearch/WebFetch, capped at the request's `max_uses`, told to copy passages character-for-character and never invent a url. The relay now records `tools` on each request so this is decided per call, not globally. | By design of the exercise | Closest available stand-in for the server tool; passages are the subagent's copy of fetched pages (same limitation as F-48). |
| F-50 | **When the evidence contradicts the beat's purpose, the page has no honest move — and the pipeline kills the Foray for it.** Run 2, act 1, page p2: the deepen stage's purpose says *"the feature store exists as a product category for one reason: those two code paths drift apart silently…"*; WS-A's retrieval returned, among others, *Why Feature Stores Didn't Fix Training-Serving Skew* ("feature stores manage data artifacts; they do not control execution"). Attempt 1 asserted the purpose → rejected: unsupported by its quotes AND contested by its own source (rule 3). Attempts 2–3 narrowed to what the documents support and never mentioned feature stores → rejected: *"the concept the purpose names is dropped"* (`purposeAccomplished: false`). Every verdict was correct; the page that would have passed — *the feature store was sold as the fix, and the people who built them say it isn't* — is exactly the one neither prompt permits: the writer is told the purpose is direction, not a source, and the verifier is told to judge whether the purpose was accomplished. Nothing lets a page correct its purpose from the evidence, which is the single most valuable thing evidence-first narration could do. | **Critical** (the failure that ended run 2; and the same shape will recur on any beat the deepen stage got wrong, which is many — F-28, F-45) | Claim-selection/prose prompts: *if the documents contradict or complicate the purpose, say so — a page that reports the tension accomplishes the purpose*; verifier: `purposeAccomplished` = the page addresses the purpose's subject with the evidence available, including by contradicting it; record a `purposeRevised: true` flag on such pages for the editor. |
| F-51 | **A narration beat's third rejection still fails the whole Foray** — WS-A kept F-17's policy for content pages ("narration beats fail hard"), so run 2 died at act 1 with 10 of 12 pages verified, 34 calls spent, and (unlike run 1) a veracity gate downstream that exists precisely to judge a flawed candidate. With the gate in place, throwing is the wrong policy: keep the last attempt with `verified: false` and the verifier's objection in `attempts`/`verifierNotes`, count it as `unverifiedPages` in `meta.veracity`, and let `publishForay`'s gate refuse (or `--force` with a note). The per-act checkpoint (WS-F) does not help here — `narrate:0` is only written when the whole act succeeds, so a re-run repeats all 12 pages. | **Critical** | Never throw for a page; gate on the metric. Also checkpoint per SLOT inside `narrate:<act>` so a re-run pays only for the slot that failed. |
| F-52 | *(from the requirements-doc audit, §8.1)* **Tape beats never get their transcript text.** `writeNarration.ts` builds its evidence gatherer with `createEvidenceGatherer()` and no options, so `DefaultEvidenceGatherer` falls back to `NullTranscriptCueProvider`; a tape beat's pack holds the show/episode titles but not the ±90 s cue window WS-A specified. Verified in code. | High (invisible in run 2 only because there was no tape) | Thread `deps.cueProvider` into the narrate stage (in the F-50/F-51 PR). |
| F-53 | *(§8.2)* **Tier-2 anchors are unpublishable.** `sourceBeats` returns `newSegments` (minted from transcript cues) and `transcriptionQueueCandidates`; nothing consumes either beyond the checkpoint schema. A tier-2 beat references a `segment_id` absent from `data/segments.json`, and `check-forays.mjs` scores an unknown segment at 0 s — so any Foray that uses tier-2 tape fails finalize. Verified in code. | **Critical** the moment F-49's fix yields tape | Carry minted segments on the candidate through finalize and publish (in the F-49 PR). |
| F-54 | *(§8.10)* **The per-Foray budget cap is inert in the batch path.** `EPISODE_BUDGET_USD` is enforced only when a `sessionId` reaches the guard, and `generateForays.ts` never passes one. Verified in code. | Medium | Pass the checkpoint key as `sessionId` (in the F-50/F-51 PR). |
| F-55 | *(§8.6)* `slotsFromSpine` de-duplicates slot ids while `toForayItem` re-slugifies without de-duplication, so duplicate slot titles collapse and can trip the contiguity rule at finalize. | Low | Card. |
| F-56 | *(§8.7)* The minted title is capped at 120 characters but `check-forays` rejects titles over 18 words; the mismatch surfaces only at finalize, after every stage has been paid for. | Low | Cap by words at mint time. |
| F-57 | *(§8.11)* The ±15 % runtime tolerance in `generation-architecture.md` is not implemented anywhere; only act/slot/beat counts are bounded. | Medium (a medium Foray can finalize far from ~60 min) | Card: enforce at spine or stitch. |
| F-58 | *(§5.2)* `check-narration.mjs` structurally cannot validate generated narration — the pipeline writes no curation artifacts, so its digit / reference-leak / sentence rules never apply to generated pages; only `validateNarratedBeat`'s rules do. | Medium (a whole checker believed to be in the path is not) | Card: run the same rules over `NarratedBeat.script` in `finalizeForay`. |
| F-59 | **`resolveTopic` resolved run 2's AI/ML prompt to `engineering/energy-fusion`**, so WS-C's lineage gate refused every AI-adjacent show as off-topic (found by the F-49 offline replay, #552). This is the same magnet label behind founder feedback F19 / #547 (CBC Ideas, Lex Fridman and Catalyst all carry `energy-fusion` as their only node): whatever vocabulary that node advertises in `data/taxonomy.json` is broad enough to win on generic technology text, in show classification AND in topic resolution. Not the binding constraint in run 2 (title matching failed first, F-49/F-06) but it will be the moment tier 2 reaches transcript text. | High | Inspect the `energy-fusion` node's terms/aliases in the taxonomy and the resolver's scoring; add a regression test that the run-2 topic text resolves under `engineering/ai-robotics` (or `technology/*`), and that `energy-fusion` needs a fusion-specific term to win. Pair with #547's fix. |
| F-49 → resolved (cause) | The F-49 offline replay (#552) settles the second half: *Practical AI* IS in the digest with 63 of 63 bodies present, and tier 2 never reached the anchored-window test for any of the 23 searching beats — the title-token bar (`TIER2_MATCH_THRESHOLD = 3` content words in `show_title + title`) stops it first; the best title scores 1. Lowering the bar re-admits run 1's wrong-anchor class (pinned by tests). So **F-06 is the binding constraint**: tier 2 must match claims against transcript TEXT (the `data-local/corpus/corpus.db` FTS index or an embedding over cue windows), not titles. | **Critical** for tape yield | New workstream WS-H (below the fix plan's WS-F): text-level tier-2 candidate search, with the same anchored-window and lineage gates after it. |
| I-23 | *(transport)* **Attempt 2's first call deadlocked the relay.** The relay's SDK-retry dedupe keys on the request body and lives in memory; attempt 2's call #1 is byte-identical to attempt 1's, its `res-1.json` had been cleared with the queue, so the relay treated it as an in-flight retry of a slot no request file described and waited on a file nothing would write. Killing the relay to clear the map dropped the pipeline's open request — the SDK's two retries hit the dead port and the driver recorded `Connection error` (the checkpoint had nothing yet). Relaunched with a clean relay; `/reset` added so the map is cleared with the queue next time. Cost: ~15 min, no model calls. | Harness | A production transport would key idempotency on the SDK's own request id, not the body. |
| F-60 | **A page with no evidence still costs three model calls.** Run 2 attempt 2, act 1 page p5 ("mature pipelines treat a dataset the way a build system treats source code…"): retrieval returned `passages: []`, and `writeSlot` then made three claim-selection calls for a page whose prompt says *"Documents: none were retrieved for this page"* — each rejected by the mechanical rule (a Carry page cannot be written unsourced) before any model could have helped. The retry note even tells the model to "fix" a rejection it has no way to fix. | Medium (cost; and the case then falls to F-51's remaining fatal branch — no page at all) | When a Carry page's evidence pack is empty: (1) re-run retrieval once with a rephrased query (the beat purpose's key noun phrase, not the full claim); (2) if still empty, do not call the writer — mark the page `unverified` with reason `no-evidence` and continue (F-51 policy), or degrade the beat to a connective page; never spend a selection call on zero documents. |
| F-61 | **The verbatim-anchor rule is the next wall after WS-H.** With the topic corrected and text-level tier 2 (#553), all 23 searching beats of the run-2 fixture reach real *Practical AI* transcripts (BM25 13–21, `foundBy: text-index`) and every one stops at `tier2:no-anchor`: `resolveAnchorFromCues` requires a run of ≥ 4 of the CLAIM's own words spoken verbatim in the tape. Claims are written prose; tape is speech — across 63 bodies exactly one claim has such a run, in an episode ranked 53rd, and the window test correctly refuses it. The rule conflates two things: *which window carries the beat* (a relevance judgement, now answered by BM25 + the anchored-window overlap) and *which spoken phrases mark the window's edges* for ADR-0007's drift-tolerant anchoring (which must be verbatim tape words — but the tape's, not the claim's). | **Critical** for tape yield (0 of 35 on the richest subject in the archive) | Pick the window by overlap; then mint `startAnchor`/`endAnchor` from the first/last distinctive phrases INSIDE that window's cue text; keep the ≥ 3 content-word overlap and lineage gates; add a relevance floor so a weak window is refused. Regression: run 1's Chernobyl/griddle/San Bruno anchors stay refused. |
| F-62 | *(from #553's one successful mint)* The cut-to-cue-boundaries step grows a short window symmetrically to reach `MIN_TAPE_SEGMENT_SEC` (45 s); this archive's cues average ~28 s, so the padding pulled in a leading cue that is off-claim — ~28 s of unrelated tape before the relevant passage. | Medium (listener-visible once tape flows) | Grow by overlap (prefer the side whose next cue shares claim terms), never symmetrically; allow a shorter segment over an off-claim lead-in. |
| I-24 | *(environment incident)* **The machine-local transcript archive was emptied mid-session.** `data-local/transcripts/normalized/` (1,713 bodies) had its contents deleted at 2026-09-09 18:46:26Z (directory mtime), 62 s after the text index wrote its first cache file at 18:45:24Z; `raw/` (876 srt + 837 vtt) and `index/` were untouched. Found by the F-61 agent when the two WS-H offline tests failed; every generation run after that time would have sourced zero tape from an archive that looked present. No code on the branch deletes under that path (`rmSync` audit: only temp dirs and the checkpoint file); the process that did it is not identified. Regenerated at 20:40Z from `raw/` with `tools/segments/transcript-normalize.mjs` (587 via `selectTargets`, 1,126 by filename; 0 failures; byte-shape identical to `fetch-transcripts.mjs`'s writer, with `regenerated_from` on the second set). | High (silent zero-tape) | Guard: the cue provider must report *how many* bodies it can read at run start and the driver must refuse to run sourcing against an archive whose body count fell below the digest's (an `archive:empty` trace + a hard stop); keep the raw bodies as the source of truth and make regeneration a documented one-liner. Forensics: add an audit log line to every `data-local/` writer. |
| I-25 | *(harness)* **The session's web-search budget ran out and every evidence retrieval after that returned empty.** The retrieval stand-in (Haiku subagents with WebSearch, I-22) shares this Claude Code session's cap of 200 WebSearch calls. The cap was reached during attempt 5's act 2; from then on every retrieval subagent searched, got the budget refusal, and honestly returned `passages: []` — 8 of 11 act-2 first queries in attempt 5 and all 11 act-1 queries in attempt 6. Found by running one search from the orchestrator: *"this session has used its web search budget (200 of 200 WebSearch calls)"*. Attempt 6 paused after the source stage with its checkpoint intact (understand, research-shape, spine, deepen, source banked; narration not started). | Harness | The empties from attempt 5's act 2 onward are NOT evidence for F-69; attempt 5's act-1 empties (7 of 11, before the cap) stand. Next session: raise `CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION` (a run needs ≈ 45 retrievals plus retries, so ≥ 150 per attempt) and resume attempt 6 from `run/out-2`. A production run uses the server-side `web_search` tool and has no such cap. |
| F-63 | **The spine is written without reading the tape, so its beats claim things the archive never says.** Run 2 attempt 3, with WS-H, F-59 and F-61 all merged and the archive restored: every one of the 29 `account` beats reached real *Practical AI* transcripts by text and was refused by the relevance floor (`tier2:window-overlap`, best-window shares 0.08–0.20 against 0.35); the F-61 replay counted zero occurrences of `imagenet`, `hidden technical debt`, `garbage in`, `feature store`, `concept drift` across all 63 bodies. The matcher is now right to refuse. The cause is upstream: §4.2's research map hands the spine *concept labels and item counts* ("Ai: 761 items, tape: strong"), never a line of what those items say, so Opus writes a spine from its own knowledge of the subject and the tape is asked to illustrate claims it was never consulted on. Three attempts, three matchers, zero tape — and the archive holds 63 episodes on the subject. | **Critical** (the product premise: a Foray plays the tape) | Tape-first spine: research-shape returns, per candidate subtopic, the 3–5 highest-BM25 transcript windows (episode, 60–120 s, the actual sentences) so the spine prompt can build `account` beats *around* what the archive says (with a rule: every act carries ≥ N beats seeded from a listed window); keep the current path for subjects with weak tape. Card WS-L. |
| F-64 | **The record's `summary` is the understander's restatement of the prompt, and nothing bounds it.** Run 2 attempt 3 reached finalize after 76 minutes and `check-forays.mjs` refused the Foray for a `summary` of 26 words (limit 18): `runPipeline` sets `summary: intent.subject` and `title: subject: angle` cut at 120 characters (the title passed at 16 words only because the cut landed there, ending in a comma). The understand prompt never mentioned a length; the checker's copy rule was the one rule in the run no model had seen, and it was applied last. | High (a full run lost to a sentence) | The understander is asked for `title` (≤ 10 words) and `summary` (≤ 16 words) by name, `subject` is bounded to 8 words; `forayCopy` clamps both to the checker's 18 at a word boundary and warns when it had to. Fixed on the branch before attempt 4. |
| F-65 | **A zero-tape run is a guaranteed loss and the pipeline only says so at the end.** `check-forays.mjs` rejects any Foray with no resolvable segment item, so once §4.5 sources every beat to narration the run cannot produce a publishable Foray — attempt 3's sourcing lines said "0 tape" 51 s in, and the checker said "no resolvable segment items" 76 minutes and 139,803 tokens later, after 66 fresh model calls. | High (cost; three attempts have now run to or towards this wall) | `runPipeline` returns a new `no-tape` outcome straight after §4.5 when the tape count is zero, carrying the per-slot sourcing summary so the report names the deciding gate; the CLI prints `NO TAPE — …`. The fix for the tape itself stays upstream (WS-L). Fixed on the branch before attempt 4. |
| F-66 | **WS-D2's "time to first listen" is measured after every act is narrated, so it is the whole run.** The partial candidate is written by `stitchForay`'s `onActReady`, and stitch runs once, after the sequential narration of all acts — so attempt 3's `ttlA1Ms` is 4,579,741 ms (76.3 min, the full run) although act 1's narration was checkpointed at 18 min. A listener could not have started at 18 min either: nothing wrote a candidate until stitch. The streaming design (WS-D) is wired one stage too late. | Medium (KPI is wrong; the streaming promise is not kept) | Stitch act *i* as soon as act *i* is narrated (`stitchAct` already exists; continuity only smooths the act's introduction against the previous act, which is complete by then) and write the partial there; `ttlA1` then measures what its name says. Card: WS-D follow-up. |
| F-67 | **The taxonomy node is resolved from the understander's paraphrase, so a shorter paraphrase can move the Foray to the wrong branch and close the lineage gate on every episode.** Run 2 attempt 4 (first run on WS-L): the F-64 understander returned the 8-word subject "Real-world ML production: infrastructure, data, and operational challenges"; `resolveTopic(subject + angle)` scored `architecture/infrastructure` 1.5 on the single token *infrastructure* (the AI node `engineering/ai-robotics` advertised no terms and "ML" matches nothing), research-shape's lineage filter then refused every *Practical AI* episode the text index ranked, and the spine prompt went out with `tapeWindows: []` on all seven subtopics — the exact prompt of attempt 3, the WS-L machinery inert. Caught before the Opus call was answered (the relay lets the orchestrator read a prompt before paying for it). The user's prompt — "How AI systems really get built… deploying machine learning" — resolves to `engineering/ai-robotics` at 2.75 on its own. | **Critical** (WS-L cannot fire; F-59 in a new coat) | The listener's prompt joins the topic text at both resolution sites (research-shape's lineage filter and the pipeline's own), and `engineering/ai-robotics` advertises its vocabulary (`ai`, `machine learning`, `ml`, `mlops`, `llm`, …) the way #554 gave `energy-fusion` its terms. Fixed on the branch; attempt 4 relaunched. Longer term the resolver should be a classifier over the taxonomy, not token overlap. |
| F-68 | **Deepen paraphrases the seeded claims, and the tape-overlap floor then refuses the paraphrase.** Run 2 attempt 4b — the first run in which the spine saw tape: Opus wrote 16 of 33 beats with a `seed` and claims worded close to the quoted window. The deepen stage kept every seed (as `deepenActs` guarantees) but rewrote 11 of the 16 claims ("an aspiration that one person could figure out the modeling, build the prototype, and also deploy it to an actual cloud environment" → "a hiring aspiration built around one person — someone who could pick the model, build the prototype notebook, and also stand the thing up on real cloud infrastructure — and that job description mostly did not survive contact with a production incident"); §4.5 scored the rewrite against the tape (weighted share 0.349 against the 0.35 floor, 2 rare words against 3) and refused it: `tier2:window-overlap`, `seedWindowWon: false`. **5 tape beats of 33** — the 5 whose claims deepen left nearly verbatim; act 1 (4 seeded) got none. Downstream cost: three of the six act-1 pages with empty retrieval are these lost tape anecdotes, now being searched for on the web. | **Critical** (WS-L's yield: 5 of 16 seeded) | A seeded beat's claim is frozen through deepen: the prompt says so, `deepenActs` restores the spine's claim mechanically (as it does the seed) and logs when it had to; the seed window is tried first. The floor stays where it is. PR in flight (`fix/f68-seeded-claims-frozen`). |
| F-69 | *(observed in the same run)* F-60's rephrased retry query is a bag of the claim's rarest words, not a query: "Conway's bites pipeline boundaries wherever postmortem exactly places", "rotation spanning training diagnose failures materially separate rotations". Six of eleven act-1 pages had an empty first retrieval, so the retry fired six times. | Medium (retrieval cost; the retry rarely rescues) | Rephrase with the beat's *purpose* sentence (deepen writes one) or the claim's subject noun phrase, not an IDF-sorted word list; and skip the web entirely for a beat whose seed named the tape — the evidence is the transcript window. |
| F-70 | **Tier 2 skips the same-episode ledger tier 1 keeps, so a Foray with tape fails the curation rules M3/M4.** Attempt 4b's finished record carried 5 tape segments and `check-forays.mjs` refused it twice: *M3* — two seeded beats in one slot came from the same episode with the later beat's window earlier in the tape (1925 s then 1019 s), and *M4* — that episode was 40 % of segments / 42 % of runtime against the 25 % cap. `tier1VetoFor` already refuses `m3-order` and `m4-share` from a ledger (`lastStartByItem`, `usedCountByItem`); the minted-window path never reads or writes it. With four to six tape items any second segment from one episode breaks M4 by arithmetic, so the cap has to be reasoned about, not just applied. | **Critical** (the first Foray with tape was refused on it) | Tier 2 consults and updates the ledger with the same gates, falling through to the next episode; a per-episode cap that cannot produce an M4 failure at small counts (1 per episode until ≥ 8 tape items); the spine prompt asks for seeds from different episodes and in tape order. PR in flight (`fix/f70-f71-tier2-episode-ledger`). |
| F-71 | **The streaming partial candidate never validates when tier-2 tape exists.** `buildPartialCandidate` hands the real finalize an input without the minted `segments`/`segmentSources` the final candidate carries, so the partial's own validation read `unknown segment_id … not in data/segments.json` ×5 and `no resolvable segment items` while the final record resolved all five. A listener could never have started on the partial (F-66's fix stitches per act, but the partial it writes still fails this). | High (WS-D2's promise) | Thread `sourced.newSegments`/`newSegmentSources` into the partial's finalize input. Same PR as F-70. |
| F-72 | **The rare-word floor refuses the spine's own seed window.** Attempt 5, with claims frozen (F-68) and the seed window tried first (#568): three seeded beats matched their own window at weighted share 0.53, 0.72 and 0.85 and were still refused `window-overlap` because the floor also demands ≥ 3 rare words (`TIER2_WINDOW_MIN_TERMS`) and plain-worded claims carried 1–2. That test exists to refuse a window found by whole-archive search on common words (run 1's Chernobyl/griddle anchors); for a seeded beat the spine already read the window and wrote the claim from it. Cost tonight: 3 of 14 available tape beats. | High (tape yield) | A seeded beat's own window is judged on share alone; every other window keeps both conditions; the trace records which floor decided. PR in flight (`fix/f72-seed-window-floor`). |
| F-73 | **The tape-first windows are the wrong size and shape for the D-tier duration rules.** Attempt 5's first partial candidate (act 1, 18 min in — F-66 and F-71 working; all 5 segments resolved) was refused by `check-forays.mjs` on D2 (ends on two consecutive segments under 60 s with no ≥ 150 s recovery), D3 (mean segment duration 76.1 s, floor 90 s) and D5 (interquartile range 15.6 s, floor 45 s), plus M4's runtime clause (one episode 20 % of segments but 34.3 % of runtime). Research-shape quotes 60–120 s windows (`RESEARCH_TAPE_WINDOW_MIN/MAX_SEC`) and tier 2 cuts to them, so every segment lands at 60–120 s: the mean is under the floor by construction and the spread is nil. The D-tier rules were written for hand-cut tape (`narration-craft.md` §0) and the pipeline never read them. | **Critical** (every tape Foray fails at finalize) | Size and vary the windows to the rules: research-shape offers 90–180 s windows, the tier-2 cut grows a seeded window by overlap to ≥ 90 s and lets lengths vary; sourcing keeps a running D3/D5 reading and refuses a window that would break the mean; M4's runtime clause joins the ledger with the same small-count exemption. Where a rule is unsatisfiable at generated-Foray sizes, change the rule for generated Forays explicitly rather than game it — founder-visible. PR in flight (`fix/f73-f74-duration-rules-dai`). |
| F-74 | **Rule #65 refuses every tier-2 segment from a `dai_suspected` show, and 146 of the 220 shows in `data/transcript-availability.json` are flagged — Practical AI included.** `check-forays.mjs` line ~899: *source is dai_suspected, so its out-point cannot be anchored*. That was true when segments carried only timestamps; a minted tier-2 segment now carries `startAnchor`/`endAnchor` phrases from the tape (F-61, ADR-0007's drift-tolerant anchoring exists for exactly this case). The rule predates the anchors and reads the flag alone. | **Critical** (two thirds of the archive unusable) | Rule #65 accepts a segment from a dai_suspected source when it carries both phrase anchors (and the anchors resolve in the transcript); timestamp-only segments stay refused. Founder-visible rule change, same PR. |
| F-08 | The **stub run** (no key) on "the history of food and cooking" *built* a Foray and then failed **M4** (one show at 30.9% of runtime, cap 25%) — the quality gate works, and the small pool concentrates on few shows. | Info | Expected with a 212-row pool; watch whether tier 2 fixes it. |
| F-79 | **The partial candidate is refused on share-of-whole rules computed over a one-act slice.** Run 5 (2026-09-11, the AI-systems prompt, main with #614 G-30) sourced 25 tape beats over 34, and G-30's new abort-on-refused-partial default ended it after act 1 because `check-forays` on the act-1 partial reported `M4 FAIL: "practical-ai--open-source-self-driving-with-comma-ai" is 16.7 % of segments and 26.4 % of runtime, over the 25 % cap` — a share over 6 segments that the whole 25-segment Foray would dilute to one segment and roughly 7 % of the tape. Attempt 6's partial showed the same artefact at 3 segments (33 % each). M4 (both clauses), D3, D5's IQR floor, D2's end-of-Foray clause and D4's share clause are not monotone under partial assembly — a later act changes their denominator; only the copy/shape/per-item rules, D1, D2's run clause, D5's triple clause, L2–L4, M3 and the anchor rule are, and only those mean anything on a partial. | **Critical** (G-30 ends every tape-rich run after act 1) | `backend/src/generation/partialProjection.ts`: the partial's share-of-whole rules are judged on the PROJECTED whole — the partial's items followed by every later act's planned segments (the sourcing stage's own pointers) and band-midpoint narration estimates — through the same `finalize`, and every monotone rule stays strict on the partial; `ruleScope` on the candidate and an `F-79:` warning line say which rule was judged where. No rule text in `check-forays.mjs` changes; the abort default stays and now fires only on a failure the finished Foray would also have. |
| F-80 | **D5's consecutive-triple clause was a preference at placement, and run 5 was refused on it at finalize.** Run 5 (2026-09-11, main with #614–#620) narrated a 25-segment Foray end to end and `check-forays.mjs` refused it: `practical-ai--tiny-recursive-networks#603 / practical-ai--model-context-protocol-deep-dive#2297 / practical-ai--federated-learning-in-production-part-1#1650 are 152.0 / 142.2 / 169.4 s — three consecutive durations within +/-20 % of each other (max/min 1.191)`. F-73 (#571) built the length ladder and gated `d3-mean`, `d2-short-run` and `m4-runtime` as rules, but gated the triple clause (`d5-uniform`) as a *preference*: the candidate it alone refused was kept and taken when nothing better turned up, on the argument that a tape-starved Foray fails worse than a metronomic one. Every one of run 5's three was a tier-2 cut of a window that could have been cut longer or shorter at a cue boundary; sourcing asked for one length (the ladder rung), found it inside the band, and took it anyway. The clause is monotone on a prefix and #620 keeps it strict on the partial, so the same placement now ends a run after act 1 under G-30. | **Critical** (the first fully narrated tape Foray, refused at the last gate) | `backend/src/generation/d5Triple.ts` mirrors the checker's arithmetic (`max/min > 1.2` escapes, pairwise, consecutive SEGMENT items in play order) and `test/d5Triple.test.ts` pins it to `check-forays.mjs`'s own `d5Triples` row by row. At placement the clause is a rule for both tiers (`d5-triple`): a pool segment that would make the triple is passed over; a tier-2 window is cut to the ladder rung first and, when that cut alone is inside the band, to every other length the window admits — the ladder's other rungs, then every cue-boundary length the growth rule reaches, farthest from the two previous durations first — and the first that escapes and clears M3/D2/D3/M4 is placed, with `lengthGate: "d5-triple"` on the tape-relevance row. Only a window no cut of which escapes is refused (`d5-triple` in the trace, a fall-through to the next candidate), and only a beat none of whose candidates can escape is narrated. No rule text in `check-forays.mjs` changes. |

## 1b. Interventions ledger — every deviation from the as-designed workflow

Rule (Wyatt, 2026-09-09): *any time the orchestrator has to intervene, that is a deviation
from the final workflow and therefore an issue.* Logged exhaustively, including the ones
that were necessary to run at all.

| # | Intervention | Why it was needed | What it deviates from | Status |
|---|---|---|---|---|
| I-01 | Set `DAILY_BUDGET_USD=1000 EPISODE_BUDGET_USD=1000` in the run environment. | Default `$2.00/day` would have halted a medium Foray mid-run (F-04). | Production would run into the guard; the run env hides it. | Open — needs a per-run budget flag or a realistic default. |
| I-02 | Wrote `FileTranscriptCueProvider` and wired it into the batch driver before run 1. | Tier-2 sourcing was unwired (F-01); without it the run could only use the 212-row pool. | Code change to the pipeline. Necessary; but it means run 1 tests code that did not exist an hour earlier. | Fixed (F-01). Needs tests + PR. |
| I-03 | The relay transport itself (`ANTHROPIC_BASE_URL` → local relay → subagent). | Requested by Wyatt: agents instead of an API key. | Answers arrive minutes later than an API call; token counts are estimates; the "model" is a subagent at the same tier, not the pinned id. | By design of this exercise; not a production deviation. |
| I-04 | Aborted attempt 1 of run 1 at call #3 and restarted from call #1. | Orchestrator bug: the answer helper decoded stdin as cp1252, double-encoding the em dash in call #2's reply; the mojibake was already in the spine prompt. | Restart. Cost: 2 Haiku calls, ~3 min. | Fixed (stdin forced to UTF-8, verified). |
| I-05 | Reused calls #1 and #2's answers from attempt 1 instead of re-dispatching agents. | Identical prompts; saved two calls. | A production run would make fresh calls; determinism of Haiku on these two prompts was assumed, not measured. | Accepted deviation; noted. |
| I-06 | The answer helper strips one markdown fence pair before the reply reaches the relay. | Subagents fence their JSON ~half the time (F-10). | `parseWithRetry` already strips one fence pair, so behaviour is the same — but the helper masks how often the model does it. Frequency logged from the raw agent replies instead. | Kept; frequency tracked separately. |
| I-07 | For call #7 (narration writer) the transport wrapper added: *"where you cannot reproduce a source verbatim, quote only what you are confident is real and mark anything uncertain as contested rather than inventing a citation."* | Reflex to avoid fabricated citations. | **A prompt deviation.** Production's writer gets no such instruction, so this biases the F-14 observation for call #7. | **Error.** Stopped after call #7: from call #8 on, the wrapper carries only the transport necessities (JSON-only reply, no tools — production Sonnet has no tools either). Call #7's answer is flagged in the KPI log. |
| I-08 | `--duration medium` chosen for "aim for about an hour". | §3 maps ~60 min to *medium*; *long* is up to 180 min and unproven. | Interpretation, not deviation; recorded so the choice is visible. | — |
| I-10 | **Relay bug served the pipeline's retry a stale answer.** The dedupe (I-09) keyed only on body hash, so the writer's legitimate re-ask (identical prompt, F-16) was treated as an SDK retry and given the previous reply — guaranteeing the retry failed. | Orchestrator error. | Directly contributed to run 1 attempt 2's failure (together with I-07 and F-16/F-19). | **Fixed:** dedupe now applies only to an *in-flight* duplicate. |
| I-11 | Run 1 attempt 3 reuses the answers to calls #1–#6 from attempt 2 (identical prompts: clarity, intent, spine, three deepen-acts). | Saves ~10 min of orchestration and re-tests the stage that failed rather than the ones that passed. | A production re-run would regenerate everything (F-18). | Accepted deviation; the reused answers are the exact JSON the pipeline already accepted. |
| I-12 | Code change to `writeNarration.ts`: the retry now appends the rejection reasons (F-16). | Reasonable fix between runs, per the brief. | Pipeline code differs from what run 1 attempt 2 exercised. | Fixed; needs a unit test + PR. |
| I-09 | Relay dedupes identical retried requests (SDK 10-min timeout). | A slow orchestrator turn would otherwise mint duplicate requests. | Production never sees this path. | Transport-only. |
| I-13 | **Fix set applied between attempt 3 and attempt 4 (2026-09-09 ~03:00Z).** Five code/prompt changes in `backend/src/generation`, each traced to a finding: (1) `tokenizeForSourcing` — a function-word stopword list for the two §4.5 scorers (F-29); (2) tier-2 scoring requires ≥1 *episode-title* token and caps the show title's contribution at 1 (F-23/F-24); (3) writer prompt gains three lines: purpose is direction not a source (F-28), tape is not a publication (F-30), and a narrow definition of *contested* (F-19/F-25); (4) `writePageAndVerify` rejects a slug-shaped `publication` with an informed retry (F-30); (5) 3 informed attempts per page, and a tape beat whose connective page still fails keeps its tape and drops the page with a warning; narration beats still fail hard (F-17/F-31). 6 regression tests added (`test/sourceBeats.test.ts`, `test/writeNarration.test.ts`); 42/43 pass, the 1 failure is pre-existing (F-21). | Deviation by design — the user asked for fixes between runs. Everything else in the pipeline (models, stage order, other prompts, budgets) is unchanged. |
| I-14 | Attempt 4 reuses attempt 3's answers to calls 1–6 (understand, research-shape, spine, 3× deepen) via `reuse.py`, because those prompts are byte-identical and re-asking would only add noise to the KPIs. Calls from §4.7 on are fresh — the writer prompt changed. | Same rationale as I-11; the reused answers are the ones whose defects are already logged (F-11, F-13). |
| I-15 | **Environment: the harness killed the background shell wrapping attempt 4 for low system memory** (~850 MB free of 16 GB at 03:12Z; the machine's own browser and update tasks held most of it, no leftover headless browsers from the earlier playback checks). The pipeline's own process tree (bash → npm → tsx → node) survived and kept waiting on the relay, so no work was lost; only the completion notification was, which the queue monitor replaces. | Harness/host incident, not a pipeline defect. Noted because a production run on a shared box would need the driver to run detached (service or container) rather than as a child of an interactive shell. |
| I-16 | `TIER1_MATCH_THRESHOLD` and `TIER2_MATCH_THRESHOLD` raised 2 → 3 mid-attempt-4 after F-33. No effect on attempt 4 (sourcing had completed); in force for run 2 onward. Regression test added for the beat-2 claim. | Same class as I-13 — a between-runs fix that happened to be diagnosed during a run. |
| I-17 | Transport: the subagent serving call #33 (attempt 4) reported one tool use despite the wrapper's "do not use any tools" instruction. The reply was still a bare JSON object and was delivered unchanged; whatever the tool call was, it did not reach the pipeline. Logged because the relay's contract is *text in, text out*, and a subagent with tools is a capability the real API call would not have. | Harness deviation, no pipeline effect. A production relay should run subagents with an empty tool set rather than an instruction. |
| I-18 | Between-runs fix, prepared during attempt 4 (no effect on the running process): `parseOrRepairJson` closes brackets a model reply forgot at its tail before the schema parse (F-39); writer/verifier/continuity builders now use the shared parser (F-40). Tests added. | Same class as I-13. |
| I-19 | **Transport change from call #41 on: the answering subagent writes its reply into the relay itself** (`answer.py <n> <tier>`) instead of returning it to the orchestrator for delivery. Halves the orchestrator turns per call — the relay wall per call had been ~75 % orchestrator latency (attempt 3 KPIs). The prompt the model answers is unchanged; only who copies the text into `res-<n>.json` changes. | Harness efficiency; the user asked for the loop to be faster and for the orchestrator to stay out of execution. The reply is still the model's own text — the agent is told to write it verbatim. |
| I-20 | **The first self-delivering subagent (call #41) used its tools to fetch the web** — 11 tool calls: it retrieved Wikipedia and a Linda Hall Library page, quoted them verbatim, and filled `url`/`retrieved`. The production writer call has no retrieval, so this page is better-grounded than the pipeline can produce on its own; its pass (if it passes) must not be counted as evidence about the as-designed writer. Wrapper tightened from #42 on: no tools beyond the one write and the one delivery command. Kept the page rather than re-asking (a re-ask would itself be an intervention, and the beat was on its last attempt). | Transport deviation caused by I-19. Also the clearest demonstration in the whole run that *retrieval fixes F-27/F-32*: given a document, the writer quotes it. |

## 2. Runs (filled in as each completes)

### Run 1 — engineering disasters

*Prompt:* "How engineering disasters actually happen: the chains of small decisions
behind collapsed bridges, failed dams and machines that broke."
*Sourcing expected:* `engineering/disasters` pool segments (23), *Causality* (12 bodies),
*Being an Engineer* (337 bodies).

**Attempt 2 (2026-09-09 02:10:10Z → 02:19:42Z, 9 m 32 s wall) — FAILED at the first narration page.**

| KPI | Value |
|---|---|
| Model calls | 7 (1 Opus spine, 3 Sonnet deepen-act in parallel, 1 Sonnet narration + 1 retry served stale by I-10, 2 Haiku) |
| Est. prompt tokens (chars/4) | 6,322 |
| Est. completion tokens | 7,678 |
| Subagent tokens incl. agent overhead | 310,427 |
| Agent compute time (sum) | 870 s |
| Relay wall (sum) | 873 s — the orchestrator's turn latency dominated: agents themselves took 6–49 s per call |
| Stage reached | §4.7 narration, beat 1 of ~31 |
| Time until a listener could start | never (no candidate) |
| Tier-2 tape hits | ≥ 1 confirmed (Hyatt beat → *Causality* transcript) before the failure |
| Failure | `contested-not-flagged-in-text` on a Frame page (F-19), uninformed retry (F-16) served a stale answer (I-10), no per-beat fallback (F-17) |

**Attempt 3 (02:24:08Z → 02:54:28Z, 30 m 20 s wall) — FAILED at beat 5 of 31 (page rejected twice, F-17/F-31).**

| KPI | Value |
|---|---|
| Model calls | 23 total: 1 Opus (spine), 2 Haiku (understand, research-shape), 20 Sonnet (3 deepen-act, 10 writer incl. 5 retries, 7 verifier). Calls 1–6 reused from attempt 2 (I-11). |
| Est. prompt tokens (chars/4) | 15,643 |
| Est. completion tokens | 10,665 |
| Subagent tokens incl. agent overhead (16 fresh calls) | 634,581 — ≈ 40 k per call, of which the prompt itself is < 1 k: the Claude Code subagent harness costs ~40× the pipeline's own tokens |
| Agent compute time (sum, fresh calls) | 470 s (3–86 s per call; verifiers 3–21 s, writers 19–86 s) |
| Relay wall (sum) | 1,817 s — orchestrator turn latency ≈ 75 % of wall |
| Beats written | 5 (pages passed: 4; 10 writer calls for 4 pages = 2.5 calls/page) |
| First-attempt pass rate | 0 / 5 |
| Tape anchors correct | 1 of 5 (Hyatt); wrong: Chernobyl, Three Mile Island, griddle-bakestone, San Bruno (F-23/24/29) |
| Fabricated or mis-attributed citations that passed verification | 2 (#11 "New Safe Confinement…", #19 publication = tape slug) — F-27/F-30 |
| Time until a listener could start | never (no candidate) |
| Projected wall to a 31-beat candidate at this rate | ≈ 3 h 10 m — if no page ever failed twice |

**Attempt 4 (started 03:06:45Z) — in progress.** Running notes: beat 1 passed on attempt 2 (first rejection was quote-span support, not the contested flag — the prompt change removed that class here). Beat 2 wrote three source-less pages in a row (F-34) and was the first live exercise of the connective-page drop: tape kept, page dropped, run continued (F-17/F-31 fix works). Beat 3 likewise: three source-less pages, dropped. Beat 4 passed on the FIRST attempt (the first first-attempt pass of the exercise; one NIST source, 'sixty percent'). Tape anchors so far: beat 1 Hyatt ✓, beat 2 Chernobyl ✗, beat 3 Flint ✗, beat 4 Hyatt ✓, beat 5 *Grill Coach* (barbecue) ✗, beat 6 *Geology Bites* ✗ — sourcing ran before I-16. Beat 5 passed on attempt 3 (the old cap would have killed the run here). Beat 6: three source-less pages, dropped; one verifier reply was malformed JSON and cost an attempt (F-39). Beat 7, the first Patch (narration-only content page): attempt 1 re-told the collapse and ignored its purpose (F-41), attempt 2 hedged the purpose as opinion, both rejected on unsourced details; attempt 3 pending — a narration beat, so a third rejection ends the run.

**Act 1 checkpoint (03:06:40Z → 04:14:02Z, 67 m 22 s to the end of Act 1 narration):**

| KPI (Act 1 only, 10 beats) | Value |
|---|---|
| Narration calls | 46 (23 writer, 23 verifier) for 10 beats → 4.6 calls/beat |
| Pages kept / dropped | 7 kept, 3 connective pages dropped (beats 2, 3, 6 — all three were mis-anchored tape) |
| First-attempt passes | 2 of 10 |
| Pipeline tokens (est., chars/4) | 29,339 in / 6,932 out |
| Subagent tokens incl. harness | 1,923,699 (≈ 42 k per call; the prompt itself is ≈ 700 tokens) |
| Agent compute (sum) | 1,911 s; relay wall 3,983 s → orchestrator latency ≈ 52 % after the I-19 transport change (was ≈ 75 %) |
| Time a listener could have started Act 1 | never yet — nothing is playable until the whole Foray finalises (see §5 plan: streaming publish) |

**Act 2 checkpoint (04:14:02Z → 05:03:41Z, 49 m 39 s for 10 beats):**

| KPI (Act 2 only, 10 beats) | Value |
|---|---|
| Narration calls | 37 (20 writer, 18 verifier; one structural rejection cost no verifier call) → 3.7 calls/beat |
| Pages kept / dropped | 9 kept, 1 dropped (beat 18) |
| First-attempt passes | 3 of 10 |
| Subagent tokens incl. harness | 1,680,247 |
| Agent compute (sum) | 2,366 s; wall 2,979 s → orchestrator latency ≈ 21 % after I-19 (was 52 % in Act 1, 75 % in attempt 3) |
| Notable | beat 12 survived rule 3's phrase list only by deleting its honest caveat (F-43); beat 18's retry asserted something false about the record (F-45); one zero-source question page passed (F-44) |

**Attempt 4 outcome (03:06:40Z → 05:41Z, 2 h 35 m): FAILED at beat 23 of 31** — a narration (Patch) beat rejected three times; the third page quoted the beat purpose itself as a source (F-46). Paused here on the user's instruction: the fix plan (`generation-fix-plan-2026-09-09.md`) is being implemented before any further build.

| KPI (attempt 4, whole run) | Value |
|---|---|
| Beats completed | 22 of 31 (Acts 1–2 complete, Act 3 beats 21–22) |
| Pipeline model calls | 103 (6 reused + 97 fresh: 1 Opus, 2 Haiku, 100 Sonnet) |
| Narration calls | 97 for 23 beats attempted → 4.2 per beat; 52 writer, 45 verifier |
| Pages kept / dropped / fatal | 18 kept, 4 dropped (beats 2, 3, 6, 18), 1 fatal (beat 23) |
| First-attempt passes | 5 of 23 |
| Pipeline tokens (est.) | ≈ 70 k in / 22 k out |
| Subagent tokens incl. harness | 4,257,478 (97 agents, ≈ 44 k each) |
| Agent compute (sum) | 5,560 s; relay wall 9,240 s |
| Tape anchors on-topic | 5 of 22 |
| Known fabricated or mis-attributed citations that reached the verifier | ≥ 6 (F-27, F-30, F-32 ×3, F-46) |
| Time until a listener could start | never |

*(post-mortem in §5 of the fix plan; full run-2 table when the fixed pipeline runs)*

### Run 2 — how AI systems get built

*Prompt:* "How AI systems really get built and put to work: the practical engineering behind deploying machine learning, from data to production". Pipeline = the merged fix-fleet branch (E, F, C, B, D, A; tip `249b8fc`), model ids `claude-opus-5` / `claude-sonnet-5` / `claude-haiku-4-5-20251001` via `config/models.ts`.

**Attempt 1 (15:51:14Z → 16:24:21Z, 33 m 07 s wall) — FAILED at act 1, page p2 of slot 2 (F-50/F-51).** Understand (2 Haiku) → spine (1 Opus, 4 acts / 6 slots / 35 beats, passed `spineStructure`) → 4 deepen-act calls in parallel → sourcing (keyless): **0 tape beats** (F-49; 29 `argument`, 6 `account` with no match; 6 transcription-queue candidates) → evidence retrieval for the first act's pages: 12 Haiku web-search calls issued in parallel (WS-A's evidence packs) — the first stage of the new pipeline to fire. Checkpoint file present after every stage (WS-F), so a crash here costs one stage, not the Foray.

| KPI (attempt 1) | Value |
|---|---|
| Model calls | 34: 14 Haiku (2 understand + 12 evidence retrieval with web search), 1 Opus (spine), 19 Sonnet (4 deepen, 5 claim-selection, 5 prose, 5 verify) |
| Pipeline tokens (est., chars/4) | 50,662 in / 24,796 out |
| Agent compute (sum) / relay wall (sum) | 4,847 s / 4,863 s — orchestrator latency ≈ 0 % (self-delivering subagents, parallel dispatch; was 21–75 % in run 1) |
| Time to the end of act 1 narration | 33 min wall for 12 pages (run 1: 67 min for 10 beats) |
| Narration calls per page (act 1) | 15 for 12 pages = **1.25** (run 1: 4.6 per beat); plus 1 retrieval call per page |
| First-attempt page pass rate | 6 of 11 verified pages first time (run 1: 2 of 10 in act 1); 10 of 12 by attempt 2; 1 page failed all 3 (F-50) |
| Mechanical gate (quote-in-document, ≥8 words, no purpose echo, publication = doc title) | passed first time on all 5 selection calls — **zero fabricated or mis-attributed citations reached the verifier** (run 1: ≥6) |
| Verifier rejections | 7 across 3 rounds, every one a real overreach beyond the quote or a dropped purpose (run 1's rejections were mostly the contested-flag formality) |
| Tape anchors | 0 of 35 beats (F-49) — every page was a `Carry` content page |
| Time until a listener could start | never — act 1 never finalised, so WS-D's partial candidate was not written |

*Fixes between attempt 1 and attempt 2 (in progress):* F-49 (deepen `kind` definition + sourcing diagnostics), F-50 (purpose may be corrected by evidence), F-51 (a page never kills the Foray; the gate decides).

**Attempt 2 (17:50:37Z → ~18:30Z, ~40 min wall) — FAILED at act 1 on the no-evidence page (F-60 → F-51's remaining fatal branch).** Pipeline = generation branch after #551 (F-50/F-51, cue provider, budget session) and #552 (F-49, sourcing trace, tier-2 segments). Calls 1–3 replayed from attempt 1 (identical prompts, I-11); deepen re-asked (prompt changed).

| KPI (attempt 2) | Value |
|---|---|
| Beat kinds after F-49 | **29 account / 6 argument** (attempt 1: 6 / 29) — the cap and the narrowed definition work |
| Tape anchors | 0 of 35 — every slot's trace names `tier2:lineage` as the deciding gate = F-59 (topic resolved to `engineering/energy-fusion`) |
| Model calls | 34: 14 Haiku (2 replayed + 12 retrieval), 1 Opus (replayed), 19 Sonnet (4 deepen, 6 selection, 4 prose, 4 verify, 1 orphaned) |
| Pipeline tokens (est.) | 41,725 in / 20,591 out |
| Agent compute | 3,893 s (fresh calls) |
| Verifier verdicts | 13 pages judged: **9 passed**, 4 rejected (all overreach beyond the quote); first round 6 of 9; **3 pages passed as `purposeRevised: true`** — F-50 working: a page reported where the evidence departed from its purpose and was accepted |
| Empty retrievals | 2 of 12 pages (`passages: []`) → each cost 3 selection calls on "Documents: none" (F-60); one of them ended the run |
| Time until a listener could start | never — act 1 never finalised; nothing narrated was checkpointed because neither slot completed |

*Fixes between attempt 2 and attempt 3 (in progress):* F-59 (topic resolver), F-60 (empty evidence: one rephrased retry, then degrade, never fatal), WS-H (text-level tier 2) if ready.

**Attempt 3 (20:21:39Z → 21:37:55Z, 76 m 16 s wall) — COMPLETED narration, stitch and finalize; the Foray was REFUSED by `check-forays.mjs` (F-64 summary length, F-65 zero tape). Nothing published.** Pipeline = generation branch after #553 (WS-H), #554 (F-59), #555 (F-60), #565 (F-61); archive regenerated (I-24). Calls 1–7 replayed from attempt 2 (identical prompts); evidence cache served 10 of 12 pages, 2 fresh retrieval calls. **Sourcing: 0 tape of 35 beats, deciding gate `tier2:window-overlap` on every slot** — tier 2 now reaches the transcripts and the relevance floor refuses them honestly (F-63).

**Act 1 checkpoint (attempt 3):** the first act ever completed on the new pipeline — `narrate:0`, `narrate:0:0`, `narrate:0:1` written. 12 pages: 2 dropped at the F-60 guard (no evidence after the rephrased retry, no writer call spent), **10 verified** — slot 1 five of five on the first attempt, slot 2 three of four first time (two accepted as `purposeRevised`, one corrected a statistic the purpose overstated) and the fourth on attempt 2. Narration calls for the act: 9 (3 selection, 3 prose, 3 verify) for 10 pages = **0.9 per page** (run 1: 4.6 per beat); retrieval 2 fresh calls, 10 evidence-cache hits.

**Act 2 checkpoint (attempt 3):** `narrate:1` written. 11 pages: **10 verified** (slot 1 five of five first time; slot 2 three of four first time, one on attempt 2) and **1 kept unverified** — the first live exercise of F-51's keep-and-count policy: the page's purpose is itself about an absence ("what the record doesn't say"), the F-45 mechanical rule refused every attempt that voiced it, and the page was kept with `verified: false` for the gate rather than ending the run. Narration calls: 12 for 11 pages (4 selection, 4 prose, 4 verify) = **1.1 per page**; retrieval 11 fresh + 1 rephrased retry (F-60) that found 3 passages the first query missed. Watch: a purpose that is *about* absence collides with F-45 by construction — the deepen stage should not write such purposes for `Carry` pages, or the rule needs an exemption when a source quote states the absence (it does allow that; the writer never found one).

**Act 3 checkpoint (attempt 3):** `narrate:2` written; one slot, 6 pages, **6 verified** (two first time, three on attempt 2, one on attempt 3). Narration calls: 9 for 6 pages = 1.5 per page; retrieval 6 fresh. Acts 1–3 together: 27 verified of 29 kept pages, 1 kept unverified, 2 dropped for no evidence; 30 narration calls for 29 pages ≈ 1.0 per page.

**Act 4 checkpoint (attempt 3):** `narrate:3` written — narration complete for all 35 beats. One slot, 6 pages, 6 verified (four first time, three of them `purposeRevised`; one on attempt 2; one dropped to the F-60 hand-off? — see the report). Retrieval 6 fresh + 2 rephrased retries, both of which found passages (one off-topic — a prediction-market page — refused at selection: F-48 in miniature). Stitch (§4.8 continuity, 3 Sonnet calls at 500 tokens) started at call #71.

**Finalize (attempt 3).** Stitch took 2 m 24 s (3 Sonnet continuity calls); finalize 8 ms. The record: 4 acts / 6 slots / **44 items, all narration** (disclosure + 33 pages + markers), `runtime_sec` 2,684 — a 45-minute all-narration Foray. `check-forays.mjs` refused it on two rules: *summary is 26 words, over the 18-word limit* (F-64) and *no resolvable segment items* (F-65, the consequence of F-63). The veracity gate was never consulted — validation failed first — but `meta.veracity` was computed and is in `report.json`: **86 of 86 checkable quotes grounded** in their documents, 0 ungrounded pages, purpose fidelity 1.0, 10 pages accepted as `purposeRevised`, 3 pages kept unverified (2 no-evidence hand-offs from F-60, 1 F-45 collision), first-attempt pass rate 0.67, 1.0 narration calls per page, 139,803 pipeline tokens. `ttlA1Ms` = 4,579,741 — the whole run, because the partial candidate is only written during stitch (F-66).

| KPI (attempt 3) | Value |
|---|---|
| Outcome | INVALID at finalize (F-64, F-65); veracity gate not reached |
| Wall clock | 76 m 16 s: understand 40 s (replayed), spine 5 s (replayed), deepen 6 s (replayed), source 2 s, narrate 17.1 + 20.5 + 20.7 + 14.8 min, stitch 2.4 min, finalize 8 ms |
| Model calls | 73: 30 Haiku (2 understand replayed + 28 evidence retrieval incl. 4 rephrased retries), 1 Opus (spine, replayed), 42 Sonnet (4 deepen replayed, 35 narration = selection/prose/verify, 3 continuity); 7 replayed, 66 fresh |
| Pipeline tokens | 139,803 (SDK usage); est. 96,848 in / 42,955 out by chars/4 |
| Agent compute (sum) / relay wall (sum) | ≈ 7,750 s / 7,794 s — orchestrator share < 1 % |
| Pages | 35 beats → 33 pages written, 30 verified, 3 kept unverified (2 no-evidence, 1 F-45), 0 fatal (F-51 held for the whole run) |
| Narration calls per page | 35 for 33 = **1.06** (run 1: 4.6 per beat) |
| First-attempt page pass rate | 0.67 (`meta.veracity.firstAttemptPassRate`) |
| Grounded quotes | 86 of 86 checkable — zero fabricated or mis-attributed citations reached the verifier |
| Pages passed as `purposeRevised` | 10 (F-50 working as designed) |
| Empty retrievals | 4 first queries empty → rephrased retry (F-60): 2 found passages, 2 did not (pages handed off unverified, no writer call spent) |
| Tape anchors | 0 of 35 — every trace `tier2:window-overlap` (F-63) |
| Beat kinds | 29 account / 6 argument |
| Time until a listener could start | never (nothing publishable); `ttlA1Ms` reads 76.3 min because the partial is written at stitch (F-66) |

*What attempt 3 proved:* the narration pipeline now runs end to end on a 35-beat spine with no fatal page, ~1 call per page, and every quote it reads to the listener is in the document it names. *What it disproved:* that fixing the matchers would produce tape — the spine has to be written from the tape (WS-L, merged, untested until attempt 4).

*Fixes between attempt 3 and attempt 4:* F-64 (bounded title/summary, clamped to the checker's rule), F-65 (`no-tape` outcome after §4.5), WS-L (tape-first spine, #566). F-66 routed to a card.

**Attempt 4a (21:52Z → stopped at call #3)** — first run on WS-L + F-64/F-65. Call 1 (clarity) replayed; call 2 (intent, new six-field prompt) fresh: subject 8 words, title "Beyond the Algorithm: Engineering Production AI Systems", summary 15 words — F-64 working. The spine prompt then arrived with **no tape windows on any of the seven subtopics** (`windowsUnavailable: "no episode in this Foray's taxonomy lineage says enough of this subtopic to open"`) and a research map of Engineering / Manufacturing / Machining / Aerospace: the shorter subject had moved the resolved topic to `architecture/infrastructure` (F-67). Stopped before the Opus call was answered; 2 Haiku calls spent. Fixed (prompt joins the topic text; AI node terms) and relaunched as attempt 4b.

**Attempt 4b (22:2xZ →)** — pipeline = generation branch at 6bb494a (WS-L, F-64, F-65, F-67). Calls 1–2 replayed from 4a. Spine prompt: 23,689 chars, 8 subtopics each with 3–4 quoted *Practical AI* windows (the lineage gate now passes: F-67 working). **Spine (Opus): 4 acts / 6 slots / 33 beats, 16 seeded from windows, passed `spineStructure` first time.** Deepen: 4 Sonnet calls, seeds all kept. **Sourcing: 5 tape / 28 narration — the first tape run 2 has ever sourced** (act 2: 2 + 2, act 3: 1; acts 1 and 4: 0); 11 of 16 seeded beats refused at `tier2:window-overlap` because deepen rewrote their claims (F-68). Act-1 evidence: 11 retrieval calls, 6 empty → 6 rephrased retries (F-69). Narration continues on the 5-tape spine so the run reaches finalize and the veracity gate on a Foray with segment items for the first time.

**Attempt 4b result (22:04:58Z → 23:36:27Z, 91 m 29 s wall) — the first Foray with tape reached finalize; REFUSED by `check-forays.mjs` on M3 (same-episode order) and M4 (one episode 40 % of segments), both curation rules tier 2 never applied (F-70). The partial candidate failed separately for missing minted segments (F-71).** The record: title *Beyond the Algorithm: Engineering Production AI Systems*, summary 15 words (F-64 working), 4 acts / 6 slots / **46 items = 41 narration + 5 tape segments**, `runtime_sec` 2,184 (36 min). Veracity: **71 of 71 checkable quotes grounded**, `tapeRelevance` 1.0 (5 of 5 anchors on topic — the first non-null reading of that metric), purpose fidelity 1.0, 12 pages `purposeRevised`, 5 kept unverified (all no-evidence hand-offs), 1 dropped, first-attempt pass rate **0.44** (attempt 3: 0.67), 1.30 narration calls per page (attempt 3: 1.06), 166,475 pipeline tokens. Both regressions track the evidence: 12 of 43 retrieval queries came back empty (F-69), and pages written from thin web passages went to a second or third round more often.

| KPI (attempt 4b) | Value |
|---|---|
| Outcome | INVALID at finalize (F-70: M3 + M4); veracity gate not reached; partial invalid (F-71) |
| Wall clock | 91 m 29 s: understand 40 s (replayed), research-shape 9 s, spine 3 m 09 s, deepen 3 m 04 s, source 15 s, narrate 32.6 + 18.1 + 19.0 + 12.4 min, stitch 1.9 min |
| Model calls | 96: 45 Haiku (2 replayed + 43 retrieval incl. 12 rephrased retries), 1 Opus (spine, fresh — tape windows), 50 Sonnet (4 deepen, 43 narration, 3 continuity); 2 replayed, 94 fresh |
| Pipeline tokens | 166,475 (SDK usage); est. 123,446 in / 43,029 out |
| Agent compute (sum) / relay wall (sum) | 13,461 s / 13,500 s — orchestrator share < 1 % |
| Spine | 4 acts / 6 slots / 33 beats; **16 seeded from tape windows** (passed `spineStructure` first time) |
| Tape | **5 of 33 beats** (16 seeded; 11 lost to F-68); 5 tier-2 segments minted with anchors from the tape; 5 of 5 on topic |
| Pages | 28 written, 22 verified, 5 kept unverified (no evidence), 1 dropped; 0 fatal |
| Narration calls per page | 43 for 33 beats = 1.30 |
| First-attempt page pass rate | 0.44 |
| Grounded quotes | 71 of 71 checkable |
| Pages passed as `purposeRevised` | 12 |
| Empty retrievals | 12 first queries empty → 12 retries; 4 recovered |
| Time until a listener could start | never — the partial fails F-71 and `ttlA1Ms` still reads the whole run (F-66 merged after this run started) |

*What attempt 4b proved:* WS-L works end to end — the spine wrote 16 of 33 beats from real transcript windows, sourcing minted tape for the ones deepen left alone, and every minted anchor was on topic. *What it found:* three rules the pipeline never knew about (F-68 deepen paraphrase, F-70 episode order/share, F-71 partial plumbing), each now fixed or in flight.

*Fixes between attempt 4b and attempt 5:* F-66 (#567), F-68 (#568), F-70/F-71 (#569) merged — #569 also found the M4 cap was measured against beats rather than segments for tier 1, so it read 6 where the binding number was 1; the runtime clause of M4 stays ungated in sourcing by choice. Expect attempt 5 to source ≥ 12 of 16 seeded beats and to be the first candidate that reaches the veracity gate.

**Attempt 5 (00:05Z 2026-09-10 →)** — pipeline = generation branch at c02d7df (WS-L + F-64…F-68, F-70/F-71). Calls 1–2 replayed. Spine (Opus, fresh — the prompt now carries the episode-spread rule): 3 acts / 6 slots / 32 beats, **18 seeded**, passed `spineStructure` first time. Deepen: 3 Sonnet calls; every seeded claim arrived frozen (no restore line in the log — the prompt rule held). **Sourcing: 11 tape / 21 narration — the first double-digit tape count** (attempt 4b: 5). Of the 18 seeds: 11 placed; 2 refused `m3-order` by the new ledger (F-70 working: same-episode windows out of tape order fell through to narration); 5 refused `window-overlap`, of which 3 with shares 0.53–0.85 on their own window (F-72) and 2 genuinely loose. Act-1 evidence: 11 retrieval calls, 7 empty first time; the F-60 retries all empty (F-69).

**Attempt 5 result — STOPPED after act 1 (00:05Z → 00:31Z, 51 calls).** Act 1: 11 pages, 6 verified first time, second round for both slots, checkpointed at 17.5 min; **`stitch:0` written and the partial candidate existed at `ttlA1Ms` = 1,085,114 ms (18.1 min) — the first time a listener-facing candidate has existed before the run ended (F-66) — with all 5 act-1 segments resolved (F-71).** The partial was refused by `check-forays.mjs` on D2, D3, D5 and M4-runtime (F-73) and on rule #65 for `dai_suspected` sources (F-74). Since the final record would fail identically, the run was stopped before act 2's narration rather than spend ~50 more calls on a known refusal; act 2's 11 retrievals had already returned (8 of 11 empty — F-69). Evidence this attempt: 22 first retrievals, 15 empty — of which act 1's 7 are real and act 2's 8 are the exhausted search budget (I-25).

**Attempt 6 (01:12Z 2026-09-10 → PAUSED at narration)** — pipeline = generation branch at 2fe88de (WS-L + F-64…F-74). Calls 1–2 replayed; spine prompt now quotes 90–180 s windows (32 windows, mean 120 s). Spine (Opus): 4 acts / 6 slots / 33 beats, 14 seeded, passed first time; deepen: 4 calls, every seeded claim frozen. **Sourcing: 11 tape / 22 narration; 10 tier-2 segments minted with durations 35–211 s, mean 135 s (D3 satisfied by construction for the first time; IQR ≈ 90 s clears D5's 45 s floor)**; 3 of 14 seeds refused `window-overlap` on share, none by the rare-word floor (F-72's share-only path was never needed to decide). Act-1 retrieval: 11 calls, all empty — the session's search budget was already exhausted (I-25), so the run was paused before narration rather than write 33 pages with no evidence. The checkpoint holds every stage through `source`; a resumed run pays only for narration.

**What the two fixes changed, and one thing they deliberately did not.** F-50: the claim-selection and prose prompts now permit a page to report a contradiction between its purpose and its documents, the prose reply carries `purposeRevised`, and the verifier's `purposeAccomplished` asks whether the page addressed the purpose's SUBJECT with the evidence available — contradicting the purpose accomplishes it; only dropping the subject fails. Both flags (writer's `purposeRevised`, verifier's `purposeRevisedByVerifier`) are kept on `NarratedBeat` and counted as `meta.veracity.purposeRevisedPages`, reported and never gated. F-51: a narration beat's third rejection keeps the page with `verified: false`, its `attempts` history and the verifier's final `verifierNotes`, and the run continues; `meta.veracity.unverifiedPages` counts them, lists them in `unverifiedPageDetails`, and `evaluateVeracityGate` refuses to publish over any of them (`--force` unchanged). `NarrationWriteError` survives only for a beat that never produced a page at all. `narrate:<act>:<slot>` keys bank each slot as it is written, so a re-run pays only for the slots that never landed.

**`check-narration.mjs` needed no change, and the reason is worth recording.** It gates the hand-authored curation artifacts under `docs/curation/narration/<foray_id>/` (arc/threads/beats JSON), not a generated candidate — `finalizeForay` calls it with a repo root and no per-Foray input, as that module's own comment says. It never reads a `verified` field (`grep -n verified tools/foray/check-narration.mjs` is empty), and neither does `check-forays.mjs`; `forayItems.ts` does not emit one into a published item (its internal-field-leak guard covers that). So an unverified page passes both checkers today, and the ONLY thing standing between it and a listener is the veracity gate — which is exactly where F-51 put the decision. The least-invasive change was therefore no change to either checker.

### Run 3 — cider and wine

(pending)

## 3. KPIs (aggregate; per-run tables above)

Every number here is copied from a per-run table above; `—` means that run did not measure it. The four
columns are the only attempts that reached narration: run 1's attempt 4 (the as-found pipeline plus the
between-attempt fixes I-13/I-16), run 2's attempt 1 (the merged fix fleet WS-A/B/C/D/E/F), run 2's
attempt 2 (that plus #551 and #552) and run 2's attempt 3 (that plus #553/#554/#555/#565 — the first
attempt to reach finalize on the new pipeline).

| KPI | Run 1, attempt 4 | Run 2, attempt 1 | Run 2, attempt 2 | Run 2, attempt 3 | Run 2, attempt 4b |
|---|---|---|---|---|---|
| Outcome | FAILED at beat 23 of 31 (F-46) | FAILED at act 1, page p2 (F-50/F-51) | FAILED at act 1, the no-evidence page (F-60) | REFUSED at finalize: summary length (F-64), zero tape (F-65) — narration complete | REFUSED at finalize: M3/M4 same-episode rules (F-70) — first Foray with tape |
| Wall clock | 2 h 35 m | 33 m 07 s | ~40 min | 76 m 16 s | 91 m 29 s |
| Spine | 3 acts / 6 slots / 31 beats | 4 acts / 6 slots / 35 beats | same spine (calls 1–3 replayed) | same spine (calls 1–7 replayed) | new spine: 4 acts / 6 slots / 33 beats, 16 seeded from tape |
| Progress | 22 of 31 beats (acts 1–2 complete) | act 1 only; 12 pages, 10 verified by attempt 2 | act 1 only; 13 pages judged, 9 passed | all 35 beats; 33 pages, 30 verified, 3 kept unverified; stitched and finalized | all 33 beats; 28 pages, 22 verified, 5 unverified, 1 dropped; 5 tape; stitched and finalized |
| Pipeline model calls | 103 (6 reused + 97 fresh: 1 Opus, 2 Haiku, 100 Sonnet) | 34 (14 Haiku, 1 Opus, 19 Sonnet) | 34 (14 Haiku, 1 Opus, 19 Sonnet incl. 1 orphaned) | 73 (30 Haiku, 1 Opus, 42 Sonnet; 7 replayed) | 96 (45 Haiku, 1 Opus, 50 Sonnet; 2 replayed) |
| Narration calls per beat / page | 4.2 per beat (52 writer, 45 verifier) | 1.25 per page (15 for 12), plus 1 retrieval call each | — | 1.06 per page (35 for 33), plus ≈ 1 retrieval call each | 1.30 per page (43 for 33), plus 1.3 retrieval calls each |
| First-attempt page pass rate | 5 of 23 | 6 of 11 verified pages | 6 of 9 (first round) | 0.67 | 0.44 |
| Pipeline tokens (est., chars/4) | ≈ 70 k in / 22 k out | 50,662 in / 24,796 out | 41,725 in / 20,591 out | 96,848 in / 42,955 out (SDK usage: 139,803) | 123,446 in / 43,029 out (SDK usage: 166,475) |
| Subagent tokens incl. harness | 4,257,478 (97 agents, ≈ 44 k each) | — | — | — | — |
| Agent compute (sum) | 5,560 s | 4,847 s | 3,893 s (fresh calls) | ≈ 7,750 s | 13,461 s |
| Relay wall (sum) | 9,240 s | 4,863 s | — | 7,794 s | 13,500 s |
| Orchestrator share of relay wall | 52 % (act 1) → 21 % (act 2) after I-19 | ≈ 0 % | — | < 1 % | < 1 % |
| Beat kinds (`account` / `argument`) | — (the field did not exist) | 6 / 29 | 29 / 6 | 29 / 6 | — (seeded beats replace the split as the tape signal) |
| Tape anchors | 5 of 22 on topic | 0 of 35 (F-49) | 0 of 35 (every trace: `tier2:lineage`, F-59) | 0 of 35 (every trace: `tier2:window-overlap`, F-63) | **5 of 33** (16 seeded; 11 refused `window-overlap` after deepen paraphrase, F-68); 5 of 5 on topic |
| Fabricated or mis-attributed citations reaching the verifier | ≥ 6 (F-27, F-30, F-32 ×3, F-46) | 0 — the mechanical gate passed first time on all 5 selection calls | — | 0 — 86 of 86 checkable quotes grounded | 0 — 71 of 71 checkable quotes grounded |
| Pages kept / dropped / fatal | 18 / 4 / 1 | — | — | 33 / 2 (no evidence, kept as unverified hand-offs) / 0 | 28 / 5 unverified + 1 dropped / 0 |
| Pages passed as `purposeRevised` | — (no such flag) | — (no such flag) | 3 | 10 | 12 |
| Empty retrievals | — | — | 2 of 12 pages (F-60) | 4 first queries; 2 recovered by the rephrased retry | 12 first queries; 4 recovered (F-69) |
| Time until a listener could start | never | never | never | never (refused); `ttlA1Ms` = 76.3 min is the whole run (F-66) | never (refused; partial invalid F-71); `ttlA1Ms` = 91.4 min (F-66 fix merged after start) |

## 4. Things to fix, ranked

**The consolidated list is `generation-findings-tracker.md`** — one row for every finding F-01…F-62 and
every non-historical intervention I-01…I-23, with the PR and the file that closed it, or the home proposed
for it. As of the branch tip: **55 fixed, 13 on cards, 13 accepted, 0 open**. What follows is the top ten
of what is *not* closed, ranked by the severity each was logged with.

1. **F-61 — Critical.** The verbatim-anchor rule refuses every window: claims are written prose, tape is
   speech, so tape yield stays 0 even after WS-H reaches real transcripts.
2. **F-15 — High.** Nothing serves the partial candidate: there is no HTTP server in the repo, and Path B
   needs four founder decisions before a listener can start Act 1.
3. **F-47 — High.** Claude 5 runs adaptive thinking by default inside `max_tokens` caps of 400–2,000; a
   keyed run can truncate a JSON reply, and the budget guard under-counts.
4. **F-19 — Medium.** A Frame's 70–170 characters still cannot voice a contested source; narrowly defining
   *contested* mitigated the cost without resolving the conflict.
5. **F-48 — Medium.** Print evidence is the retrieval model's restatement, not fetched bytes, so
   `groundedQuoteRate = 1.0` overstates what has actually been checked.
6. **F-57 — Medium.** The ± 15 % runtime tolerance is a publishability condition nowhere in the code; a
   *medium* Foray can finalize at 25 or 100 minutes.
7. **F-58 — Medium.** `check-narration.mjs`'s digit, reference-leak and sentence rules never touch generated
   narration — a whole checker believed to be in the path is not.
8. **F-62 — Medium.** Symmetric padding to the 45 s minimum pulls roughly 28 s of off-claim tape in front of
   the passage the beat is about.
9. **F-55 — Low.** Duplicate slot titles collapse in the item mapping and can fail the contiguity rule at
   finalize, after every stage has been paid for.
10. **F-56 — Low.** The minted title is capped at 120 characters while `check-forays` rejects anything over
    18 words; the mismatch surfaces only at finalize.

The rest — **F-05**, **F-20** and **I-15** — are in the tracker, on cards WS-G and WS-J of the fix plan.

**Attempt 6 resumed and COMPLETED (2026-09-10; resume 04:20Z, relaunches 04:57Z and 13:22Z; candidate written 14:03Z).** Pipeline = generation branch at 2f41618. Resumed from the paused checkpoint (understand … source banked) with two interventions: **I-26** — the tape-yield replay found beat 2/0/0 sourced to a tier-1 *Causality* Hyatt Regency segment under an agent-engineering claim (4 generic shared words of 15 cleared the unweighted tier-1 bar; the on-claim seed window was never consulted because tier 1 runs first), so the checkpoint's beat was set to narration before act 3 was written; and the 57 empty evidence-cache entries left by attempt 6's budget-starved retrievals were purged so pages re-asked instead of inheriting `passages: []`. **I-27** — relay calls were answered by a Workflow (`relay-answer-loop`: poll the queue, one Sonnet-tier agent per call, ≤ 2 searches per retrieval) instead of a person; at ~06:15Z the orchestrating session hit its usage limit for ~7 h, every answering agent died, the relay timed out calls 33–38 (55 min) and the driver exited on `Request timed out.` — the checkpoint held acts 1–2 and the run resumed at act 3 at 13:22Z. Result: `built OK beyond-the-algorithm-engineering-production-ai-s-e6533b` — **51 items, 2,421 s (40.3 min), 10 tape segments (all *Practical AI*, tapeRelevance 1.0), 50/50 quotes grounded, purposeFidelity 1.0; 2 Hinge pages kept unverified (no evidence after two retrievals); ttlA1 686 s (11.4 min) on the relay** (report.json). Total answered calls after the resume: 8 (this session, by hand) + 31 + 33 (workflow). `check-forays` passed; the veracity gate refused on the 2 unverified pages (F-51 rule) and the publish used `--force`, recorded in the PR body — **the first generated Foray to reach `data/forays.json`** (PR #583, data-only re-base onto main of the CLI's `generate/…` branch, which was cut from the generation branch and would have dragged its 100 commits into the PR — a publish-path defect: F-75). It ships as `status: draft` (drafts are listenable in-app, badged).

**Findings from the resume.** F-75 (above): `publishForay.ts` branches from the checkout's HEAD, not from `origin/main`. F-76: the relay's 55-minute call timeout plus the SDK's own timeout turn an answering-side outage into a run failure with no resume — a keyed run has neither (I-27 is harness-only). F-77: evidence-cache entries with zero passages are cached as facts; a transient retrieval failure poisons every later attempt until purged by hand (I-26 second half).

**Landing on main (15:40Z).** PR #583 merged after three gate repairs the publish path did not anticipate: main's `check-forays.mjs` still carried the pre-#571 rule #65 (ported in the PR); the checker's tests pin the exact Foray id list, a curator running-order doc, labels and roles per Foray (the pins now apply to curated Forays; `generated: true` is exempt, with the candidate + report as provenance; the 'no DAI source' test became 'every DAI-source segment carries both anchors'); and **F-78**: `publishForay.ts`'s `mintedSegmentRow` omits `why`, `transcript_source`, `dai_suspected` and `batch_id`, which `merge-segments.mjs --check` (the pool CI gate) requires — filled by hand for the ten rows. Release build dispatched (run 34497366409).

**F-75 and F-78 fixed (FD-07, 2026-09-10).** `publishForay.ts` now fetches and cuts `generate/<id>` from `origin/main` (`--no-track`), refuses before validation if the checkout's three data files differ from `origin/main`, and refuses to push unless `origin/main..HEAD` is one commit touching exactly the files it wrote. `mintedSegmentRow` fills the four gate fields from where each is known: `why` = the beat's claim clamped to 18 words at a word boundary, ASCII punctuation (`mintedSegmentCopy.ts`, checked against the same copy rules the gate uses); `transcript_source` = what the cue provider read (`FileTranscriptCueProvider.transcriptSource`: `publisher` for a body with `source_url`/`regenerated_from: raw/…`, `asr-local` otherwise); `dai_suspected` = the minted `segment-sources` row's verdict, a thrown error when absent — never `false` by default; `batch_id` = `generation-<foray id>`. The PR body now says phones pick the Foray up on next launch after the deploy (no store build, FD-03), and `--report <report.json>` records the PR and `deploy_id` (`null` until known) on the candidate's row. `backend/test/mintedSegmentRow.test.ts` runs the real `merge-segments.mjs --check` over a minted row with one named mutation per field; `backend/test/publishForay.test.ts` proves the branch base. The `hold` default is unchanged — lifting it is the phase-2 decision (§1.3), not this card's.

**Run 4 — the history of India (2026-09-11 07:40Z, main @ ea33871, relay + `relay-answer-loop` Workflow).** Prompt: "The history of India: from the Indus Valley cities and the Mauryan and Mughal empires to the British Raj, Partition and the modern republic". Outcome `no-tape` after **7 calls / 4.4 min**: understand, research-shape, spine and deepen ran; sourcing refused every one of the beats at `tier2:lineage` — no show in the 17-show archive carries a history/India taxonomy node, so the lineage gate closed on all of them (the F-65 fail-fast ended the run before any narration was paid for, as designed). Reading: a supply result, not a pipeline defect; it matches G-18's picture (`docs/curation/corpus-tape-measurement-2026-09-10.md`). Next lever is Phase 1 supply (G-10/G-11) or a targeted fetch of India-history shows with timed transcripts into the archive (their DAI tier decides playability). Archived: `relay/run/out-4/report.json`.

**Run 5 — how AI systems get built, third pass (2026-09-11, main with #614 G-30 / #615 / #616 / #617, AI-systems prompt).** Sourcing placed **25 tape beats of 34** — the best yield of any run. Then G-30’s new abort-on-refused-partial default fired after act 1: `check-forays` on the act-1 PARTIAL (6 segments) reported `M4 FAIL: "practical-ai--open-source-self-driving-with-comma-ai" is 16.7 % of segments and 26.4 % of runtime, over the 25 % cap` and the run ended with reason `refused-partial`. The same artefact appeared on attempt 6’s partial (3 segments, so 33 % each). The share was true of the slice and false of the Foray: over the 25 segments the sourcing stage had already placed, that episode is one segment and roughly 7 % of the tape. **F-79.** M4 (both clauses), D3, D5’s IQR floor, D2’s end-of-Foray clause and D4’s share clause are not monotone under partial assembly — a later act changes their denominator — so the partial candidate now judges them on the projected whole: its own items followed by every later act’s planned segments (the sourcing stage’s pointers carry the segment, the episode and the cut) and band-midpoint narration estimates for the unwritten pages, run through the same `finalize` (`backend/src/generation/partialProjection.ts`). Every monotone rule — copy/shape/per-item, D1, D2’s run clause, D5’s triple clause, L2–L4, M3, the anchor rule — stays strict on the partial alone. `ruleScope` on the candidate and an `F-79:` warning line say which rule was judged where, and the partial’s own set-aside share lines are kept under `ruleScope.projection.supersededOnPartial` for the log. No rule text in `check-forays.mjs` changes; the abort default stays and now fires only on a failure the finished Foray would also have.

**Run 5 — the AI-systems prompt again, on main after G-24/G-25/G-30 (2026-09-11 07:50Z → 09:1xZ, relay + `relay-answer-loop` Workflow).** Sourcing: **25 tape / 34 beats, 28 seeded, all 25 through the seed** (attempt 6: 11 of 33) — G-24/G-25 measured. First attempt ABORTED after act 1 by G-30's new refused-partial default on a one-act M4-runtime share (**F-79**, fixed in #620, projection over the sourcing plan); resumed with `--continue-on-refused-partial` from the banked act 1. Whole run: 49 calls, 40.6 min relay wall, `ttlA1Ms` 8.3 s (artifact of the resume — act 1 was banked), grounded quotes 100 %, purposeFidelity 1.0, tapeRelevance 1.0, 2 pages kept unverified. Finalize: **INVALID on D5** — `tiny-recursive-networks#603 / model-context-protocol-deep-dive#2297 / federated-learning-in-production-part-1#1650` at 152.0 / 142.2 / 169.4 s, three consecutive durations within ±20 % (max/min 1.191). No candidate written; checkpoint + partial kept in `relay/run/out-5/`.

**F-80 — D5's consecutive-triple clause is not enforced at placement.** F-73 built the length ladder (105/165/135/210 s) and the tracker noted D5's IQR clause was ungated; the *triple* clause is ungated too, so a 25-segment Foray with three mid-length cuts in a row fails after the whole run is paid for. Fix: at placement (`sourceBeats.ts` cut-to-ladder), choose the next rung so that no three consecutive placed segments fall within ±20 % (mirror `check-forays` D5 exactly), and refuse the beat to narration when no rung can satisfy it. *Fixed (F-80 PR, §1 row): `d5Triple.ts` mirrors the checker's arithmetic and is pinned to it row by row; the clause is a rule at both tiers (`d5-triple`), tier 2 re-cuts the same window to the ladder's other rungs and then every cue-boundary length before refusing a candidate, and `lengthGate: "d5-triple"` on the tape-relevance row counts the placements the clause decided.* **F-81 — Frame pages for tape beats are dropped for stating what the tape says.** Eight Frames in run 5 (and two in attempt 6) were rejected three times for "declares no sources but its script states something about the world" (F-36/F-37/F-44) and dropped, so tape enters on silence. A Frame that introduces a tape segment is describing the tape, and the tape IS its source: let a Frame cite the segment it introduces (`sources: [{kind: "tape", segment_id}]`), have the verifier check the Frame's statements against the segment's transcript window, and keep the no-source rule for every other page kind.

**Run 6 — the AI-systems prompt on main after F-80/F-81 + G-32/G-34/G-35 (2026-09-11 ~10:35Z → 11:30Z, relay + Workflow, 74 calls answered in 16 rounds).** `built OK how-ai-actually-gets-built-3b83e1` — **50 items, 2,624 s (43.7 min), validated at finalize (first run to pass every gate with no hand repair)**; 72 calls, 55 min relay wall, `ttlA1Ms` 21.2 min (relay), firstAttemptPassRate 0.73, **0.8 narration calls per beat** (G-34), retryRounds 5, **0 pages dropped** (F-81: Frames cite the tape), 4 Hinge pages kept unverified, groundedQuoteRate 1.0, purposeFidelity 1.0, tapeRelevance 1.0; prefetch 30/30 pages, narration hit rate 1.0, narration-time retrieval 176 ms (G-35). Sourcing: **18 of 30 beats seeded, 11 tape** — down from run 5's 28/25 on the same prompt with the same archive: the spine (a relay subagent at the Opus tier) seeded fewer beats this time, and F-80's `d5-triple` placement gate is now live; the per-beat trace (`lengthGate` / `d5-triple` counts) is the watch item the F-80 row named. Published with `--force` past the veracity gate (4 unverified Hinges) as a test-track draft.

**F-82 — Hinge pages that restate what the tape said are kept unverified for want of web evidence.** All four unverified pages in run 6 are Hinges paraphrasing the surrounding tape ("One host argued …", "An engineer described …"); two retrieval queries find nothing because the claim is the episode's own content. F-81 admitted a `tape` source for Frame/Hinge/Marker but the writer did not cite it on these Hinges (the Hinge's holding tape is the segment it follows, not the one it introduces). Fix: give the Hinge both adjacent segments' windows as citable tape sources and instruct the writer to cite the tape when the Hinge restates it; then the F-51 rule holds without a web query. Also note **F-83**: `ttlA1Ms` on the relay (21 min) is dominated by act 1's 8–16 serial calls at ~2 min each; the keyed estimate stands. *Fixed (F-82 PR): a page holds the windows of BOTH adjacent segments in play order — `adjacentTapeFor` in `writeNarration.ts`, slot edges included, each a tape document under `tapeDocIdFor(segmentId)` with its position (`previous`/`next`); the prefetch stage builds its beats through the same `evidenceBeatFor` and keys the memo on the neighbours, so the G-35 hit rate stays 1. The writer's shared selection rules say a Frame/Hinge/Marker that restates, summarises or attributes what the tape said MUST cite that tape — never an outside publication, never nothing — and the verifier is handed the window the source NAMES and told to judge against it and not the other. The gate and the structural validator resolve a tape source against the window it names, and a phrase the previous segment said, cited to the next, is refused with the window that does say it. A content beat with no print but a neighbouring window is written as a Hinge from that tape instead of degraded (F-60's placeholder was the run-6 symptom); a content beat that found print is unchanged. `meta.veracity.tapeCitedPages` counts the pages that cite tape. `backend/test/hingeTapeSource.test.ts` pins it, the four run-6 claims included.*

**F-84 — A tier-2 mint at a start the pool already holds was suffixed, and the pool gate refused it.** Run 6's publish (PR #624, 2026-09-11) minted `practical-ai--ai-policy-and-the-battle-for-computing-power#826-2` (826.36–957.07 s) while `data/segments.json` already held `#826` (826.36–921.04 s), minted by the previous generated Foray on the same episode. `mintSegmentId` resolved the id collision with a numeric suffix (mirroring `mintItemIds`), the publish's "skip ids already on disk" rule saw two different ids and wrote both, and `merge-segments.mjs --check` refused the row ("id does not match its item_id + start_sec"). Fixed by hand in #624: the Foray repointed at `#826` and its `runtime_sec` cut by the 36 s difference (2,624 → 2,588 s). Tier 1 never saw the pool row: the seed pass runs before it (G-24 R2), and its claim-overlap bar had not admitted the row on the earlier pass either. *Fixed (F-84 PR): the pool's id is its start, so a start the pool holds is the pool's. At sourcing, before a tier-2 segment is minted, `committedCutAt` asks the pool (and this run's own mints) for a row of the same episode beginning within 0.5 s or on the same rounded second (`startsCoincide`, `segmentPoolLookup.ts`); if one exists the POOL'S cut is placed under the pool's id — tier 1 in the pointer, nothing minted, no source row, no re-cut — after the same ledger a tier-1 hit faces (already played, M3, D2/D3/D5/M4), and a pool cut the ledger refuses refuses the candidate with the new gate `pool-cut` rather than minting beside it. The row is REUSED, not extended to the longer window: the previous Foray's `runtime_sec` was computed from that row's `end_sec`, and lengthening it would fail that Foray's runtime check in the same commit. `mintSegmentId` no longer suffixes — it throws, since a collision is decided before it is called — and the D5 spread pass will not re-cut onto a taken start. At publish, `mintedPoolCollisions` (`finalizeForay.ts`, asked by both `buildCandidateFiles` and the write in `publishForay.ts`) refuses a minted id that is not `item_id#round(start)`, a row at a start a committed row of the same episode holds, and a row that would shadow its on-disk twin with a different cut; the identical twin is the idempotent re-publish it always was. `runtimeSecFor`'s pool is now `[...minted, ...disk]` so the committed row wins an id tie and the Foray is timed on the cut that plays. `tapeRelevance` rows carry `poolCut: "reused"` so a run can count the beats the previous Forays' tape served again. `backend/test/mintDedupe.test.ts` (11) pins it, the run-6 numbers included.*

**Run 7 — engineering disasters, on main after F-82 (#626) (2026-09-11 ~12:00Z → ~13:00Z, relay + Workflow).** Two attempts, neither reached deepening. **Attempt 1** died inside research-shape with `list.push is not a function` after the understand call: the transcript text index kept its postings in a plain object, and the spoken token "constructor" — ordinary speech on an engineering episode — resolved to `Object.prototype.constructor` instead of a posting list. **F-85**, fixed in #627: postings are a null-prototype map, a cached index parsed back from JSON is re-keyed the same way, and the walk uses `Object.hasOwn`; `transcriptTextIndex.test.ts` pins "constructor", "__proto__", "hasOwnProperty" and "toString" as words like any other. **Attempt 2 (~13:00Z)** ran understand and research-shape and then the spine's one Opus call was refused by the structural gate before deepening — `Spine failed the structural check before deepening (F-13), so no deepen call was made: act 2 ("The Anatomy of a Defensible Shortcut"), slot "Specs are guesses about the future": beat claim is 2 sentences, must be one — "The epoxy that held the Big Dig's ceiling panels wasn't chosen recklessly; it passed the tests it was given. The failure…"` — and the driver recorded `error` and ended the run (G-30's resume loop is right to treat a gate refusal as non-transient). Checkpoint kept `understand` and `research-shape`; no candidate.

**F-86 — a fixable spine reply fails the whole run.** The gate did its job: one beat, two sentences, exactly the defect F-41's page-writer cannot absorb. What was wrong is what happened next — nothing. A 70-call run stopped on a defect the model would have fixed in one turn, and the only way forward was a fresh run repaying the spine. The pre-F-86 requirement said it outright (§3.3.4: "Retries: none at this stage … the spine is not re-asked automatically"). *Fixed (F-86 PR): `buildSpine.ts` re-asks the builder ONCE when the reply fails the structural gate — `SPINE_STRUCTURAL_REASKS`, default 1, `0` is the old behaviour — with the exact violations appended as a "fix only these" turn on the same conversation (the prompt, the refused reply as the assistant's own turn, one user turn naming the violations: `buildSpineFixInstruction` in `AnthropicSpineBuilder.ts`), so the model keeps every act, slot, beat and seed the gate did not name; the gate runs again on the reply and only then does the run fail, with the same F-13 message. The gate is not loosened: one sentence per claim stays. The re-ask meters itself as any spine call does (`operation: "spine_build"`), the checkpoint banks only the spine that passed, the run log prints the violations at the moment the re-ask is decided, and `report.json` carries `spineReasks` (attempt number and violations) on the row. The stub can be told to write run 7's two-sentence claim (`twoSentenceClaimReplies`) so the path is fixtured; `backend/test/spineReask.test.ts` (7, mutations named) pins re-ask-once, second-failure-fails, the env cap, clean-never-re-asks, the banked spine, the three-turn conversation and the instruction text.*

**Run 7, attempt 3 (2026-09-11 ~15:20Z), on main after F-86 (#629).** The spine passed and the run went the distance: **34 beats, 12 seeded, 12 tape**, deepening, and **81 calls** of narration with every act in flight at once (G-32) — and then the ordered stitch loop reached act 1's partial and `check-forays.mjs` refused it: `causality-engineered-network--1-bp-texas-city#2292: end_sec 2375.72 is past the episode's 2071 s`. G-30 aborted the run (`refused-partial`); checkpoint kept through narration, no candidate. Sourcing had minted the segment from a *Causality* transcript whose timeline runs past the feed's declared duration — the class **#315** describes, on the very episode #315 measured (declared 2,071 s, transcript to 3,033 s, ratio 1.464, `span_implausible` false at the 1.5 bar) — and nothing between §4.5 and the partial gate asked the question. The cost of G-32's ordering was visible for the first time: act 1's partial is judged only after all four acts' narration has started, so the 81 calls were spent before the first partial was read.

**F-87 — sourcing mints a cut that ends past the episode's declared duration, and the checker finds out after every act is narrated.** `check-forays.mjs` refuses an `end_sec` past the source row's `duration_sec` (the number `audioSourceLookup.ts` copies from the digest's `feed_duration_sec`), and the D5 spread re-cut already declined to *lengthen* a cut past it — but the placement itself never asked, so a window from a transcript that overruns its audio was cut, minted, narrated around, and refused five stages later. The tempting fix — clamp the cut at the duration — is the wrong one: #315's point is that a transcript overrunning its declared audio may be *shifted* rather than merely longer, and a shorter cut of it would pass every gate and play the wrong words; ADR-0008 measured feed duration and last cue agreeing within a minute on every honest row. *Fixed (F-87 PR): at sourcing, a window whose cut would end past `feed_duration_sec` (when known) is refused with the new gate `past-duration` — the first question asked of the cut span, before M3 and the length ledger (`pastDurationGate` / `placementGateFor` in `sourceBeats.ts`); never clamped; the beat falls through to the next candidate or to narration; the trace row carries both numbers (`spanEndSec`, `feedDurationSec`), the narration reason names the rule, and the transcription-queue row says a local transcription of the delivered audio would settle the timeline. Research-shape no longer offers such windows to the spine (`tapeWindowsFor` drops a window whose `endSec` is past the duration and any `span_implausible` digest, and says so in `windowsUnavailable`), so §4.3 cannot seed a claim from tape §4.5 is bound to refuse. The projection (#620) and the per-act partial inherit the rule for free. G-32's ordering stays — the narration was the right narration; only the tape was wrong — but `report.json` now carries `refusedAtAct` (`{act, totalActs}`) on a refused-partial abort so the cost is countable. `backend/test/pastDuration.test.ts` (7, mutations named) uses run 7's own numbers (2375.72 s vs 2071 s) as the fixture; one research-shape test and the hands-free driver test cover the other two sites. #315 stays open: telling "longer" from "shifted" needs a ranged GET against the audio (the prepare stage, #109); this gate refuses the class at the cut, it does not diagnose it.*

**Run 7 attempt 4 — engineering disasters, on main after F-87 (2026-09-11 ~16:00Z → 17:05Z, relay + Workflow, 84 calls answered in 18 rounds).** `built OK the-chain-reaction-how-engineering-disasters-rea-25f1b7` — **49 items, 1,724 s (28.7 min), validated at finalize** (the second Foray to pass every gate with no hand repair; the first on a second subject). 84 calls, 64 min relay wall, `ttlA1Ms` 20.4 min (relay); groundedQuoteRate 1.0, purposeFidelity 0.95, tapeRelevance 1.0, tapeCitedPages 13 (F-81/F-82 in use), pagesDropped 2 (two Frames: a Davis-Besse precedent purpose the writer never landed; a contested source the script did not voice — §4.7 rule 3, F-19 class), **firstAttemptPassRate 0.55, retryRounds 10, unverifiedPages 10, narrationCallsPerBeat 1.03**. Sourcing: 12 tape / 34 (12 seeded) from *Causality* — the disasters archive is one show, as G-18 measured. Published `--force` past the veracity gate (10 unverified pages) as a test-track draft.

**Watch item F-88 — ten pages kept unverified on the disasters prompt.** Unlike run 6's four (tape restatements, fixed by F-82), these are content pages whose print retrieval returned nothing for claims about specific incidents (the run log shows repeated 'nothing was retrieved … by either query'); the disasters domain has ample print (NTSB/CSB reports) but the two-query retrieval is not finding it. Next lever is retrieval quality (query rewriting toward the incident's proper nouns, or a third query built from the tape's own terms), not the gate.

*Re-read (2026-09-11):* nine of the ten were not incident claims at all but the Foray's **thesis** — "Most retellings of engineering disasters compress months or years of decisions into a single moment", "Treating a disaster as one bad decision by one bad actor is comforting", "A design substitution is rarely made carelessly…", "The next time a headline calls something 'a single catastrophic failure'…". Each generalises across the cases the Foray's other, verified pages establish; print will never hold that sentence, and F-82 does not reach it because it restates no one segment. Every one was an F-60 hand-off (`mode: Hinge`, `unverifiedReason: "no-evidence"`, placeholder script). **Lever (1), synthesis verification, is built** (`backend/src/generation/synthesisVerify.ts`): after retrieval has failed such a Hinge or Frame — never instead of it, and never for a Patch or Carry — the page is written from the Foray's own tape-cited or print-verified pages as its documents (through the same drafting path and mechanical rules every page pays) and the verifier is asked, in one call of the same family, whether it is a fair generalisation of those pages and only them, answering with the page ids it rests on or refusing. The page records `verification: { kind: "synthesis", restsOn, attempt }`; `meta.veracity.synthesisVerifiedPages` counts them beside `unverifiedPages`, the gate treats them as verified, and the publish PR says "verified by synthesis of pages X, Y, Z". A Hinge naming a case no verified page covers stays unverified. The step sits in `runPipeline.ts`'s ordered loop between an act's narration landing and its stitch (`synthesis:<i>` checkpoint), because it needs the whole Foray's pages and the stitcher owns a page's edges; an act holding such a page waits for the other acts' narration to settle first. Retrieval quality for the one genuine incident page remains the open lever.

**F-89 / F-90 — the first Foray with a jingle item broke three consumers at publish, and would have loaded a TBD asset (run 7 attempt 4, publish PR #632, fixed in #634).** The pipeline placed one authored jingle (`jingle-cut-act-4-1`) at an act boundary — the first in `data/` — and CI on the publish PR went red in three places that had never seen one: the lock-screen metadata gave a jingle an empty publisher credit (`player/media-session.js`), the checker test asserted the listener's clock as tape + narration while the checker itself already counted `jingle_sec`, and the bundle-slice alarm (bundled bytes < 50 % of the file on disk) fired honestly because three generated Forays now reference more than half of an 82-row source registry — restated as set identity. Reading the queue code for the credit fix showed `JINGLE_ASSET_URL` still read `TBD:jingle-asset`, so the phone would have failed the load at the boundary; it now names the interlude placeholder (3.0 s asset; `JINGLE_DURATION_SEC` stays 1.5 s until the real asset is cut). Lesson for the roadmap: every first-of-a-kind item the generator can emit needs a committed fixture Foray in `data/` before a real run emits it — the checker passed, the consumers did not. Publish-gate defects on the three generated Forays so far: F-78, F-84, F-89, F-90 — all found by CI on the data PR, none by the pipeline's own validate step.

**F-91 — run 8 attempt 1 stopped NO TAPE on a topic the archive was never allowed to carry (2026-09-11 ~22:55Z; fixed in this PR).** Prompt: "What engineers actually do all day: how engineering careers really work, from the first job and the first failure to leading a team, told by working engineers". Seven model calls (the Opus spine call and four Sonnet deepen calls among them), ~5 min, 0 of 29 beats with tape, 0 seeds; every beat traced `tier2:text-index:no-candidate`. Mechanism: `resolveTopic` scored `business/careers` 1.5 on the single token "careers" over `computing/software-engineering` and `engineering/ai-robotics` at 0.75. The only show with transcript supply for the subject, *Being an Engineer* (334 indexed episodes; the text index returns strong hits for career-shaped claims when unfiltered), carries `engineering`-rooted nodes, so `familyGateAllows("business/careers", …)` was false for every entry of every show and `archiveIsUsable` in sourceBeats.ts vetoed the whole field before a single body was opened; §4.2's map was filtered by the same topic, so the spine had no seeds either. "The history of India…" fails identically through `cities/urbanism` (token "cities"). This is the topic-lineage collapse G-24 named — the gate is right (run 1 sourced a disasters beat to a *Geology Bites* episode without it); the topic feeding it was a lottery. Fix (`backend/src/generation/topicSupply.ts`): the topic is now decided ONCE, before the research map and the spine, from the prompt + subject + angle. `measureTopicSupply` counts per candidate how much tape the lineage gate admits (archive entries through `nodesForArchiveEntry`, pool segments through `nodesForPoolSegment` — the predicates §4.5 already applies, now shared exports of `taxonomyFamily.ts`; narrowed to entries with a transcript body when the cue provider can say so), and `chooseTopic` keeps the resolver's pick if it has ≥ 3 usable transcripts, otherwise moves to the highest-scoring candidate within 0.5× the best score that does (a child whose whole case is a generic token also stands in for its root, which is what reaches *Being an Engineer* under main's classification, `engineering/precision-mfg`); with no supply in any candidate's family the run stops `no-supply` before the spine, having paid for one Haiku call. Run log: `topic: business/careers -> engineering (supply-aware: 0 vs 334 usable transcripts; candidates …)`; `report.json` carries `topicDecision` and a `no-supply` row says `stopped before spine`. Replayed on run 8's data the prompt now proceeds under `engineering` with 334 usable transcripts; the India prompt stops `no-supply` naming `cities/urbanism=0`. Caveat for anyone reading main's data rather than the generation machine's: there `business/careers` admits 990 breadth digest rows of *Becker's Healthcare Podcast* (classified at its `business` root) that no text index has a body for — which is why supply is counted on bodies whenever a body source is wired, and why the report names the basis.

**Run 8 attempt 2 — engineering careers, on main after F-91 + #641 (2026-09-11 23:51Z → 00:37Z, relay + Workflow, 78 calls in 26 rounds).** `built OK what-engineers-actually-do-all-day-e08236` — **56 items, 2,522 s (42.0 min), validated at finalize; the third Foray to pass every gate with no hand repair, on a third subject and a third show.** `topic: business/careers (best: 334 usable transcripts, basis bodies)` — the F-91 decision line, first time printed. Sourcing: **26 of 32 beats seeded from the research map, 16 tape / 16 narration (all 16 through the seed)** — the best tape yield of any run (run 7: 12/34) and the first where G-25 seeding carried every tape beat; the show is *Being an Engineer*, admitted by #641's `business/careers` node. Veracity: groundedQuoteRate 1.0, tapeRelevance 1.0, tapeCitedPages 24, purposeFidelity 0.83, firstAttemptPassRate 0.68, retryRounds 10, narrationCallsPerBeat 1.13, pagesDropped 1, **unverifiedPages 4, synthesisVerifiedPages 7 (F-88's first live run: seven thesis Hinges that run 7 would have left unverified now rest on named verified pages).** The four still unverified are Hinges whose purpose names a guest's own story ('Joe Couitt's engineering path started at eighteen, mopping floors…') that the writer restated without the specifics the purpose asked for — a writer-purpose mismatch, not a retrieval miss (F-93 watch item). ttlA1 34.0 min (relay) — the F-88 wait-for-all-acts cost is visible here and is the next cycle-time lever. Published `--force` (veracity gate, 4 pages) as PR #642; **the G-21c suite gate made its first real catch** — see F-92.

**F-92 — the fourth generated Foray tripped the bundle budgets through the G-21c gate, the gate's first real catch (measured 2026-09-12 00:40Z on PR #642; fixed in this PR).** `publishForay` wrote `what-engineers-actually-do-all-day-e08236` (+16 minted segments, +16 source rows) and the new real-data suite went red in five bundle tests: `shell-invariants.test.mjs` "the real data/segment-sources.json slice is 42.4 KB, over its budget" (40 KB), and four `prepare-webdir.test.mjs` REAL REPO tests that all share `prepare()` throwing on the same budget. With PR #642's data on main's code the seed weighed forays.json 100.7 KB (copied whole, 8 Forays), segments.json 78.1 KB of 100 KB (123 referenced), segment-sources.json 42.4 KB of 40 KB (73 episodes) — and all four generated Forays were drafts, which only the founder's switch (`cp_show_drafts`, #631) can list. `prepare-webdir.mjs`'s own header says re-baselining a budget is not a fix and that the bundle carries a CAPPED slice while the directory carries everything (FD-04). **The rule:** the offline seed carries every Foray except a generated draft — `seedCarries(foray)` in `tools/mobile/prepare-webdir.mjs`, one exported predicate; `data/forays.json` is no longer `COPIED_WHOLE` but projected (`seedForays`, verified on disk by `assertSeedForaysComplete`, budget 64 KB), and the two segment slices are computed against the SEEDED document rather than the repo's. A draft reaches the phone through the directory on first launch, so the seed's `data/forays-directory.json` is written with `partial: true` (`seedPointerDoc`) and `player/foray-directory.js` no longer answers `current` for a partial held set at the live version — a fresh install built from the same deploy as the site fetches the whole set once, caches it whole, and is `current` from then on (page-level test in `test/foray-directory.test.js`; unit test in `player/foray-directory.test.js`). **The numbers, PR #642's data:** forays.json 100.7 KB → 17.0 KB of 64 KB (4 of 8 Forays; `bundled_from.directory_only` names the four drafts), segments.json 78.1 → 46.9 KB of 100 KB (76 referenced), segment-sources.json 42.4 → 20.6 KB of 40 KB (30 episodes); the two mobile suites 126 pass / 5 fail of 131 → 134 / 0. On main's data the bundle is 1.78 MB of 3 MB. What the seed now grows with is PUBLISHED generated Forays (~20 KB of narration each into forays.json plus their segments), and the 64 KB budget is the alarm for when the seed needs "the newest N published" as its rule rather than a bigger number.

**F-94 (Q-01/Q-04) — clips extend to thought boundaries and run while relevant (60-1,800 s); the placement rules re-derived for those lengths (2026-09-12, listening-quality deck, cards Q-01 and Q-04).** Wyatt's listening notes on the four generated Forays: clips "trimmed too aggressively", opening mid-answer and cutting off as soon as the claim's words ran out — `selectTapeWindow` sized a 30-180 s window to cover the CLAIM's vocabulary, `cutWindowToSegment` grew it by claim-overlap to a ladder rung (F-73/F-80), and nothing asked where the speaker's thought started or ended (122-139 s mean, 31 s shortest, on `main` @ #642). **Q-01:** a new `backend/src/generation/tapeExtent.ts` (`extendToThought`) runs after the claim window clears its relevance floor and before the cut: the start moves back to the speaker's turn start (the archive's normalized bodies carry `speaker` per cue — *Being an Engineer* 334 of 337 — now read through `TranscriptCue.speaker`) or, without turns, to the previous sentence boundary marked by a pause (`THOUGHT_PAUSE_SEC` 0.7 s; median inter-cue gap 0.12 s, 80th percentile 1.26 s on S5E50) or closing a paragraph-sized cue (`PARAGRAPH_CUE_SEC` 30 s — the same show's bodies hold 57-173 s cues whose break IS the paragraph); then further back and forward in `RELEVANCE_WINDOW_SEC` (60 s) windows while the idf-weighted share of the claim's plus the act's thesis's terms (the search's own `claimTermWeigher`) stays at or above `RELEVANCE_FLOOR`, stopping at the first drift or hole in the tape and backing up to the boundary before it; a host's question under `QUESTION_TURN_MAX_SEC` (60 s) that prompted the answer opens the clip. Floor measured on the run-8 candidate with the show's text-index idf: minutes inside the claim windows 0.30-0.58, continuing answers 0.09-0.27, off-topic minutes 0.00-0.07; the sixteen extents are identical at 0.07 and 0.08, 0.06 admits one more minute on one clip, and 0.10 loses continuing answers on four — **0.08**. Bands: the claim SEARCH keeps 30-180 s (`TAPE_CLAIM_SEARCH_MIN/MAX_SEC`, the values it was measured with — a search band of 1,800 s would rank the window that is about everything first); the TAPE WINDOW is `TAPE_WINDOW_MIN_SEC` 60 s (was 30) to `TAPE_WINDOW_MAX_SEC` 1,800 s (was 180), and `MIN/MAX_TAPE_SEGMENT_SEC` are now those (were 45/240). The floor is a target the cut grows towards through claim-sharing cues (F-62 unchanged), never a pad: a 50 s thought between strangers stays 50 s. Edges are still cue boundaries, so the anchors are minted exactly as before and the ad-insertion constraint is untouched. The minted row carries `boundary` (`turn` / `sentence` / `claim-only`, the weaker of the two edges; `claim-only` also when floor growth moved an edge) and `extended_by_sec`; the relevance row and the trace row carry the same two; `NewSegmentSchema`, `mintedSegmentRow`, `check-forays.mjs` (`segment.boundary` enumerated, G-21c, three `KNOWN_UNCOVERED` entries until a Q-01 Foray publishes) accept them. **Measured on the run-8 candidate's 16 clips** (old cut vs the same claim window re-found inside it and extended; claim + slot title as the thesis, the act thesis not being in the candidate file): mean 122 s -> **213 s**, longest 177 -> 671 s; 12 longer, 3 unchanged, 1 shorter (#10 177 -> 112 s: the ladder had grown it on the trade's everyday words, which the idf weighting discounts); boundary `turn` 10, `sentence` 5, `claim-only` 1 (a clip the floor growth carried past its boundary); every extent still minted both anchors. The table is in the Q-01/Q-04 PR. **Q-04:** each length rule re-asked what listening purpose it serves. D1 (starts in a rolling 600 s window) kept — ad-safety. D2 kept — a tier-2 clip is never under 60 s now; the rule still binds hand-cut tape. D3 (mean >= 90 s) **retired** — served per clip by the 60 s floor; `mean_sec` still reported. D5 **restated as one clause: no two consecutive clips within +/-20 % of the same length** (`d5UniformPairs` in the checker, `d5Pair.ts` in sourcing, pinned row by row by `test/d5Pair.test.ts`; the triple clause, the IQR floor of 45 s, the 105/165/135/210 s ladder and the post-placement re-cut pass are gone). It is stricter than the triple (a uniform triple holds two uniform pairs) and every Foray on `main` holds 1-4 adjacent pairs inside the band (hand-cut 3/1/2/4, generated 1/3/1/3), so the checker **gates it only on tape cut under Q-01** (a generated Foray with `boundary` on a played row) and **reports it on older tape** (`d5_uniform_pairs`, `d5_gated`); all eight committed Forays stay green. At placement a tier-2 window whose full thought would pair with the previous clip is extended again to the longest thought under `previous / 1.2` (`d5EscapeBelow`) — still at a boundary, still only relevant tape — and the row records `lengthGate: "d5-pair"`; a window no shorter thought of which escapes is refused (`d5-pair`), a fall-through like every gate. M4: the count clause kept; the runtime clause restated — **at most one clip over `M4_LONG_CLIP_SEC` (300 s) per episode, and an episode's tape seconds BEYOND ITS LONGEST CLIP at most 25 % of the tape** — because a 1,800 s let-it-ride clip is over 25 % of any Foray under two hours of tape by construction and the founder asked for exactly that clip; the number is unchanged, what it is measured on leaves out the one clip the founder allowed and holds everything else to the old line (two 290 s clips from one episode in 2,000 s: 14.5 %, pass; four: 43.5 %, refused, as before). Old vs new: D3 90 s -> none; D5 triple +/-20 % and IQR 45 s -> pair +/-20 %; M4 25 % of runtime -> 25 % beyond the longest clip plus one long clip; ladder 105/165/135/210 s -> none; `TAPE_WINDOW_MIN/MAX_SEC` 30/180 -> 60/1,800; `MIN/MAX_TAPE_SEGMENT_SEC` 45/240 -> 60/1,800. Legacy trace spellings `d5-uniform` and `d5-triple` read as `d5-pair` on resume; `d3-mean` stays in the gate unions, never emitted. Not done here: no generation run (the deck holds runs until Q-06), so the "re-sourced run-8 candidate passes with zero rule errors" acceptance is measured by the extension table and the unit suites, not by a run; and the checker's Q-01 detection is by row field, so a Q-01 Foray that reuses only pool cuts (F-84) is judged as older tape.

**F-95 (Q-02/Q-03) — narration is written per act and verified per beat; every clip gets a light introduction (listening-quality deck, 2026-09-12; this PR).** Wyatt, after listening to the four generated Forays: "The AI narration script was super clunky. It seems the focus is definitely on explicitly hitting all the beats rather than telling a smooth, cohesive story" and "There was inadequate, if any, introduction to podcast segments … the AI narrator would make the point from the previous beat then it would cut to the part of the podcast that made the point." Measured cause (deck §1): one page per beat, verified per page, with no stage that ever read two adjacent pages together — 38–40 pages per Foray over 10–16 clips (`nnnnnnnSnS…`), 2.35–3.45 pages per seam, narration 22–41 % of runtime [measured on `data/forays.json` @ #642]; and F-81/F-82 made the page before a clip *cite the tape* by restating it, so the narrator said the point and then the guest said the point. **The new writer contract** (`backend/src/generation/writeAct.ts`, `actSeams.ts`; prompts in `AnthropicNarrationWriterBuilder.ts` `buildActWritePrompt` and `AnthropicNarrationVerifierBuilder.ts` `buildActVerifyPrompt`): the writer receives the WHOLE ACT — its clips (opening text from the start anchor on, show and episode from the segment source row or the pack's tape context, the transcript window as a document), its evidence, its beats with their claims — laid out in play order as SEAMS (the narration before the first clip, between two clips, after the last), and writes continuous prose in one voice, one script per seam: the Intro before each clip, the bridges, the argument. The beats are the checklist the prose must carry, not the template it fills. Every mechanical rule the per-page path built runs on every seam (`gateSelectedClaims`, `sourcesFor`, `validateNarratedBeat` with the seam's own band — the sum of the beats' bands plus an Intro's, capped at the 150 s soft max — the slug guard), then ONE verifier call answers per BEAT (`carried`: does the act's prose, any seam, or a clip carry the claim, supported) and per seam (support, contested). A missed beat comes back as a NOTE naming it and its claim ("the prose does not carry beat b3: …; add it where it belongs"); the retry hands the writer the act again with every seam's previous script and edits it. Three rounds per act at most; a clean act is two requests. F-51's failure policy holds at the seam (last clean draft kept unverified, the gate refuses); F-88 is unchanged. **Intro (Q-02)**, a seventh mode: one or two sentences — who is speaking, on which show, what to listen for — written from the clip's OPENING, never its point. Three weights decided in code from the tape (`decideIntro`): `none` when the host's own introduction is in the clip's first minute (`hostIntroducesGuest`, explicit patterns), `light` (one clause or nothing) when the previous clip was the same episode, `full` otherwise. Verified structurally, not by a model: a full Intro must name the show or someone the episode title names (`introNamesSource`, waived with a log line when the source row names nothing — the dry-run stub's tape), and NO seam before a clip may repeat the clip's first sentences (`introRestatesClip`, a six-word run against the first sixty words); both are refused in code before any verifier call. The restate-the-tape rule of F-81/F-82 no longer applies to the page before a clip; the page after one may still restate it once, cited to the window (a seam page is tape-citable whatever its mode, `tapeCitable`). **Data model**: `narration` items with `script`, unchanged — there are simply fewer and longer of them. In `WrittenAct` a seam's page sits on the seam's first narration beat (or as an intro-only seam's clip `connectiveNarration`, mode `Intro`); the seam's other narration beats carry `carriedBy` and no page; `stitchAct` emits one item per seam. `narrate:<i>` keys from runs 1–8 parse unchanged; a fresh run writes no `narrate:<i>:<slot>` key (the act is the unit of writing) but still reads a fully banked set of them; the per-slot path remains the fallback for a writer or verifier without the act contract and F-88's drafting path. The prefetch now gathers every tape beat's window (a same-episode continuation included), so the memo hit rate stays 1. **What the KPIs now mean** (`veracityMetrics.ts`, `report.json meta.veracity`): `firstAttemptPassRate` is measured against BEATS — the share of narration beats whose claim the verifier confirmed on round 1 (`verifiedAtAttempt`), not the share of pages accepted first time; `pagesDropped` counts a clip with no seam page before it in play order (a seam page on the preceding narration beat is not a drop); `callsPerBeat`'s per-page denominator is the old page count and reads high by construction — read `narrationCallsPerAct` (writer + verifier requests per act, ≤ 3 on a clean pass) and `narrationCallsPerBeat` instead; `purposeFidelity` averages seam pages' `purposeAccomplished`, which is now "every beat positioned in the seam was carried". New (Q-05): `narrationPagesPerSeam` (target ≤ 1), `narrationShare` (narration seconds / tape + narration at 17 chars/s, target ≤ 0.25 — narration-craft §0's whole-Foray target), `tapeShare` (proposed ≥ 0.70), `introRestates` (must be 0). Not measured on a run — the deck forbids a generation run before Q-06; the before numbers above are the four committed Forays, and the run-8 candidate file is not in the repository. Tests: `backend/test/actNarration.test.ts` (24, one named mutation each: a four-beat two-clip act passes on all four in two calls; dropping a beat is red; the Intro present/absent/light cases; a restating Intro refused; the KPIs). Design doubts for Q-06's listening: (1) thesis beats that generalise across the whole Foray are verified against the act's own clips and print, and the cross-act synthesis pass (F-88) no longer reaches them because the act writer never produces a `no-evidence` Hinge — if run 9's report shows such beats unverified, F-88's eligibility should admit a seam page whose uncarried beats had no print; (2) an act refused in code three times keeps its clean seams unverified too (they never reached the verifier), which is honest but costly — the retry note tells the writer which seam it was sent back for.

**Run 9 — the Q-06 listening candidate: the run-8 prompt on main after Q-01..Q-05 (#646, #647), 2026-09-12 04:38Z → 05:10Z, 69 calls, relay + Workflow.** `built OK what-engineers-actually-do-all-day-731735` — 32 items, 32.2 min. **Narration moved the way the deck intended: 21 pages (run 8: 40), 0.86 per seam, 122 words each, two light Intros, `introRestates` 0, narrationCallsPerAct 4.25 (≈17 narration calls; run 8 spent ~45).** **Tape did not: 10 clips, mean 107 s, max 188 s, tape share 56 % (run 8: 16 clips, mean 122 s, 78 %).** Three causes, all in the sourcing lane's first pass (F-96): (1) `extended_by_sec` on the minted rows is 0–40 s — the relevance walk stopped almost at once live, though the same code measured 122→213 s offline on the run-8 candidate; (2) four of ten clips came through the seed path, which never calls `extendToThought`; (3) every slot's top rejection was `tier2:window-overlap` — with extension, a second beat's window in the same episode overlapped the first's and was rejected instead of merged, so the seed yield fell to 9/31 (run 8: 26/32). Verification did not converge on the per-act contract (F-97): firstAttemptPassRate 0.05, purposeFidelity 0.33, 8 of 21 pages unverified, a clean Intro dropped because its act was sent back for other seams — the per-page mechanical rules (F-36/F-37 no-source pages, §4.7 rule 1, contested-record assertions) were applied per seam, and a bridge that rests on the previous clip's source is unsupported by construction under seam-scoped attachment. Published `--force` as PR #648 and left on `hold`: the G-21c gate also caught two checker tests that go red the moment a Q-01 Foray is committed (the D5-pair test assumes every committed Foray predates Q-01). Not offered for the listen — it would test the narration change but not the clip change. Second passes on both lanes follow before run 10.
