const { run } = require("./rt_harness.js");

// Honest empty/sparse behavior for real-but-uncovered obscure topics
run("bagpipes");
run("macrame");
run("blacksmithing");
run("competitive chess");
run("underwater basket weaving"); // truly absurd, sanity check
