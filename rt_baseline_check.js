const { run } = require("./rt_harness.js");

// Sanity: the originally-fixed class should stay fixed (not re-reporting,
// just confirming baseline before reporting NEW instances)
run("Electrical Circuit Design Dummies");
run("circuit design");
